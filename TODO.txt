* don't add duplicates to the queue in Srp.ml

* Check for fixedpoint more efficiently in Srp.ml

* Add a command line flag to export to json

* Check for recursion

* General way to shrink an SRP model by simplifying the symbolic variables
  Related: can we figure out, in the interpreter, what values are unused?
  Then we can remove these from the symbolic values etc or use it for fuzzing.
