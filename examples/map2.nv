type attribute = dict[int]

let nodes = 2

let edges = {
 0=1;
}

let merge node x y =
  combine (fun i j -> if i < j then i else j) x y

let trans edge x = map (fun i -> i + 1) x

let init node = 
  let m = createMap 10 in
  if node = 0 then m[3 := 0] else m

let assert n x = 
  let y = x[2] in 
  y < 10
