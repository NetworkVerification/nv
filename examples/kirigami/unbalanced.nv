type attribute = int

let nodes = 2
let edges = {
    0-1;
}

let trans edge x = x + 1

let init n = match n with
| 0n -> 0
| 1n -> 10

let merge n x y = match n with
| 1n -> if x < y then x else y
| 0n -> if x < y then x else y

let foo edge = match edge with
| 0~1 -> 1

let assert n x = x < 3
