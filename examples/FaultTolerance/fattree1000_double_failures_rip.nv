include "../generic-topologies/fattree1000.nv"
include "../generic-algebras/allTwoFailuresRip.nv"


let init u =
  let m = createDict None in
  if (u = 0n) then
     map (fun v -> Some 0) m
  else
     m
