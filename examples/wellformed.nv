type attribute = option[dict[(int,dict[int,int]), int]]

let nodes = 2

let edges = {
 0=1;
}

let merge node x y = None

let trans edge x = None

let init node = None

let sol = solution {init = init; trans = trans; merge = merge}
