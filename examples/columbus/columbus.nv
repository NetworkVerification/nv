type ospfType = {ospfAd: int8; weight: int16; areaType:int2; areaId: int;}
type bgpType = {bgpAd: int8; lp: int; aslen: int; med:int; comms:set[int];}
type rib = {
  connected:option[int8];
  static:option[int8];
  ospf:option[ospfType];
  bgp:option[bgpType];
  selected:option[int2]; }
type attribute = rib

    symbolic d : (int, int5)

let edges = {
  1-51; (*usa,FastEthernet0/0 --> nodeid33,FastEthernet1/0*)
  76-56; (*mexico,FastEthernet0/0 --> belize,FastEthernet0/0*)
  51-20; (*nodeid33,FastEthernet0/0 --> nodeid45,FastEthernet0/1*)
  64-33; (*costattrica,FastEthernet0/1 --> nodeid9,FastEthernet0/0*)
  20-15; (*nodeid45,FastEthernet1/1 --> nodeid49,FastEthernet1/0*)
  24-82; (*nodeid44,FastEthernet1/1 --> nodeid59,FastEthernet1/0*)
  60-26; (*nodeid69,FastEthernet0/0 --> nicaragua,FastEthernet0/1*)
  59-9; (*nodeid28,FastEthernet0/1 --> guatemala,FastEthernet0/0*)
  67-62; (*nodeid67,FastEthernet1/0 --> nodeid29,FastEthernet1/0*)
  52-4; (*guadeloupe,FastEthernet0/1 --> dominica,FastEthernet0/0*)
  74-37; (*peernodeid39_7,FastEthernet0/0 --> nodeid39,FastEthernet1/0*)
  55-31; (*bonaire,FastEthernet0/0 --> curacao,FastEthernet0/0*)
  6-2; (*puertottrico,FastEthernet0/1 --> nodeid50,FastEthernet0/1*)
  25-85; (*grenada,FastEthernet0/1 --> trinidadttandtttobago,FastEthernet0/0*)
  32-16; (*stttmartin,FastEthernet0/1 --> peerstttmartin_14,FastEthernet0/0*)
  49-82; (*nodeid32,FastEthernet0/1 --> nodeid59,FastEthernet0/1*)
  51-15; (*nodeid33,FastEthernet0/1 --> nodeid49,FastEthernet0/1*)
  25-69; (*grenada,FastEthernet0/0 --> nodeid24,FastEthernet0/0*)
  68-40; (*peernodeid1_2,FastEthernet0/0 --> nodeid1,FastEthernet2/1*)
  45-14; (*stttkittstt&ttnevis,FastEthernet0/0 --> antigua,FastEthernet0/1*)
  26-64; (*nicaragua,FastEthernet0/0 --> costattrica,FastEthernet0/0*)
  28-10; (*nodeid7,FastEthernet0/1 --> venezuela,FastEthernet0/1*)
  66-65; (*nodeid62,FastEthernet0/0 --> nodeid63,FastEthernet1/0*)
  53-24; (*peernodeid44_8,FastEthernet0/0 --> nodeid44,FastEthernet2/0*)
  73-60; (*nodeid60,FastEthernet0/1 --> nodeid69,FastEthernet0/1*)
  32-45; (*stttmartin,FastEthernet1/1 --> stttkittstt&ttnevis,FastEthernet1/0*)
  22-58; (*nodeid46,FastEthernet1/1 --> peernodeid46_10,FastEthernet0/0*)
  56-59; (*belize,FastEthernet0/1 --> nodeid28,FastEthernet0/0*)
  47-85; (*nodeid35,FastEthernet1/0 --> trinidadttandtttobago,FastEthernet0/1*)
  44-71; (*nodeid31,FastEthernet2/0 --> peernodeid31_6,FastEthernet0/0*)
  9-65; (*guatemala,FastEthernet0/1 --> nodeid63,FastEthernet0/1*)
  20-51; (*nodeid45,FastEthernet0/1 --> nodeid33,FastEthernet0/0*)
  29-2; (*nodeid8,FastEthernet0/0 --> nodeid50,FastEthernet0/0*)
  35-67; (*honduras,FastEthernet0/1 --> nodeid67,FastEthernet0/0*)
  52-14; (*guadeloupe,FastEthernet0/0 --> antigua,FastEthernet0/0*)
  76-83; (*mexico,FastEthernet0/1 --> nodeid15,FastEthernet0/0*)
  15-51; (*nodeid49,FastEthernet0/1 --> nodeid33,FastEthernet0/1*)
  3-6; (*nodeid56,FastEthernet0/1 --> puertottrico,FastEthernet1/0*)
  60-63; (*nodeid69,FastEthernet1/0 --> peernodeid69_11,FastEthernet0/0*)
  78-80; (*nodeid17,FastEthernet0/0 --> nodeid58,FastEthernet0/0*)
  83-76; (*nodeid15,FastEthernet0/0 --> mexico,FastEthernet0/1*)
  5-44; (*nodeid13,FastEthernet1/0 --> nodeid31,FastEthernet0/0*)
  77-82; (*bahamas,FastEthernet0/1 --> nodeid59,FastEthernet0/0*)
  10-31; (*venezuela,FastEthernet0/0 --> curacao,FastEthernet2/0*)
  56-76; (*belize,FastEthernet0/0 --> mexico,FastEthernet0/0*)
  72-69; (*nodeid22,FastEthernet1/0 --> nodeid24,FastEthernet0/1*)
  44-61; (*nodeid31,FastEthernet0/1 --> nodeid26,FastEthernet0/1*)
  39-57; (*nodeid0,FastEthernet0/1 --> nodeid27,FastEthernet0/0*)
  15-44; (*nodeid49,FastEthernet0/0 --> nodeid31,FastEthernet1/1*)
  31-6; (*curacao,FastEthernet1/0 --> puertottrico,FastEthernet0/0*)
  82-24; (*nodeid59,FastEthernet1/0 --> nodeid44,FastEthernet1/1*)
  12-85; (*stttvincent,FastEthernet1/0 --> trinidadttandtttobago,FastEthernet1/0*)
  12-75; (*stttvincent,FastEthernet0/1 --> peerstttvincent_15,FastEthernet0/0*)
  20-77; (*nodeid45,FastEthernet0/0 --> bahamas,FastEthernet0/0*)
  61-44; (*nodeid26,FastEthernet0/1 --> nodeid31,FastEthernet0/1*)
  35-73; (*honduras,FastEthernet0/0 --> nodeid60,FastEthernet0/0*)
  18-13; (*nodeid41,FastEthernet0/0 --> jamaica,FastEthernet0/1*)
  61-83; (*nodeid26,FastEthernet0/0 --> nodeid15,FastEthernet0/1*)
  57-54; (*nodeid27,FastEthernet1/0 --> panama,FastEthernet0/1*)
  5-81; (*nodeid13,FastEthernet0/0 --> nodeid14,FastEthernet0/0*)
}

let nodes = 86

let ospfIntraArea = 0u2
let ospfInterArea = 1u2
let ospfE1 = 2u2
let ospfE2 = 3u2

let protoConn = 0u8
let protoStatic = 1u8
let protoOspf = 2u8
let protoBgp = 3u8

let isProtocol fib x =
  match fib with
  | None -> false
  | Some y -> x = y

let min x y = if x <u8 y then x else y

let max x y = if x <u8 y then y else x

let pickOption f x y =
  match (x,y) with
  | (None, _) -> y  | (_, None) -> x
  | (Some a, Some b) -> Some (f a b)

let betterOspf o1 o2 =
  if o1.areaType >u2 o2.areaType then o1
  else if o2.areaType >u2 o1.areaType then o2
  else if o1.weight <=u16 o2.weight then o1 else o2

let betterBgp b1 b2 =
  if b1.lp > b2.lp then b1
  else if b2.lp > b1.lp then b2
  else if b1.aslen < b2.aslen then b1
  else if b2.aslen < b1.aslen then b2
  else if b1.med >= b2.med then b1 else b2

let betterEqOption o1 o2 =
  match (o1,o2) with
  | (_, None) -> true
  | (None, _) -> false
  | (Some a, Some b) -> a <=u8 b

let best c s o b =
  match (c,s,o,b) with
  | (None,None,None,None) -> None
  | _ ->
    let o = match o with | None -> None | Some o -> Some o.ospfAd in
    let b = match b with | None -> None | Some b -> Some b.bgpAd in
    let (x,p1) = if betterEqOption c s then (c,0u2) else (s,1u2) in
    let (y,p2) = if betterEqOption o b then (o,2u2) else (b,3u2) in
    Some (if betterEqOption x y then p1 else p2)

let mergeValues x y =
  let c = pickOption min x.connected y.connected in
  let s = pickOption min x.static y.static in
  let o = pickOption betterOspf x.ospf y.ospf in
  let b = pickOption betterBgp x.bgp y.bgp in
  { connected = c;
    static = s;
    ospf = o;
    bgp = b;
    selected = best c s o b}

let merge node x y = mergeValues x y

let init node =
  match node with
  | 0n ->
    if (d = (167772192, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355592960, 24u5)) || (d = (2147483904, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 1n ->
    if (d = (167772246, 31u5)) || (d = (167772162, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355585280, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 2n ->
    if (d = (167772286, 31u5)) || (d = (167772328, 31u5)) || (d = (167772326, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355600128, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 3n ->
    if (d = (167772324, 31u5)) || (d = (167772194, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355598336, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 4n ->
    if (d = (167772204, 31u5)) || (d = (167772202, 31u5)) || (d = (167772200, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355596544, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 5n ->
    if (d = (167772174, 31u5)) || (d = (167772172, 31u5)) || (d = (167772170, 31u5)) || (d = (167772178, 31u5)) || (d = (167772176, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355590144, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 6n ->
    if (d = (167772234, 31u5)) || (d = (167772328, 31u5)) || (d = (167772324, 31u5)) || (d = (167772354, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355590400, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 7n ->
    if (d = (167772340, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355586560, 24u5)) || (d = (2147485952, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 8n ->
    if (d = (167772204, 31u5)) || (d = (167772304, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355594240, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 9n ->
    if (d = (167772190, 31u5)) || (d = (167772188, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355580672, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 10n ->
    if (d = (167772270, 31u5)) || (d = (167772352, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355581184, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 11n ->
    if (d = (167772198, 31u5)) || (d = (167772196, 31u5)) || (d = (167772194, 31u5)) || (d = (167772192, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355589632, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 12n ->
    if (d = (167772212, 31u5)) || (d = (167772242, 31u5)) || (d = (167772240, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355600640, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 13n ->
    if (d = (167772230, 31u5)) || (d = (167772228, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355581696, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 14n ->
    if (d = (167772226, 31u5)) || (d = (167772224, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355588608, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 15n ->
    if (d = (167772266, 31u5)) || (d = (167772342, 31u5)) || (d = (167772164, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355580928, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 16n ->
    if (d = (167772232, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355592192, 24u5)) || (d = (2147487232, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 17n ->
    if (d = (167772274, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355583232, 24u5)) || (d = (2147486720, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 18n ->
    if (d = (167772244, 31u5)) || (d = (167772228, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355597568, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 19n ->
    if (d = (167772334, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (2147486976, 24u5)) || (d = (3355600384, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 20n ->
    if (d = (167772318, 31u5)) || (d = (167772216, 31u5)) || (d = (167772342, 31u5)) || (d = (167772340, 31u5)) || (d = (167772160, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355600896, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 21n ->
    if (d = (167772172, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355588864, 24u5)) || (d = (2147484160, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 22n ->
    if (d = (167772294, 31u5)) || (d = (167772308, 31u5)) || (d = (167772338, 31u5)) || (d = (167772288, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355599616, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 23n ->
    if (d = (167772176, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355589888, 24u5)) || (d = (2147484416, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 24n ->
    if (d = (167772222, 31u5)) || (d = (167772220, 31u5)) || (d = (167772218, 31u5)) || (d = (167772216, 31u5)) || (d = (167772214, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355599872, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 25n ->
    if (d = (167772208, 31u5)) || (d = (167772336, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355588352, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 26n ->
    if (d = (167772348, 31u5)) || (d = (167772346, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355593728, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 27n ->
    if (d = (167772186, 31u5)) || (d = (167772196, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355594496, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 28n ->
    if (d = (167772270, 31u5)) || (d = (167772272, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355587584, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 29n ->
    if (d = (167772286, 31u5)) || (d = (167772236, 31u5)) || (d = (167772284, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355590912, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 30n ->
    if (d = (167772206, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355582976, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 31n ->
    if (d = (167772350, 31u5)) || (d = (167772344, 31u5)) || (d = (167772354, 31u5)) || (d = (167772352, 31u5)) || (d = (167772240, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355583744, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 32n ->
    if (d = (167772238, 31u5)) || (d = (167772236, 31u5)) || (d = (167772234, 31u5)) || (d = (167772232, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355599360, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 33n ->
    if (d = (167772292, 31u5)) || (d = (167772290, 31u5)) || (d = (167772288, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355591936, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 34n ->
    if (d = (167772168, 31u5)) || (d = (167772166, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355594752, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 35n ->
    if (d = (167772358, 31u5)) || (d = (167772322, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355584256, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 36n ->
    if (d = (167772246, 31u5)) || (d = (167772166, 31u5)) || (d = (167772244, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355588096, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 37n ->
    if (d = (167772248, 31u5)) || (d = (167772230, 31u5)) || (d = (167772198, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355587840, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 38n ->
    if (d = (167772252, 31u5)) || (d = (167772250, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355591680, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 39n ->
    if (d = (167772278, 31u5)) || (d = (167772276, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355585536, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 40n ->
    if (d = (167772252, 31u5)) || (d = (167772282, 31u5)) || (d = (167772280, 31u5)) || (d = (167772168, 31u5)) || (d = (167772278, 31u5)) || (d = (167772272, 31u5)) || (d = (167772256, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355584768, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 41n ->
    if (d = (167772334, 31u5)) || (d = (167772284, 31u5)) || (d = (167772298, 31u5)) || (d = (167772326, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355595776, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 42n ->
    if (d = (167772250, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355587328, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 43n ->
    if (d = (167772338, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355599104, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 44n ->
    if (d = (167772222, 31u5)) || (d = (167772268, 31u5)) || (d = (167772266, 31u5)) || (d = (167772264, 31u5)) || (d = (167772178, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355593984, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 45n ->
    if (d = (167772238, 31u5)) || (d = (167772202, 31u5)) || (d = (167772226, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355589120, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 46n ->
    if (d = (167772254, 31u5)) || (d = (167772256, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355592704, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 47n ->
    if (d = (167772260, 31u5)) || (d = (167772210, 31u5)) || (d = (167772258, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355592448, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 48n ->
    if (d = (167772260, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355591168, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 49n ->
    if (d = (167772218, 31u5)) || (d = (167772262, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355596032, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 50n ->
    if (d = (167772258, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355595520, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 51n ->
    if (d = (167772164, 31u5)) || (d = (167772162, 31u5)) || (d = (167772160, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355595264, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 52n ->
    if (d = (167772200, 31u5)) || (d = (167772224, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355582720, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 53n ->
    if (d = (167772214, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (2147485696, 24u5)) || (d = (3355580160, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 54n ->
    if (d = (167772254, 31u5)) || (d = (167772316, 31u5)) || (d = (167772274, 31u5)) || (d = (167772306, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355589376, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 55n ->
    if (d = (167772344, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355598592, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 56n ->
    if (d = (167772332, 31u5)) || (d = (167772312, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355601152, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 57n ->
    if (d = (167772292, 31u5)) || (d = (167772276, 31u5)) || (d = (167772306, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355595008, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 58n ->
    if (d = (167772294, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (2147486208, 24u5)) || (d = (3355579904, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 59n ->
    if (d = (167772188, 31u5)) || (d = (167772314, 31u5)) || (d = (167772312, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355585792, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 60n ->
    if (d = (167772348, 31u5)) || (d = (167772296, 31u5)) || (d = (167772356, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355597312, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 61n ->
    if (d = (167772268, 31u5)) || (d = (167772182, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355596288, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 62n ->
    if (d = (167772170, 31u5)) || (d = (167772310, 31u5)) || (d = (167772308, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355587072, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 63n ->
    if (d = (167772296, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (2147486464, 24u5)) || (d = (3355597056, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 64n ->
    if (d = (167772316, 31u5)) || (d = (167772346, 31u5)) || (d = (167772290, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355593472, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 65n ->
    if (d = (167772190, 31u5)) || (d = (167772206, 31u5)) || (d = (167772360, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355581440, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 66n ->
    if (d = (167772360, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355581952, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 67n ->
    if (d = (167772314, 31u5)) || (d = (167772310, 31u5)) || (d = (167772322, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355582464, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 68n ->
    if (d = (167772280, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (2147484672, 24u5)) || (d = (3355597824, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 69n ->
    if (d = (167772300, 31u5)) || (d = (167772336, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355593216, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 70n ->
    if (d = (167772282, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (2147484928, 24u5)) || (d = (3355596800, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 71n ->
    if (d = (167772264, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355584512, 24u5)) || (d = (2147485184, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 72n ->
    if (d = (167772302, 31u5)) || (d = (167772300, 31u5)) || (d = (167772298, 31u5)) || (d = (167772304, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355590656, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 73n ->
    if (d = (167772358, 31u5)) || (d = (167772356, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355580416, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 74n ->
    if (d = (167772248, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355591424, 24u5)) || (d = (2147485440, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 75n ->
    if (d = (167772242, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (2147487488, 24u5)) || (d = (3355601408, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 76n ->
    if (d = (167772332, 31u5)) || (d = (167772180, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355601664, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 77n ->
    if (d = (167772318, 31u5)) || (d = (167772320, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355598848, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 78n ->
    if (d = (167772186, 31u5)) || (d = (167772184, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355585024, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 79n ->
    if (d = (167772350, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355582208, 24u5)) || (d = (2147483648, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 80n ->
    if (d = (167772330, 31u5)) || (d = (167772184, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355584000, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 81n ->
    if (d = (167772174, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355586048, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 82n ->
    if (d = (167772220, 31u5)) || (d = (167772330, 31u5)) || (d = (167772262, 31u5)) || (d = (167772320, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355583488, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 83n ->
    if (d = (167772182, 31u5)) || (d = (167772180, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355586816, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 84n ->
    if (d = (167772302, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355598080, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 85n ->
    if (d = (167772212, 31u5)) || (d = (167772210, 31u5)) || (d = (167772208, 31u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = None in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else if (d = (3355586304, 24u5)) then
      let c = Some 0u8 in
      let s = None in
      let o = None in
      let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
      let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
    else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | _ -> {connected=None; static=None; ospf=None; bgp=None; selected=None;}

let transferOspf edge o =
  match o with
  | None -> None
  | Some o -> (
      match edge with
      | _ -> None
    )

let transferBgpPol policy x =
  let b = match x.selected with
    | None -> None
    | Some 0u2 -> Some {bgpAd = 0u8; lp = 100; aslen = 0; med = 80; comms = {}}
    | Some 1u2 -> Some {bgpAd = 1u8; lp = 100; aslen = 0; med = 80; comms = {}}
    | Some 2u2 -> Some {bgpAd = 110u8; lp = 100; aslen = 0; med = 80; comms = {}}
    | Some 3u2 -> x.bgp
  in
  match b with
  | None -> None
  | Some b ->
    policy x.selected b

let transferBgp e x0 =
  match e with
  | (0~11) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355592960) && (prefix < 3355593216)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553601:= true];}))

  | (1~51) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355585280) && (prefix < 3355585536)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553600:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (1~36) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355585280) && (prefix < 3355585536)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (2~41) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355600128) && (prefix < 3355600384)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487232) && (prefix < 2147487488))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (2~29) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355600128) && (prefix < 3355600384)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (2~6) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355600128) && (prefix < 3355600384)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (3~6) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355598336) && (prefix < 3355598592)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
           else
             (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
                (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
              else
                (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                   None
                 else
                   (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))))

  | (3~11) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355598336) && (prefix < 3355598592)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (4~8) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355596544) && (prefix < 3355596800)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (4~52) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355596544) && (prefix < 3355596800)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (4~45) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355596544) && (prefix < 3355596800)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487232) && (prefix < 2147487488))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553614:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (5~62) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590144) && (prefix < 3355590400)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 3; aslen= b.aslen; med= b.med;comms= b.comms[6553613:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
           else
             (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672))) || (false)) then
                (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553603:= true];})
              else
                (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416))) || (false)) then
                   (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553611:= true];})
                 else
                   (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                      None
                    else
                      (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))))

  | (5~21) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590144) && (prefix < 3355590400)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    b

  | (5~23) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590144) && (prefix < 3355590400)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    b

  | (5~44) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590144) && (prefix < 3355590400)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553611:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553609:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (5~81) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590144) && (prefix < 3355590400)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (6~2) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590400) && (prefix < 3355590656)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487232) && (prefix < 2147487488))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553607:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 101; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (6~32) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590400) && (prefix < 3355590656)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (6~3) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590400) && (prefix < 3355590656)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553607:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (6~31) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590400) && (prefix < 3355590656)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553611:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 100; aslen= b.aslen; med= b.med;comms= b.comms[6553605:= true];})
           else
             (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
                (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553607:= true];})
              else
                (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                   None
                 else
                   (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))))

  | (7~20) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355586560) && (prefix < 3355586816)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147485952) && (prefix < 2147486208))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147485952) && (prefix < 2147486208)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147485952) && (prefix < 2147486208))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147485952) && (prefix < 2147486208))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553609:= true];}))

  | (8~72) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355594240) && (prefix < 3355594496)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (8~4) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355594240) && (prefix < 3355594496)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487232) && (prefix < 2147487488))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553614:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (9~65) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355580672) && (prefix < 3355580928)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (9~59) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355580672) && (prefix < 3355580928)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (10~31) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355581184) && (prefix < 3355581440)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (10~28) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355581184) && (prefix < 3355581440)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
           else
             (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
                (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
              else
                (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                   None
                 else
                   (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))))

  | (11~37) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355589632) && (prefix < 3355589888)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 99; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 99; aslen= b.aslen; med= b.med;comms= b.comms[6553601:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (11~3) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355589632) && (prefix < 3355589888)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553608:= true];})
           else
             (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
                (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
              else
                (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                   None
                 else
                   (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))))

  | (11~0) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355589632) && (prefix < 3355589888)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    b

  | (11~27) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355589632) && (prefix < 3355589888)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (12~85) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355600640) && (prefix < 3355600896)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553607:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (12~75) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355600640) && (prefix < 3355600896)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    b

  | (12~31) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355600640) && (prefix < 3355600896)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553607:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (13~37) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355581696) && (prefix < 3355581952)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553611:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553600:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (13~18) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355581696) && (prefix < 3355581952)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (14~52) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355588608) && (prefix < 3355588864)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (14~45) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355588608) && (prefix < 3355588864)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (15~20) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355580928) && (prefix < 3355581184)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (15~44) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355580928) && (prefix < 3355581184)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (15~51) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355580928) && (prefix < 3355581184)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (16~32) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355592192) && (prefix < 3355592448)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147487232) && (prefix < 2147487488))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147487232) && (prefix < 2147487488)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147487232) && (prefix < 2147487488))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147487232) && (prefix < 2147487488))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553614:= true];}))

  | (17~54) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355583232) && (prefix < 3355583488)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147486720) && (prefix < 2147486976))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147486720) && (prefix < 2147486976)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147486720) && (prefix < 2147486976))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147486720) && (prefix < 2147486976))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553612:= true];}))

  | (18~36) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355597568) && (prefix < 3355597824)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (18~13) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355597568) && (prefix < 3355597824)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
           else
             (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
                (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553610:= true];})
              else
                (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                   None
                 else
                   (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))))

  | (19~41) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355600384) && (prefix < 3355600640)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147486976) && (prefix < 2147487232))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147486976) && (prefix < 2147487232)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147486976) && (prefix < 2147487232))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147486976) && (prefix < 2147487232))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553613:= true];}))

  | (20~24) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355600896) && (prefix < 3355601152)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (20~51) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355600896) && (prefix < 3355601152)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485952) && (prefix < 2147486208))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (20~77) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355600896) && (prefix < 3355601152)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (20~15) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355600896) && (prefix < 3355601152)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485952) && (prefix < 2147486208))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553600:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (20~7) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355600896) && (prefix < 3355601152)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    b

  | (21~5) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355588864) && (prefix < 3355589120)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553604:= true];}))

  | (22~33) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599616) && (prefix < 3355599872)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553610:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553614:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (22~58) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599616) && (prefix < 3355599872)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    b

  | (22~43) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599616) && (prefix < 3355599872)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (22~62) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599616) && (prefix < 3355599872)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 3; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (23~5) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355589888) && (prefix < 3355590144)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553605:= true];}))

  | (24~49) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599872) && (prefix < 3355600128)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485696) && (prefix < 2147485952))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553600:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (24~44) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599872) && (prefix < 3355600128)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (24~82) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599872) && (prefix < 3355600128)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485696) && (prefix < 2147485952))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (24~20) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599872) && (prefix < 3355600128)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (24~53) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599872) && (prefix < 3355600128)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    b

  | (25~85) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355588352) && (prefix < 3355588608)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (25~69) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355588352) && (prefix < 3355588608)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (26~64) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355593728) && (prefix < 3355593984)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (26~60) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355593728) && (prefix < 3355593984)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (27~11) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355594496) && (prefix < 3355594752)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (27~78) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355594496) && (prefix < 3355594752)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (28~10) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355587584) && (prefix < 3355587840)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553612:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (28~40) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355587584) && (prefix < 3355587840)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 100; aslen= b.aslen; med= b.med;comms= b.comms[6553607:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (29~2) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590912) && (prefix < 3355591168)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486976) && (prefix < 2147487232))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553605:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (29~32) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590912) && (prefix < 3355591168)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (29~41) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590912) && (prefix < 3355591168)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (30~65) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355582976) && (prefix < 3355583232)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (31~10) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355583744) && (prefix < 3355584000)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 101; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
           else
             (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
                (Some {bgpAd= b.bgpAd; lp= 100; aslen= b.aslen; med= b.med;comms= b.comms[6553607:= true];})
              else
                (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
                   (Some {bgpAd= b.bgpAd; lp= 100; aslen= b.aslen; med= b.med;comms= b.comms[6553601:= true];})
                 else
                   (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                      None
                    else
                      (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))))

  | (31~55) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355583744) && (prefix < 3355584000)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (31~79) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355583744) && (prefix < 3355584000)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    b

  | (31~6) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355583744) && (prefix < 3355584000)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553605:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (31~12) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355583744) && (prefix < 3355584000)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (32~45) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599360) && (prefix < 3355599616)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487232) && (prefix < 2147487488))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553614:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (32~16) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599360) && (prefix < 3355599616)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    b

  | (32~6) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599360) && (prefix < 3355599616)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487232) && (prefix < 2147487488))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (32~29) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599360) && (prefix < 3355599616)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (33~64) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355591936) && (prefix < 3355592192)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486720) && (prefix < 2147486976))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553612:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (33~22) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355591936) && (prefix < 3355592192)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (33~57) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355591936) && (prefix < 3355592192)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553612:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (34~36) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355594752) && (prefix < 3355595008)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553602:= true];})
           else
             (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
                (Some {bgpAd= b.bgpAd; lp= 100; aslen= b.aslen; med= b.med;comms= b.comms[6553612:= true];})
              else
                (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
                   (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553609:= true];})
                 else
                   (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                      None
                    else
                      (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))))

  | (34~40) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355594752) && (prefix < 3355595008)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553604:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (35~67) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355584256) && (prefix < 3355584512)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (35~73) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355584256) && (prefix < 3355584512)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (36~1) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355588096) && (prefix < 3355588352)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (36~18) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355588096) && (prefix < 3355588352)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553609:= true];})
           else
             (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
                (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553610:= true];})
              else
                (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                   None
                 else
                   (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))))

  | (36~34) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355588096) && (prefix < 3355588352)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (37~74) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355587840) && (prefix < 3355588096)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    b

  | (37~13) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355587840) && (prefix < 3355588096)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553614:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553612:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (37~11) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355587840) && (prefix < 3355588096)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553608:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553600:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (38~40) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355591680) && (prefix < 3355591936)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (38~42) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355591680) && (prefix < 3355591936)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (39~57) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355585536) && (prefix < 3355585792)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484928) && (prefix < 2147485184))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (39~40) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355585536) && (prefix < 3355585792)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553611:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (40~70) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355584768) && (prefix < 3355585024)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    b

  | (40~39) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355584768) && (prefix < 3355585024)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484928) && (prefix < 2147485184))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553604:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (40~38) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355584768) && (prefix < 3355585024)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (40~46) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355584768) && (prefix < 3355585024)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484928) && (prefix < 2147485184))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (40~68) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355584768) && (prefix < 3355585024)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    b

  | (40~28) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355584768) && (prefix < 3355585024)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553613:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553604:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (40~34) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355584768) && (prefix < 3355585024)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553613:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
           else
             (if (((prefixLen = 24u5) && ((prefix >= 2147483904) && (prefix < 2147484160))) || (false)) then
                (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
              else
                (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
                   (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
                 else
                   (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                      None
                    else
                      (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))))

  | (41~29) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355595776) && (prefix < 3355596032)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486976) && (prefix < 2147487232))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553605:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (41~2) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355595776) && (prefix < 3355596032)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486976) && (prefix < 2147487232))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553614:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (41~19) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355595776) && (prefix < 3355596032)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    b

  | (41~72) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355595776) && (prefix < 3355596032)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487232) && (prefix < 2147487488))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553604:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 100; aslen= b.aslen; med= b.med;comms= b.comms[6553603:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (42~38) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355587328) && (prefix < 3355587584)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (43~22) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355599104) && (prefix < 3355599360)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (44~24) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355593984) && (prefix < 3355594240)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (44~5) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355593984) && (prefix < 3355594240)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553613:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (44~15) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355593984) && (prefix < 3355594240)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (44~61) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355593984) && (prefix < 3355594240)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553601:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 3; aslen= b.aslen; med= b.med;comms= b.comms[6553612:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (44~71) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355593984) && (prefix < 3355594240)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    b

  | (45~32) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355589120) && (prefix < 3355589376)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (45~14) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355589120) && (prefix < 3355589376)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (45~4) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355589120) && (prefix < 3355589376)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (46~40) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355592704) && (prefix < 3355592960)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (46~54) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355592704) && (prefix < 3355592960)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (47~85) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355592448) && (prefix < 3355592704)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (47~50) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355592448) && (prefix < 3355592704)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (47~48) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355592448) && (prefix < 3355592704)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (48~47) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355591168) && (prefix < 3355591424)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (49~24) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355596032) && (prefix < 3355596288)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (49~82) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355596032) && (prefix < 3355596288)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (50~47) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355595520) && (prefix < 3355595776)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (51~1) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355595264) && (prefix < 3355595520)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (51~20) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355595264) && (prefix < 3355595520)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (51~15) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355595264) && (prefix < 3355595520)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485952) && (prefix < 2147486208))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553605:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (52~14) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355582720) && (prefix < 3355582976)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (52~4) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355582720) && (prefix < 3355582976)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (53~24) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355580160) && (prefix < 3355580416)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147485696) && (prefix < 2147485952))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147485696) && (prefix < 2147485952)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147485696) && (prefix < 2147485952))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147485696) && (prefix < 2147485952))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553608:= true];}))

  | (54~57) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355589376) && (prefix < 3355589632)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486720) && (prefix < 2147486976))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553604:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (54~46) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355589376) && (prefix < 3355589632)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484928) && (prefix < 2147485184))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553612:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (54~17) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355589376) && (prefix < 3355589632)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    b

  | (54~64) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355589376) && (prefix < 3355589632)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486720) && (prefix < 2147486976))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553603:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (55~31) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355598592) && (prefix < 3355598848)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (56~59) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355601152) && (prefix < 3355601408)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553602:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (56~76) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355601152) && (prefix < 3355601408)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553611:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (57~39) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355595008) && (prefix < 3355595264)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (57~33) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355595008) && (prefix < 3355595264)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486720) && (prefix < 2147486976))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553614:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (57~54) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355595008) && (prefix < 3355595264)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484928) && (prefix < 2147485184))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (58~22) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355579904) && (prefix < 3355580160)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147486208) && (prefix < 2147486464))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553610:= true];}))

  | (59~67) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355585792) && (prefix < 3355586048)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553612:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (59~9) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355585792) && (prefix < 3355586048)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (59~56) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355585792) && (prefix < 3355586048)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553609:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (60~63) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355597312) && (prefix < 3355597568)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    b

  | (60~26) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355597312) && (prefix < 3355597568)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553603:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (60~73) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355597312) && (prefix < 3355597568)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553611:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (61~44) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355596288) && (prefix < 3355596544)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553612:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (61~83) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355596288) && (prefix < 3355596544)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553605:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (62~5) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355587072) && (prefix < 3355587328)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (62~22) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355587072) && (prefix < 3355587328)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553608:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (62~67) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355587072) && (prefix < 3355587328)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 100; aslen= b.aslen; med= b.med;comms= b.comms[6553607:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (63~60) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355597056) && (prefix < 3355597312)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553611:= true];}))

  | (64~33) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355593472) && (prefix < 3355593728)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (64~26) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355593472) && (prefix < 3355593728)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553605:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (64~54) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355593472) && (prefix < 3355593728)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (65~9) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355581440) && (prefix < 3355581696)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (65~66) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355581440) && (prefix < 3355581696)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (65~30) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355581440) && (prefix < 3355581696)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (66~65) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355581952) && (prefix < 3355582208)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (67~59) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355582464) && (prefix < 3355582720)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (67~62) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355582464) && (prefix < 3355582720)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553604:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 2; aslen= b.aslen; med= b.med;comms= b.comms[6553600:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (67~35) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355582464) && (prefix < 3355582720)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (68~40) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355597824) && (prefix < 3355598080)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147484672) && (prefix < 2147484928))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553602:= true];}))

  | (69~25) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355593216) && (prefix < 3355593472)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (69~72) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355593216) && (prefix < 3355593472)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (70~40) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355596800) && (prefix < 3355597056)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147484928) && (prefix < 2147485184))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147484928) && (prefix < 2147485184)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147484928) && (prefix < 2147485184))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147484928) && (prefix < 2147485184))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553603:= true];}))

  | (71~44) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355584512) && (prefix < 3355584768)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];}))

  | (72~84) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590656) && (prefix < 3355590912)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (72~69) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590656) && (prefix < 3355590912)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 101; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (72~41) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590656) && (prefix < 3355590912)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (72~8) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355590656) && (prefix < 3355590912)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147487232) && (prefix < 2147487488))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553607:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (73~35) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355580416) && (prefix < 3355580672)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147486464) && (prefix < 2147486720))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553614:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (73~60) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355580416) && (prefix < 3355580672)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (74~37) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355591424) && (prefix < 3355591680)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147485440) && (prefix < 2147485696))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553607:= true];}))

  | (75~12) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355601408) && (prefix < 3355601664)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147487488) && (prefix < 2147487744))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];}))

  | (76~56) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355601664) && (prefix < 3355601920)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (76~83) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355601664) && (prefix < 3355601920)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553607:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553613:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (77~20) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355598848) && (prefix < 3355599104)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (77~82) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355598848) && (prefix < 3355599104)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (78~80) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355585024) && (prefix < 3355585280)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (78~27) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355585024) && (prefix < 3355585280)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (79~31) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355582208) && (prefix < 3355582464)))) then
           (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904)))) then
              (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              (if (isProtocol prot 3u2) then
                 (if (((prefixLen = 24u5) && ((prefix >= 2147483648) && (prefix < 2147483904))) || (false)) then
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 2 + 1; med= b.med;comms= b.comms;})
                  else
                    (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
               else
                 None))))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553600:= true];}))

  | (80~82) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355584000) && (prefix < 3355584256)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (80~78) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355584000) && (prefix < 3355584256)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (81~5) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355586048) && (prefix < 3355586304)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (82~24) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355583488) && (prefix < 3355583744)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (82~49) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355583488) && (prefix < 3355583744)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485696) && (prefix < 2147485952))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553614:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (82~80) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355583488) && (prefix < 3355583744)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (82~77) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355583488) && (prefix < 3355583744)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (83~76) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355586816) && (prefix < 3355587072)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147484416) && (prefix < 2147484672))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
        else
          (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
             None
           else
             (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;}))))

  | (83~61) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355586816) && (prefix < 3355587072)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 24u5) && ((prefix >= 2147485184) && (prefix < 2147485440))) || (false)) then
          (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553615:= true];})
        else
          (if (((prefixLen = 24u5) && ((prefix >= 2147484160) && (prefix < 2147484416))) || (false)) then
             (Some {bgpAd= b.bgpAd; lp= 1; aslen= b.aslen; med= b.med;comms= b.comms[6553606:= true];})
           else
             (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                None
              else
                (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))))

  | (84~72) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355598080) && (prefix < 3355598336)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (85~12) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355586304) && (prefix < 3355586560)))) then
           (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
         else
           (if (isProtocol prot 3u2) then
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (85~25) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355586304) && (prefix < 3355586560)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

  | (85~47) ->
    let (prefix, prefixLen) = d in
    let b = (transferBgpPol (fun prot b ->
        (if (((prefixLen = 24u5) && ((prefix >= 3355586304) && (prefix < 3355586560)))) then
           (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
              None
            else
              (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
         else
           (if (isProtocol prot 3u2) then
              (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
                 None
               else
                 (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;}))
            else
              None)))) x0
    in
    (match b with
     | None -> None
     | Some b ->
       (if (((prefixLen = 20u5) && ((prefix >= 2147483648) && (prefix < 2147487744))) || (false)) then
          None
        else
          (Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen; med= b.med;comms= b.comms;})))

let trans edge x =
  let o = transferOspf edge x.ospf in
  let b = transferBgp edge x in
  {connected=None; static=None; ospf=o; bgp=b; selected=None}

let assert_node u x =
  if (d=(3355592960, 24u5)) then
    match x.selected with
    | Some 3u2 ->
      (match x.bgp with
       | None -> false
       | Some b -> b.aslen < 20)
    | _ -> true
  else
    true

(*
{peerdominicanttrepublic_1=0,
 usa=1,
 nodeid50=2,
 nodeid56=3,
 dominica=4,
 nodeid13=5,
 puertottrico=6,
 peernodeid45_9=7,
 martinique=8,
 guatemala=9,
 venezuela=10,
 dominicanttrepublic=11,
 stttvincent=12,
 jamaica=13,
 antigua=14,
 nodeid49=15,
 peerstttmartin_14=16,
 peerpanama_12=17,
 nodeid41=18,
 peerstttcroix_13=19,
 nodeid45=20,
 peernodeid13_4=21,
 nodeid46=22,
 peernodeid13_5=23,
 nodeid44=24,
 grenada=25,
 nicaragua=26,
 turksttandttcaicos=27,
 nodeid7=28,
 nodeid8=29,
 elttsalvador=30,
 curacao=31,
 stttmartin=32,
 nodeid9=33,
 columbia=34,
 honduras=35,
 nodeid38=36,
 nodeid39=37,
 nodeid37=38,
 nodeid0=39,
 nodeid1=40,
 stttcroix=41,
 ecuador=42,
 caymanttislands=43,
 nodeid31=44,
 stttkittstt&ttnevis=45,
 nodeid34=46,
 nodeid35=47,
 guyana=48,
 nodeid32=49,
 suriname=50,
 nodeid33=51,
 guadeloupe=52,
 peernodeid44_8=53,
 panama=54,
 bonaire=55,
 belize=56,
 nodeid27=57,
 peernodeid46_10=58,
 nodeid28=59,
 nodeid69=60,
 nodeid26=61,
 nodeid29=62,
 peernodeid69_11=63,
 costattrica=64,
 nodeid63=65,
 nodeid62=66,
 nodeid67=67,
 peernodeid1_2=68,
 nodeid24=69,
 peernodeid1_3=70,
 peernodeid31_6=71,
 nodeid22=72,
 nodeid60=73,
 peernodeid39_7=74,
 peerstttvincent_15=75,
 mexico=76,
 bahamas=77,
 nodeid17=78,
 peercuracao_0=79,
 nodeid58=80,
 nodeid14=81,
 nodeid59=82,
 nodeid15=83,
 barbados=84,
 trinidadttandtttobago=85}*)

let sol = solution {init = init; trans = trans; merge = merge}

assert foldNodes (fun k v acc -> acc && assert_node k v) sol true
