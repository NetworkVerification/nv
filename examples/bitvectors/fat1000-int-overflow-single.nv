include "../generic-algebras/distance-overflow-single.nv"
include "../generic-topologies/fattree1000.nv"
(*
let assert u x =
  match x with
  | None -> false
  | Some k -> if (k < 5) then true else false
*)
let assert u x =
  match x with
  | None -> false
  | Some _ -> true
