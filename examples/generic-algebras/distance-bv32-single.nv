include "../utils.nv"

type attribute = option[int31]

let trans e x = mapo (fun n -> n +u31 1u31) x

let merge n x1 x2 = mapbetter (fun n1 n2 -> if n1 <u31 n2 then n1 else n2) x1 x2

let init n =
  match n with
  | 0n -> Some 0u31
  | _ -> None
