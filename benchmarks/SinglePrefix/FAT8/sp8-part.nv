(* vim: set syntax=ocaml: *)
(* Vertically partitioned version of sp8.nv *)
(* Automatically generated by gen_part_nv.py *)
include "../../../examples/utils.nv"
type ospfType = {ospfAd: int8; weight: int16; areaType:int2; areaId: int;}
type bgpType = {bgpAd: int8; lp: int; aslen: int; med:int; comms:set[int];}
type rib = {
    connected:option[int8];
    static:option[int8];
    ospf:option[ospfType];
    bgp:option[bgpType];
    selected:option[int2]; }
type attribute = rib

let d = (70.0.20.0/24u6)

let edges = {
  19-20; (*aggregation-32,Serial5 --> edge-37,Serial0*)
  54-6; (*core-7,Serial5 --> aggregation-73,Serial3*)
  11-20; (*aggregation-34,Serial1 --> edge-37,Serial2*)
  60-49; (*aggregation-57,Serial1 --> core-5,Serial7*)
  67-63; (*core-11,Serial7 --> aggregation-58,Serial3*)
  39-37; (*aggregation-65,Serial3 --> edge-71,Serial1*)
  65-49; (*aggregation-49,Serial1 --> core-5,Serial2*)
  74-65; (*edge-53,Serial1 --> aggregation-49,Serial5*)
  58-64; (*aggregation-51,Serial7 --> edge-55,Serial3*)
  58-73; (*aggregation-51,Serial4 --> edge-52,Serial3*)
  25-31; (*aggregation-27,Serial7 --> edge-31,Serial3*)
  24-7; (*edge-78,Serial0 --> aggregation-72,Serial6*)
  1-34; (*edge-29,Serial1 --> aggregation-25,Serial5*)
  19-40; (*aggregation-32,Serial1 --> core-1,Serial0*)
  68-15; (*core-10,Serial5 --> aggregation-74,Serial2*)
  79-72; (*aggregation-43,Serial3 --> core-13,Serial3*)
  3-44; (*edge-23,Serial1 --> aggregation-17,Serial7*)
  35-37; (*aggregation-64,Serial7 --> edge-71,Serial0*)
  34-55; (*aggregation-25,Serial2 --> core-6,Serial6*)
  75-46; (*aggregation-48,Serial2 --> core-2,Serial1*)
  59-64; (*aggregation-50,Serial7 --> edge-55,Serial2*)
  41-28; (*core-0,Serial6 --> aggregation-24,Serial0*)
  54-60; (*core-7,Serial7 --> aggregation-57,Serial3*)
  60-16; (*aggregation-57,Serial4 --> edge-60,Serial1*)
  6-52; (*aggregation-73,Serial0 --> core-4,Serial5*)
  37-35; (*edge-71,Serial0 --> aggregation-64,Serial7*)
  36-38; (*edge-70,Serial2 --> aggregation-66,Serial2*)
  49-39; (*core-5,Serial1 --> aggregation-65,Serial1*)
  79-48; (*aggregation-43,Serial4 --> edge-46,Serial3*)
  22-11; (*edge-39,Serial2 --> aggregation-34,Serial3*)
  63-16; (*aggregation-58,Serial4 --> edge-60,Serial2*)
  55-6; (*core-6,Serial5 --> aggregation-73,Serial2*)
  26-14; (*edge-79,Serial3 --> aggregation-75,Serial7*)
  9-20; (*aggregation-35,Serial1 --> edge-37,Serial3*)
  58-66; (*aggregation-51,Serial0 --> core-12,Serial0*)
  51-79; (*edge-47,Serial3 --> aggregation-43,Serial7*)
  11-68; (*aggregation-34,Serial6 --> core-10,Serial0*)
  25-66; (*aggregation-27,Serial0 --> core-12,Serial7*)
  52-39; (*core-4,Serial1 --> aggregation-65,Serial0*)
  5-38; (*edge-69,Serial2 --> aggregation-66,Serial1*)
  34-1; (*aggregation-25,Serial5 --> edge-29,Serial1*)
  62-10; (*aggregation-59,Serial7 --> edge-63,Serial3*)
  32-31; (*aggregation-26,Serial7 --> edge-31,Serial2*)
  23-14; (*edge-77,Serial3 --> aggregation-75,Serial5*)
  53-56; (*aggregation-18,Serial1 --> core-9,Serial3*)
  61-16; (*aggregation-56,Serial4 --> edge-60,Serial0*)
  35-5; (*aggregation-64,Serial5 --> edge-69,Serial0*)
  40-28; (*core-1,Serial6 --> aggregation-24,Serial1*)
  61-10; (*aggregation-56,Serial7 --> edge-63,Serial0*)
  39-55; (*aggregation-65,Serial6 --> core-6,Serial1*)
  29-34; (*edge-30,Serial1 --> aggregation-25,Serial6*)
  60-10; (*aggregation-57,Serial7 --> edge-63,Serial1*)
  59-73; (*aggregation-50,Serial4 --> edge-52,Serial2*)
  72-14; (*core-13,Serial5 --> aggregation-75,Serial3*)
  25-1; (*aggregation-27,Serial5 --> edge-29,Serial3*)
  6-54; (*aggregation-73,Serial3 --> core-7,Serial5*)
  57-77; (*core-8,Serial2 --> aggregation-42,Serial0*)
  53-67; (*aggregation-18,Serial3 --> core-11,Serial3*)
  63-10; (*aggregation-58,Serial7 --> edge-63,Serial2*)
  34-31; (*aggregation-25,Serial7 --> edge-31,Serial1*)
  58-70; (*aggregation-51,Serial3 --> core-15,Serial0*)
  17-27; (*aggregation-33,Serial5 --> edge-36,Serial1*)
  32-1; (*aggregation-26,Serial5 --> edge-29,Serial2*)
  50-72; (*aggregation-19,Serial1 --> core-13,Serial6*)
  47-40; (*aggregation-16,Serial1 --> core-1,Serial4*)
  50-12; (*aggregation-19,Serial4 --> edge-20,Serial3*)
  39-4; (*aggregation-65,Serial5 --> edge-68,Serial1*)
  56-77; (*core-9,Serial2 --> aggregation-42,Serial1*)
  4-38; (*edge-68,Serial2 --> aggregation-66,Serial0*)
  31-34; (*edge-31,Serial1 --> aggregation-25,Serial7*)
  70-62; (*core-15,Serial4 --> aggregation-59,Serial3*)
  50-3; (*aggregation-19,Serial7 --> edge-23,Serial3*)
  46-28; (*core-2,Serial6 --> aggregation-24,Serial2*)
  63-67; (*aggregation-58,Serial3 --> core-11,Serial7*)
  59-68; (*aggregation-50,Serial2 --> core-10,Serial4*)
  49-60; (*core-5,Serial7 --> aggregation-57,Serial1*)
  12-44; (*edge-20,Serial1 --> aggregation-17,Serial4*)
  43-75; (*core-3,Serial1 --> aggregation-48,Serial3*)
  44-13; (*aggregation-17,Serial5 --> edge-21,Serial1*)
  52-17; (*core-4,Serial0 --> aggregation-33,Serial0*)
  40-35; (*core-1,Serial3 --> aggregation-64,Serial1*)
  66-14; (*core-12,Serial5 --> aggregation-75,Serial2*)
  6-26; (*aggregation-73,Serial7 --> edge-79,Serial1*)
  65-64; (*aggregation-49,Serial7 --> edge-55,Serial1*)
  65-73; (*aggregation-49,Serial4 --> edge-52,Serial1*)
  28-31; (*aggregation-24,Serial7 --> edge-31,Serial0*)
  43-7; (*core-3,Serial7 --> aggregation-72,Serial3*)
  6-33; (*aggregation-73,Serial4 --> edge-76,Serial1*)
  41-35; (*core-0,Serial3 --> aggregation-64,Serial0*)
  8-60; (*edge-62,Serial1 --> aggregation-57,Serial6*)
  61-46; (*aggregation-56,Serial2 --> core-2,Serial5*)
  63-56; (*aggregation-58,Serial1 --> core-9,Serial7*)
  9-72; (*aggregation-35,Serial5 --> core-13,Serial2*)
  53-3; (*aggregation-18,Serial7 --> edge-23,Serial2*)
  70-79; (*core-15,Serial3 --> aggregation-43,Serial0*)
  71-62; (*core-14,Serial4 --> aggregation-59,Serial2*)
  7-26; (*aggregation-72,Serial7 --> edge-79,Serial0*)
  30-37; (*aggregation-67,Serial3 --> edge-71,Serial3*)
  28-41; (*aggregation-24,Serial0 --> core-0,Serial6*)
  75-74; (*aggregation-48,Serial5 --> edge-53,Serial0*)
  18-60; (*edge-61,Serial1 --> aggregation-57,Serial5*)
  30-70; (*aggregation-67,Serial7 --> core-15,Serial1*)
  49-17; (*core-5,Serial0 --> aggregation-33,Serial1*)
  47-13; (*aggregation-16,Serial5 --> edge-21,Serial0*)
  56-53; (*core-9,Serial3 --> aggregation-18,Serial1*)
  17-54; (*aggregation-33,Serial4 --> core-7,Serial0*)
  55-60; (*core-6,Serial7 --> aggregation-57,Serial2*)
  10-62; (*edge-63,Serial3 --> aggregation-59,Serial7*)
  75-73; (*aggregation-48,Serial4 --> edge-52,Serial0*)
  77-42; (*aggregation-42,Serial4 --> edge-44,Serial2*)
  15-26; (*aggregation-74,Serial7 --> edge-79,Serial2*)
  15-33; (*aggregation-74,Serial4 --> edge-76,Serial2*)
  75-64; (*aggregation-48,Serial7 --> edge-55,Serial0*)
  30-72; (*aggregation-67,Serial5 --> core-13,Serial1*)
  46-35; (*core-2,Serial3 --> aggregation-64,Serial2*)
  8-62; (*edge-62,Serial3 --> aggregation-59,Serial6*)
  0-34; (*edge-28,Serial1 --> aggregation-25,Serial4*)
  46-7; (*core-2,Serial7 --> aggregation-72,Serial2*)
  77-45; (*aggregation-42,Serial5 --> edge-45,Serial2*)
  63-57; (*aggregation-58,Serial0 --> core-8,Serial7*)
  70-9; (*core-15,Serial2 --> aggregation-35,Serial7*)
  16-60; (*edge-60,Serial1 --> aggregation-57,Serial4*)
  78-42; (*aggregation-41,Serial4 --> edge-44,Serial1*)
  78-51; (*aggregation-41,Serial7 --> edge-47,Serial1*)
  39-52; (*aggregation-65,Serial0 --> core-4,Serial1*)
  11-57; (*aggregation-34,Serial4 --> core-8,Serial0*)
  44-49; (*aggregation-17,Serial1 --> core-5,Serial4*)
  38-37; (*aggregation-66,Serial3 --> edge-71,Serial2*)
  44-3; (*aggregation-17,Serial7 --> edge-23,Serial1*)
  52-60; (*core-4,Serial7 --> aggregation-57,Serial0*)
  69-46; (*aggregation-40,Serial2 --> core-2,Serial2*)
  73-65; (*edge-52,Serial1 --> aggregation-49,Serial4*)
  64-65; (*edge-55,Serial1 --> aggregation-49,Serial7*)
  79-51; (*aggregation-43,Serial7 --> edge-47,Serial3*)
  78-45; (*aggregation-41,Serial5 --> edge-45,Serial1*)
  18-62; (*edge-61,Serial3 --> aggregation-59,Serial5*)
  57-59; (*core-8,Serial4 --> aggregation-50,Serial0*)
  6-23; (*aggregation-73,Serial5 --> edge-77,Serial1*)
  63-68; (*aggregation-58,Serial2 --> core-10,Serial7*)
  7-33; (*aggregation-72,Serial4 --> edge-76,Serial0*)
  2-44; (*edge-22,Serial1 --> aggregation-17,Serial6*)
  43-47; (*core-3,Serial4 --> aggregation-16,Serial3*)
  30-66; (*aggregation-67,Serial4 --> core-12,Serial1*)
  35-46; (*aggregation-64,Serial2 --> core-2,Serial3*)
  68-63; (*core-10,Serial7 --> aggregation-58,Serial2*)
  76-65; (*edge-54,Serial1 --> aggregation-49,Serial6*)
  69-45; (*aggregation-40,Serial5 --> edge-45,Serial0*)
  14-26; (*aggregation-75,Serial7 --> edge-79,Serial3*)
  43-35; (*core-3,Serial3 --> aggregation-64,Serial3*)
  13-44; (*edge-21,Serial1 --> aggregation-17,Serial5*)
  7-23; (*aggregation-72,Serial5 --> edge-77,Serial0*)
  46-47; (*core-2,Serial4 --> aggregation-16,Serial2*)
  77-51; (*aggregation-42,Serial7 --> edge-47,Serial2*)
  25-70; (*aggregation-27,Serial3 --> core-15,Serial7*)
  29-25; (*edge-30,Serial3 --> aggregation-27,Serial6*)
  72-25; (*core-13,Serial7 --> aggregation-27,Serial1*)
  4-39; (*edge-68,Serial1 --> aggregation-65,Serial5*)
  71-50; (*core-14,Serial6 --> aggregation-19,Serial2*)
  50-71; (*aggregation-19,Serial2 --> core-14,Serial6*)
  61-43; (*aggregation-56,Serial3 --> core-3,Serial5*)
  69-41; (*aggregation-40,Serial0 --> core-0,Serial2*)
  38-4; (*aggregation-66,Serial0 --> edge-68,Serial2*)
  38-56; (*aggregation-66,Serial5 --> core-9,Serial1*)
  47-12; (*aggregation-16,Serial4 --> edge-20,Serial0*)
  48-78; (*edge-46,Serial1 --> aggregation-41,Serial6*)
  49-34; (*core-5,Serial6 --> aggregation-25,Serial1*)
  9-70; (*aggregation-35,Serial7 --> core-15,Serial2*)
  13-53; (*edge-21,Serial2 --> aggregation-18,Serial5*)
  69-42; (*aggregation-40,Serial4 --> edge-44,Serial0*)
  62-8; (*aggregation-59,Serial6 --> edge-62,Serial3*)
  69-51; (*aggregation-40,Serial7 --> edge-47,Serial0*)
  71-9; (*core-14,Serial2 --> aggregation-35,Serial6*)
  33-6; (*edge-76,Serial1 --> aggregation-73,Serial4*)
  67-53; (*core-11,Serial3 --> aggregation-18,Serial3*)
  12-53; (*edge-20,Serial2 --> aggregation-18,Serial4*)
  41-7; (*core-0,Serial7 --> aggregation-72,Serial0*)
  66-25; (*core-12,Serial7 --> aggregation-27,Serial0*)
  9-27; (*aggregation-35,Serial0 --> edge-36,Serial3*)
  72-9; (*core-13,Serial2 --> aggregation-35,Serial5*)
  38-67; (*aggregation-66,Serial7 --> core-11,Serial1*)
  23-6; (*edge-77,Serial1 --> aggregation-73,Serial5*)
  40-7; (*core-1,Serial7 --> aggregation-72,Serial1*)
  31-25; (*edge-31,Serial3 --> aggregation-27,Serial7*)
  68-53; (*core-10,Serial3 --> aggregation-18,Serial2*)
  30-4; (*aggregation-67,Serial0 --> edge-68,Serial3*)
  43-61; (*core-3,Serial5 --> aggregation-56,Serial3*)
  35-36; (*aggregation-64,Serial6 --> edge-70,Serial0*)
  66-9; (*core-12,Serial2 --> aggregation-35,Serial4*)
  34-52; (*aggregation-25,Serial0 --> core-4,Serial6*)
  27-11; (*edge-36,Serial2 --> aggregation-34,Serial0*)
  63-8; (*aggregation-58,Serial6 --> edge-62,Serial2*)
  10-60; (*edge-63,Serial1 --> aggregation-57,Serial7*)
  71-25; (*core-14,Serial7 --> aggregation-27,Serial2*)
  44-12; (*aggregation-17,Serial4 --> edge-20,Serial1*)
  70-50; (*core-15,Serial6 --> aggregation-19,Serial3*)
  47-46; (*aggregation-16,Serial2 --> core-2,Serial4*)
  66-50; (*core-12,Serial6 --> aggregation-19,Serial0*)
  70-25; (*core-15,Serial7 --> aggregation-27,Serial3*)
  71-14; (*core-14,Serial5 --> aggregation-75,Serial6*)
  19-22; (*aggregation-32,Serial7 --> edge-39,Serial0*)
  53-12; (*aggregation-18,Serial4 --> edge-20,Serial2*)
  28-43; (*aggregation-24,Serial3 --> core-3,Serial6*)
  72-50; (*core-13,Serial6 --> aggregation-19,Serial1*)
  68-77; (*core-10,Serial2 --> aggregation-42,Serial2*)
  58-76; (*aggregation-51,Serial6 --> edge-54,Serial3*)
  60-8; (*aggregation-57,Serial6 --> edge-62,Serial1*)
  62-66; (*aggregation-59,Serial0 --> core-12,Serial4*)
  6-49; (*aggregation-73,Serial1 --> core-5,Serial5*)
  52-6; (*core-4,Serial5 --> aggregation-73,Serial0*)
  67-77; (*core-11,Serial2 --> aggregation-42,Serial3*)
  32-29; (*aggregation-26,Serial6 --> edge-30,Serial2*)
  28-1; (*aggregation-24,Serial5 --> edge-29,Serial0*)
  55-39; (*core-6,Serial1 --> aggregation-65,Serial6*)
  20-17; (*edge-37,Serial1 --> aggregation-33,Serial7*)
  9-21; (*aggregation-35,Serial2 --> edge-38,Serial3*)
  75-41; (*aggregation-48,Serial0 --> core-0,Serial1*)
  58-72; (*aggregation-51,Serial1 --> core-13,Serial0*)
  20-11; (*edge-37,Serial2 --> aggregation-34,Serial1*)
  79-71; (*aggregation-43,Serial6 --> core-14,Serial3*)
  26-6; (*edge-79,Serial1 --> aggregation-73,Serial7*)
  45-79; (*edge-45,Serial3 --> aggregation-43,Serial5*)
  7-43; (*aggregation-72,Serial3 --> core-3,Serial7*)
  68-38; (*core-10,Serial1 --> aggregation-66,Serial6*)
  25-29; (*aggregation-27,Serial6 --> edge-30,Serial3*)
  21-11; (*edge-38,Serial2 --> aggregation-34,Serial2*)
  60-52; (*aggregation-57,Serial0 --> core-4,Serial7*)
  11-27; (*aggregation-34,Serial0 --> edge-36,Serial2*)
  14-24; (*aggregation-75,Serial4 --> edge-78,Serial3*)
  54-34; (*core-7,Serial6 --> aggregation-25,Serial3*)
  42-78; (*edge-44,Serial1 --> aggregation-41,Serial4*)
  17-49; (*aggregation-33,Serial1 --> core-5,Serial0*)
  45-78; (*edge-45,Serial1 --> aggregation-41,Serial5*)
  55-34; (*core-6,Serial6 --> aggregation-25,Serial2*)
  62-16; (*aggregation-59,Serial4 --> edge-60,Serial3*)
  64-58; (*edge-55,Serial3 --> aggregation-51,Serial7*)
  24-6; (*edge-78,Serial1 --> aggregation-73,Serial6*)
  78-55; (*aggregation-41,Serial2 --> core-6,Serial3*)
  27-17; (*edge-36,Serial1 --> aggregation-33,Serial5*)
  67-38; (*core-11,Serial1 --> aggregation-66,Serial7*)
  32-57; (*aggregation-26,Serial0 --> core-8,Serial6*)
  60-55; (*aggregation-57,Serial2 --> core-6,Serial7*)
  49-6; (*core-5,Serial5 --> aggregation-73,Serial1*)
  17-20; (*aggregation-33,Serial7 --> edge-37,Serial1*)
  37-38; (*edge-71,Serial2 --> aggregation-66,Serial3*)
  73-58; (*edge-52,Serial3 --> aggregation-51,Serial4*)
  57-63; (*core-8,Serial7 --> aggregation-58,Serial0*)
  51-78; (*edge-47,Serial1 --> aggregation-41,Serial7*)
  52-34; (*core-4,Serial6 --> aggregation-25,Serial0*)
  74-58; (*edge-53,Serial3 --> aggregation-51,Serial5*)
  68-32; (*core-10,Serial6 --> aggregation-26,Serial2*)
  39-5; (*aggregation-65,Serial7 --> edge-69,Serial1*)
  11-21; (*aggregation-34,Serial2 --> edge-38,Serial2*)
  67-15; (*core-11,Serial5 --> aggregation-74,Serial3*)
  14-70; (*aggregation-75,Serial0 --> core-15,Serial5*)
  56-63; (*core-9,Serial7 --> aggregation-58,Serial1*)
  32-68; (*aggregation-26,Serial2 --> core-10,Serial6*)
  76-58; (*edge-54,Serial3 --> aggregation-51,Serial6*)
  67-32; (*core-11,Serial6 --> aggregation-26,Serial3*)
  7-40; (*aggregation-72,Serial1 --> core-1,Serial7*)
  17-21; (*aggregation-33,Serial2 --> edge-38,Serial1*)
  39-49; (*aggregation-65,Serial1 --> core-5,Serial1*)
  53-2; (*aggregation-18,Serial6 --> edge-22,Serial2*)
  15-67; (*aggregation-74,Serial3 --> core-11,Serial5*)
  75-76; (*aggregation-48,Serial6 --> edge-54,Serial0*)
  28-40; (*aggregation-24,Serial1 --> core-1,Serial6*)
  41-69; (*core-0,Serial2 --> aggregation-40,Serial0*)
  77-68; (*aggregation-42,Serial2 --> core-10,Serial2*)
  7-24; (*aggregation-72,Serial6 --> edge-78,Serial0*)
  55-17; (*core-6,Serial0 --> aggregation-33,Serial6*)
  52-78; (*core-4,Serial3 --> aggregation-41,Serial0*)
  42-77; (*edge-44,Serial2 --> aggregation-42,Serial4*)
  30-71; (*aggregation-67,Serial6 --> core-14,Serial1*)
  66-79; (*core-12,Serial3 --> aggregation-43,Serial2*)
  33-14; (*edge-76,Serial3 --> aggregation-75,Serial1*)
  54-44; (*core-7,Serial4 --> aggregation-17,Serial3*)
  62-71; (*aggregation-59,Serial2 --> core-14,Serial4*)
  65-76; (*aggregation-49,Serial6 --> edge-54,Serial1*)
  15-56; (*aggregation-74,Serial1 --> core-9,Serial5*)
  78-52; (*aggregation-41,Serial0 --> core-4,Serial3*)
  45-77; (*edge-45,Serial2 --> aggregation-42,Serial5*)
  10-63; (*edge-63,Serial2 --> aggregation-58,Serial7*)
  12-50; (*edge-20,Serial3 --> aggregation-19,Serial4*)
  72-79; (*core-13,Serial3 --> aggregation-43,Serial3*)
  44-2; (*aggregation-17,Serial6 --> edge-22,Serial1*)
  68-11; (*core-10,Serial0 --> aggregation-34,Serial6*)
  8-63; (*edge-62,Serial2 --> aggregation-58,Serial6*)
  55-44; (*core-6,Serial4 --> aggregation-17,Serial2*)
  34-29; (*aggregation-25,Serial6 --> edge-30,Serial1*)
  28-0; (*aggregation-24,Serial4 --> edge-28,Serial0*)
  39-54; (*aggregation-65,Serial4 --> core-7,Serial1*)
  5-39; (*edge-69,Serial1 --> aggregation-65,Serial7*)
  49-44; (*core-5,Serial4 --> aggregation-17,Serial1*)
  50-2; (*aggregation-19,Serial6 --> edge-22,Serial3*)
  33-15; (*edge-76,Serial2 --> aggregation-74,Serial4*)
  31-32; (*edge-31,Serial2 --> aggregation-26,Serial7*)
  47-41; (*aggregation-16,Serial0 --> core-0,Serial4*)
  17-55; (*aggregation-33,Serial6 --> core-6,Serial0*)
  47-43; (*aggregation-16,Serial3 --> core-3,Serial4*)
  59-67; (*aggregation-50,Serial3 --> core-11,Serial4*)
  50-66; (*aggregation-19,Serial0 --> core-12,Serial6*)
  50-70; (*aggregation-19,Serial3 --> core-15,Serial6*)
  61-8; (*aggregation-56,Serial6 --> edge-62,Serial0*)
  49-78; (*core-5,Serial3 --> aggregation-41,Serial1*)
  14-72; (*aggregation-75,Serial3 --> core-13,Serial5*)
  28-29; (*aggregation-24,Serial6 --> edge-30,Serial0*)
  34-0; (*aggregation-25,Serial4 --> edge-28,Serial1*)
  55-78; (*core-6,Serial3 --> aggregation-41,Serial2*)
  77-57; (*aggregation-42,Serial0 --> core-8,Serial2*)
  59-56; (*aggregation-50,Serial1 --> core-9,Serial4*)
  73-75; (*edge-52,Serial0 --> aggregation-48,Serial4*)
  35-40; (*aggregation-64,Serial1 --> core-1,Serial3*)
  25-72; (*aggregation-27,Serial1 --> core-13,Serial7*)
  11-56; (*aggregation-34,Serial5 --> core-9,Serial0*)
  48-79; (*edge-46,Serial3 --> aggregation-43,Serial4*)
  44-55; (*aggregation-17,Serial2 --> core-6,Serial4*)
  2-47; (*edge-22,Serial0 --> aggregation-16,Serial6*)
  2-50; (*edge-22,Serial3 --> aggregation-19,Serial6*)
  59-76; (*aggregation-50,Serial6 --> edge-54,Serial2*)
  70-30; (*core-15,Serial1 --> aggregation-67,Serial7*)
  56-15; (*core-9,Serial5 --> aggregation-74,Serial1*)
  16-63; (*edge-60,Serial2 --> aggregation-58,Serial4*)
  32-0; (*aggregation-26,Serial4 --> edge-28,Serial2*)
  40-61; (*core-1,Serial5 --> aggregation-56,Serial1*)
  43-69; (*core-3,Serial2 --> aggregation-40,Serial3*)
  13-50; (*edge-21,Serial3 --> aggregation-19,Serial5*)
  65-54; (*aggregation-49,Serial3 --> core-7,Serial2*)
  47-2; (*aggregation-16,Serial6 --> edge-22,Serial0*)
  67-11; (*core-11,Serial0 --> aggregation-34,Serial7*)
  0-28; (*edge-28,Serial0 --> aggregation-24,Serial4*)
  19-27; (*aggregation-32,Serial4 --> edge-36,Serial0*)
  29-32; (*edge-30,Serial2 --> aggregation-26,Serial6*)
  11-67; (*aggregation-34,Serial7 --> core-11,Serial0*)
  1-28; (*edge-29,Serial0 --> aggregation-24,Serial5*)
  69-40; (*aggregation-40,Serial1 --> core-1,Serial2*)
  34-49; (*aggregation-25,Serial1 --> core-5,Serial6*)
  19-41; (*aggregation-32,Serial0 --> core-0,Serial0*)
  61-40; (*aggregation-56,Serial1 --> core-1,Serial5*)
  65-55; (*aggregation-49,Serial2 --> core-6,Serial2*)
  9-71; (*aggregation-35,Serial6 --> core-14,Serial2*)
  79-66; (*aggregation-43,Serial2 --> core-12,Serial3*)
  3-47; (*edge-23,Serial0 --> aggregation-16,Serial7*)
  69-48; (*aggregation-40,Serial6 --> edge-46,Serial0*)
  6-24; (*aggregation-73,Serial6 --> edge-78,Serial1*)
  3-50; (*edge-23,Serial3 --> aggregation-19,Serial7*)
  46-61; (*core-2,Serial5 --> aggregation-56,Serial2*)
  34-54; (*aggregation-25,Serial3 --> core-7,Serial6*)
  18-63; (*edge-61,Serial2 --> aggregation-58,Serial5*)
  57-15; (*core-8,Serial5 --> aggregation-74,Serial0*)
  1-32; (*edge-29,Serial2 --> aggregation-26,Serial5*)
  14-23; (*aggregation-75,Serial5 --> edge-77,Serial3*)
  19-46; (*aggregation-32,Serial2 --> core-2,Serial0*)
  41-47; (*core-0,Serial4 --> aggregation-16,Serial0*)
  57-32; (*core-8,Serial6 --> aggregation-26,Serial0*)
  35-43; (*aggregation-64,Serial3 --> core-3,Serial3*)
  35-41; (*aggregation-64,Serial0 --> core-0,Serial3*)
  39-36; (*aggregation-65,Serial2 --> edge-70,Serial1*)
  0-25; (*edge-28,Serial3 --> aggregation-27,Serial4*)
  13-47; (*edge-21,Serial0 --> aggregation-16,Serial5*)
  25-71; (*aggregation-27,Serial2 --> core-14,Serial7*)
  65-52; (*aggregation-49,Serial0 --> core-4,Serial2*)
  76-75; (*edge-54,Serial0 --> aggregation-48,Serial6*)
  77-48; (*aggregation-42,Serial6 --> edge-46,Serial2*)
  3-53; (*edge-23,Serial2 --> aggregation-18,Serial7*)
  56-32; (*core-9,Serial6 --> aggregation-26,Serial1*)
  46-69; (*core-2,Serial2 --> aggregation-40,Serial2*)
  78-48; (*aggregation-41,Serial6 --> edge-46,Serial1*)
  1-25; (*edge-29,Serial3 --> aggregation-27,Serial5*)
  44-54; (*aggregation-17,Serial3 --> core-7,Serial4*)
  74-75; (*edge-53,Serial0 --> aggregation-48,Serial5*)
  19-43; (*aggregation-32,Serial3 --> core-3,Serial0*)
  15-24; (*aggregation-74,Serial6 --> edge-78,Serial2*)
  41-61; (*core-0,Serial5 --> aggregation-56,Serial0*)
  56-59; (*core-9,Serial4 --> aggregation-50,Serial1*)
  16-62; (*edge-60,Serial3 --> aggregation-59,Serial4*)
  2-53; (*edge-22,Serial2 --> aggregation-18,Serial6*)
  40-69; (*core-1,Serial2 --> aggregation-40,Serial1*)
  0-32; (*edge-28,Serial2 --> aggregation-26,Serial4*)
  12-47; (*edge-20,Serial0 --> aggregation-16,Serial4*)
  30-36; (*aggregation-67,Serial2 --> edge-70,Serial3*)
  20-19; (*edge-37,Serial0 --> aggregation-32,Serial5*)
  29-28; (*edge-30,Serial0 --> aggregation-24,Serial6*)
  71-58; (*core-14,Serial0 --> aggregation-51,Serial2*)
  32-67; (*aggregation-26,Serial3 --> core-11,Serial6*)
  47-3; (*aggregation-16,Serial7 --> edge-23,Serial0*)
  49-65; (*core-5,Serial2 --> aggregation-49,Serial1*)
  61-41; (*aggregation-56,Serial0 --> core-0,Serial5*)
  37-39; (*edge-71,Serial1 --> aggregation-65,Serial3*)
  69-43; (*aggregation-40,Serial3 --> core-3,Serial2*)
  40-47; (*core-1,Serial4 --> aggregation-16,Serial1*)
  66-30; (*core-12,Serial1 --> aggregation-67,Serial4*)
  66-62; (*core-12,Serial4 --> aggregation-59,Serial0*)
  9-66; (*aggregation-35,Serial4 --> core-12,Serial2*)
  64-75; (*edge-55,Serial0 --> aggregation-48,Serial7*)
  21-9; (*edge-38,Serial3 --> aggregation-35,Serial2*)
  54-39; (*core-7,Serial1 --> aggregation-65,Serial4*)
  15-23; (*aggregation-74,Serial5 --> edge-77,Serial2*)
  64-59; (*edge-55,Serial2 --> aggregation-50,Serial7*)
  56-38; (*core-9,Serial1 --> aggregation-66,Serial5*)
  40-75; (*core-1,Serial1 --> aggregation-48,Serial1*)
  18-61; (*edge-61,Serial0 --> aggregation-56,Serial5*)
  32-56; (*aggregation-26,Serial1 --> core-9,Serial6*)
  22-9; (*edge-39,Serial3 --> aggregation-35,Serial3*)
  41-75; (*core-0,Serial1 --> aggregation-48,Serial0*)
  38-36; (*aggregation-66,Serial2 --> edge-70,Serial2*)
  16-61; (*edge-60,Serial0 --> aggregation-56,Serial4*)
  44-52; (*aggregation-17,Serial0 --> core-4,Serial4*)
  75-40; (*aggregation-48,Serial1 --> core-1,Serial1*)
  55-65; (*core-6,Serial2 --> aggregation-49,Serial2*)
  79-45; (*aggregation-43,Serial5 --> edge-45,Serial3*)
  24-14; (*edge-78,Serial3 --> aggregation-75,Serial4*)
  57-38; (*core-8,Serial1 --> aggregation-66,Serial4*)
  35-4; (*aggregation-64,Serial4 --> edge-68,Serial0*)
  65-74; (*aggregation-49,Serial5 --> edge-53,Serial1*)
  53-68; (*aggregation-18,Serial2 --> core-10,Serial3*)
  14-66; (*aggregation-75,Serial2 --> core-12,Serial5*)
  28-46; (*aggregation-24,Serial2 --> core-2,Serial6*)
  20-9; (*edge-37,Serial3 --> aggregation-35,Serial1*)
  25-0; (*aggregation-27,Serial4 --> edge-28,Serial3*)
  60-18; (*aggregation-57,Serial5 --> edge-61,Serial1*)
  15-57; (*aggregation-74,Serial0 --> core-8,Serial5*)
  57-53; (*core-8,Serial3 --> aggregation-18,Serial0*)
  41-19; (*core-0,Serial0 --> aggregation-32,Serial0*)
  6-55; (*aggregation-73,Serial2 --> core-6,Serial5*)
  62-72; (*aggregation-59,Serial1 --> core-13,Serial4*)
  46-75; (*core-2,Serial1 --> aggregation-48,Serial2*)
  8-61; (*edge-62,Serial0 --> aggregation-56,Serial6*)
  79-70; (*aggregation-43,Serial0 --> core-15,Serial3*)
  70-14; (*core-15,Serial5 --> aggregation-75,Serial0*)
  43-19; (*core-3,Serial0 --> aggregation-32,Serial3*)
  52-65; (*core-4,Serial2 --> aggregation-49,Serial0*)
  19-21; (*aggregation-32,Serial6 --> edge-38,Serial0*)
  21-19; (*edge-38,Serial0 --> aggregation-32,Serial6*)
  53-13; (*aggregation-18,Serial5 --> edge-21,Serial2*)
  72-62; (*core-13,Serial4 --> aggregation-59,Serial1*)
  58-71; (*aggregation-51,Serial2 --> core-14,Serial0*)
  53-57; (*aggregation-18,Serial0 --> core-8,Serial3*)
  15-68; (*aggregation-74,Serial2 --> core-10,Serial5*)
  27-9; (*edge-36,Serial3 --> aggregation-35,Serial0*)
  77-67; (*aggregation-42,Serial3 --> core-11,Serial2*)
  59-57; (*aggregation-50,Serial0 --> core-8,Serial4*)
  4-30; (*edge-68,Serial3 --> aggregation-67,Serial0*)
  43-28; (*core-3,Serial6 --> aggregation-24,Serial3*)
  71-30; (*core-14,Serial1 --> aggregation-67,Serial6*)
  46-19; (*core-2,Serial0 --> aggregation-32,Serial2*)
  4-35; (*edge-68,Serial0 --> aggregation-64,Serial4*)
  50-13; (*aggregation-19,Serial5 --> edge-21,Serial3*)
  58-74; (*aggregation-51,Serial5 --> edge-53,Serial3*)
  27-19; (*edge-36,Serial0 --> aggregation-32,Serial4*)
  31-28; (*edge-31,Serial0 --> aggregation-24,Serial7*)
  10-61; (*edge-63,Serial0 --> aggregation-56,Serial7*)
  59-74; (*aggregation-50,Serial5 --> edge-53,Serial2*)
  61-18; (*aggregation-56,Serial5 --> edge-61,Serial0*)
  5-30; (*edge-69,Serial3 --> aggregation-67,Serial1*)
  72-30; (*core-13,Serial1 --> aggregation-67,Serial5*)
  40-19; (*core-1,Serial0 --> aggregation-32,Serial1*)
  5-35; (*edge-69,Serial0 --> aggregation-64,Serial5*)
  17-22; (*aggregation-33,Serial3 --> edge-39,Serial1*)
  30-5; (*aggregation-67,Serial1 --> edge-69,Serial3*)
  57-11; (*core-8,Serial0 --> aggregation-34,Serial4*)
  22-17; (*edge-39,Serial1 --> aggregation-33,Serial3*)
  74-59; (*edge-53,Serial2 --> aggregation-50,Serial5*)
  36-30; (*edge-70,Serial3 --> aggregation-67,Serial2*)
  38-68; (*aggregation-66,Serial6 --> core-10,Serial1*)
  54-78; (*core-7,Serial3 --> aggregation-41,Serial3*)
  77-56; (*aggregation-42,Serial1 --> core-9,Serial2*)
  51-77; (*edge-47,Serial2 --> aggregation-42,Serial7*)
  63-18; (*aggregation-58,Serial5 --> edge-61,Serial2*)
  52-44; (*core-4,Serial4 --> aggregation-17,Serial0*)
  45-69; (*edge-45,Serial0 --> aggregation-40,Serial5*)
  75-43; (*aggregation-48,Serial3 --> core-3,Serial1*)
  79-42; (*aggregation-43,Serial1 --> edge-44,Serial3*)
  54-17; (*core-7,Serial0 --> aggregation-33,Serial4*)
  23-15; (*edge-77,Serial2 --> aggregation-74,Serial5*)
  14-71; (*aggregation-75,Serial6 --> core-14,Serial5*)
  38-57; (*aggregation-66,Serial4 --> core-8,Serial1*)
  22-19; (*edge-39,Serial0 --> aggregation-32,Serial7*)
  71-79; (*core-14,Serial3 --> aggregation-43,Serial6*)
  24-15; (*edge-78,Serial2 --> aggregation-74,Serial6*)
  37-30; (*edge-71,Serial3 --> aggregation-67,Serial3*)
  21-17; (*edge-38,Serial1 --> aggregation-33,Serial2*)
  42-79; (*edge-44,Serial3 --> aggregation-43,Serial1*)
  62-18; (*aggregation-59,Serial5 --> edge-61,Serial3*)
  56-11; (*core-9,Serial0 --> aggregation-34,Serial5*)
  36-35; (*edge-70,Serial0 --> aggregation-64,Serial6*)
  78-49; (*aggregation-41,Serial1 --> core-5,Serial3*)
  62-70; (*aggregation-59,Serial3 --> core-15,Serial4*)
  14-33; (*aggregation-75,Serial1 --> edge-76,Serial3*)
  33-7; (*edge-76,Serial0 --> aggregation-72,Serial4*)
  26-15; (*edge-79,Serial2 --> aggregation-74,Serial7*)
  42-69; (*edge-44,Serial0 --> aggregation-40,Serial4*)
  48-77; (*edge-46,Serial2 --> aggregation-42,Serial6*)
  23-7; (*edge-77,Serial0 --> aggregation-72,Serial5*)
  67-59; (*core-11,Serial4 --> aggregation-50,Serial3*)
  7-46; (*aggregation-72,Serial2 --> core-2,Serial7*)
  38-5; (*aggregation-66,Serial1 --> edge-69,Serial2*)
  70-58; (*core-15,Serial0 --> aggregation-51,Serial3*)
  17-52; (*aggregation-33,Serial0 --> core-4,Serial0*)
  54-65; (*core-7,Serial2 --> aggregation-49,Serial3*)
  76-59; (*edge-54,Serial2 --> aggregation-50,Serial6*)
  36-39; (*edge-70,Serial1 --> aggregation-65,Serial2*)
  66-58; (*core-12,Serial0 --> aggregation-51,Serial0*)
  78-54; (*aggregation-41,Serial3 --> core-7,Serial3*)
  7-41; (*aggregation-72,Serial0 --> core-0,Serial7*)
  26-7; (*edge-79,Serial0 --> aggregation-72,Serial7*)
  60-54; (*aggregation-57,Serial3 --> core-7,Serial7*)
  48-69; (*edge-46,Serial0 --> aggregation-40,Serial6*)
  9-22; (*aggregation-35,Serial3 --> edge-39,Serial3*)
  73-59; (*edge-52,Serial2 --> aggregation-50,Serial4*)
  72-58; (*core-13,Serial0 --> aggregation-51,Serial1*)
  68-59; (*core-10,Serial4 --> aggregation-50,Serial2*)
  11-22; (*aggregation-34,Serial3 --> edge-39,Serial2*)
  51-69; (*edge-47,Serial0 --> aggregation-40,Serial7*)
}

let nodes = 80

let ospfIntraArea = 0u2
let ospfInterArea = 1u2
let ospfE1 = 2u2
let ospfE2 = 3u2

let protoConn = 0u8
let protoStatic = 1u8
let protoOspf = 2u8
let protoBgp = 3u8

let isProtocol fib x = fib = x
let min x y = x <u8 y

let pickOption f x y =
  match (x,y) with
  | (None, _) -> false  | (_, None) -> true
  | (Some a, Some b) -> f a b

let betterOspf o1 o2 =
  if o1.areaType >u2 o2.areaType then true
  else if o2.areaType >u2 o1.areaType then false
  else if o1.weight <=u16 o2.weight then true else false

let betterBgp b1 b2 =
  if b1.lp > b2.lp then true
  else if b2.lp > b1.lp then false
  else if b1.aslen < b2.aslen then true
  else if b2.aslen < b1.aslen then false
  else if b1.med >= b2.med then true else false

let betterEqOption o1 o2 =
  match (o1,o2) with
  | (_, None) -> true
  | (None, _) -> false
  | (Some a, Some b) -> a <=u8 b

let best c s o b =
  match (c,s,o,b) with
  | (None,None,None,None) -> None
  | _ ->
      let o = match o with | None -> None | Some o -> Some o.ospfAd in
      let b = match b with | None -> None | Some b -> Some b.bgpAd in
      let (x,p1) = if betterEqOption c s then (c,0u2) else (s,1u2) in
      let (y,p2) = if betterEqOption o b then (o,2u2) else (b,3u2) in
      Some (if betterEqOption x y then p1 else p2)

let mergeValues x y =
  let c = if (pickOption min x.connected y.connected) then x.connected else y.connected in
  let s = if (pickOption min x.static y.static) then x.static else y.static in
  let o = if (pickOption betterOspf x.ospf y.ospf) then x.ospf else y.ospf in
  let b = if (pickOption betterBgp x.bgp y.bgp) then x.bgp else y.bgp in
  { connected = c;
    static = s;
    ospf = o;
    bgp = b;
    selected = best c s o b}

let merge node x y = mergeValues x y

let init node =
  match node with
  | 0n ->
if (d = (167778304, 31u6)) || (d = (167777280, 31u6)) || (d = (1174412288, 32u6)) || (d = (167779328, 31u6)) || (d = (1174412288, 31u6)) || (d = (167776256, 31u6)) || (d = (1174412290, 31u6)) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if (d = (1174412288, 24u6)) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 1n ->
if (d = (167777536, 31u6)) || (d = (167776512, 31u6)) || (d = (1174412544, 32u6)) || (d = (167779584, 31u6)) || (d = (167778560, 31u6)) || (d = (1174412544, 31u6)) || (d = (1174412546, 31u6)) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if (d = (1174412544, 24u6)) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 2n ->
if (d = (1174410752, 31u6)) || (d = (1174410752, 32u6)) || (d = (167773696, 31u6)) || (d = (167772672, 31u6)) || (d = (1174410754, 31u6)) || (d = (167775744, 31u6)) || (d = (167774720, 31u6)) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if (d = (1174410752, 24u6)) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 3n ->
if (d = (1174411008, 31u6)) || (d = (1174411008, 32u6)) || (d = (167773952, 31u6)) || (d = (167772928, 31u6)) || (d = (1174411010, 31u6)) || (d = (167776000, 31u6)) || (d = (167774976, 31u6)) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if (d = (1
)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (1~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412544) && (prefix < 1174412800)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (1~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412544) && (prefix < 1174412800)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (1~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412544) && (prefix < 1174412800)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (1~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412544) && (prefix < 1174412800)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (2~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174410752) && (prefix < 1174411008)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (2~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174410752) && (prefix < 1174411008)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (2~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174410752) && (prefix < 1174411008)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (2~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174410752) && (prefix < 1174411008)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (3~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174411008) && (prefix < 1174411264)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (3~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174411008) && (prefix < 1174411264)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (3~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174411008) && (prefix < 1174411264)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (3~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174411008) && (prefix < 1174411264)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (4~39) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174422528) && (prefix < 1174422784)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (4~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174422528) && (prefix < 1174422784)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (4~35) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174422528) && (prefix < 1174422784)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (4~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174422528) && (prefix < 1174422784)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (5~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174422784) && (prefix < 1174423040)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (5~39) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174422784) && (prefix < 1174423040)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (5~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174422784) && (prefix < 1174423040)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (5~35) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174422784) && (prefix < 1174423040)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (6~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (6~26) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (6~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (6~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (6~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (6~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (6~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (6~33) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (7~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (7~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (7~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (7~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (7~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (7~33) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (7~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (7~26) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (8~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420992) && (prefix < 1174421248)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (8~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420992) && (prefix < 1174421248)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (8~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420992) && (prefix < 1174421248)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (8~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420992) && (prefix < 1174421248)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (9~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (9~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (9~20) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (9~21) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (9~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (9~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (9~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (9~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (10~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421248) && (prefix < 1174421504)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (10~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421248) && (prefix < 1174421504)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (10~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421248) && (prefix < 1174421504)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (10~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421248) && (prefix < 1174421504)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (11~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (11~20) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (11~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (11~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (11~21) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (11~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (11~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (11~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (12~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174410240) && (prefix < 1174410496)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (12~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174410240) && (prefix < 1174410496)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (12~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174410240) && (prefix < 1174410496)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (12~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174410240) && (prefix < 1174410496)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (13~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174410496) && (prefix < 1174410752)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (13~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174410496) && (prefix < 1174410752)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (13~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174410496) && (prefix < 1174410752)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (13~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174410496) && (prefix < 1174410752)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (14~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (14~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (14~26) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (14~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (14~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (14~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (14~33) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (14~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (15~26) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (15~33) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (15~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (15~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (15~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (15~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (15~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (15~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (16~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420480) && (prefix < 1174420736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (16~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420480) && (prefix < 1174420736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (16~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420480) && (prefix < 1174420736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (16~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420480) && (prefix < 1174420736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (17~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~21) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~20) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (18~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420736) && (prefix < 1174420992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (18~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420736) && (prefix < 1174420992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (18~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420736) && (prefix < 1174420992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (18~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420736) && (prefix < 1174420992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (19~20) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~21) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (20~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174414592) && (prefix < 1174414848)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (20~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174414592) && (prefix < 1174414848)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (20~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174414592) && (prefix < 1174414848)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (20~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174414592) && (prefix < 1174414848)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (21~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174414848) && (prefix < 1174415104)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (21~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174414848) && (prefix < 1174415104)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (21~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174414848) && (prefix < 1174415104)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (21~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174414848) && (prefix < 1174415104)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (22~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415104) && (prefix < 1174415360)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (22~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415104) && (prefix < 1174415360)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (22~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415104) && (prefix < 1174415360)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (22~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415104) && (prefix < 1174415360)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (23~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424832) && (prefix < 1174425088)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (23~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424832) && (prefix < 1174425088)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (23~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424832) && (prefix < 1174425088)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (23~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424832) && (prefix < 1174425088)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (24~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425088) && (prefix < 1174425344)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (24~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425088) && (prefix < 1174425344)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (24~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425088) && (prefix < 1174425344)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (24~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425088) && (prefix < 1174425344)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (25~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~31) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~1) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~0) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (26~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425344) && (prefix < 1174425600)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (26~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425344) && (prefix < 1174425600)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (26~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425344) && (prefix < 1174425600)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (26~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425344) && (prefix < 1174425600)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (27~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174414336) && (prefix < 1174414592)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (27~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174414336) && (prefix < 1174414592)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (27~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174414336) && (prefix < 1174414592)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (27~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174414336) && (prefix < 1174414592)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (28~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~1) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~0) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~31) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (29~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412800) && (prefix < 1174413056)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (29~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412800) && (prefix < 1174413056)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (29~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412800) && (prefix < 1174413056)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (29~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412800) && (prefix < 1174413056)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (30~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~37) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (31~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413056) && (prefix < 1174413312)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (31~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413056) && (prefix < 1174413312)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (31~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413056) && (prefix < 1174413312)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (31~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413056) && (prefix < 1174413312)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (32~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~1) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~31) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~0) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (33~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424576) && (prefix < 1174424832)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (33~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424576) && (prefix < 1174424832)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (33~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424576) && (prefix < 1174424832)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (33~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424576) && (prefix < 1174424832)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (34~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~1) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~0) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~31) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (35~37) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (35~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (35~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (35~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (35~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (35~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (35~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (35~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (36~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423040) && (prefix < 1174423296)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (36~39) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423040) && (prefix < 1174423296)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (36~35) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423040) && (prefix < 1174423296)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (36~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423040) && (prefix < 1174423296)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (37~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423296) && (prefix < 1174423552)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (37~35) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423296) && (prefix < 1174423552)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (37~39) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423296) && (prefix < 1174423552)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (37~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423296) && (prefix < 1174423552)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (38~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~37) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (39~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (39~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (39~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (39~37) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (39~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (39~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (39~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (39~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~35) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~35) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (42~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416384) && (prefix < 1174416640)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (42~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416384) && (prefix < 1174416640)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (42~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416384) && (prefix < 1174416640)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (42~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416384) && (prefix < 1174416640)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (43~35) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~2) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~12) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~3) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~13) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (45~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416640) && (prefix < 1174416896)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (45~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416640) && (prefix < 1174416896)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (45~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416640) && (prefix < 1174416896)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (45~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416640) && (prefix < 1174416896)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (46~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~35) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~13) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~2) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~3) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~12) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (48~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416896) && (prefix < 1174417152)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (48~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416896) && (prefix < 1174417152)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (48~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416896) && (prefix < 1174417152)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (48~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416896) && (prefix < 1174417152)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (49~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~39) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (50~2) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (50~3) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (50~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (50~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (50~12) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (50~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (50~13) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (50~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (51~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174417152) && (prefix < 1174417408)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (51~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174417152) && (prefix < 1174417408)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (51~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174417152) && (prefix < 1174417408)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (51~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174417152) && (prefix < 1174417408)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (52~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (52~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (52~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (52~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (52~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (52~39) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (52~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (52~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~2) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~3) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~12) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~13) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (54~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (54~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (54~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (54~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (54~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (54~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (54~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (54~39) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~39) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (57~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (57~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (57~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (57~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (57~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (57~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (57~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (57~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~64) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~74) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (59~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (59~64) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (59~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (59~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (59~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (59~74) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (59~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (59~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (60~10) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (60~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (60~16) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (60~18) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (60~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (60~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (60~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (60~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~16) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~10) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~18) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~16) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~10) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~18) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (63~10) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (63~16) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (63~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (63~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (63~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (63~18) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (63~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (63~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (64~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174419200) && (prefix < 1174419456)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (64~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174419200) && (prefix < 1174419456)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (64~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174419200) && (prefix < 1174419456)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (64~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174419200) && (prefix < 1174419456)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (65~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~64) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~74) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (68~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (68~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (68~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (68~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (68~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (68~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (68~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (68~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (69~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (69~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (69~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (69~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (69~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (69~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (69~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (69~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (72~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (72~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (72~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (72~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (72~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (72~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (72~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (72~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (73~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418432) && (prefix < 1174418688)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (73~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418432) && (prefix < 1174418688)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (73~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418432) && (prefix < 1174418688)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (73~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418432) && (prefix < 1174418688)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (74~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418688) && (prefix < 1174418944)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (74~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418688) && (prefix < 1174418944)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (74~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418688) && (prefix < 1174418944)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (74~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418688) && (prefix < 1174418944)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (75~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~64) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~74) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (76~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418944) && (prefix < 1174419200)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (76~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418944) && (prefix < 1174419200)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (76~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418944) && (prefix < 1174419200)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (76~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418944) && (prefix < 1174419200)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (77~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (78~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (78~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (78~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (78~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (78~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (78~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (78~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (78~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b
)

let trans edge x =
  let o = transferOspf edge x.ospf in
  let b = transferBgp edge x in
  {connected=None; static=None; ospf=o; bgp=b; selected=None}

let assert_node node x =
  if (d = (70.0.20.0/24u6)) then
    match x.selected with
      | None -> false
      | _ -> true
  else true

(* {edge-28=0, edge-29=1, edge-22=2, edge-23=3, edge-68=4, edge-69=5,
aggregation-73=6, aggregation-72=7, edge-62=8, aggregation-35=9, edge-63=10,
aggregation-34=11, edge-20=12, edge-21=13, aggregation-75=14, aggregation-74=15,
edge-60=16, aggregation-33=17, edge-61=18, aggregation-32=19, edge-37=20,
edge-38=21, edge-39=22, edge-77=23, edge-78=24, aggregation-27=25, edge-79=26,
edge-36=27, aggregation-24=28, edge-30=29, aggregation-67=30, edge-31=31,
aggregation-26=32, edge-76=33, aggregation-25=34, aggregation-64=35, edge-70=36,
edge-71=37, aggregation-66=38, aggregation-65=39, core-1=40, core-0=41,
edge-44=42, core-3=43, aggregation-17=44, edge-45=45, core-2=46,
aggregation-16=47, edge-46=48, core-5=49, aggregation-19=50, edge-47=51,
core-4=52, aggregation-18=53, core-7=54, core-6=55, core-9=56, core-8=57,
aggregation-51=58, aggregation-50=59, aggregation-57=60, aggregation-56=61,
aggregation-59=62, aggregation-58=63, edge-55=64, aggregation-49=65, core-12=66,
core-11=67, core-10=68, aggregation-40=69, core-15=70, core-14=71, core-13=72,
edge-52=73, edge-53=74, aggregation-48=75, edge-54=76, aggregation-42=77,
aggregation-41=78, aggregation-43=79}*)

let partition node = match node with
  | 2n -> 0u8
  | 3n -> 0u8
  | 12n -> 0u8
  | 13n -> 0u8
  | 44n -> 0u8
  | 47n -> 0u8
  | 50n -> 0u8
  | 53n -> 0u8
  | 40n -> 1u8
  | 41n -> 1u8
  | 43n -> 1u8
  | 46n -> 1u8
  | 49n -> 1u8
  | 52n -> 1u8
  | 54n -> 1u8
  | 55n -> 1u8
  | 56n -> 1u8
  | 57n -> 1u8
  | 66n -> 1u8
  | 67n -> 1u8
  | 68n -> 1u8
  | 70n -> 1u8
  | 71n -> 1u8
  | 72n -> 1u8
  | 0n -> 2u8
  | 1n -> 2u8
  | 4n -> 2u8
  | 5n -> 2u8
  | 6n -> 2u8
  | 7n -> 2u8
  | 8n -> 2u8
  | 9n -> 2u8
  | 10n -> 2u8
  | 11n -> 2u8
  | 14n -> 2u8
  | 15n -> 2u8
  | 16n -> 2u8
  | 17n -> 2u8
  | 18n -> 2u8
  | 19n -> 2u8
  | 20n -> 2u8
  | 21n -> 2u8
  | 22n -> 2u8
  | 23n -> 2u8
  | 24n -> 2u8
  | 25n -> 2u8
  | 26n -> 2u8
  | 27n -> 2u8
  | 28n -> 2u8
  | 29n -> 2u8
  | 30n -> 2u8
  | 31n -> 2u8
  | 32n -> 2u8
  | 33n -> 2u8
  | 34n -> 2u8
  | 35n -> 2u8
  | 36n -> 2u8
  | 37n -> 2u8
  | 38n -> 2u8
  | 39n -> 2u8
  | 42n -> 2u8
  | 45n -> 2u8
  | 48n -> 2u8
  | 51n -> 2u8
  | 58n -> 2u8
  | 59n -> 2u8
  | 60n -> 2u8
  | 61n -> 2u8
  | 62n -> 2u8
  | 63n -> 2u8
  | 64n -> 2u8
  | 65n -> 2u8
  | 69n -> 2u8
  | 73n -> 2u8
  | 74n -> 2u8
  | 75n -> 2u8
  | 76n -> 2u8
  | 77n -> 2u8
  | 78n -> 2u8
  | 79n -> 2u8

let interface edge =
  let any x = true
    in
    let nothingButBgp x =
      match x with
      | { connected = c; static = s; ospf = o; bgp = b; selected = sel } ->
        c = None && s = None && o = None
    in
    let hasOnlyBgp x =
      match x with
      | { connected = c; static = s; ospf = o; bgp = b; selected = sel } ->
        c = None && s = None && o = None && isSome b && sel = Some 3u2
    in
    match edge with
  | 40~7 -> hasOnlyBgp
  | 40~19 -> hasOnlyBgp
  | 40~28 -> hasOnlyBgp
  | 40~35 -> hasOnlyBgp
  | 40~61 -> hasOnlyBgp
  | 40~69 -> hasOnlyBgp
  | 40~75 -> hasOnlyBgp
  | 41~7 -> hasOnlyBgp
  | 41~19 -> hasOnlyBgp
  | 41~28 -> hasOnlyBgp
  | 41~35 -> hasOnlyBgp
  | 41~61 -> hasOnlyBgp
  | 41~69 -> hasOnlyBgp
  | 41~75 -> hasOnlyBgp
  | 43~7 -> hasOnlyBgp
  | 43~19 -> hasOnlyBgp
  | 43~28 -> hasOnlyBgp
  | 43~35 -> hasOnlyBgp
  | 43~61 -> hasOnlyBgp
  | 43~69 -> hasOnlyBgp
  | 43~75 -> hasOnlyBgp
  | 44~49 -> hasOnlyBgp
  | 44~52 -> hasOnlyBgp
  | 44~54 -> hasOnlyBgp
  | 44~55 -> hasOnlyBgp
  | 46~7 -> hasOnlyBgp
  | 46~19 -> hasOnlyBgp
  | 46~28 -> hasOnlyBgp
  | 46~35 -> hasOnlyBgp
  | 46~61 -> hasOnlyBgp
  | 46~69 -> hasOnlyBgp
  | 46~75 -> hasOnlyBgp
  | 47~40 -> hasOnlyBgp
  | 47~41 -> hasOnlyBgp
  | 47~43 -> hasOnlyBgp
  | 47~46 -> hasOnlyBgp
  | 49~6 -> hasOnlyBgp
  | 49~17 -> hasOnlyBgp
  | 49~34 -> hasOnlyBgp
  | 49~39 -> hasOnlyBgp
  | 49~60 -> hasOnlyBgp
  | 49~65 -> hasOnlyBgp
  | 49~78 -> hasOnlyBgp
  | 50~66 -> hasOnlyBgp
  | 50~70 -> hasOnlyBgp
  | 50~71 -> hasOnlyBgp
  | 50~72 -> hasOnlyBgp
  | 52~6 -> hasOnlyBgp
  | 52~17 -> hasOnlyBgp
  | 52~34 -> hasOnlyBgp
  | 52~39 -> hasOnlyBgp
  | 52~60 -> hasOnlyBgp
  | 52~65 -> hasOnlyBgp
  | 52~78 -> hasOnlyBgp
  | 53~56 -> hasOnlyBgp
  | 53~57 -> hasOnlyBgp
  | 53~67 -> hasOnlyBgp
  | 53~68 -> hasOnlyBgp
  | 54~6 -> hasOnlyBgp
  | 54~17 -> hasOnlyBgp
  | 54~34 -> hasOnlyBgp
  | 54~39 -> hasOnlyBgp
  | 54~60 -> hasOnlyBgp
  | 54~65 -> hasOnlyBgp
  | 54~78 -> hasOnlyBgp
  | 55~6 -> hasOnlyBgp
  | 55~17 -> hasOnlyBgp
  | 55~34 -> hasOnlyBgp
  | 55~39 -> hasOnlyBgp
  | 55~60 -> hasOnlyBgp
  | 55~65 -> hasOnlyBgp
  | 55~78 -> hasOnlyBgp
  | 56~11 -> hasOnlyBgp
  | 56~15 -> hasOnlyBgp
  | 56~32 -> hasOnlyBgp
  | 56~38 -> hasOnlyBgp
  | 56~59 -> hasOnlyBgp
  | 56~63 -> hasOnlyBgp
  | 56~77 -> hasOnlyBgp
  | 57~11 -> hasOnlyBgp
  | 57~15 -> hasOnlyBgp
  | 57~32 -> hasOnlyBgp
  | 57~38 -> hasOnlyBgp
  | 57~59 -> hasOnlyBgp
  | 57~63 -> hasOnlyBgp
  | 57~77 -> hasOnlyBgp
  | 66~9 -> hasOnlyBgp
  | 66~14 -> hasOnlyBgp
  | 66~25 -> hasOnlyBgp
  | 66~30 -> hasOnlyBgp
  | 66~58 -> hasOnlyBgp
  | 66~62 -> hasOnlyBgp
  | 66~79 -> hasOnlyBgp
  | 67~11 -> hasOnlyBgp
  | 67~15 -> hasOnlyBgp
  | 67~32 -> hasOnlyBgp
  | 67~38 -> hasOnlyBgp
  | 67~59 -> hasOnlyBgp
  | 67~63 -> hasOnlyBgp
  | 67~77 -> hasOnlyBgp
  | 68~11 -> hasOnlyBgp
  | 68~15 -> hasOnlyBgp
  | 68~32 -> hasOnlyBgp
  | 68~38 -> hasOnlyBgp
  | 68~59 -> hasOnlyBgp
  | 68~63 -> hasOnlyBgp
  | 68~77 -> hasOnlyBgp
  | 70~9 -> hasOnlyBgp
  | 70~14 -> hasOnlyBgp
  | 70~25 -> hasOnlyBgp
  | 70~30 -> hasOnlyBgp
  | 70~58 -> hasOnlyBgp
  | 70~62 -> hasOnlyBgp
  | 70~79 -> hasOnlyBgp
  | 71~9 -> hasOnlyBgp
  | 71~14 -> hasOnlyBgp
  | 71~25 -> hasOnlyBgp
  | 71~30 -> hasOnlyBgp
  | 71~58 -> hasOnlyBgp
  | 71~62 -> hasOnlyBgp
  | 71~79 -> hasOnlyBgp
  | 72~9 -> hasOnlyBgp
  | 72~14 -> hasOnlyBgp
  | 72~25 -> hasOnlyBgp
  | 72~30 -> hasOnlyBgp
  | 72~58 -> hasOnlyBgp
  | 72~62 -> hasOnlyBgp
  | 72~79 -> hasOnlyBgp
  | _ -> any


let sol = solution { init = init; trans = trans; merge = merge; interface = interface; rtrans = trans }

assert foldNodes (fun k v acc -> acc && assert_node k v) sol true
