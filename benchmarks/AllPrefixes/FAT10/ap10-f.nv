(* vim: set syntax=ocaml: *)
(* Fully partitioned version of ap10.nv *)
(* Automatically generated by gen_part_nv.py *)
type ospfType = {ospfAd: int8; weight: int16; areaType:int2; areaId: int;}
type bgpType = {bgpAd: int8; lp: int; aslen: int; med:int; comms:set[int];}
type rib = {
    connected:option[int8];
    static:option[int8];
    ospf:option[ospfType];
    bgp:option[bgpType];
    selected:option[int2]; }
type attribute = rib

symbolic d : (int, int6)
symbolic pod : int

let edges = {
  24-74; (*aggregation-65,Serial7 --> edge-72,Serial0*)
  54-47; (*edge-94,Serial3 --> aggregation-86,Serial9*)
  63-101; (*edge-60,Serial3 --> aggregation-59,Serial6*)
  63-99; (*edge-60,Serial0 --> aggregation-56,Serial6*)
  116-121; (*edge-51,Serial0 --> aggregation-48,Serial6*)
  82-70; (*core-7,Serial9 --> aggregation-26,Serial5*)
  69-19; (*edge-74,Serial2 --> aggregation-67,Serial4*)
  66-21; (*aggregation-27,Serial0 --> edge-32,Serial2*)
  66-115; (*aggregation-27,Serial6 --> core-13,Serial9*)
  107-29; (*core-11,Serial6 --> aggregation-117,Serial1*)
  116-123; (*edge-51,Serial4 --> aggregation-47,Serial6*)
  112-51; (*core-16,Serial7 --> aggregation-88,Serial1*)
  115-66; (*core-13,Serial9 --> aggregation-27,Serial6*)
  8-87; (*aggregation-76,Serial1 --> core-6,Serial4*)
  8-88; (*aggregation-76,Serial4 --> core-9,Serial4*)
  56-96; (*aggregation-38,Serial0 --> edge-40,Serial3*)
  66-108; (*aggregation-27,Serial3 --> core-10,Serial9*)
  56-102; (*aggregation-38,Serial3 --> edge-43,Serial3*)
  0-27; (*edge-122,Serial4 --> aggregation-119,Serial7*)
  95-105; (*core-24,Serial5 --> aggregation-49,Serial7*)
  0-25; (*edge-122,Serial1 --> aggregation-116,Serial7*)
  14-44; (*edge-113,Serial1 --> aggregation-106,Serial8*)
  96-56; (*edge-40,Serial3 --> aggregation-38,Serial0*)
  51-113; (*aggregation-88,Serial0 --> core-15,Serial7*)
  62-98; (*aggregation-36,Serial4 --> edge-41,Serial1*)
  121-120; (*aggregation-48,Serial8 --> edge-53,Serial0*)
  39-6; (*edge-83,Serial0 --> aggregation-75,Serial8*)
  69-24; (*edge-74,Serial0 --> aggregation-65,Serial9*)
  103-110; (*aggregation-58,Serial4 --> core-18,Serial8*)
  27-1; (*aggregation-119,Serial6 --> edge-121,Serial4*)
  42-13; (*aggregation-108,Serial3 --> edge-110,Serial3*)
  84-22; (*core-23,Serial1 --> aggregation-69,Serial8*)
  56-113; (*aggregation-38,Serial5 --> core-15,Serial2*)
  104-81; (*aggregation-55,Serial4 --> core-4,Serial7*)
  26-81; (*aggregation-115,Serial4 --> core-4,Serial5*)
  66-18; (*aggregation-27,Serial8 --> edge-30,Serial2*)
  13-43; (*edge-110,Serial2 --> aggregation-107,Serial4*)
  35-6; (*edge-81,Serial0 --> aggregation-75,Serial6*)
  67-89; (*aggregation-29,Serial4 --> core-21,Serial9*)
  74-24; (*edge-72,Serial0 --> aggregation-65,Serial7*)
  119-76; (*aggregation-45,Serial0 --> core-0,Serial4*)
  35-5; (*edge-81,Serial3 --> aggregation-78,Serial6*)
  78-62; (*edge-44,Serial1 --> aggregation-36,Serial7*)
  105-120; (*aggregation-49,Serial4 --> edge-53,Serial1*)
  18-65; (*edge-30,Serial3 --> aggregation-28,Serial8*)
  120-121; (*edge-53,Serial0 --> aggregation-48,Serial8*)
  94-32; (*edge-101,Serial1 --> aggregation-97,Serial1*)
  114-43; (*core-14,Serial3 --> aggregation-107,Serial9*)
  63-104; (*edge-60,Serial4 --> aggregation-55,Serial6*)
  71-18; (*aggregation-25,Serial8 --> edge-30,Serial0*)
  121-124; (*aggregation-48,Serial5 --> edge-50,Serial0*)
  102-61; (*edge-43,Serial2 --> aggregation-37,Serial4*)
  30-79; (*aggregation-95,Serial2 --> core-2,Serial9*)
  117-116; (*aggregation-46,Serial6 --> edge-51,Serial3*)
  18-71; (*edge-30,Serial0 --> aggregation-25,Serial8*)
  88-70; (*core-9,Serial9 --> aggregation-26,Serial7*)
  122-105; (*edge-54,Serial1 --> aggregation-49,Serial5*)
  39-5; (*edge-83,Serial3 --> aggregation-78,Serial8*)
  71-21; (*aggregation-25,Serial0 --> edge-32,Serial0*)
  47-54; (*aggregation-86,Serial9 --> edge-94,Serial3*)
  42-14; (*aggregation-108,Serial6 --> edge-113,Serial3*)
  2-25; (*edge-124,Serial1 --> aggregation-116,Serial9*)
  19-72; (*aggregation-67,Serial0 --> edge-70,Serial2*)
  80-25; (*core-5,Serial6 --> aggregation-116,Serial0*)
  30-81; (*aggregation-95,Serial9 --> core-4,Serial9*)
  7-106; (*aggregation-77,Serial2 --> core-12,Serial4*)
  98-61; (*edge-41,Serial2 --> aggregation-37,Serial2*)
  61-100; (*aggregation-37,Serial3 --> edge-42,Serial2*)
  76-26; (*core-0,Serial5 --> aggregation-115,Serial0*)
  34-94; (*aggregation-96,Serial9 --> edge-101,Serial0*)
  89-22; (*core-21,Serial1 --> aggregation-69,Serial6*)
  83-36; (*edge-104,Serial3 --> aggregation-99,Serial4*)
  99-82; (*aggregation-56,Serial3 --> core-7,Serial8*)
  46-50; (*aggregation-89,Serial7 --> edge-92,Serial1*)
  80-99; (*core-5,Serial8 --> aggregation-56,Serial1*)
  51-45; (*aggregation-88,Serial5 --> edge-90,Serial0*)
  67-18; (*aggregation-29,Serial8 --> edge-30,Serial4*)
  74-19; (*edge-72,Serial2 --> aggregation-67,Serial2*)
  75-6; (*core-1,Serial3 --> aggregation-75,Serial1*)
  47-48; (*aggregation-86,Serial6 --> edge-91,Serial3*)
  110-51; (*core-18,Serial7 --> aggregation-88,Serial3*)
  23-80; (*aggregation-66,Serial0 --> core-5,Serial1*)
  91-41; (*core-20,Serial3 --> aggregation-109,Serial5*)
  120-123; (*edge-53,Serial4 --> aggregation-47,Serial8*)
  19-68; (*aggregation-67,Serial3 --> edge-73,Serial2*)
  99-57; (*aggregation-56,Serial8 --> edge-62,Serial0*)
  23-82; (*aggregation-66,Serial6 --> core-7,Serial1*)
  57-104; (*edge-62,Serial4 --> aggregation-55,Serial8*)
  26-75; (*aggregation-115,Serial1 --> core-1,Serial5*)
  101-64; (*aggregation-59,Serial7 --> edge-61,Serial3*)
  22-95; (*aggregation-69,Serial9 --> core-24,Serial1*)
  82-99; (*core-7,Serial8 --> aggregation-56,Serial3*)
  81-71; (*core-4,Serial8 --> aggregation-25,Serial4*)
  82-34; (*core-7,Serial0 --> aggregation-96,Serial6*)
  22-89; (*aggregation-69,Serial6 --> core-21,Serial1*)
  40-77; (*aggregation-105,Serial3 --> core-3,Serial2*)
  67-95; (*aggregation-29,Serial7 --> core-24,Serial9*)
  40-76; (*aggregation-105,Serial0 --> core-0,Serial2*)
  48-53; (*edge-91,Serial4 --> aggregation-87,Serial6*)
  111-56; (*core-17,Serial2 --> aggregation-38,Serial7*)
  45-47; (*edge-90,Serial3 --> aggregation-86,Serial5*)
  122-117; (*edge-54,Serial3 --> aggregation-46,Serial9*)
  72-19; (*edge-70,Serial2 --> aggregation-67,Serial0*)
  77-6; (*core-3,Serial3 --> aggregation-75,Serial3*)
  73-17; (*edge-71,Serial3 --> aggregation-68,Serial1*)
  78-56; (*edge-44,Serial3 --> aggregation-38,Serial4*)
  107-32; (*core-11,Serial0 --> aggregation-97,Serial6*)
  53-45; (*aggregation-87,Serial5 --> edge-90,Serial4*)
  91-105; (*core-20,Serial5 --> aggregation-49,Serial3*)
  49-75; (*aggregation-85,Serial1 --> core-1,Serial6*)
  97-64; (*aggregation-57,Serial7 --> edge-61,Serial1*)
  103-113; (*aggregation-58,Serial1 --> core-15,Serial8*)
  104-57; (*aggregation-55,Serial8 --> edge-62,Serial4*)
  17-113; (*aggregation-68,Serial5 --> core-15,Serial1*)
  103-57; (*aggregation-58,Serial8 --> edge-62,Serial2*)
  23-74; (*aggregation-66,Serial3 --> edge-72,Serial1*)
  107-123; (*core-11,Serial5 --> aggregation-47,Serial1*)
  109-56; (*core-19,Serial2 --> aggregation-38,Serial9*)
  50-47; (*edge-92,Serial3 --> aggregation-86,Serial7*)
  77-58; (*core-3,Serial1 --> aggregation-35,Serial3*)
  34-88; (*aggregation-96,Serial5 --> core-9,Serial0*)
  115-32; (*core-13,Serial0 --> aggregation-97,Serial8*)
  90-8; (*core-8,Serial4 --> aggregation-76,Serial3*)
  94-34; (*edge-101,Serial0 --> aggregation-96,Serial9*)
  100-56; (*edge-42,Serial3 --> aggregation-38,Serial2*)
  101-89; (*aggregation-59,Serial2 --> core-21,Serial8*)
  115-29; (*core-13,Serial6 --> aggregation-117,Serial3*)
  97-108; (*aggregation-57,Serial1 --> core-10,Serial8*)
  30-93; (*aggregation-95,Serial6 --> edge-102,Serial4*)
  53-108; (*aggregation-87,Serial0 --> core-10,Serial7*)
  41-13; (*aggregation-109,Serial0 --> edge-110,Serial4*)
  117-88; (*aggregation-46,Serial4 --> core-9,Serial5*)
  86-41; (*core-22,Serial3 --> aggregation-109,Serial7*)
  123-114; (*aggregation-47,Serial4 --> core-14,Serial5*)
  19-115; (*aggregation-67,Serial7 --> core-13,Serial1*)
  105-86; (*aggregation-49,Serial0 --> core-22,Serial5*)
  10-70; (*edge-34,Serial1 --> aggregation-26,Serial2*)
  15-41; (*edge-112,Serial4 --> aggregation-109,Serial2*)
  29-106; (*aggregation-117,Serial2 --> core-12,Serial6*)
  24-77; (*aggregation-65,Serial3 --> core-3,Serial0*)
  31-4; (*edge-84,Serial4 --> aggregation-79,Serial4*)
  80-44; (*core-5,Serial3 --> aggregation-106,Serial0*)
  6-35; (*aggregation-75,Serial6 --> edge-81,Serial0*)
  113-56; (*core-15,Serial2 --> aggregation-38,Serial5*)
  3-28; (*edge-123,Serial3 --> aggregation-118,Serial8*)
  107-43; (*core-11,Serial3 --> aggregation-107,Serial1*)
  124-119; (*edge-50,Serial2 --> aggregation-45,Serial5*)
  58-102; (*aggregation-35,Serial8 --> edge-43,Serial0*)
  112-38; (*core-16,Serial0 --> aggregation-98,Serial6*)
  71-76; (*aggregation-25,Serial5 --> core-0,Serial8*)
  112-103; (*core-16,Serial8 --> aggregation-58,Serial2*)
  41-86; (*aggregation-109,Serial7 --> core-22,Serial3*)
  58-79; (*aggregation-35,Serial2 --> core-2,Serial1*)
  26-2; (*aggregation-115,Serial9 --> edge-124,Serial0*)
  44-11; (*aggregation-106,Serial9 --> edge-114,Serial1*)
  105-95; (*aggregation-49,Serial7 --> core-24,Serial5*)
  37-8; (*edge-82,Serial1 --> aggregation-76,Serial7*)
  53-52; (*aggregation-87,Serial8 --> edge-93,Serial4*)
  110-28; (*core-18,Serial6 --> aggregation-118,Serial3*)
  22-68; (*aggregation-69,Serial3 --> edge-73,Serial4*)
  92-34; (*edge-100,Serial0 --> aggregation-96,Serial0*)
  49-81; (*aggregation-85,Serial4 --> core-4,Serial6*)
  92-30; (*edge-100,Serial4 --> aggregation-95,Serial4*)
  42-111; (*aggregation-108,Serial1 --> core-17,Serial3*)
  110-38; (*core-18,Serial0 --> aggregation-98,Serial8*)
  70-20; (*aggregation-26,Serial9 --> edge-31,Serial1*)
  95-41; (*core-24,Serial3 --> aggregation-109,Serial9*)
  93-34; (*edge-102,Serial0 --> aggregation-96,Serial2*)
  77-49; (*core-3,Serial6 --> aggregation-85,Serial3*)
  65-113; (*aggregation-28,Serial3 --> core-15,Serial9*)
  112-5; (*core-16,Serial4 --> aggregation-78,Serial1*)
  70-9; (*aggregation-26,Serial1 --> edge-33,Serial1*)
  33-8; (*edge-80,Serial1 --> aggregation-76,Serial5*)
  49-52; (*aggregation-85,Serial8 --> edge-93,Serial2*)
  110-103; (*core-18,Serial8 --> aggregation-58,Serial4*)
  5-109; (*aggregation-78,Serial0 --> core-19,Serial4*)
  6-39; (*aggregation-75,Serial8 --> edge-83,Serial0*)
  93-30; (*edge-102,Serial4 --> aggregation-95,Serial6*)
  4-91; (*aggregation-79,Serial9 --> core-20,Serial4*)
  44-88; (*aggregation-106,Serial4 --> core-9,Serial3*)
  65-9; (*aggregation-28,Serial1 --> edge-33,Serial3*)
  123-120; (*aggregation-47,Serial8 --> edge-53,Serial4*)
  83-34; (*edge-104,Serial0 --> aggregation-96,Serial4*)
  1-28; (*edge-121,Serial3 --> aggregation-118,Serial6*)
  107-61; (*core-11,Serial2 --> aggregation-37,Serial0*)
  91-46; (*core-20,Serial7 --> aggregation-89,Serial0*)
  64-97; (*edge-61,Serial1 --> aggregation-57,Serial7*)
  115-43; (*core-13,Serial3 --> aggregation-107,Serial3*)
  79-30; (*core-2,Serial9 --> aggregation-95,Serial2*)
  34-92; (*aggregation-96,Serial0 --> edge-100,Serial0*)
  9-65; (*edge-33,Serial3 --> aggregation-28,Serial1*)
  36-93; (*aggregation-99,Serial2 --> edge-102,Serial3*)
  110-65; (*core-18,Serial9 --> aggregation-28,Serial6*)
  28-16; (*aggregation-118,Serial5 --> edge-120,Serial3*)
  31-8; (*edge-84,Serial1 --> aggregation-76,Serial9*)
  36-95; (*aggregation-99,Serial9 --> core-24,Serial0*)
  112-121; (*core-16,Serial5 --> aggregation-48,Serial3*)
  29-2; (*aggregation-117,Serial9 --> edge-124,Serial2*)
  13-41; (*edge-110,Serial4 --> aggregation-109,Serial0*)
  21-70; (*edge-32,Serial1 --> aggregation-26,Serial0*)
  40-15; (*aggregation-105,Serial7 --> edge-112,Serial0*)
  12-44; (*edge-111,Serial1 --> aggregation-106,Serial6*)
  16-25; (*edge-120,Serial1 --> aggregation-116,Serial5*)
  34-83; (*aggregation-96,Serial4 --> edge-104,Serial0*)
  46-89; (*aggregation-89,Serial1 --> core-21,Serial7*)
  86-46; (*core-22,Serial7 --> aggregation-89,Serial2*)
  75-104; (*core-1,Serial7 --> aggregation-55,Serial5*)
  32-93; (*aggregation-97,Serial2 --> edge-102,Serial1*)
  7-35; (*aggregation-77,Serial6 --> edge-81,Serial2*)
  119-77; (*aggregation-45,Serial3 --> core-3,Serial4*)
  36-86; (*aggregation-99,Serial7 --> core-22,Serial0*)
  88-23; (*core-9,Serial1 --> aggregation-66,Serial8*)
  25-0; (*aggregation-116,Serial7 --> edge-122,Serial1*)
  55-89; (*aggregation-39,Serial6 --> core-21,Serial2*)
  28-113; (*aggregation-118,Serial0 --> core-15,Serial6*)
  38-112; (*aggregation-98,Serial6 --> core-16,Serial0*)
  57-101; (*edge-62,Serial3 --> aggregation-59,Serial8*)
  95-46; (*core-24,Serial7 --> aggregation-89,Serial4*)
  46-84; (*aggregation-89,Serial3 --> core-23,Serial7*)
  25-87; (*aggregation-116,Serial1 --> core-6,Serial6*)
  27-2; (*aggregation-119,Serial9 --> edge-124,Serial4*)
  38-83; (*aggregation-98,Serial4 --> edge-104,Serial2*)
  60-97; (*edge-64,Serial1 --> aggregation-57,Serial0*)
  28-111; (*aggregation-118,Serial2 --> core-17,Serial6*)
  55-84; (*aggregation-39,Serial8 --> core-23,Serial2*)
  82-23; (*core-7,Serial1 --> aggregation-66,Serial6*)
  104-60; (*aggregation-55,Serial0 --> edge-64,Serial4*)
  28-0; (*aggregation-118,Serial7 --> edge-122,Serial3*)
  88-34; (*core-9,Serial0 --> aggregation-96,Serial5*)
  97-106; (*aggregation-57,Serial3 --> core-12,Serial8*)
  11-41; (*edge-114,Serial4 --> aggregation-109,Serial4*)
  76-30; (*core-0,Serial9 --> aggregation-95,Serial0*)
  26-16; (*aggregation-115,Serial5 --> edge-120,Serial0*)
  44-13; (*aggregation-106,Serial5 --> edge-110,Serial1*)
  113-65; (*core-15,Serial9 --> aggregation-28,Serial3*)
  77-119; (*core-3,Serial4 --> aggregation-45,Serial3*)
  82-47; (*core-7,Serial7 --> aggregation-86,Serial2*)
  38-110; (*aggregation-98,Serial8 --> core-18,Serial0*)
  70-90; (*aggregation-26,Serial6 --> core-8,Serial9*)
  36-85; (*aggregation-99,Serial3 --> edge-103,Serial3*)
  58-76; (*aggregation-35,Serial0 --> core-0,Serial1*)
  79-40; (*core-2,Serial2 --> aggregation-105,Serial2*)
  5-113; (*aggregation-78,Serial4 --> core-15,Serial4*)
  34-90; (*aggregation-96,Serial7 --> core-8,Serial0*)
  112-65; (*core-16,Serial9 --> aggregation-28,Serial4*)
  25-16; (*aggregation-116,Serial5 --> edge-120,Serial1*)
  85-38; (*edge-103,Serial2 --> aggregation-98,Serial3*)
  60-103; (*edge-64,Serial2 --> aggregation-58,Serial0*)
  118-105; (*edge-52,Serial1 --> aggregation-49,Serial8*)
  84-27; (*core-23,Serial6 --> aggregation-119,Serial4*)
  95-101; (*core-24,Serial8 --> aggregation-59,Serial5*)
  7-39; (*aggregation-77,Serial8 --> edge-83,Serial2*)
  101-84; (*aggregation-59,Serial4 --> core-23,Serial8*)
  46-48; (*aggregation-89,Serial6 --> edge-91,Serial1*)
  21-66; (*edge-32,Serial2 --> aggregation-27,Serial0*)
  45-46; (*edge-90,Serial1 --> aggregation-89,Serial5*)
  113-28; (*core-15,Serial6 --> aggregation-118,Serial0*)
  89-4; (*core-21,Serial4 --> aggregation-79,Serial5*)
  7-114; (*aggregation-77,Serial4 --> core-14,Serial4*)
  119-120; (*aggregation-45,Serial8 --> edge-53,Serial2*)
  49-79; (*aggregation-85,Serial2 --> core-2,Serial6*)
  17-69; (*aggregation-68,Serial4 --> edge-74,Serial3*)
  59-97; (*edge-63,Serial1 --> aggregation-57,Serial9*)
  107-19; (*core-11,Serial1 --> aggregation-67,Serial5*)
  38-92; (*aggregation-98,Serial0 --> edge-100,Serial2*)
  97-59; (*aggregation-57,Serial9 --> edge-63,Serial1*)
  26-3; (*aggregation-115,Serial8 --> edge-123,Serial0*)
  44-14; (*aggregation-106,Serial8 --> edge-113,Serial1*)
  65-20; (*aggregation-28,Serial9 --> edge-31,Serial3*)
  11-42; (*edge-114,Serial3 --> aggregation-108,Serial7*)
  99-88; (*aggregation-56,Serial5 --> core-9,Serial8*)
  10-66; (*edge-34,Serial2 --> aggregation-27,Serial2*)
  12-40; (*edge-111,Serial0 --> aggregation-105,Serial6*)
  16-26; (*edge-120,Serial0 --> aggregation-115,Serial5*)
  109-42; (*core-19,Serial3 --> aggregation-108,Serial8*)
  33-7; (*edge-80,Serial2 --> aggregation-77,Serial5*)
  75-49; (*core-1,Serial6 --> aggregation-85,Serial1*)
  4-35; (*aggregation-79,Serial1 --> edge-81,Serial4*)
  71-10; (*aggregation-25,Serial2 --> edge-34,Serial0*)
  4-31; (*aggregation-79,Serial4 --> edge-84,Serial4*)
  115-19; (*core-13,Serial1 --> aggregation-67,Serial7*)
  117-80; (*aggregation-46,Serial0 --> core-5,Serial5*)
  117-90; (*aggregation-46,Serial3 --> core-8,Serial5*)
  95-55; (*core-24,Serial2 --> aggregation-39,Serial9*)
  44-80; (*aggregation-106,Serial0 --> core-5,Serial3*)
  47-50; (*aggregation-86,Serial7 --> edge-92,Serial3*)
  19-106; (*aggregation-67,Serial6 --> core-12,Serial1*)
  46-54; (*aggregation-89,Serial9 --> edge-94,Serial1*)
  80-47; (*core-5,Serial7 --> aggregation-86,Serial0*)
  105-84; (*aggregation-49,Serial6 --> core-23,Serial5*)
  20-70; (*edge-31,Serial1 --> aggregation-26,Serial9*)
  119-124; (*aggregation-45,Serial5 --> edge-50,Serial2*)
  7-107; (*aggregation-77,Serial1 --> core-11,Serial4*)
  75-119; (*core-1,Serial4 --> aggregation-45,Serial1*)
  86-55; (*core-22,Serial2 --> aggregation-39,Serial7*)
  41-11; (*aggregation-109,Serial4 --> edge-114,Serial4*)
  17-73; (*aggregation-68,Serial1 --> edge-71,Serial3*)
  50-46; (*edge-92,Serial1 --> aggregation-89,Serial7*)
  86-101; (*core-22,Serial8 --> aggregation-59,Serial3*)
  52-49; (*edge-93,Serial2 --> aggregation-85,Serial8*)
  37-4; (*edge-82,Serial4 --> aggregation-79,Serial2*)
  68-22; (*edge-73,Serial4 --> aggregation-69,Serial3*)
  75-71; (*core-1,Serial8 --> aggregation-25,Serial6*)
  111-28; (*core-17,Serial6 --> aggregation-118,Serial2*)
  76-58; (*core-0,Serial1 --> aggregation-35,Serial0*)
  123-124; (*aggregation-47,Serial5 --> edge-50,Serial4*)
  55-98; (*aggregation-39,Serial1 --> edge-41,Serial4*)
  71-79; (*aggregation-25,Serial7 --> core-2,Serial8*)
  55-78; (*aggregation-39,Serial4 --> edge-44,Serial4*)
  91-55; (*core-20,Serial2 --> aggregation-39,Serial5*)
  54-46; (*edge-94,Serial1 --> aggregation-89,Serial9*)
  121-109; (*aggregation-48,Serial0 --> core-19,Serial5*)
  101-59; (*aggregation-59,Serial9 --> edge-63,Serial3*)
  101-63; (*aggregation-59,Serial6 --> edge-60,Serial3*)
  67-86; (*aggregation-29,Serial5 --> core-22,Serial9*)
  15-42; (*edge-112,Serial3 --> aggregation-108,Serial5*)
  76-40; (*core-0,Serial2 --> aggregation-105,Serial0*)
  121-111; (*aggregation-48,Serial4 --> core-17,Serial5*)
  56-100; (*aggregation-38,Serial2 --> edge-42,Serial3*)
  6-76; (*aggregation-75,Serial0 --> core-0,Serial3*)
  90-62; (*core-8,Serial2 --> aggregation-36,Serial8*)
  116-119; (*edge-51,Serial2 --> aggregation-45,Serial6*)
  96-55; (*edge-40,Serial4 --> aggregation-39,Serial0*)
  113-38; (*core-15,Serial0 --> aggregation-98,Serial5*)
  94-38; (*edge-101,Serial2 --> aggregation-98,Serial1*)
  107-53; (*core-11,Serial7 --> aggregation-87,Serial1*)
  116-117; (*edge-51,Serial3 --> aggregation-46,Serial6*)
  61-107; (*aggregation-37,Serial0 --> core-11,Serial2*)
  84-4; (*core-23,Serial4 --> aggregation-79,Serial7*)
  87-62; (*core-6,Serial2 --> aggregation-36,Serial1*)
  120-119; (*edge-53,Serial2 --> aggregation-45,Serial8*)
  65-109; (*aggregation-28,Serial7 --> core-19,Serial9*)
  115-123; (*core-13,Serial5 --> aggregation-47,Serial3*)
  100-55; (*edge-42,Serial4 --> aggregation-39,Serial2*)
  23-88; (*aggregation-66,Serial8 --> core-9,Serial1*)
  87-44; (*core-6,Serial3 --> aggregation-106,Serial1*)
  6-81; (*aggregation-75,Serial4 --> core-4,Serial3*)
  27-95; (*aggregation-119,Serial0 --> core-24,Serial6*)
  71-81; (*aggregation-25,Serial4 --> core-4,Serial8*)
  61-98; (*aggregation-37,Serial2 --> edge-41,Serial2*)
  32-115; (*aggregation-97,Serial8 --> core-13,Serial0*)
  54-53; (*edge-94,Serial4 --> aggregation-87,Serial9*)
  91-101; (*core-20,Serial8 --> aggregation-59,Serial1*)
  92-36; (*edge-100,Serial3 --> aggregation-99,Serial0*)
  4-95; (*aggregation-79,Serial8 --> core-24,Serial4*)
  48-49; (*edge-91,Serial2 --> aggregation-85,Serial6*)
  115-53; (*core-13,Serial7 --> aggregation-87,Serial3*)
  33-4; (*edge-80,Serial4 --> aggregation-79,Serial0*)
  73-22; (*edge-71,Serial4 --> aggregation-69,Serial1*)
  78-55; (*edge-44,Serial4 --> aggregation-39,Serial4*)
  22-86; (*aggregation-69,Serial7 --> core-22,Serial1*)
  6-77; (*aggregation-75,Serial3 --> core-3,Serial3*)
  71-77; (*aggregation-25,Serial3 --> core-3,Serial8*)
  81-40; (*core-4,Serial2 --> aggregation-105,Serial4*)
  101-91; (*aggregation-59,Serial1 --> core-20,Serial8*)
  90-44; (*core-8,Serial3 --> aggregation-106,Serial3*)
  79-58; (*core-2,Serial1 --> aggregation-35,Serial2*)
  103-112; (*aggregation-58,Serial2 --> core-16,Serial8*)
  40-75; (*aggregation-105,Serial1 --> core-1,Serial2*)
  97-63; (*aggregation-57,Serial6 --> edge-60,Serial1*)
  23-73; (*aggregation-66,Serial2 --> edge-71,Serial1*)
  2-26; (*edge-124,Serial0 --> aggregation-115,Serial9*)
  31-7; (*edge-84,Serial2 --> aggregation-77,Serial9*)
  41-14; (*aggregation-109,Serial3 --> edge-113,Serial4*)
  40-12; (*aggregation-105,Serial6 --> edge-111,Serial0*)
  53-115; (*aggregation-87,Serial3 --> core-13,Serial7*)
  123-107; (*aggregation-47,Serial1 --> core-11,Serial5*)
  43-114; (*aggregation-107,Serial9 --> core-14,Serial3*)
  11-43; (*edge-114,Serial2 --> aggregation-107,Serial8*)
  19-74; (*aggregation-67,Serial2 --> edge-72,Serial2*)
  52-51; (*edge-93,Serial0 --> aggregation-88,Serial8*)
  81-104; (*core-4,Serial7 --> aggregation-55,Serial4*)
  67-10; (*aggregation-29,Serial2 --> edge-34,Serial4*)
  87-8; (*core-6,Serial4 --> aggregation-76,Serial1*)
  90-99; (*core-8,Serial8 --> aggregation-56,Serial4*)
  98-58; (*edge-41,Serial0 --> aggregation-35,Serial6*)
  112-17; (*core-16,Serial1 --> aggregation-68,Serial6*)
  117-118; (*aggregation-46,Serial7 --> edge-52,Serial3*)
  43-13; (*aggregation-107,Serial4 --> edge-110,Serial2*)
  116-105; (*edge-51,Serial1 --> aggregation-49,Serial2*)
  77-26; (*core-3,Serial5 --> aggregation-115,Serial3*)
  120-117; (*edge-53,Serial3 --> aggregation-46,Serial8*)
  77-71; (*core-3,Serial8 --> aggregation-25,Serial3*)
  38-94; (*aggregation-98,Serial1 --> edge-101,Serial2*)
  1-29; (*edge-121,Serial2 --> aggregation-117,Serial6*)
  43-106; (*aggregation-107,Serial2 --> core-12,Serial3*)
  17-110; (*aggregation-68,Serial8 --> core-18,Serial1*)
  112-42; (*core-16,Serial3 --> aggregation-108,Serial0*)
  106-7; (*core-12,Serial4 --> aggregation-77,Serial2*)
  74-23; (*edge-72,Serial1 --> aggregation-66,Serial3*)
  4-33; (*aggregation-79,Serial0 --> edge-80,Serial4*)
  93-36; (*edge-102,Serial3 --> aggregation-99,Serial2*)
  68-17; (*edge-73,Serial3 --> aggregation-68,Serial3*)
  65-110; (*aggregation-28,Serial6 --> core-18,Serial9*)
  113-5; (*core-15,Serial4 --> aggregation-78,Serial4*)
  49-45; (*aggregation-85,Serial5 --> edge-90,Serial2*)
  114-97; (*core-14,Serial8 --> aggregation-57,Serial5*)
  101-95; (*aggregation-59,Serial5 --> core-24,Serial8*)
  77-24; (*core-3,Serial0 --> aggregation-65,Serial3*)
  84-67; (*core-23,Serial9 --> aggregation-29,Serial6*)
  88-25; (*core-9,Serial6 --> aggregation-116,Serial4*)
  76-6; (*core-0,Serial3 --> aggregation-75,Serial0*)
  110-42; (*core-18,Serial3 --> aggregation-108,Serial2*)
  34-80; (*aggregation-96,Serial1 --> core-5,Serial0*)
  82-117; (*core-7,Serial5 --> aggregation-46,Serial2*)
  98-62; (*edge-41,Serial1 --> aggregation-36,Serial4*)
  7-108; (*aggregation-77,Serial0 --> core-10,Serial4*)
  70-82; (*aggregation-26,Serial5 --> core-7,Serial9*)
  114-7; (*core-14,Serial4 --> aggregation-77,Serial4*)
  24-69; (*aggregation-65,Serial9 --> edge-74,Serial0*)
  42-112; (*aggregation-108,Serial0 --> core-16,Serial3*)
  89-67; (*core-21,Serial9 --> aggregation-29,Serial4*)
  9-71; (*edge-33,Serial0 --> aggregation-25,Serial1*)
  24-76; (*aggregation-65,Serial0 --> core-0,Serial0*)
  37-7; (*edge-82,Serial2 --> aggregation-77,Serial7*)
  58-75; (*aggregation-35,Serial1 --> core-1,Serial1*)
  76-104; (*core-0,Serial7 --> aggregation-55,Serial1*)
  0-26; (*edge-122,Serial0 --> aggregation-115,Serial7*)
  14-40; (*edge-113,Serial0 --> aggregation-105,Serial8*)
  72-24; (*edge-70,Serial0 --> aggregation-65,Serial5*)
  102-58; (*edge-43,Serial0 --> aggregation-35,Serial8*)
  106-61; (*core-12,Serial2 --> aggregation-37,Serial7*)
  80-117; (*core-5,Serial5 --> aggregation-46,Serial0*)
  20-66; (*edge-31,Serial2 --> aggregation-27,Serial9*)
  22-74; (*aggregation-69,Serial2 --> edge-72,Serial4*)
  48-51; (*edge-91,Serial0 --> aggregation-88,Serial6*)
  66-10; (*aggregation-27,Serial2 --> edge-34,Serial2*)
  8-31; (*aggregation-76,Serial9 --> edge-84,Serial1*)
  51-52; (*aggregation-88,Serial8 --> edge-93,Serial0*)
  85-32; (*edge-103,Serial1 --> aggregation-97,Serial3*)
  75-26; (*core-1,Serial5 --> aggregation-115,Serial1*)
  120-105; (*edge-53,Serial1 --> aggregation-49,Serial4*)
  105-122; (*aggregation-49,Serial5 --> edge-54,Serial1*)
  44-87; (*aggregation-106,Serial1 --> core-6,Serial3*)
  62-100; (*aggregation-36,Serial5 --> edge-42,Serial1*)
  106-97; (*core-12,Serial8 --> aggregation-57,Serial3*)
  108-7; (*core-10,Serial4 --> aggregation-77,Serial0*)
  17-72; (*aggregation-68,Serial0 --> edge-70,Serial3*)
  97-60; (*aggregation-57,Serial0 --> edge-64,Serial1*)
  55-86; (*aggregation-39,Serial7 --> core-22,Serial2*)
  72-23; (*edge-70,Serial1 --> aggregation-66,Serial1*)
  104-77; (*aggregation-55,Serial3 --> core-3,Serial7*)
  62-80; (*aggregation-36,Serial0 --> core-5,Serial2*)
  119-122; (*aggregation-45,Serial9 --> edge-54,Serial2*)
  46-86; (*aggregation-89,Serial2 --> core-22,Serial7*)
  82-25; (*core-7,Serial6 --> aggregation-116,Serial2*)
  75-24; (*core-1,Serial0 --> aggregation-65,Serial1*)
  3-29; (*edge-123,Serial2 --> aggregation-117,Serial8*)
  89-36; (*core-21,Serial0 --> aggregation-99,Serial6*)
  57-99; (*edge-62,Serial0 --> aggregation-56,Serial8*)
  5-31; (*aggregation-78,Serial9 --> edge-84,Serial3*)
  15-43; (*edge-112,Serial2 --> aggregation-107,Serial6*)
  24-72; (*aggregation-65,Serial5 --> edge-70,Serial0*)
  66-114; (*aggregation-27,Serial7 --> core-14,Serial9*)
  80-70; (*core-5,Serial9 --> aggregation-26,Serial3*)
  88-117; (*core-9,Serial5 --> aggregation-46,Serial4*)
  25-88; (*aggregation-116,Serial4 --> core-9,Serial6*)
  38-111; (*aggregation-98,Serial7 --> core-17,Serial0*)
  107-66; (*core-11,Serial9 --> aggregation-27,Serial4*)
  79-104; (*core-2,Serial7 --> aggregation-55,Serial2*)
  87-23; (*core-6,Serial1 --> aggregation-66,Serial9*)
  110-17; (*core-18,Serial1 --> aggregation-68,Serial8*)
  2-27; (*edge-124,Serial4 --> aggregation-119,Serial9*)
  89-27; (*core-21,Serial6 --> aggregation-119,Serial2*)
  27-89; (*aggregation-119,Serial2 --> core-21,Serial6*)
  18-67; (*edge-30,Serial4 --> aggregation-29,Serial8*)
  7-37; (*aggregation-77,Serial7 --> edge-82,Serial2*)
  8-90; (*aggregation-76,Serial3 --> core-8,Serial4*)
  104-76; (*aggregation-55,Serial1 --> core-0,Serial7*)
  114-61; (*core-14,Serial2 --> aggregation-37,Serial8*)
  109-121; (*core-19,Serial5 --> aggregation-48,Serial0*)
  28-1; (*aggregation-118,Serial6 --> edge-121,Serial3*)
  69-23; (*edge-74,Serial1 --> aggregation-66,Serial5*)
  84-36; (*core-23,Serial0 --> aggregation-99,Serial8*)
  61-108; (*aggregation-37,Serial9 --> core-10,Serial2*)
  105-124; (*aggregation-49,Serial1 --> edge-50,Serial1*)
  59-103; (*edge-63,Serial2 --> aggregation-58,Serial9*)
  9-67; (*edge-33,Serial4 --> aggregation-29,Serial1*)
  43-11; (*aggregation-107,Serial8 --> edge-114,Serial2*)
  8-33; (*aggregation-76,Serial5 --> edge-80,Serial1*)
  25-80; (*aggregation-116,Serial0 --> core-5,Serial6*)
  29-3; (*aggregation-117,Serial8 --> edge-123,Serial2*)
  97-107; (*aggregation-57,Serial2 --> core-11,Serial8*)
  80-23; (*core-5,Serial1 --> aggregation-66,Serial0*)
  51-110; (*aggregation-88,Serial3 --> core-18,Serial7*)
  25-1; (*aggregation-116,Serial6 --> edge-121,Serial1*)
  36-89; (*aggregation-99,Serial6 --> core-21,Serial0*)
  101-60; (*aggregation-59,Serial0 --> edge-64,Serial3*)
  47-82; (*aggregation-86,Serial2 --> core-7,Serial7*)
  32-85; (*aggregation-97,Serial3 --> edge-103,Serial1*)
  108-97; (*core-10,Serial8 --> aggregation-57,Serial1*)
  28-112; (*aggregation-118,Serial1 --> core-16,Serial6*)
  64-103; (*edge-61,Serial2 --> aggregation-58,Serial7*)
  5-33; (*aggregation-78,Serial5 --> edge-80,Serial3*)
  23-87; (*aggregation-66,Serial9 --> core-6,Serial1*)
  119-81; (*aggregation-45,Serial4 --> core-4,Serial4*)
  105-118; (*aggregation-49,Serial8 --> edge-52,Serial1*)
  56-110; (*aggregation-38,Serial8 --> core-18,Serial2*)
  27-3; (*aggregation-119,Serial8 --> edge-123,Serial4*)
  16-27; (*edge-120,Serial4 --> aggregation-119,Serial5*)
  81-6; (*core-4,Serial3 --> aggregation-75,Serial4*)
  6-79; (*aggregation-75,Serial2 --> core-2,Serial3*)
  42-110; (*aggregation-108,Serial2 --> core-18,Serial3*)
  78-61; (*edge-44,Serial2 --> aggregation-37,Serial5*)
  12-42; (*edge-111,Serial3 --> aggregation-108,Serial9*)
  121-112; (*aggregation-48,Serial3 --> core-16,Serial5*)
  49-48; (*aggregation-85,Serial6 --> edge-91,Serial2*)
  111-51; (*core-17,Serial7 --> aggregation-88,Serial2*)
  95-22; (*core-24,Serial1 --> aggregation-69,Serial9*)
  108-32; (*core-10,Serial0 --> aggregation-97,Serial5*)
  118-121; (*edge-52,Serial0 --> aggregation-48,Serial7*)
  92-32; (*edge-100,Serial1 --> aggregation-97,Serial0*)
  3-27; (*edge-123,Serial4 --> aggregation-119,Serial8*)
  55-102; (*aggregation-39,Serial3 --> edge-43,Serial4*)
  84-105; (*core-23,Serial5 --> aggregation-49,Serial6*)
  104-75; (*aggregation-55,Serial5 --> core-1,Serial7*)
  43-115; (*aggregation-107,Serial3 --> core-13,Serial3*)
  64-99; (*edge-61,Serial0 --> aggregation-56,Serial7*)
  65-21; (*aggregation-28,Serial0 --> edge-32,Serial3*)
  37-6; (*edge-82,Serial0 --> aggregation-75,Serial7*)
  68-24; (*edge-73,Serial0 --> aggregation-65,Serial8*)
  106-66; (*core-12,Serial9 --> aggregation-27,Serial5*)
  115-61; (*core-13,Serial2 --> aggregation-37,Serial6*)
  123-122; (*aggregation-47,Serial9 --> edge-54,Serial4*)
  36-84; (*aggregation-99,Serial8 --> core-23,Serial0*)
  86-22; (*core-22,Serial1 --> aggregation-69,Serial7*)
  118-123; (*edge-52,Serial4 --> aggregation-47,Serial7*)
  87-25; (*core-6,Serial6 --> aggregation-116,Serial1*)
  1-25; (*edge-121,Serial1 --> aggregation-116,Serial6*)
  15-44; (*edge-112,Serial1 --> aggregation-106,Serial7*)
  31-5; (*edge-84,Serial3 --> aggregation-78,Serial9*)
  64-104; (*edge-61,Serial4 --> aggregation-55,Serial7*)
  6-37; (*aggregation-75,Serial7 --> edge-82,Serial0*)
  70-21; (*aggregation-26,Serial0 --> edge-32,Serial1*)
  68-19; (*edge-73,Serial2 --> aggregation-67,Serial3*)
  87-70; (*core-6,Serial9 --> aggregation-26,Serial4*)
  49-54; (*aggregation-85,Serial9 --> edge-94,Serial2*)
  124-123; (*edge-50,Serial4 --> aggregation-47,Serial5*)
  55-96; (*aggregation-39,Serial0 --> edge-40,Serial4*)
  3-25; (*edge-123,Serial1 --> aggregation-116,Serial8*)
  11-44; (*edge-114,Serial1 --> aggregation-106,Serial9*)
  102-62; (*edge-43,Serial1 --> aggregation-36,Serial6*)
  100-61; (*edge-42,Serial2 --> aggregation-37,Serial3*)
  91-67; (*core-20,Serial9 --> aggregation-29,Serial3*)
  117-82; (*aggregation-46,Serial2 --> core-7,Serial5*)
  10-71; (*edge-34,Serial0 --> aggregation-25,Serial2*)
  70-18; (*aggregation-26,Serial8 --> edge-30,Serial1*)
  43-108; (*aggregation-107,Serial0 --> core-10,Serial3*)
  19-107; (*aggregation-67,Serial5 --> core-11,Serial1*)
  71-75; (*aggregation-25,Serial6 --> core-1,Serial8*)
  90-70; (*core-8,Serial9 --> aggregation-26,Serial6*)
  37-5; (*edge-82,Serial3 --> aggregation-78,Serial7*)
  123-115; (*aggregation-47,Serial3 --> core-13,Serial5*)
  123-116; (*aggregation-47,Serial6 --> edge-51,Serial4*)
  53-48; (*aggregation-87,Serial6 --> edge-91,Serial4*)
  73-19; (*edge-71,Serial2 --> aggregation-67,Serial1*)
  41-84; (*aggregation-109,Serial8 --> core-23,Serial3*)
  106-32; (*core-12,Serial0 --> aggregation-97,Serial7*)
  91-4; (*core-20,Serial4 --> aggregation-79,Serial9*)
  20-71; (*edge-31,Serial0 --> aggregation-25,Serial9*)
  53-54; (*aggregation-87,Serial9 --> edge-94,Serial4*)
  88-8; (*core-9,Serial4 --> aggregation-76,Serial4*)
  49-77; (*aggregation-85,Serial3 --> core-3,Serial6*)
  49-76; (*aggregation-85,Serial0 --> core-0,Serial6*)
  93-32; (*edge-102,Serial1 --> aggregation-97,Serial2*)
  17-68; (*aggregation-68,Serial3 --> edge-73,Serial3*)
  114-29; (*core-14,Serial6 --> aggregation-117,Serial4*)
  104-63; (*aggregation-55,Serial6 --> edge-60,Serial4*)
  17-111; (*aggregation-68,Serial7 --> core-17,Serial1*)
  104-59; (*aggregation-55,Serial9 --> edge-63,Serial4*)
  108-19; (*core-10,Serial1 --> aggregation-67,Serial9*)
  24-81; (*aggregation-65,Serial4 --> core-4,Serial0*)
  58-100; (*aggregation-35,Serial7 --> edge-42,Serial0*)
  24-75; (*aggregation-65,Serial1 --> core-1,Serial0*)
  65-18; (*aggregation-28,Serial8 --> edge-30,Serial3*)
  101-86; (*aggregation-59,Serial3 --> core-22,Serial8*)
  65-111; (*aggregation-28,Serial5 --> core-17,Serial9*)
  33-5; (*edge-80,Serial3 --> aggregation-78,Serial5*)
  109-51; (*core-19,Serial7 --> aggregation-88,Serial4*)
  79-6; (*core-2,Serial3 --> aggregation-75,Serial2*)
  85-36; (*edge-103,Serial3 --> aggregation-99,Serial3*)
  45-53; (*edge-90,Serial4 --> aggregation-87,Serial5*)
  122-123; (*edge-54,Serial4 --> aggregation-47,Serial9*)
  53-107; (*aggregation-87,Serial1 --> core-11,Serial7*)
  30-85; (*aggregation-95,Serial7 --> edge-103,Serial4*)
  90-34; (*core-8,Serial0 --> aggregation-96,Serial7*)
  70-88; (*aggregation-26,Serial7 --> core-9,Serial9*)
  59-104; (*edge-63,Serial4 --> aggregation-55,Serial9*)
  87-99; (*core-6,Serial8 --> aggregation-56,Serial2*)
  41-91; (*aggregation-109,Serial5 --> core-20,Serial3*)
  123-108; (*aggregation-47,Serial0 --> core-10,Serial5*)
  108-29; (*core-10,Serial6 --> aggregation-117,Serial0*)
  29-107; (*aggregation-117,Serial1 --> core-11,Serial6*)
  29-114; (*aggregation-117,Serial4 --> core-14,Serial6*)
  113-51; (*core-15,Serial7 --> aggregation-88,Serial0*)
  97-57; (*aggregation-57,Serial8 --> edge-62,Serial1*)
  46-45; (*aggregation-89,Serial5 --> edge-90,Serial1*)
  98-56; (*edge-41,Serial3 --> aggregation-38,Serial1*)
  52-47; (*edge-93,Serial3 --> aggregation-86,Serial8*)
  99-64; (*aggregation-56,Serial7 --> edge-61,Serial0*)
  108-123; (*core-10,Serial5 --> aggregation-47,Serial0*)
  81-58; (*core-4,Serial1 --> aggregation-35,Serial4*)
  19-108; (*aggregation-67,Serial9 --> core-10,Serial1*)
  114-32; (*core-14,Serial0 --> aggregation-97,Serial9*)
  106-29; (*core-12,Serial6 --> aggregation-117,Serial2*)
  82-8; (*core-7,Serial4 --> aggregation-76,Serial2*)
  40-79; (*aggregation-105,Serial2 --> core-2,Serial2*)
  109-5; (*core-19,Serial4 --> aggregation-78,Serial0*)
  53-114; (*aggregation-87,Serial4 --> core-14,Serial7*)
  72-17; (*edge-70,Serial3 --> aggregation-68,Serial0*)
  102-56; (*edge-43,Serial3 --> aggregation-38,Serial3*)
  30-92; (*aggregation-95,Serial4 --> edge-100,Serial4*)
  105-89; (*aggregation-49,Serial9 --> core-21,Serial5*)
  61-96; (*aggregation-37,Serial1 --> edge-40,Serial2*)
  103-64; (*aggregation-58,Serial7 --> edge-61,Serial2*)
  32-106; (*aggregation-97,Serial7 --> core-12,Serial0*)
  99-80; (*aggregation-56,Serial1 --> core-5,Serial8*)
  4-84; (*aggregation-79,Serial7 --> core-23,Serial4*)
  50-53; (*edge-92,Serial4 --> aggregation-87,Serial7*)
  110-56; (*core-18,Serial2 --> aggregation-38,Serial8*)
  48-47; (*edge-91,Serial3 --> aggregation-86,Serial6*)
  13-42; (*edge-110,Serial3 --> aggregation-108,Serial3*)
  2-28; (*edge-124,Serial3 --> aggregation-118,Serial9*)
  40-11; (*aggregation-105,Serial9 --> edge-114,Serial0*)
  47-52; (*aggregation-86,Serial8 --> edge-93,Serial3*)
  71-9; (*aggregation-25,Serial1 --> edge-33,Serial0*)
  5-112; (*aggregation-78,Serial1 --> core-16,Serial4*)
  27-86; (*aggregation-119,Serial3 --> core-22,Serial6*)
  32-83; (*aggregation-97,Serial4 --> edge-104,Serial1*)
  30-75; (*aggregation-95,Serial1 --> core-1,Serial9*)
  84-46; (*core-23,Serial7 --> aggregation-89,Serial3*)
  26-76; (*aggregation-115,Serial0 --> core-0,Serial5*)
  43-14; (*aggregation-107,Serial7 --> edge-113,Serial2*)
  111-38; (*core-17,Serial0 --> aggregation-98,Serial7*)
  22-84; (*aggregation-69,Serial8 --> core-23,Serial1*)
  22-91; (*aggregation-69,Serial5 --> core-20,Serial1*)
  112-56; (*core-16,Serial2 --> aggregation-38,Serial6*)
  108-43; (*core-10,Serial3 --> aggregation-107,Serial0*)
  23-90; (*aggregation-66,Serial7 --> core-8,Serial1*)
  12-41; (*edge-111,Serial4 --> aggregation-109,Serial1*)
  61-102; (*aggregation-37,Serial4 --> edge-43,Serial2*)
  103-109; (*aggregation-58,Serial5 --> core-19,Serial8*)
  99-90; (*aggregation-56,Serial4 --> core-8,Serial8*)
  44-82; (*aggregation-106,Serial2 --> core-7,Serial3*)
  67-84; (*aggregation-29,Serial6 --> core-23,Serial9*)
  8-35; (*aggregation-76,Serial6 --> edge-81,Serial1*)
  62-87; (*aggregation-36,Serial1 --> core-6,Serial2*)
  105-116; (*aggregation-49,Serial2 --> edge-51,Serial1*)
  8-80; (*aggregation-76,Serial0 --> core-5,Serial4*)
  56-98; (*aggregation-38,Serial1 --> edge-41,Serial3*)
  106-43; (*core-12,Serial3 --> aggregation-107,Serial2*)
  109-103; (*core-19,Serial8 --> aggregation-58,Serial5*)
  111-5; (*core-17,Serial4 --> aggregation-78,Serial2*)
  9-70; (*edge-33,Serial1 --> aggregation-26,Serial1*)
  51-50; (*aggregation-88,Serial7 --> edge-92,Serial0*)
  94-30; (*edge-101,Serial4 --> aggregation-95,Serial5*)
  7-115; (*aggregation-77,Serial3 --> core-13,Serial4*)
  96-61; (*edge-40,Serial2 --> aggregation-37,Serial1*)
  89-41; (*core-21,Serial3 --> aggregation-109,Serial6*)
  24-73; (*aggregation-65,Serial6 --> edge-71,Serial0*)
  71-20; (*aggregation-25,Serial9 --> edge-31,Serial0*)
  42-15; (*aggregation-108,Serial5 --> edge-112,Serial3*)
  66-9; (*aggregation-27,Serial1 --> edge-33,Serial2*)
  81-49; (*core-4,Serial6 --> aggregation-85,Serial4*)
  85-34; (*edge-103,Serial0 --> aggregation-96,Serial3*)
  109-28; (*core-19,Serial6 --> aggregation-118,Serial4*)
  42-11; (*aggregation-108,Serial7 --> edge-114,Serial3*)
  111-103; (*core-17,Serial8 --> aggregation-58,Serial3*)
  35-8; (*edge-81,Serial1 --> aggregation-76,Serial6*)
  60-104; (*edge-64,Serial4 --> aggregation-55,Serial0*)
  66-107; (*aggregation-27,Serial4 --> core-11,Serial9*)
  24-68; (*aggregation-65,Serial8 --> edge-73,Serial0*)
  70-87; (*aggregation-26,Serial4 --> core-6,Serial9*)
  4-39; (*aggregation-79,Serial3 --> edge-83,Serial4*)
  91-22; (*core-20,Serial1 --> aggregation-69,Serial5*)
  84-41; (*core-23,Serial3 --> aggregation-109,Serial8*)
  109-38; (*core-19,Serial0 --> aggregation-98,Serial9*)
  113-103; (*core-15,Serial8 --> aggregation-58,Serial1*)
  20-65; (*edge-31,Serial3 --> aggregation-28,Serial9*)
  121-118; (*aggregation-48,Serial7 --> edge-52,Serial0*)
  26-0; (*aggregation-115,Serial7 --> edge-122,Serial0*)
  44-15; (*aggregation-106,Serial7 --> edge-112,Serial1*)
  56-111; (*aggregation-38,Serial7 --> core-17,Serial2*)
  113-42; (*core-15,Serial3 --> aggregation-108,Serial4*)
  13-44; (*edge-110,Serial1 --> aggregation-106,Serial5*)
  59-101; (*edge-63,Serial3 --> aggregation-59,Serial9*)
  38-93; (*aggregation-98,Serial2 --> edge-102,Serial2*)
  51-111; (*aggregation-88,Serial2 --> core-17,Serial7*)
  119-116; (*aggregation-45,Serial6 --> edge-51,Serial2*)
  5-35; (*aggregation-78,Serial6 --> edge-81,Serial3*)
  25-2; (*aggregation-116,Serial9 --> edge-124,Serial1*)
  0-28; (*edge-122,Serial3 --> aggregation-118,Serial7*)
  27-91; (*aggregation-119,Serial1 --> core-20,Serial6*)
  58-81; (*aggregation-35,Serial4 --> core-4,Serial1*)
  28-109; (*aggregation-118,Serial4 --> core-19,Serial6*)
  41-15; (*aggregation-109,Serial2 --> edge-112,Serial4*)
  8-82; (*aggregation-76,Serial2 --> core-7,Serial4*)
  77-30; (*core-3,Serial9 --> aggregation-95,Serial3*)
  88-47; (*core-9,Serial7 --> aggregation-86,Serial4*)
  32-92; (*aggregation-97,Serial0 --> edge-100,Serial1*)
  119-75; (*aggregation-45,Serial1 --> core-1,Serial4*)
  95-27; (*core-24,Serial6 --> aggregation-119,Serial0*)
  10-65; (*edge-34,Serial3 --> aggregation-28,Serial2*)
  85-30; (*edge-103,Serial4 --> aggregation-95,Serial7*)
  89-46; (*core-21,Serial7 --> aggregation-89,Serial1*)
  39-8; (*edge-83,Serial1 --> aggregation-76,Serial8*)
  111-121; (*core-17,Serial5 --> aggregation-48,Serial4*)
  27-16; (*aggregation-119,Serial5 --> edge-120,Serial4*)
  109-65; (*core-19,Serial9 --> aggregation-28,Serial7*)
  5-110; (*aggregation-78,Serial3 --> core-18,Serial4*)
  67-9; (*aggregation-29,Serial1 --> edge-33,Serial4*)
  27-0; (*aggregation-119,Serial7 --> edge-122,Serial4*)
  60-101; (*edge-64,Serial3 --> aggregation-59,Serial0*)
  16-28; (*edge-120,Serial3 --> aggregation-118,Serial5*)
  57-97; (*edge-62,Serial1 --> aggregation-57,Serial8*)
  47-87; (*aggregation-86,Serial1 --> core-6,Serial7*)
  51-109; (*aggregation-88,Serial4 --> core-19,Serial7*)
  14-41; (*edge-113,Serial4 --> aggregation-109,Serial3*)
  75-30; (*core-1,Serial9 --> aggregation-95,Serial1*)
  108-61; (*core-10,Serial2 --> aggregation-37,Serial9*)
  21-65; (*edge-32,Serial3 --> aggregation-28,Serial0*)
  40-13; (*aggregation-105,Serial5 --> edge-110,Serial0*)
  25-90; (*aggregation-116,Serial3 --> core-8,Serial6*)
  111-65; (*core-17,Serial9 --> aggregation-28,Serial5*)
  29-16; (*aggregation-117,Serial5 --> edge-120,Serial2*)
  97-114; (*aggregation-57,Serial5 --> core-14,Serial8*)
  28-2; (*aggregation-118,Serial9 --> edge-124,Serial3*)
  121-122; (*aggregation-48,Serial9 --> edge-54,Serial0*)
  113-121; (*core-15,Serial5 --> aggregation-48,Serial2*)
  56-109; (*aggregation-38,Serial9 --> core-19,Serial2*)
  90-23; (*core-8,Serial1 --> aggregation-66,Serial7*)
  62-90; (*aggregation-36,Serial8 --> core-8,Serial2*)
  121-110; (*aggregation-48,Serial1 --> core-18,Serial5*)
  29-0; (*aggregation-117,Serial7 --> edge-122,Serial2*)
  104-79; (*aggregation-55,Serial2 --> core-2,Serial7*)
  63-97; (*edge-60,Serial1 --> aggregation-57,Serial6*)
  47-90; (*aggregation-86,Serial3 --> core-8,Serial7*)
  64-101; (*edge-61,Serial3 --> aggregation-59,Serial7*)
  62-102; (*aggregation-36,Serial6 --> edge-43,Serial1*)
  34-93; (*aggregation-96,Serial2 --> edge-102,Serial0*)
  36-91; (*aggregation-99,Serial5 --> core-20,Serial0*)
  36-83; (*aggregation-99,Serial4 --> edge-104,Serial3*)
  38-113; (*aggregation-98,Serial5 --> core-15,Serial0*)
  70-80; (*aggregation-26,Serial3 --> core-5,Serial9*)
  22-73; (*aggregation-69,Serial1 --> edge-71,Serial4*)
  22-69; (*aggregation-69,Serial4 --> edge-74,Serial4*)
  36-92; (*aggregation-99,Serial0 --> edge-100,Serial3*)
  58-77; (*aggregation-35,Serial3 --> core-3,Serial1*)
  25-3; (*aggregation-116,Serial8 --> edge-123,Serial1*)
  67-20; (*aggregation-29,Serial9 --> edge-31,Serial4*)
  51-48; (*aggregation-88,Serial6 --> edge-91,Serial0*)
  51-54; (*aggregation-88,Serial9 --> edge-94,Serial0*)
  86-4; (*core-22,Serial4 --> aggregation-79,Serial6*)
  80-62; (*core-5,Serial2 --> aggregation-36,Serial0*)
  48-46; (*edge-91,Serial1 --> aggregation-89,Serial6*)
  58-78; (*aggregation-35,Serial9 --> edge-44,Serial0*)
  105-91; (*aggregation-49,Serial3 --> core-20,Serial5*)
  76-49; (*core-0,Serial6 --> aggregation-85,Serial0*)
  83-30; (*edge-104,Serial4 --> aggregation-95,Serial8*)
  93-38; (*edge-102,Serial2 --> aggregation-98,Serial2*)
  106-19; (*core-12,Serial1 --> aggregation-67,Serial6*)
  20-67; (*edge-31,Serial4 --> aggregation-29,Serial9*)
  53-50; (*aggregation-87,Serial7 --> edge-92,Serial4*)
  62-82; (*aggregation-36,Serial2 --> core-7,Serial2*)
  41-89; (*aggregation-109,Serial6 --> core-21,Serial3*)
  99-59; (*aggregation-56,Serial9 --> edge-63,Serial0*)
  5-39; (*aggregation-78,Serial8 --> edge-83,Serial3*)
  81-119; (*core-4,Serial4 --> aggregation-45,Serial4*)
  55-91; (*aggregation-39,Serial5 --> core-20,Serial2*)
  9-66; (*edge-33,Serial2 --> aggregation-27,Serial1*)
  46-91; (*aggregation-89,Serial0 --> core-20,Serial7*)
  80-34; (*core-5,Serial0 --> aggregation-96,Serial1*)
  13-40; (*edge-110,Serial0 --> aggregation-105,Serial5*)
  17-109; (*aggregation-68,Serial9 --> core-19,Serial1*)
  24-79; (*aggregation-65,Serial2 --> core-2,Serial0*)
  40-81; (*aggregation-105,Serial4 --> core-4,Serial2*)
  117-120; (*aggregation-46,Serial8 --> edge-53,Serial3*)
  89-55; (*core-21,Serial2 --> aggregation-39,Serial6*)
  117-124; (*aggregation-46,Serial5 --> edge-50,Serial3*)
  30-77; (*aggregation-95,Serial3 --> core-3,Serial9*)
  30-76; (*aggregation-95,Serial0 --> core-0,Serial9*)
  86-27; (*core-22,Serial6 --> aggregation-119,Serial3*)
  65-112; (*aggregation-28,Serial4 --> core-16,Serial9*)
  43-12; (*aggregation-107,Serial5 --> edge-111,Serial2*)
  79-119; (*core-2,Serial4 --> aggregation-45,Serial2*)
  90-47; (*core-8,Serial7 --> aggregation-86,Serial3*)
  89-105; (*core-21,Serial5 --> aggregation-49,Serial9*)
  8-39; (*aggregation-76,Serial8 --> edge-83,Serial1*)
  61-78; (*aggregation-37,Serial5 --> edge-44,Serial2*)
  30-83; (*aggregation-95,Serial8 --> edge-104,Serial4*)
  58-98; (*aggregation-35,Serial6 --> edge-41,Serial0*)
  84-55; (*core-23,Serial2 --> aggregation-39,Serial8*)
  123-106; (*aggregation-47,Serial2 --> core-12,Serial5*)
  19-73; (*aggregation-67,Serial1 --> edge-71,Serial2*)
  52-46; (*edge-93,Serial1 --> aggregation-89,Serial8*)
  27-84; (*aggregation-119,Serial4 --> core-23,Serial6*)
  2-29; (*edge-124,Serial2 --> aggregation-117,Serial9*)
  41-95; (*aggregation-109,Serial9 --> core-24,Serial3*)
  35-7; (*edge-81,Serial2 --> aggregation-77,Serial6*)
  66-20; (*aggregation-27,Serial9 --> edge-31,Serial2*)
  87-47; (*core-6,Serial7 --> aggregation-86,Serial1*)
  87-34; (*core-6,Serial0 --> aggregation-96,Serial8*)
  83-38; (*edge-104,Serial2 --> aggregation-98,Serial4*)
  18-70; (*edge-30,Serial1 --> aggregation-26,Serial8*)
  79-49; (*core-2,Serial6 --> aggregation-85,Serial2*)
  92-38; (*edge-100,Serial2 --> aggregation-98,Serial0*)
  114-19; (*core-14,Serial1 --> aggregation-67,Serial8*)
  70-10; (*aggregation-26,Serial2 --> edge-34,Serial1*)
  82-44; (*core-7,Serial3 --> aggregation-106,Serial2*)
  32-107; (*aggregation-97,Serial6 --> core-11,Serial0*)
  49-50; (*aggregation-85,Serial7 --> edge-92,Serial2*)
  32-114; (*aggregation-97,Serial9 --> core-14,Serial0*)
  110-121; (*core-18,Serial5 --> aggregation-48,Serial1*)
  43-107; (*aggregation-107,Serial1 --> core-11,Serial3*)
  124-117; (*edge-50,Serial3 --> aggregation-46,Serial5*)
  14-42; (*edge-113,Serial3 --> aggregation-108,Serial6*)
  110-5; (*core-18,Serial4 --> aggregation-78,Serial3*)
  88-62; (*core-9,Serial2 --> aggregation-36,Serial9*)
  103-111; (*aggregation-58,Serial3 --> core-17,Serial8*)
  114-123; (*core-14,Serial5 --> aggregation-47,Serial4*)
  118-119; (*edge-52,Serial2 --> aggregation-45,Serial7*)
  98-55; (*edge-41,Serial4 --> aggregation-39,Serial1*)
  4-89; (*aggregation-79,Serial5 --> core-21,Serial4*)
  69-17; (*edge-74,Serial3 --> aggregation-68,Serial4*)
  108-53; (*core-10,Serial7 --> aggregation-87,Serial0*)
  19-69; (*aggregation-67,Serial4 --> edge-74,Serial2*)
  26-79; (*aggregation-115,Serial2 --> core-2,Serial5*)
  29-108; (*aggregation-117,Serial0 --> core-10,Serial6*)
  61-106; (*aggregation-37,Serial7 --> core-12,Serial2*)
  53-106; (*aggregation-87,Serial2 --> core-12,Serial7*)
  34-87; (*aggregation-96,Serial8 --> core-6,Serial0*)
  54-49; (*edge-94,Serial2 --> aggregation-85,Serial9*)
  39-4; (*edge-83,Serial4 --> aggregation-79,Serial3*)
  69-22; (*edge-74,Serial4 --> aggregation-69,Serial4*)
  79-71; (*core-2,Serial8 --> aggregation-25,Serial7*)
  42-109; (*aggregation-108,Serial8 --> core-19,Serial3*)
  23-69; (*aggregation-66,Serial5 --> edge-74,Serial1*)
  112-28; (*core-16,Serial6 --> aggregation-118,Serial1*)
  75-58; (*core-1,Serial1 --> aggregation-35,Serial1*)
  80-8; (*core-5,Serial4 --> aggregation-76,Serial0*)
  121-113; (*aggregation-48,Serial2 --> core-15,Serial5*)
  56-78; (*aggregation-38,Serial4 --> edge-44,Serial3*)
  99-87; (*aggregation-56,Serial2 --> core-6,Serial8*)
  4-86; (*aggregation-79,Serial6 --> core-22,Serial4*)
  103-59; (*aggregation-58,Serial9 --> edge-63,Serial2*)
  103-63; (*aggregation-58,Serial6 --> edge-60,Serial2*)
  84-101; (*core-23,Serial8 --> aggregation-59,Serial4*)
  23-72; (*aggregation-66,Serial1 --> edge-70,Serial1*)
  23-68; (*aggregation-66,Serial4 --> edge-73,Serial1*)
  50-49; (*edge-92,Serial2 --> aggregation-85,Serial7*)
  75-40; (*core-1,Serial2 --> aggregation-105,Serial1*)
  106-53; (*core-12,Serial7 --> aggregation-87,Serial2*)
  76-71; (*core-0,Serial8 --> aggregation-25,Serial5*)
  6-75; (*aggregation-75,Serial1 --> core-1,Serial3*)
  35-4; (*edge-81,Serial4 --> aggregation-79,Serial1*)
  74-22; (*edge-72,Serial4 --> aggregation-69,Serial2*)
  30-94; (*aggregation-95,Serial5 --> edge-101,Serial4*)
  32-108; (*aggregation-97,Serial5 --> core-10,Serial0*)
  99-63; (*aggregation-56,Serial6 --> edge-60,Serial0*)
  88-44; (*core-9,Serial3 --> aggregation-106,Serial4*)
  88-99; (*core-9,Serial8 --> aggregation-56,Serial5*)
  42-113; (*aggregation-108,Serial4 --> core-15,Serial3*)
  52-53; (*edge-93,Serial4 --> aggregation-87,Serial8*)
  29-115; (*aggregation-117,Serial3 --> core-13,Serial6*)
  89-101; (*core-21,Serial8 --> aggregation-59,Serial2*)
  42-12; (*aggregation-108,Serial9 --> edge-111,Serial3*)
  45-49; (*edge-90,Serial2 --> aggregation-85,Serial5*)
  122-119; (*edge-54,Serial2 --> aggregation-45,Serial9*)
  82-62; (*core-7,Serial2 --> aggregation-36,Serial2*)
  95-4; (*core-24,Serial4 --> aggregation-79,Serial8*)
  101-57; (*aggregation-59,Serial8 --> edge-62,Serial3*)
  104-64; (*aggregation-55,Serial7 --> edge-61,Serial4*)
  106-123; (*core-12,Serial5 --> aggregation-47,Serial2*)
  17-112; (*aggregation-68,Serial6 --> core-16,Serial1*)
  114-53; (*core-14,Serial7 --> aggregation-87,Serial4*)
  77-40; (*core-3,Serial2 --> aggregation-105,Serial3*)
  72-22; (*edge-70,Serial4 --> aggregation-69,Serial0*)
  102-55; (*edge-43,Serial4 --> aggregation-39,Serial3*)
  18-66; (*edge-30,Serial2 --> aggregation-27,Serial8*)
  94-36; (*edge-101,Serial3 --> aggregation-99,Serial1*)
  25-82; (*aggregation-116,Serial2 --> core-7,Serial6*)
  97-115; (*aggregation-57,Serial4 --> core-13,Serial8*)
  74-17; (*edge-72,Serial3 --> aggregation-68,Serial2*)
  47-45; (*aggregation-86,Serial5 --> edge-90,Serial3*)
  115-7; (*core-13,Serial4 --> aggregation-77,Serial3*)
  117-87; (*aggregation-46,Serial1 --> core-6,Serial5*)
  86-105; (*core-22,Serial5 --> aggregation-49,Serial0*)
  32-94; (*aggregation-97,Serial1 --> edge-101,Serial1*)
  16-29; (*edge-120,Serial2 --> aggregation-117,Serial5*)
  26-77; (*aggregation-115,Serial3 --> core-3,Serial5*)
  96-62; (*edge-40,Serial1 --> aggregation-36,Serial3*)
  68-23; (*edge-73,Serial1 --> aggregation-66,Serial4*)
  57-103; (*edge-62,Serial2 --> aggregation-58,Serial8*)
  10-67; (*edge-34,Serial4 --> aggregation-29,Serial2*)
  56-112; (*aggregation-38,Serial6 --> core-16,Serial2*)
  96-58; (*edge-40,Serial0 --> aggregation-35,Serial5*)
  111-17; (*core-17,Serial1 --> aggregation-68,Serial7*)
  119-79; (*aggregation-45,Serial2 --> core-2,Serial4*)
  77-104; (*core-3,Serial7 --> aggregation-55,Serial3*)
  81-24; (*core-4,Serial0 --> aggregation-65,Serial4*)
  118-117; (*edge-52,Serial3 --> aggregation-46,Serial7*)
  124-105; (*edge-50,Serial1 --> aggregation-49,Serial1*)
  95-67; (*core-24,Serial9 --> aggregation-29,Serial7*)
  91-36; (*core-20,Serial0 --> aggregation-99,Serial5*)
  111-42; (*core-17,Serial3 --> aggregation-108,Serial1*)
  62-96; (*aggregation-36,Serial3 --> edge-40,Serial1*)
  47-80; (*aggregation-86,Serial0 --> core-5,Serial7*)
  55-100; (*aggregation-39,Serial2 --> edge-42,Serial4*)
  119-118; (*aggregation-45,Serial7 --> edge-52,Serial2*)
  100-58; (*edge-42,Serial0 --> aggregation-35,Serial7*)
  7-31; (*aggregation-77,Serial9 --> edge-84,Serial2*)
  46-52; (*aggregation-89,Serial8 --> edge-93,Serial1*)
  81-26; (*core-4,Serial5 --> aggregation-115,Serial4*)
  54-51; (*edge-94,Serial0 --> aggregation-88,Serial9*)
  21-71; (*edge-32,Serial0 --> aggregation-25,Serial0*)
  86-67; (*core-22,Serial9 --> aggregation-29,Serial5*)
  61-114; (*aggregation-37,Serial8 --> core-14,Serial2*)
  38-109; (*aggregation-98,Serial9 --> core-19,Serial0*)
  39-7; (*edge-83,Serial2 --> aggregation-77,Serial8*)
  22-72; (*aggregation-69,Serial0 --> edge-70,Serial4*)
  121-116; (*aggregation-48,Serial6 --> edge-51,Serial0*)
  17-74; (*aggregation-68,Serial2 --> edge-72,Serial3*)
  87-117; (*core-6,Serial5 --> aggregation-46,Serial1*)
  50-51; (*edge-92,Serial0 --> aggregation-88,Serial7*)
  65-10; (*aggregation-28,Serial2 --> edge-34,Serial3*)
  79-26; (*core-2,Serial5 --> aggregation-115,Serial2*)
  6-31; (*aggregation-75,Serial9 --> edge-84,Serial0*)
  123-118; (*aggregation-47,Serial7 --> edge-52,Serial4*)
  83-32; (*edge-104,Serial1 --> aggregation-97,Serial4*)
  3-26; (*edge-123,Serial0 --> aggregation-115,Serial8*)
  11-40; (*edge-114,Serial0 --> aggregation-105,Serial9*)
  67-91; (*aggregation-29,Serial3 --> core-20,Serial9*)
  76-119; (*core-0,Serial4 --> aggregation-45,Serial0*)
  66-106; (*aggregation-27,Serial5 --> core-12,Serial9*)
  100-62; (*edge-42,Serial1 --> aggregation-36,Serial5*)
  58-96; (*aggregation-35,Serial5 --> edge-40,Serial0*)
  4-37; (*aggregation-79,Serial2 --> edge-82,Serial4*)
  45-51; (*edge-90,Serial0 --> aggregation-88,Serial5*)
  81-30; (*core-4,Serial9 --> aggregation-95,Serial9*)
  122-121; (*edge-54,Serial0 --> aggregation-48,Serial9*)
  109-17; (*core-19,Serial1 --> aggregation-68,Serial9*)
  59-99; (*edge-63,Serial0 --> aggregation-56,Serial9*)
  86-36; (*core-22,Serial0 --> aggregation-99,Serial7*)
  1-26; (*edge-121,Serial0 --> aggregation-115,Serial6*)
  15-40; (*edge-112,Serial0 --> aggregation-105,Serial7*)
  33-6; (*edge-80,Serial0 --> aggregation-75,Serial5*)
  73-24; (*edge-71,Serial0 --> aggregation-65,Serial6*)
  78-58; (*edge-44,Serial0 --> aggregation-35,Serial9*)
  34-85; (*aggregation-96,Serial3 --> edge-103,Serial0*)
  108-66; (*core-10,Serial9 --> aggregation-27,Serial3*)
  8-37; (*aggregation-76,Serial7 --> edge-82,Serial1*)
  43-15; (*aggregation-107,Serial6 --> edge-112,Serial2*)
  115-97; (*core-13,Serial8 --> aggregation-57,Serial4*)
  67-21; (*aggregation-29,Serial0 --> edge-32,Serial4*)
  5-111; (*aggregation-78,Serial2 --> core-17,Serial4*)
  91-27; (*core-20,Serial6 --> aggregation-119,Serial1*)
  117-122; (*aggregation-46,Serial9 --> edge-54,Serial3*)
  40-14; (*aggregation-105,Serial8 --> edge-113,Serial0*)
  90-25; (*core-8,Serial6 --> aggregation-116,Serial3*)
  0-29; (*edge-122,Serial2 --> aggregation-117,Serial7*)
  41-12; (*aggregation-109,Serial1 --> edge-111,Serial4*)
  73-23; (*edge-71,Serial1 --> aggregation-66,Serial2*)
  6-33; (*aggregation-75,Serial5 --> edge-80,Serial0*)
  12-43; (*edge-111,Serial2 --> aggregation-107,Serial5*)
  19-114; (*aggregation-67,Serial8 --> core-14,Serial1*)
  90-117; (*core-8,Serial5 --> aggregation-46,Serial3*)
  36-94; (*aggregation-99,Serial1 --> edge-101,Serial3*)
  60-99; (*edge-64,Serial0 --> aggregation-56,Serial0*)
  61-115; (*aggregation-37,Serial6 --> core-13,Serial2*)
  113-17; (*core-15,Serial1 --> aggregation-68,Serial5*)
  46-95; (*aggregation-89,Serial4 --> core-24,Serial7*)
  28-110; (*aggregation-118,Serial3 --> core-18,Serial6*)
  47-88; (*aggregation-86,Serial4 --> core-9,Serial7*)
  51-112; (*aggregation-88,Serial1 --> core-16,Serial7*)
  1-27; (*edge-121,Serial4 --> aggregation-119,Serial6*)
  79-24; (*core-2,Serial0 --> aggregation-65,Serial2*)
  5-37; (*aggregation-78,Serial7 --> edge-82,Serial3*)
  62-88; (*aggregation-36,Serial9 --> core-9,Serial2*)
  26-1; (*aggregation-115,Serial6 --> edge-121,Serial0*)
  44-12; (*aggregation-106,Serial6 --> edge-111,Serial1*)
  107-7; (*core-11,Serial4 --> aggregation-77,Serial1*)
  99-60; (*aggregation-56,Serial0 --> edge-64,Serial0*)
  34-82; (*aggregation-96,Serial6 --> core-7,Serial0*)
  124-121; (*edge-50,Serial0 --> aggregation-48,Serial5*)
  14-43; (*edge-113,Serial2 --> aggregation-107,Serial7*)
  31-6; (*edge-84,Serial0 --> aggregation-75,Serial9*)
  44-90; (*aggregation-106,Serial3 --> core-8,Serial3*)
  76-24; (*core-0,Serial0 --> aggregation-65,Serial0*)
  62-78; (*aggregation-36,Serial7 --> edge-44,Serial1*)
  114-66; (*core-14,Serial9 --> aggregation-27,Serial7*)
  7-33; (*aggregation-77,Serial5 --> edge-80,Serial2*)
  55-95; (*aggregation-39,Serial9 --> core-24,Serial2*)
  28-3; (*aggregation-118,Serial8 --> edge-123,Serial3*)
  95-36; (*core-24,Serial0 --> aggregation-99,Serial9*)
  29-1; (*aggregation-117,Serial6 --> edge-121,Serial2*)
  63-103; (*edge-60,Serial2 --> aggregation-58,Serial6*)
  38-85; (*aggregation-98,Serial3 --> edge-103,Serial2*)
  103-60; (*aggregation-58,Serial0 --> edge-64,Serial2*)
  107-97; (*core-11,Serial8 --> aggregation-57,Serial2*)
  21-67; (*edge-32,Serial4 --> aggregation-29,Serial0*)
}

let nodes = 125

let ospfIntraArea = 0u2
let ospfInterArea = 1u2
let ospfE1 = 2u2
let ospfE2 = 3u2

let protoConn = 0u8
let protoStatic = 1u8
let protoOspf = 2u8
let protoBgp = 3u8

let isProtocol fib x = fib = x
let min x y = x <u8 y

let pickOption f x y =
  match (x,y) with
  | (None, _) -> false  | (_, None) -> true
  | (Some a, Some b) -> f a b

let betterOspf o1 o2 =
  if o1.areaType >u2 o2.areaType then true
  else if o2.areaType >u2 o1.areaType then false
  else if o1.weight <=u16 o2.weight then true else false

let betterBgp b1 b2 =
  if b1.lp > b2.lp then true
  else if b2.lp > b1.lp then false
  else if b1.aslen < b2.aslen then true
  else if b2.aslen < b1.aslen then false
  else if b1.med >= b2.med then true else false

let betterEqOption o1 o2 =
  match (o1,o2) with
  | (_, None) -> true
  | (None, _) -> false
  | (Some a, Some b) -> a <=u8 b

let best c s o b =
  match (c,s,o,b) with
  | (None,None,None,None) -> None
  | _ ->
      let o = match o with | None -> None | Some o -> Some o.ospfAd in
      let b = match b with | None -> None | Some b -> Some b.bgpAd in
      let (x,p1) = if betterEqOption c s then (c,0u2) else (s,1u2) in
      let (y,p2) = if betterEqOption o b then (o,2u2) else (b,3u2) in
      Some (if betterEqOption x y then p1 else p2)

let mergeValues x y =
  let c = if (pickOption min x.connected y.connected) then x.connected else y.connected in
  let s = if (pickOption min x.static y.static) then x.static else y.static in
  let o = if (pickOption betterOspf x.ospf y.ospf) then x.ospf else y.ospf in
  let b = if (pickOption betterBgp x.bgp y.bgp) then x.bgp else y.bgp in
  { connected = c;
    static = s;
    ospf = o;
    bgp = b;
    selected = best c s o b}

let merge node x y = mergeValues x y

let init node =
  match node with
  | 0n ->
)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (9~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413568) && (prefix < 1174413824)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (10~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413824) && (prefix < 1174414080)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (10~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413824) && (prefix < 1174414080)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (10~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413824) && (prefix < 1174414080)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (10~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413824) && (prefix < 1174414080)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (10~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413824) && (prefix < 1174414080)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (11~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174434304) && (prefix < 1174434560)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (11~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174434304) && (prefix < 1174434560)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (11~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174434304) && (prefix < 1174434560)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (11~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174434304) && (prefix < 1174434560)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (11~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174434304) && (prefix < 1174434560)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (12~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433536) && (prefix < 1174433792)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (12~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433536) && (prefix < 1174433792)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (12~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433536) && (prefix < 1174433792)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (12~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433536) && (prefix < 1174433792)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (12~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433536) && (prefix < 1174433792)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (13~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433280) && (prefix < 1174433536)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (13~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433280) && (prefix < 1174433536)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (13~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433280) && (prefix < 1174433536)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (13~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433280) && (prefix < 1174433536)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (13~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433280) && (prefix < 1174433536)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (14~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174434048) && (prefix < 1174434304)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (14~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174434048) && (prefix < 1174434304)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (14~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174434048) && (prefix < 1174434304)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (14~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174434048) && (prefix < 1174434304)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (14~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174434048) && (prefix < 1174434304)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (15~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433792) && (prefix < 1174434048)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (15~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433792) && (prefix < 1174434048)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (15~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433792) && (prefix < 1174434048)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (15~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433792) && (prefix < 1174434048)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (15~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174433792) && (prefix < 1174434048)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (16~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174435840) && (prefix < 1174436096)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (16~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174435840) && (prefix < 1174436096)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (16~26) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174435840) && (prefix < 1174436096)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (16~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174435840) && (prefix < 1174436096)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (16~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174435840) && (prefix < 1174436096)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (17~74) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~111) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~113) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~112) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~109) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (17~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (18~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412800) && (prefix < 1174413056)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (18~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412800) && (prefix < 1174413056)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (18~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412800) && (prefix < 1174413056)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (18~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412800) && (prefix < 1174413056)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (18~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174412800) && (prefix < 1174413056)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (19~108) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~107) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~114) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~115) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (19~74) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (20~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413056) && (prefix < 1174413312)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (20~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413056) && (prefix < 1174413312)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (20~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413056) && (prefix < 1174413312)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (20~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413056) && (prefix < 1174413312)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (20~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413056) && (prefix < 1174413312)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (21~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413312) && (prefix < 1174413568)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (21~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413312) && (prefix < 1174413568)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (21~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413312) && (prefix < 1174413568)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (21~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413312) && (prefix < 1174413568)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (21~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174413312) && (prefix < 1174413568)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (22~95) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (22~74) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (22~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (22~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (22~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (22~86) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (22~84) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (22~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (22~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (22~91) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (23~87) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (23~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (23~90) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (23~80) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (23~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (23~88) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (23~74) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (23~82) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (23~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (23~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (24~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (24~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (24~74) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (24~81) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (24~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (24~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (24~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (24~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (24~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (24~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~2) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~82) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~1) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~0) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~80) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~90) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~3) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~87) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~16) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (25~88) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (26~2) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (26~81) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (26~1) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (26~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (26~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (26~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (26~0) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (26~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (26~3) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (26~16) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (27~0) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (27~91) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (27~84) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (27~3) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (27~95) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (27~16) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (27~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (27~86) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (27~2) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (27~1) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~1) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~111) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~0) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~109) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~3) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~16) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~113) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~2) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (28~112) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (29~2) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (29~1) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (29~107) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (29~114) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (29~0) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (29~108) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (29~115) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (29~3) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (29~16) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (29~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~81) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~93) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~85) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~92) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~94) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (30~83) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (31~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426624) && (prefix < 1174426880)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (31~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426624) && (prefix < 1174426880)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (31~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426624) && (prefix < 1174426880)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (31~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426624) && (prefix < 1174426880)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (31~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426624) && (prefix < 1174426880)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (32~94) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~83) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~93) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~107) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~114) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~85) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~92) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~108) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (32~115) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (33~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425600) && (prefix < 1174425856)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (33~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425600) && (prefix < 1174425856)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (33~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425600) && (prefix < 1174425856)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (33~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425600) && (prefix < 1174425856)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (33~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425600) && (prefix < 1174425856)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (34~83) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~94) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~88) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~87) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~93) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~80) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~82) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~85) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~90) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (34~92) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (35~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425856) && (prefix < 1174426112)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (35~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425856) && (prefix < 1174426112)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (35~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425856) && (prefix < 1174426112)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (35~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425856) && (prefix < 1174426112)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (35~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174425856) && (prefix < 1174426112)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (36~86) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (36~93) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (36~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (36~92) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (36~91) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (36~85) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (36~95) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (36~83) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (36~84) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (36~94) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (37~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426112) && (prefix < 1174426368)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (37~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426112) && (prefix < 1174426368)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (37~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426112) && (prefix < 1174426368)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (37~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426112) && (prefix < 1174426368)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (37~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426112) && (prefix < 1174426368)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (38~113) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~94) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~93) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~109) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~85) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~92) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~112) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~83) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (38~111) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (39~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426368) && (prefix < 1174426624)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (39~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426368) && (prefix < 1174426624)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (39~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426368) && (prefix < 1174426624)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (39~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426368) && (prefix < 1174426624)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (39~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174426368) && (prefix < 1174426624)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (40~13) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~12) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (40~81) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~86) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~12) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~84) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~13) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~91) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (41~95) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (42~13) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (42~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (42~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (42~111) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (42~113) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (42~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (42~12) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (42~109) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (42~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (42~112) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~108) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~115) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~13) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~114) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~12) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (43~107) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~11) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~12) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~82) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~87) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~15) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~88) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~90) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~13) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (44~80) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (45~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428160) && (prefix < 1174428416)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (45~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428160) && (prefix < 1174428416)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (45~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428160) && (prefix < 1174428416)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (45~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428160) && (prefix < 1174428416)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (45~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428160) && (prefix < 1174428416)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (46~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~95) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~91) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~84) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~86) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (46~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~90) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~82) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~88) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~87) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~80) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (47~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (48~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428416) && (prefix < 1174428672)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (48~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428416) && (prefix < 1174428672)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (48~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428416) && (prefix < 1174428672)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (48~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428416) && (prefix < 1174428672)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (48~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428416) && (prefix < 1174428672)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (49~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~81) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (49~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (50~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428672) && (prefix < 1174428928)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (50~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428672) && (prefix < 1174428928)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (50~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428672) && (prefix < 1174428928)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (50~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428672) && (prefix < 1174428928)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (50~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428672) && (prefix < 1174428928)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (51~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (51~113) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (51~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (51~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (51~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (51~111) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (51~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (51~112) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (51~109) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (51~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (52~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428928) && (prefix < 1174429184)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (52~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428928) && (prefix < 1174429184)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (52~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428928) && (prefix < 1174429184)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (52~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428928) && (prefix < 1174429184)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (52~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174428928) && (prefix < 1174429184)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (53~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~108) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~115) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~54) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~114) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~107) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~52) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (53~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (54~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174429184) && (prefix < 1174429440)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (54~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174429184) && (prefix < 1174429440)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (54~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174429184) && (prefix < 1174429440)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (54~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174429184) && (prefix < 1174429440)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (54~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174429184) && (prefix < 1174429440)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (55~84) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~91) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~98) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~95) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~96) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~102) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~86) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (55~100) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~112) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~109) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~98) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~113) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~96) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~102) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~111) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (56~100) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (57~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420992) && (prefix < 1174421248)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (57~99) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420992) && (prefix < 1174421248)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (57~97) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420992) && (prefix < 1174421248)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (57~104) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420992) && (prefix < 1174421248)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (57~103) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420992) && (prefix < 1174421248)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (58~98) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~100) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~81) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~102) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (58~96) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (59~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421248) && (prefix < 1174421504)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (59~99) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421248) && (prefix < 1174421504)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (59~97) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421248) && (prefix < 1174421504)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (59~103) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421248) && (prefix < 1174421504)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (59~104) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421248) && (prefix < 1174421504)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (60~99) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421504) && (prefix < 1174421760)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (60~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421504) && (prefix < 1174421760)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (60~103) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421504) && (prefix < 1174421760)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (60~104) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421504) && (prefix < 1174421760)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (60~97) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174421504) && (prefix < 1174421760)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (61~102) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~96) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~115) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~107) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~114) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~100) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~98) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~108) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (61~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~88) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~98) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~102) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~90) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~96) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~80) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~100) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~87) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (62~82) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (63~104) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420480) && (prefix < 1174420736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (63~103) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420480) && (prefix < 1174420736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (63~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420480) && (prefix < 1174420736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (63~99) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420480) && (prefix < 1174420736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (63~97) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420480) && (prefix < 1174420736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (64~104) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420736) && (prefix < 1174420992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (64~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420736) && (prefix < 1174420992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (64~99) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420736) && (prefix < 1174420992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (64~97) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420736) && (prefix < 1174420992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (64~103) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174420736) && (prefix < 1174420992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (65~20) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~113) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~10) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~18) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~111) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~21) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~112) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~109) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (65~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~115) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~20) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~18) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~108) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~10) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~107) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~114) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (66~21) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~86) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~20) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~10) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~84) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~91) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~18) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~21) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~95) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (67~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (68~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423808) && (prefix < 1174424064)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (68~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423808) && (prefix < 1174424064)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (68~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423808) && (prefix < 1174424064)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (68~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423808) && (prefix < 1174424064)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (68~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423808) && (prefix < 1174424064)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (69~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424064) && (prefix < 1174424320)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (69~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424064) && (prefix < 1174424320)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (69~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424064) && (prefix < 1174424320)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (69~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424064) && (prefix < 1174424320)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (69~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174424064) && (prefix < 1174424320)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (70~80) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~90) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~18) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~10) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~82) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~87) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~88) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~21) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (70~20) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~18) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~9) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~10) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~81) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~20) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (71~21) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (72~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423040) && (prefix < 1174423296)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (72~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423040) && (prefix < 1174423296)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (72~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423040) && (prefix < 1174423296)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (72~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423040) && (prefix < 1174423296)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (72~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423040) && (prefix < 1174423296)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (73~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423296) && (prefix < 1174423552)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (73~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423296) && (prefix < 1174423552)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (73~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423296) && (prefix < 1174423552)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (73~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423296) && (prefix < 1174423552)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (73~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423296) && (prefix < 1174423552)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (74~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423552) && (prefix < 1174423808)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (74~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423552) && (prefix < 1174423808)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (74~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423552) && (prefix < 1174423808)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (74~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423552) && (prefix < 1174423808)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (74~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174423552) && (prefix < 1174423808)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (75~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~104) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~26) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~119) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (75~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (76~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (76~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (76~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (76~104) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (76~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (76~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (76~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (76~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (76~119) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (76~26) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~119) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~104) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~26) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (77~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (78~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416384) && (prefix < 1174416640)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (78~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416384) && (prefix < 1174416640)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (78~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416384) && (prefix < 1174416640)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (78~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416384) && (prefix < 1174416640)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (78~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416384) && (prefix < 1174416640)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (79~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~119) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~104) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~26) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (79~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (80~99) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (80~117) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (80~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (80~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (80~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (80~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (80~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (80~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (80~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (80~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (81~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (81~104) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (81~119) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (81~71) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (81~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (81~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (81~26) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (81~24) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (81~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (81~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (82~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (82~99) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (82~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (82~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (82~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (82~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (82~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (82~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (82~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (82~117) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (83~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431744) && (prefix < 1174432000)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (83~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431744) && (prefix < 1174432000)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (83~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431744) && (prefix < 1174432000)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (83~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431744) && (prefix < 1174432000)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (83~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431744) && (prefix < 1174432000)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (84~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (84~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (84~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (84~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (84~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (84~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (84~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (84~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (84~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (84~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (85~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431488) && (prefix < 1174431744)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (85~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431488) && (prefix < 1174431744)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (85~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431488) && (prefix < 1174431744)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (85~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431488) && (prefix < 1174431744)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (85~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431488) && (prefix < 1174431744)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (86~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (86~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (86~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (86~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (86~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (86~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (86~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (86~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (86~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (86~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (87~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (87~117) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (87~99) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (87~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (87~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (87~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (87~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (87~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (87~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (87~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (88~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (88~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (88~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (88~99) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (88~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (88~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (88~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (88~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (88~117) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (88~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (89~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (89~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (89~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (89~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (89~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (89~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (89~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (89~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (89~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (89~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (90~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (90~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (90~99) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (90~23) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (90~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (90~70) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (90~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (90~117) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (90~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (90~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (91~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (91~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (91~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (91~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (91~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (91~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (91~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (91~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (91~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (91~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (92~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174430720) && (prefix < 1174430976)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (92~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174430720) && (prefix < 1174430976)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (92~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174430720) && (prefix < 1174430976)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (92~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174430720) && (prefix < 1174430976)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (92~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174430720) && (prefix < 1174430976)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (93~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431232) && (prefix < 1174431488)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (93~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431232) && (prefix < 1174431488)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (93~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431232) && (prefix < 1174431488)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (93~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431232) && (prefix < 1174431488)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (93~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174431232) && (prefix < 1174431488)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (94~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174430976) && (prefix < 1174431232)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (94~30) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174430976) && (prefix < 1174431232)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (94~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174430976) && (prefix < 1174431232)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (94~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174430976) && (prefix < 1174431232)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (94~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174430976) && (prefix < 1174431232)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (95~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (95~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (95~46) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (95~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (95~4) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (95~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (95~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (95~67) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (95~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (95~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (96~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415360) && (prefix < 1174415616)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (96~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415360) && (prefix < 1174415616)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (96~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415360) && (prefix < 1174415616)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (96~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415360) && (prefix < 1174415616)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (96~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415360) && (prefix < 1174415616)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (97~108) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (97~115) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (97~64) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (97~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (97~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (97~107) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (97~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (97~114) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (97~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (97~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (98~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415616) && (prefix < 1174415872)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (98~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415616) && (prefix < 1174415872)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (98~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415616) && (prefix < 1174415872)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (98~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415616) && (prefix < 1174415872)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (98~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415616) && (prefix < 1174415872)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (99~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (99~90) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (99~80) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (99~87) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (99~64) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (99~88) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (99~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (99~82) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (99~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (99~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (100~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415872) && (prefix < 1174416128)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (100~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415872) && (prefix < 1174416128)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (100~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415872) && (prefix < 1174416128)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (100~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415872) && (prefix < 1174416128)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (100~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174415872) && (prefix < 1174416128)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (101~64) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (101~84) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (101~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (101~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (101~86) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (101~95) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (101~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (101~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (101~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (101~91) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (102~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416128) && (prefix < 1174416384)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (102~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416128) && (prefix < 1174416384)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (102~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416128) && (prefix < 1174416384)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (102~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416128) && (prefix < 1174416384)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (102~55) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174416128) && (prefix < 1174416384)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (103~109) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (103~64) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (103~113) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (103~112) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (103~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (103~111) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (103~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (103~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (103~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (103~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (104~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (104~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (104~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (104~57) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (104~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (104~64) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (104~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (104~63) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (104~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (104~81) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (105~95) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (105~91) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (105~120) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (105~118) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (105~84) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (105~86) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (105~124) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (105~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (105~116) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (105~122) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (106~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (106~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (106~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (106~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (106~123) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (106~97) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (106~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (106~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (106~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (106~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (107~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (107~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (107~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (107~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (107~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (107~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (107~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (107~123) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (107~97) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (107~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (108~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (108~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (108~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (108~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (108~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (108~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (108~123) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (108~97) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (108~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (108~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (109~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (109~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (109~103) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (109~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (109~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (109~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (109~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (109~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (109~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (109~121) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (110~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (110~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (110~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (110~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (110~103) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (110~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (110~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (110~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (110~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (110~121) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (111~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (111~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (111~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (111~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (111~103) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (111~121) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (111~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (111~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (111~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (111~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (112~103) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (112~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (112~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (112~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (112~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (112~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (112~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (112~121) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (112~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (112~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (113~56) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (113~17) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (113~103) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (113~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (113~5) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (113~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (113~28) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (113~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (113~121) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (113~65) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (114~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (114~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (114~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (114~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (114~97) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (114~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (114~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (114~123) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (114~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (114~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (115~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (115~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (115~7) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (115~97) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (115~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (115~19) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (115~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (115~61) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (115~123) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (115~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (116~123) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418176) && (prefix < 1174418432)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (116~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418176) && (prefix < 1174418432)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (116~121) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418176) && (prefix < 1174418432)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (116~117) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418176) && (prefix < 1174418432)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (116~119) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418176) && (prefix < 1174418432)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (117~82) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (117~120) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (117~124) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (117~118) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (117~87) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (117~88) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (117~116) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (117~80) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (117~90) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (117~122) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (118~121) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418432) && (prefix < 1174418688)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (118~123) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418432) && (prefix < 1174418688)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (118~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418432) && (prefix < 1174418688)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (118~117) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418432) && (prefix < 1174418688)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (118~119) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418432) && (prefix < 1174418688)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (119~75) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (119~81) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (119~120) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (119~79) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (119~118) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (119~122) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (119~116) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (119~76) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (119~124) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (119~77) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (120~123) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418688) && (prefix < 1174418944)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (120~121) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418688) && (prefix < 1174418944)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (120~117) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418688) && (prefix < 1174418944)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (120~119) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418688) && (prefix < 1174418944)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (120~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418688) && (prefix < 1174418944)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (121~124) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (121~113) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (121~120) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (121~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (121~109) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (121~111) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (121~118) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (121~116) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (121~122) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (121~112) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (122~119) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418944) && (prefix < 1174419200)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (122~123) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418944) && (prefix < 1174419200)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (122~121) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418944) && (prefix < 1174419200)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (122~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418944) && (prefix < 1174419200)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (122~117) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174418944) && (prefix < 1174419200)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (123~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (123~120) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (123~124) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (123~114) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (123~107) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (123~118) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (123~115) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (123~122) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (123~116) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (123~108) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (124~123) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174417920) && (prefix < 1174418176)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (124~117) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174417920) && (prefix < 1174418176)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (124~119) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174417920) && (prefix < 1174418176)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (124~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174417920) && (prefix < 1174418176)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (124~121) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174417920) && (prefix < 1174418176)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b
)

let trans edge x =
  let o = transferOspf edge x.ospf in
  let b = transferBgp edge x in
  {connected=None; static=None; ospf=o; bgp=b; selected=None}

let assert_node node x =
  if true then
    match x.selected with
      | None -> false
      | _ -> true
  else true


(* {edge-122=0, edge-121=1, edge-124=2, edge-123=3, aggregation-79=4,
   aggregation-78=5, aggregation-75=6, aggregation-77=7, aggregation-76=8,
   edge-33=9, edge-34=10, edge-114=11, edge-111=12, edge-110=13, edge-113=14,
   edge-112=15, edge-120=16, aggregation-68=17, edge-30=18, aggregation-67=19,
   edge-31=20, edge-32=21, aggregation-69=22, aggregation-66=23, aggregation-65=24,
   aggregation-116=25, aggregation-115=26, aggregation-119=27, aggregation-118=28,
   aggregation-117=29, aggregation-95=30, edge-84=31, aggregation-97=32,
   edge-80=33, aggregation-96=34, edge-81=35, aggregation-99=36, edge-82=37,
   aggregation-98=38, edge-83=39, aggregation-105=40, aggregation-109=41,
   aggregation-108=42, aggregation-107=43, aggregation-106=44, edge-90=45,
   aggregation-89=46, aggregation-86=47, edge-91=48, aggregation-85=49, edge-92=50,
   aggregation-88=51, edge-93=52, aggregation-87=53, edge-94=54, aggregation-39=55,
   aggregation-38=56, edge-62=57, aggregation-35=58, edge-63=59, edge-64=60,
   aggregation-37=61, aggregation-36=62, edge-60=63, edge-61=64, aggregation-28=65,
   aggregation-27=66, aggregation-29=67, edge-73=68, edge-74=69, aggregation-26=70,
   aggregation-25=71, edge-70=72, edge-71=73, edge-72=74, core-1=75, core-0=76,
   core-3=77, edge-44=78, core-2=79, core-5=80, core-4=81, core-7=82, edge-104=83,
   core-23=84, edge-103=85, core-6=86, core-22=87, core-9=88, core-21=89,
   core-8=90, core-20=91, edge-100=92, edge-102=93, edge-101=94, core-24=95,
   edge-40=96, aggregation-57=97, edge-41=98, aggregation-56=99, edge-42=100,
   aggregation-59=101, edge-43=102, aggregation-58=103, aggregation-55=104,
   aggregation-49=105, core-12=106, core-11=107, core-10=108, core-19=109,
   core-18=110, core-17=111, core-16=112, core-15=113, core-14=114, core-13=115,
   edge-51=116, aggregation-46=117, edge-52=118, aggregation-45=119, edge-53=120,
   aggregation-48=121, edge-54=122, aggregation-47=123,
   edge-50=124}*)

let sol = solution { init = init; trans = trans; merge = merge; interface = interface; rtrans = trans }

assert foldNodes (fun k v acc -> acc && assert_node k v) sol true
require (match (d, pod) with
 | ((1174436352, 24u6), 9) -> true
 | ((1174436096, 24u6), 9) -> true
 | ((1174436864, 24u6), 9) -> true
 | ((1174436608, 24u6), 9) -> true
 | ((1174413568, 24u6), 0) -> true
 | ((1174413824, 24u6), 0) -> true
 | ((1174434304, 24u6), 8) -> true
 | ((1174433536, 24u6), 8) -> true
 | ((1174433280, 24u6), 8) -> true
 | ((1174434048, 24u6), 8) -> true
 | ((1174433792, 24u6), 8) -> true
 | ((1174435840, 24u6), 9) -> true
 | ((1174412800, 24u6), 0) -> true
 | ((1174413056, 24u6), 0) -> true
 | ((1174413312, 24u6), 0) -> true
 | ((1174426624, 24u6), 5) -> true
 | ((1174425600, 24u6), 5) -> true
 | ((1174425856, 24u6), 5) -> true
 | ((1174426112, 24u6), 5) -> true
 | ((1174426368, 24u6), 5) -> true
 | ((1174428160, 24u6), 6) -> true
 | ((1174428416, 24u6), 6) -> true
 | ((1174428672, 24u6), 6) -> true
 | ((1174428928, 24u6), 6) -> true
 | ((1174429184, 24u6), 6) -> true
 | ((1174420992, 24u6), 3) -> true
 | ((1174421248, 24u6), 3) -> true
 | ((1174421504, 24u6), 3) -> true
 | ((1174420480, 24u6), 3) -> true
 | ((1174420736, 24u6), 3) -> true
 | ((1174423808, 24u6), 4) -> true
 | ((1174424064, 24u6), 4) -> true
 | ((1174423040, 24u6), 4) -> true
 | ((1174423296, 24u6), 4) -> true
 | ((1174423552, 24u6), 4) -> true
 | ((1174416384, 24u6), 1) -> true
 | ((1174431744, 24u6), 7) -> true
 | ((1174431488, 24u6), 7) -> true
 | ((1174430720, 24u6), 7) -> true
 | ((1174431232, 24u6), 7) -> true
 | ((1174430976, 24u6), 7) -> true
 | ((1174415360, 24u6), 1) -> true
 | ((1174415616, 24u6), 1) -> true
 | ((1174415872, 24u6), 1) -> true
 | ((1174416128, 24u6), 1) -> true
 | ((1174418176, 24u6), 2) -> true
 | ((1174418432, 24u6), 2) -> true
 | ((1174418688, 24u6), 2) -> true
 | ((1174418944, 24u6), 2) -> true
 | ((1174417920, 24u6), 2) -> true
 | _ -> false)

let partition node = match node with
  | 0n -> 0
  | 1n -> 1
  | 2n -> 2
  | 3n -> 3
  | 4n -> 4
  | 5n -> 5
  | 6n -> 6
  | 7n -> 7
  | 8n -> 8
  | 9n -> 9
  | 10n -> 10
  | 11n -> 11
  | 12n -> 12
  | 13n -> 13
  | 14n -> 14
  | 15n -> 15
  | 16n -> 16
  | 17n -> 17
  | 18n -> 18
  | 19n -> 19
  | 20n -> 20
  | 21n -> 21
  | 22n -> 22
  | 23n -> 23
  | 24n -> 24
  | 25n -> 25
  | 26n -> 26
  | 27n -> 27
  | 28n -> 28
  | 29n -> 29
  | 30n -> 30
  | 31n -> 31
  | 32n -> 32
  | 33n -> 33
  | 34n -> 34
  | 35n -> 35
  | 36n -> 36
  | 37n -> 37
  | 38n -> 38
  | 39n -> 39
  | 40n -> 40
  | 41n -> 41
  | 42n -> 42
  | 43n -> 43
  | 44n -> 44
  | 45n -> 45
  | 46n -> 46
  | 47n -> 47
  | 48n -> 48
  | 49n -> 49
  | 50n -> 50
  | 51n -> 51
  | 52n -> 52
  | 53n -> 53
  | 54n -> 54
  | 55n -> 55
  | 56n -> 56
  | 57n -> 57
  | 58n -> 58
  | 59n -> 59
  | 60n -> 60
  | 61n -> 61
  | 62n -> 62
  | 63n -> 63
  | 64n -> 64
  | 65n -> 65
  | 66n -> 66
  | 67n -> 67
  | 68n -> 68
  | 69n -> 69
  | 70n -> 70
  | 71n -> 71
  | 72n -> 72
  | 73n -> 73
  | 74n -> 74
  | 75n -> 75
  | 76n -> 76
  | 77n -> 77
  | 78n -> 78
  | 79n -> 79
  | 80n -> 80
  | 81n -> 81
  | 82n -> 82
  | 83n -> 83
  | 84n -> 84
  | 85n -> 85
  | 86n -> 86
  | 87n -> 87
  | 88n -> 88
  | 89n -> 89
  | 90n -> 90
  | 91n -> 91
  | 92n -> 92
  | 93n -> 93
  | 94n -> 94
  | 95n -> 95
  | 96n -> 96
  | 97n -> 97
  | 98n -> 98
  | 99n -> 99
  | 100n -> 100
  | 101n -> 101
  | 102n -> 102
  | 103n -> 103
  | 104n -> 104
  | 105n -> 105
  | 106n -> 106
  | 107n -> 107
  | 108n -> 108
  | 109n -> 109
  | 110n -> 110
  | 111n -> 111
  | 112n -> 112
  | 113n -> 113
  | 114n -> 114
  | 115n -> 115
  | 116n -> 116
  | 117n -> 117
  | 118n -> 118
  | 119n -> 119
  | 120n -> 120
  | 121n -> 121
  | 122n -> 122
  | 123n -> 123
  | 124n -> 124

let interface edge a =
  match edge with
  | 0~25 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436352, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436352, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436352, 24u6) then Some 1u8 else None; }
  | 0~26 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436352, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436352, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436352, 24u6) then Some 1u8 else None; }
  | 0~27 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436352, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436352, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436352, 24u6) then Some 1u8 else None; }
  | 0~28 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436352, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436352, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436352, 24u6) then Some 1u8 else None; }
  | 0~29 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436352, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436352, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436352, 24u6) then Some 1u8 else None; }
  | 1~25 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436096, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436096, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436096, 24u6) then Some 1u8 else None; }
  | 1~26 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436096, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436096, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436096, 24u6) then Some 1u8 else None; }
  | 1~27 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436096, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436096, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436096, 24u6) then Some 1u8 else None; }
  | 1~28 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436096, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436096, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436096, 24u6) then Some 1u8 else None; }
  | 1~29 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436096, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436096, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436096, 24u6) then Some 1u8 else None; }
  | 2~25 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436864, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436864, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436864, 24u6) then Some 1u8 else None; }
  | 2~26 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436864, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436864, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436864, 24u6) then Some 1u8 else None; }
  | 2~27 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436864, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436864, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436864, 24u6) then Some 1u8 else None; }
  | 2~28 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436864, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436864, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436864, 24u6) then Some 1u8 else None; }
  | 2~29 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436864, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436864, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436864, 24u6) then Some 1u8 else None; }
  | 3~25 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436608, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436608, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436608, 24u6) then Some 1u8 else None; }
  | 3~26 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436608, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436608, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436608, 24u6) then Some 1u8 else None; }
  | 3~27 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436608, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436608, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436608, 24u6) then Some 1u8 else None; }
  | 3~28 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436608, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436608, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436608, 24u6) then Some 1u8 else None; }
  | 3~29 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174436608, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174436608, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174436608, 24u6) then Some 1u8 else None; }
  | 4~31 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 4~33 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 4~35 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 4~37 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 4~39 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 4~84 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 4~86 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 4~89 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 4~91 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 4~95 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 5~31 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 5~33 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 5~35 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 5~37 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 5~39 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 5~109 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 5~110 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 5~111 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 5~112 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 5~113 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 6~31 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 6~33 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 6~35 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 6~37 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 6~39 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 6~75 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 6~76 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 6~77 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 6~79 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 6~81 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 7~31 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 7~33 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 7~35 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 7~37 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 7~39 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 7~106 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 7~107 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 7~108 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 7~114 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 7~115 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 8~31 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 8~33 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 8~35 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 8~37 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 8~39 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 8~80 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 8~82 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 8~87 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 8~88 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 8~90 -> a = {  bgp= Some {  aslen= if pod = 5 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 9~65 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413568, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413568, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413568, 24u6) then Some 1u8 else None; }
  | 9~66 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413568, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413568, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413568, 24u6) then Some 1u8 else None; }
  | 9~67 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413568, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413568, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413568, 24u6) then Some 1u8 else None; }
  | 9~70 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413568, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413568, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413568, 24u6) then Some 1u8 else None; }
  | 9~71 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413568, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413568, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413568, 24u6) then Some 1u8 else None; }
  | 10~65 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413824, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413824, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413824, 24u6) then Some 1u8 else None; }
  | 10~66 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413824, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413824, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413824, 24u6) then Some 1u8 else None; }
  | 10~67 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413824, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413824, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413824, 24u6) then Some 1u8 else None; }
  | 10~70 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413824, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413824, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413824, 24u6) then Some 1u8 else None; }
  | 10~71 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413824, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413824, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413824, 24u6) then Some 1u8 else None; }
  | 11~40 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174434304, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174434304, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174434304, 24u6) then Some 1u8 else None; }
  | 11~41 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174434304, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174434304, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174434304, 24u6) then Some 1u8 else None; }
  | 11~42 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174434304, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174434304, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174434304, 24u6) then Some 1u8 else None; }
  | 11~43 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174434304, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174434304, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174434304, 24u6) then Some 1u8 else None; }
  | 11~44 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174434304, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174434304, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174434304, 24u6) then Some 1u8 else None; }
  | 12~40 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433536, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433536, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433536, 24u6) then Some 1u8 else None; }
  | 12~41 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433536, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433536, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433536, 24u6) then Some 1u8 else None; }
  | 12~42 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433536, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433536, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433536, 24u6) then Some 1u8 else None; }
  | 12~43 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433536, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433536, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433536, 24u6) then Some 1u8 else None; }
  | 12~44 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433536, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433536, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433536, 24u6) then Some 1u8 else None; }
  | 13~40 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433280, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433280, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433280, 24u6) then Some 1u8 else None; }
  | 13~41 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433280, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433280, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433280, 24u6) then Some 1u8 else None; }
  | 13~42 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433280, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433280, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433280, 24u6) then Some 1u8 else None; }
  | 13~43 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433280, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433280, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433280, 24u6) then Some 1u8 else None; }
  | 13~44 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433280, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433280, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433280, 24u6) then Some 1u8 else None; }
  | 14~40 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174434048, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174434048, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174434048, 24u6) then Some 1u8 else None; }
  | 14~41 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174434048, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174434048, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174434048, 24u6) then Some 1u8 else None; }
  | 14~42 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174434048, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174434048, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174434048, 24u6) then Some 1u8 else None; }
  | 14~43 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174434048, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174434048, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174434048, 24u6) then Some 1u8 else None; }
  | 14~44 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174434048, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174434048, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174434048, 24u6) then Some 1u8 else None; }
  | 15~40 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433792, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433792, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433792, 24u6) then Some 1u8 else None; }
  | 15~41 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433792, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433792, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433792, 24u6) then Some 1u8 else None; }
  | 15~42 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433792, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433792, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433792, 24u6) then Some 1u8 else None; }
  | 15~43 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433792, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433792, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433792, 24u6) then Some 1u8 else None; }
  | 15~44 -> a = {  bgp= Some {  aslen= if pod = 8 && d = (1174433792, 24u6) then 0 else if pod = 8 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174433792, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174433792, 24u6) then Some 1u8 else None; }
  | 16~25 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174435840, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174435840, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174435840, 24u6) then Some 1u8 else None; }
  | 16~26 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174435840, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174435840, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174435840, 24u6) then Some 1u8 else None; }
  | 16~27 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174435840, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174435840, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174435840, 24u6) then Some 1u8 else None; }
  | 16~28 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174435840, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174435840, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174435840, 24u6) then Some 1u8 else None; }
  | 16~29 -> a = {  bgp= Some {  aslen= if pod = 9 && d = (1174435840, 24u6) then 0 else if pod = 9 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174435840, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174435840, 24u6) then Some 1u8 else None; }
  | 17~68 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 17~69 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 17~72 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 17~73 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 17~74 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 17~109 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 17~110 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 17~111 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 17~112 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 17~113 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 18~65 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174412800, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174412800, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174412800, 24u6) then Some 1u8 else None; }
  | 18~66 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174412800, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174412800, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174412800, 24u6) then Some 1u8 else None; }
  | 18~67 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174412800, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174412800, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174412800, 24u6) then Some 1u8 else None; }
  | 18~70 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174412800, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174412800, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174412800, 24u6) then Some 1u8 else None; }
  | 18~71 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174412800, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174412800, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174412800, 24u6) then Some 1u8 else None; }
  | 19~68 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 19~69 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 19~72 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 19~73 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 19~74 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 19~106 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 19~107 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 19~108 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 19~114 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 19~115 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 20~65 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413056, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413056, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413056, 24u6) then Some 1u8 else None; }
  | 20~66 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413056, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413056, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413056, 24u6) then Some 1u8 else None; }
  | 20~67 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413056, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413056, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413056, 24u6) then Some 1u8 else None; }
  | 20~70 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413056, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413056, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413056, 24u6) then Some 1u8 else None; }
  | 20~71 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413056, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413056, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413056, 24u6) then Some 1u8 else None; }
  | 21~65 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413312, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413312, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413312, 24u6) then Some 1u8 else None; }
  | 21~66 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413312, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413312, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413312, 24u6) then Some 1u8 else None; }
  | 21~67 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413312, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413312, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413312, 24u6) then Some 1u8 else None; }
  | 21~70 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413312, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413312, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413312, 24u6) then Some 1u8 else None; }
  | 21~71 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174413312, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174413312, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174413312, 24u6) then Some 1u8 else None; }
  | 22~68 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 22~69 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 22~72 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 22~73 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 22~74 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 22~84 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 22~86 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 22~89 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 22~91 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 22~95 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 23~68 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 23~69 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 23~72 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 23~73 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 23~74 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 23~80 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 23~82 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 23~87 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 23~88 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 23~90 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 24~68 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 24~69 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 24~72 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 24~73 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 24~74 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 24~75 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 24~76 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 24~77 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 24~79 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 24~81 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 25~0 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 25~1 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 25~2 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 25~3 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 25~16 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 25~80 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 25~82 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 25~87 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 25~88 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 25~90 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 26~0 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 26~1 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 26~2 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 26~3 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 26~16 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 26~75 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 26~76 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 26~77 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 26~79 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 26~81 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 27~0 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 27~1 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 27~2 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 27~3 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 27~16 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 27~84 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 27~86 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 27~89 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 27~91 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 27~95 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 28~0 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 28~1 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 28~2 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 28~3 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 28~16 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 28~109 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 28~110 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 28~111 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 28~112 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 28~113 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 29~0 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 29~1 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 29~2 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 29~3 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 29~16 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 29~106 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 29~107 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 29~108 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 29~114 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 29~115 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 30~75 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 30~76 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 30~77 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 30~79 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 30~81 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 30~83 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 30~85 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 30~92 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 30~93 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 30~94 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 31~4 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426624, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426624, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426624, 24u6) then Some 1u8 else None; }
  | 31~5 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426624, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426624, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426624, 24u6) then Some 1u8 else None; }
  | 31~6 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426624, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426624, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426624, 24u6) then Some 1u8 else None; }
  | 31~7 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426624, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426624, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426624, 24u6) then Some 1u8 else None; }
  | 31~8 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426624, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426624, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426624, 24u6) then Some 1u8 else None; }
  | 32~83 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 32~85 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 32~92 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 32~93 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 32~94 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 32~106 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 32~107 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 32~108 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 32~114 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 32~115 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 33~4 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174425600, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425600, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425600, 24u6) then Some 1u8 else None; }
  | 33~5 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174425600, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425600, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425600, 24u6) then Some 1u8 else None; }
  | 33~6 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174425600, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425600, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425600, 24u6) then Some 1u8 else None; }
  | 33~7 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174425600, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425600, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425600, 24u6) then Some 1u8 else None; }
  | 33~8 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174425600, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425600, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425600, 24u6) then Some 1u8 else None; }
  | 34~80 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 34~82 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 34~83 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 34~85 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 34~87 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 34~88 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 34~90 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 34~92 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 34~93 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 34~94 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 35~4 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174425856, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425856, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425856, 24u6) then Some 1u8 else None; }
  | 35~5 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174425856, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425856, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425856, 24u6) then Some 1u8 else None; }
  | 35~6 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174425856, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425856, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425856, 24u6) then Some 1u8 else None; }
  | 35~7 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174425856, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425856, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425856, 24u6) then Some 1u8 else None; }
  | 35~8 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174425856, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425856, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425856, 24u6) then Some 1u8 else None; }
  | 36~83 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 36~84 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 36~85 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 36~86 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 36~89 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 36~91 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 36~92 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 36~93 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 36~94 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 36~95 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 37~4 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426112, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426112, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426112, 24u6) then Some 1u8 else None; }
  | 37~5 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426112, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426112, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426112, 24u6) then Some 1u8 else None; }
  | 37~6 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426112, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426112, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426112, 24u6) then Some 1u8 else None; }
  | 37~7 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426112, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426112, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426112, 24u6) then Some 1u8 else None; }
  | 37~8 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426112, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426112, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426112, 24u6) then Some 1u8 else None; }
  | 38~83 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 38~85 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 38~92 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 38~93 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 38~94 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 38~109 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 38~110 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 38~111 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 38~112 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 38~113 -> a = {  bgp= Some {  aslen= if pod = 7 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 39~4 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426368, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426368, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426368, 24u6) then Some 1u8 else None; }
  | 39~5 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426368, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426368, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426368, 24u6) then Some 1u8 else None; }
  | 39~6 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426368, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426368, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426368, 24u6) then Some 1u8 else None; }
  | 39~7 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426368, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426368, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426368, 24u6) then Some 1u8 else None; }
  | 39~8 -> a = {  bgp= Some {  aslen= if pod = 5 && d = (1174426368, 24u6) then 0 else if pod = 5 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174426368, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174426368, 24u6) then Some 1u8 else None; }
  | 40~11 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 40~12 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 40~13 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 40~14 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 40~15 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 40~75 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 40~76 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 40~77 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 40~79 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 40~81 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 41~11 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 41~12 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 41~13 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 41~14 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 41~15 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 41~84 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 41~86 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 41~89 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 41~91 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 41~95 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 42~11 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 42~12 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 42~13 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 42~14 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 42~15 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 42~109 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 42~110 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 42~111 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 42~112 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 42~113 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 43~11 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 43~12 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 43~13 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 43~14 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 43~15 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 43~106 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 43~107 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 43~108 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 43~114 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 43~115 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 44~11 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 44~12 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 44~13 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 44~14 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 44~15 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 44~80 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 44~82 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 44~87 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 44~88 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 44~90 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 45~46 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428160, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428160, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428160, 24u6) then Some 1u8 else None; }
  | 45~47 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428160, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428160, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428160, 24u6) then Some 1u8 else None; }
  | 45~49 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428160, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428160, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428160, 24u6) then Some 1u8 else None; }
  | 45~51 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428160, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428160, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428160, 24u6) then Some 1u8 else None; }
  | 45~53 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428160, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428160, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428160, 24u6) then Some 1u8 else None; }
  | 46~45 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 46~48 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 46~50 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 46~52 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 46~54 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 46~84 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 46~86 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 46~89 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 46~91 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 46~95 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 47~45 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 47~48 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 47~50 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 47~52 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 47~54 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 47~80 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 47~82 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 47~87 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 47~88 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 47~90 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 48~46 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428416, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428416, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428416, 24u6) then Some 1u8 else None; }
  | 48~47 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428416, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428416, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428416, 24u6) then Some 1u8 else None; }
  | 48~49 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428416, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428416, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428416, 24u6) then Some 1u8 else None; }
  | 48~51 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428416, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428416, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428416, 24u6) then Some 1u8 else None; }
  | 48~53 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428416, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428416, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428416, 24u6) then Some 1u8 else None; }
  | 49~45 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 49~48 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 49~50 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 49~52 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 49~54 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 49~75 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 49~76 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 49~77 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 49~79 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 49~81 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 50~46 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428672, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428672, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428672, 24u6) then Some 1u8 else None; }
  | 50~47 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428672, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428672, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428672, 24u6) then Some 1u8 else None; }
  | 50~49 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428672, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428672, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428672, 24u6) then Some 1u8 else None; }
  | 50~51 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428672, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428672, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428672, 24u6) then Some 1u8 else None; }
  | 50~53 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428672, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428672, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428672, 24u6) then Some 1u8 else None; }
  | 51~45 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 51~48 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 51~50 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 51~52 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 51~54 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 51~109 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 51~110 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 51~111 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 51~112 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 51~113 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 52~46 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428928, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428928, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428928, 24u6) then Some 1u8 else None; }
  | 52~47 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428928, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428928, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428928, 24u6) then Some 1u8 else None; }
  | 52~49 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428928, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428928, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428928, 24u6) then Some 1u8 else None; }
  | 52~51 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428928, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428928, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428928, 24u6) then Some 1u8 else None; }
  | 52~53 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174428928, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174428928, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174428928, 24u6) then Some 1u8 else None; }
  | 53~45 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 53~48 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 53~50 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 53~52 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 53~54 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 53~106 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 53~107 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 53~108 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 53~114 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 53~115 -> a = {  bgp= Some {  aslen= if pod = 6 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 54~46 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174429184, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174429184, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174429184, 24u6) then Some 1u8 else None; }
  | 54~47 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174429184, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174429184, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174429184, 24u6) then Some 1u8 else None; }
  | 54~49 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174429184, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174429184, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174429184, 24u6) then Some 1u8 else None; }
  | 54~51 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174429184, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174429184, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174429184, 24u6) then Some 1u8 else None; }
  | 54~53 -> a = {  bgp= Some {  aslen= if pod = 6 && d = (1174429184, 24u6) then 0 else if pod = 6 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174429184, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174429184, 24u6) then Some 1u8 else None; }
  | 55~78 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 55~84 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 55~86 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 55~89 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 55~91 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 55~95 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 55~96 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 55~98 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 55~100 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 55~102 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 56~78 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 56~96 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 56~98 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 56~100 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 56~102 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 56~109 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 56~110 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 56~111 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 56~112 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 56~113 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 57~97 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420992, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420992, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420992, 24u6) then Some 1u8 else None; }
  | 57~99 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420992, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420992, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420992, 24u6) then Some 1u8 else None; }
  | 57~101 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420992, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420992, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420992, 24u6) then Some 1u8 else None; }
  | 57~103 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420992, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420992, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420992, 24u6) then Some 1u8 else None; }
  | 57~104 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420992, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420992, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420992, 24u6) then Some 1u8 else None; }
  | 58~75 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 58~76 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 58~77 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 58~78 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 58~79 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 58~81 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 58~96 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 58~98 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 58~100 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 58~102 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 59~97 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174421248, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174421248, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174421248, 24u6) then Some 1u8 else None; }
  | 59~99 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174421248, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174421248, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174421248, 24u6) then Some 1u8 else None; }
  | 59~101 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174421248, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174421248, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174421248, 24u6) then Some 1u8 else None; }
  | 59~103 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174421248, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174421248, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174421248, 24u6) then Some 1u8 else None; }
  | 59~104 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174421248, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174421248, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174421248, 24u6) then Some 1u8 else None; }
  | 60~97 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174421504, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174421504, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174421504, 24u6) then Some 1u8 else None; }
  | 60~99 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174421504, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174421504, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174421504, 24u6) then Some 1u8 else None; }
  | 60~101 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174421504, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174421504, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174421504, 24u6) then Some 1u8 else None; }
  | 60~103 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174421504, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174421504, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174421504, 24u6) then Some 1u8 else None; }
  | 60~104 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174421504, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174421504, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174421504, 24u6) then Some 1u8 else None; }
  | 61~78 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 61~96 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 61~98 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 61~100 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 61~102 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 61~106 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 61~107 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 61~108 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 61~114 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 61~115 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 62~78 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 62~80 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 62~82 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 62~87 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 62~88 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 62~90 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 62~96 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 62~98 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 62~100 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 62~102 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 63~97 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420480, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420480, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420480, 24u6) then Some 1u8 else None; }
  | 63~99 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420480, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420480, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420480, 24u6) then Some 1u8 else None; }
  | 63~101 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420480, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420480, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420480, 24u6) then Some 1u8 else None; }
  | 63~103 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420480, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420480, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420480, 24u6) then Some 1u8 else None; }
  | 63~104 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420480, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420480, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420480, 24u6) then Some 1u8 else None; }
  | 64~97 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420736, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420736, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420736, 24u6) then Some 1u8 else None; }
  | 64~99 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420736, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420736, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420736, 24u6) then Some 1u8 else None; }
  | 64~101 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420736, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420736, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420736, 24u6) then Some 1u8 else None; }
  | 64~103 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420736, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420736, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420736, 24u6) then Some 1u8 else None; }
  | 64~104 -> a = {  bgp= Some {  aslen= if pod = 3 && d = (1174420736, 24u6) then 0 else if pod = 3 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174420736, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174420736, 24u6) then Some 1u8 else None; }
  | 65~9 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 65~10 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 65~18 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 65~20 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 65~21 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 65~109 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 65~110 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 65~111 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 65~112 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 65~113 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 66~9 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 66~10 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 66~18 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 66~20 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 66~21 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 66~106 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 66~107 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 66~108 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 66~114 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 66~115 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 67~9 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 67~10 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 67~18 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 67~20 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 67~21 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 67~84 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 67~86 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 67~89 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 67~91 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 67~95 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 68~17 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423808, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423808, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423808, 24u6) then Some 1u8 else None; }
  | 68~19 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423808, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423808, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423808, 24u6) then Some 1u8 else None; }
  | 68~22 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423808, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423808, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423808, 24u6) then Some 1u8 else None; }
  | 68~23 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423808, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423808, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423808, 24u6) then Some 1u8 else None; }
  | 68~24 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423808, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423808, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423808, 24u6) then Some 1u8 else None; }
  | 69~17 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174424064, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424064, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424064, 24u6) then Some 1u8 else None; }
  | 69~19 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174424064, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424064, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424064, 24u6) then Some 1u8 else None; }
  | 69~22 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174424064, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424064, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424064, 24u6) then Some 1u8 else None; }
  | 69~23 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174424064, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424064, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424064, 24u6) then Some 1u8 else None; }
  | 69~24 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174424064, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424064, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424064, 24u6) then Some 1u8 else None; }
  | 70~9 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 70~10 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 70~18 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 70~20 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 70~21 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 70~80 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 70~82 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 70~87 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 70~88 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 70~90 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 71~9 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 71~10 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 71~18 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 71~20 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 71~21 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 71~75 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 71~76 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 71~77 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 71~79 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 71~81 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 72~17 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423040, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423040, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423040, 24u6) then Some 1u8 else None; }
  | 72~19 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423040, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423040, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423040, 24u6) then Some 1u8 else None; }
  | 72~22 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423040, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423040, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423040, 24u6) then Some 1u8 else None; }
  | 72~23 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423040, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423040, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423040, 24u6) then Some 1u8 else None; }
  | 72~24 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423040, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423040, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423040, 24u6) then Some 1u8 else None; }
  | 73~17 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423296, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423296, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423296, 24u6) then Some 1u8 else None; }
  | 73~19 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423296, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423296, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423296, 24u6) then Some 1u8 else None; }
  | 73~22 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423296, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423296, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423296, 24u6) then Some 1u8 else None; }
  | 73~23 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423296, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423296, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423296, 24u6) then Some 1u8 else None; }
  | 73~24 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423296, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423296, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423296, 24u6) then Some 1u8 else None; }
  | 74~17 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423552, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423552, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423552, 24u6) then Some 1u8 else None; }
  | 74~19 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423552, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423552, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423552, 24u6) then Some 1u8 else None; }
  | 74~22 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423552, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423552, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423552, 24u6) then Some 1u8 else None; }
  | 74~23 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423552, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423552, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423552, 24u6) then Some 1u8 else None; }
  | 74~24 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174423552, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423552, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423552, 24u6) then Some 1u8 else None; }
  | 75~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 75~24 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 75~26 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 75~30 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 75~40 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 75~49 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 75~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 75~71 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 75~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 75~119 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 76~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 76~24 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 76~26 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 76~30 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 76~40 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 76~49 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 76~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 76~71 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 76~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 76~119 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 77~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 77~24 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 77~26 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 77~30 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 77~40 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 77~49 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 77~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 77~71 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 77~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 77~119 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 78~55 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174416384, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174416384, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174416384, 24u6) then Some 1u8 else None; }
  | 78~56 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174416384, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174416384, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174416384, 24u6) then Some 1u8 else None; }
  | 78~58 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174416384, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174416384, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174416384, 24u6) then Some 1u8 else None; }
  | 78~61 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174416384, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174416384, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174416384, 24u6) then Some 1u8 else None; }
  | 78~62 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174416384, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174416384, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174416384, 24u6) then Some 1u8 else None; }
  | 79~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 79~24 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 79~26 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 79~30 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 79~40 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 79~49 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 79~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 79~71 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 79~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 79~119 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 80~8 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 80~23 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 80~25 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 80~34 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 80~44 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 80~47 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 80~62 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 80~70 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 80~99 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 80~117 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 81~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 81~24 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 81~26 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 81~30 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 81~40 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 81~49 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 81~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 81~71 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 81~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 81~119 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 82~8 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 82~23 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 82~25 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 82~34 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 82~44 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 82~47 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 82~62 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 82~70 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 82~99 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 82~117 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 83~30 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431744, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431744, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431744, 24u6) then Some 1u8 else None; }
  | 83~32 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431744, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431744, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431744, 24u6) then Some 1u8 else None; }
  | 83~34 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431744, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431744, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431744, 24u6) then Some 1u8 else None; }
  | 83~36 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431744, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431744, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431744, 24u6) then Some 1u8 else None; }
  | 83~38 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431744, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431744, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431744, 24u6) then Some 1u8 else None; }
  | 84~4 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 84~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 84~27 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 84~36 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 84~41 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 84~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 84~55 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 84~67 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 84~101 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 84~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 85~30 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431488, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431488, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431488, 24u6) then Some 1u8 else None; }
  | 85~32 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431488, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431488, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431488, 24u6) then Some 1u8 else None; }
  | 85~34 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431488, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431488, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431488, 24u6) then Some 1u8 else None; }
  | 85~36 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431488, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431488, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431488, 24u6) then Some 1u8 else None; }
  | 85~38 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431488, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431488, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431488, 24u6) then Some 1u8 else None; }
  | 86~4 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 86~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 86~27 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 86~36 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 86~41 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 86~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 86~55 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 86~67 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 86~101 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 86~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 87~8 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 87~23 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 87~25 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 87~34 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 87~44 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 87~47 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 87~62 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 87~70 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 87~99 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 87~117 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 88~8 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 88~23 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 88~25 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 88~34 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 88~44 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 88~47 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 88~62 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 88~70 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 88~99 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 88~117 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 89~4 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 89~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 89~27 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 89~36 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 89~41 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 89~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 89~55 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 89~67 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 89~101 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 89~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 90~8 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 90~23 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 90~25 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 90~34 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 90~44 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 90~47 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 90~62 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 90~70 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 90~99 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 90~117 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 91~4 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 91~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 91~27 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 91~36 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 91~41 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 91~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 91~55 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 91~67 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 91~101 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 91~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 92~30 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174430720, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174430720, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174430720, 24u6) then Some 1u8 else None; }
  | 92~32 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174430720, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174430720, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174430720, 24u6) then Some 1u8 else None; }
  | 92~34 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174430720, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174430720, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174430720, 24u6) then Some 1u8 else None; }
  | 92~36 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174430720, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174430720, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174430720, 24u6) then Some 1u8 else None; }
  | 92~38 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174430720, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174430720, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174430720, 24u6) then Some 1u8 else None; }
  | 93~30 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431232, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431232, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431232, 24u6) then Some 1u8 else None; }
  | 93~32 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431232, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431232, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431232, 24u6) then Some 1u8 else None; }
  | 93~34 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431232, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431232, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431232, 24u6) then Some 1u8 else None; }
  | 93~36 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431232, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431232, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431232, 24u6) then Some 1u8 else None; }
  | 93~38 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174431232, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174431232, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174431232, 24u6) then Some 1u8 else None; }
  | 94~30 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174430976, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174430976, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174430976, 24u6) then Some 1u8 else None; }
  | 94~32 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174430976, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174430976, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174430976, 24u6) then Some 1u8 else None; }
  | 94~34 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174430976, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174430976, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174430976, 24u6) then Some 1u8 else None; }
  | 94~36 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174430976, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174430976, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174430976, 24u6) then Some 1u8 else None; }
  | 94~38 -> a = {  bgp= Some {  aslen= if pod = 7 && d = (1174430976, 24u6) then 0 else if pod = 7 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174430976, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174430976, 24u6) then Some 1u8 else None; }
  | 95~4 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 95~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 95~27 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 95~36 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 95~41 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 95~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 95~55 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 95~67 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 95~101 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 95~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 96~55 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415360, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415360, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415360, 24u6) then Some 1u8 else None; }
  | 96~56 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415360, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415360, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415360, 24u6) then Some 1u8 else None; }
  | 96~58 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415360, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415360, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415360, 24u6) then Some 1u8 else None; }
  | 96~61 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415360, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415360, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415360, 24u6) then Some 1u8 else None; }
  | 96~62 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415360, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415360, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415360, 24u6) then Some 1u8 else None; }
  | 97~57 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 97~59 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 97~60 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 97~63 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 97~64 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 97~106 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 97~107 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 97~108 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 97~114 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 97~115 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 98~55 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415616, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415616, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415616, 24u6) then Some 1u8 else None; }
  | 98~56 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415616, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415616, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415616, 24u6) then Some 1u8 else None; }
  | 98~58 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415616, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415616, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415616, 24u6) then Some 1u8 else None; }
  | 98~61 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415616, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415616, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415616, 24u6) then Some 1u8 else None; }
  | 98~62 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415616, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415616, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415616, 24u6) then Some 1u8 else None; }
  | 99~57 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 99~59 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 99~60 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 99~63 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 99~64 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 99~80 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 99~82 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 99~87 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 99~88 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 99~90 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 100~55 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415872, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415872, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415872, 24u6) then Some 1u8 else None; }
  | 100~56 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415872, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415872, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415872, 24u6) then Some 1u8 else None; }
  | 100~58 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415872, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415872, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415872, 24u6) then Some 1u8 else None; }
  | 100~61 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415872, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415872, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415872, 24u6) then Some 1u8 else None; }
  | 100~62 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174415872, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174415872, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174415872, 24u6) then Some 1u8 else None; }
  | 101~57 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 101~59 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 101~60 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 101~63 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 101~64 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 101~84 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 101~86 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 101~89 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 101~91 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 101~95 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 102~55 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174416128, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174416128, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174416128, 24u6) then Some 1u8 else None; }
  | 102~56 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174416128, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174416128, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174416128, 24u6) then Some 1u8 else None; }
  | 102~58 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174416128, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174416128, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174416128, 24u6) then Some 1u8 else None; }
  | 102~61 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174416128, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174416128, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174416128, 24u6) then Some 1u8 else None; }
  | 102~62 -> a = {  bgp= Some {  aslen= if pod = 1 && d = (1174416128, 24u6) then 0 else if pod = 1 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174416128, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174416128, 24u6) then Some 1u8 else None; }
  | 103~57 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 103~59 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 103~60 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 103~63 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 103~64 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 103~109 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 103~110 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 103~111 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 103~112 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 103~113 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 104~57 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 104~59 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 104~60 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 104~63 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 104~64 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 104~75 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 104~76 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 104~77 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 104~79 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 104~81 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 105~84 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 105~86 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 105~89 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 105~91 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 105~95 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 105~116 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 105~118 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 105~120 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 105~122 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 105~124 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 106~7 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 106~19 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 106~29 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 106~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 106~43 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 106~53 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 106~61 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 106~66 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 106~97 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 106~123 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 107~7 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 107~19 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 107~29 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 107~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 107~43 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 107~53 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 107~61 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 107~66 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 107~97 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 107~123 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 108~7 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 108~19 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 108~29 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 108~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 108~43 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 108~53 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 108~61 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 108~66 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 108~97 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 108~123 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 109~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 109~17 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 109~28 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 109~38 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 109~42 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 109~51 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 109~56 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 109~65 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 109~103 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 109~121 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 110~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 110~17 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 110~28 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 110~38 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 110~42 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 110~51 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 110~56 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 110~65 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 110~103 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 110~121 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 111~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 111~17 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 111~28 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 111~38 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 111~42 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 111~51 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 111~56 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 111~65 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 111~103 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 111~121 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 112~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 112~17 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 112~28 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 112~38 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 112~42 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 112~51 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 112~56 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 112~65 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 112~103 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 112~121 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 113~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 113~17 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 113~28 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 113~38 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 113~42 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 113~51 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 113~56 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 113~65 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 113~103 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 113~121 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 114~7 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 114~19 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 114~29 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 114~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 114~43 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 114~53 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 114~61 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 114~66 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 114~97 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 114~123 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 115~7 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 115~19 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 115~29 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 115~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 115~43 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 115~53 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 115~61 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 115~66 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 115~97 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 115~123 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 116~105 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418176, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418176, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418176, 24u6) then Some 1u8 else None; }
  | 116~117 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418176, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418176, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418176, 24u6) then Some 1u8 else None; }
  | 116~119 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418176, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418176, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418176, 24u6) then Some 1u8 else None; }
  | 116~121 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418176, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418176, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418176, 24u6) then Some 1u8 else None; }
  | 116~123 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418176, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418176, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418176, 24u6) then Some 1u8 else None; }
  | 117~80 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 117~82 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 117~87 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 117~88 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 117~90 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 117~116 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 117~118 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 117~120 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 117~122 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 117~124 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 118~105 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418432, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418432, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418432, 24u6) then Some 1u8 else None; }
  | 118~117 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418432, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418432, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418432, 24u6) then Some 1u8 else None; }
  | 118~119 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418432, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418432, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418432, 24u6) then Some 1u8 else None; }
  | 118~121 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418432, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418432, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418432, 24u6) then Some 1u8 else None; }
  | 118~123 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418432, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418432, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418432, 24u6) then Some 1u8 else None; }
  | 119~75 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 119~76 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 119~77 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 119~79 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 119~81 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 119~116 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 119~118 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 119~120 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 119~122 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 119~124 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 120~105 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418688, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418688, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418688, 24u6) then Some 1u8 else None; }
  | 120~117 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418688, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418688, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418688, 24u6) then Some 1u8 else None; }
  | 120~119 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418688, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418688, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418688, 24u6) then Some 1u8 else None; }
  | 120~121 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418688, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418688, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418688, 24u6) then Some 1u8 else None; }
  | 120~123 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418688, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418688, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418688, 24u6) then Some 1u8 else None; }
  | 121~109 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 121~110 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 121~111 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 121~112 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 121~113 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 121~116 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 121~118 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 121~120 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 121~122 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 121~124 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 122~105 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418944, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418944, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418944, 24u6) then Some 1u8 else None; }
  | 122~117 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418944, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418944, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418944, 24u6) then Some 1u8 else None; }
  | 122~119 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418944, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418944, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418944, 24u6) then Some 1u8 else None; }
  | 122~121 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418944, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418944, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418944, 24u6) then Some 1u8 else None; }
  | 122~123 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174418944, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174418944, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174418944, 24u6) then Some 1u8 else None; }
  | 123~106 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 123~107 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 123~108 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 123~114 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 123~115 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 123~116 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 123~118 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 123~120 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 123~122 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 123~124 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 124~105 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174417920, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174417920, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174417920, 24u6) then Some 1u8 else None; }
  | 124~117 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174417920, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174417920, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174417920, 24u6) then Some 1u8 else None; }
  | 124~119 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174417920, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174417920, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174417920, 24u6) then Some 1u8 else None; }
  | 124~121 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174417920, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174417920, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174417920, 24u6) then Some 1u8 else None; }
  | 124~123 -> a = {  bgp= Some {  aslen= if pod = 2 && d = (1174417920, 24u6) then 0 else if pod = 2 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174417920, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174417920, 24u6) then Some 1u8 else None; }
