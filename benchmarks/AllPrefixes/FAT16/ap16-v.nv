(* vim: set syntax=ocaml: *)
(* Vertically partitioned version of ap16.nv *)
(* Automatically generated by gen_part_nv.py *)
type ospfType = {ospfAd: int8; weight: int16; areaType:int2; areaId: int;}
type bgpType = {bgpAd: int8; lp: int; aslen: int; med:int; comms:set[int];}
type rib = {
    connected:option[int8];
    static:option[int8];
    ospf:option[ospfType];
    bgp:option[bgpType];
    selected:option[int2]; }
type attribute = rib

symbolic d : (int, int6)
symbolic pod : int

let edges = {
  66-52; (*core-2,Serial7 --> aggregation-160,Serial2*)
  118-226; (*aggregation-133,Serial0 --> core-45,Serial4*)
  128-164; (*aggregation-247,Serial2 --> core-63,Serial2*)
  203-11; (*core-51,Serial0 --> aggregation-198,Serial11*)
  314-33; (*core-16,Serial13 --> aggregation-178,Serial0*)
  88-223; (*edge-222,Serial6 --> aggregation-214,Serial14*)
  62-75; (*core-0,Serial3 --> aggregation-272,Serial0*)
  104-168; (*aggregation-259,Serial0 --> edge-264,Serial3*)
  222-94; (*aggregation-215,Serial10 --> edge-218,Serial7*)
  129-243; (*aggregation-246,Serial1 --> core-49,Serial11*)
  82-279; (*edge-221,Serial1 --> aggregation-209,Serial13*)
  16-37; (*aggregation-192,Serial8 --> edge-200,Serial0*)
  220-18; (*aggregation-85,Serial11 --> edge-91,Serial5*)
  230-220; (*core-42,Serial11 --> aggregation-85,Serial2*)
  137-172; (*edge-238,Serial7 --> aggregation-231,Serial6*)
  261-76; (*edge-154,Serial7 --> aggregation-151,Serial10*)
  215-123; (*core-59,Serial11 --> aggregation-71,Serial11*)
  271-3; (*core-36,Serial11 --> aggregation-196,Serial4*)
  149-253; (*aggregation-68,Serial2 --> core-34,Serial0*)
  295-34; (*edge-190,Serial1 --> aggregation-177,Serial14*)
  228-46; (*edge-169,Serial3 --> aggregation-163,Serial1*)
  223-203; (*aggregation-214,Serial3 --> core-51,Serial1*)
  183-80; (*aggregation-98,Serial1 --> edge-105,Serial2*)
  291-233; (*core-28,Serial15 --> aggregation-211,Serial2*)
  243-129; (*core-49,Serial11 --> aggregation-246,Serial1*)
  209-118; (*edge-140,Serial5 --> aggregation-133,Serial6*)
  237-52; (*edge-168,Serial0 --> aggregation-160,Serial8*)
  98-179; (*aggregation-261,Serial8 --> edge-270,Serial5*)
  227-189; (*core-44,Serial2 --> aggregation-229,Serial11*)
  145-113; (*aggregation-242,Serial8 --> edge-248,Serial2*)
  53-305; (*aggregation-288,Serial10 --> edge-298,Serial0*)
  135-204; (*aggregation-240,Serial13 --> edge-253,Serial0*)
  5-290; (*aggregation-195,Serial13 --> core-29,Serial5*)
  251-149; (*edge-78,Serial4 --> aggregation-68,Serial14*)
  125-243; (*aggregation-70,Serial9 --> core-49,Serial7*)
  63-268; (*aggregation-278,Serial12 --> edge-284,Serial6*)
  95-250; (*edge-217,Serial0 --> aggregation-208,Serial9*)
  317-185; (*core-13,Serial8 --> aggregation-225,Serial11*)
  53-7; (*aggregation-288,Serial15 --> edge-303,Serial0*)
  130-126; (*aggregation-245,Serial12 --> edge-252,Serial5*)
  106-176; (*aggregation-257,Serial9 --> edge-271,Serial1*)
  285-6; (*core-20,Serial5 --> aggregation-194,Serial12*)
  155-117; (*aggregation-116,Serial9 --> edge-121,Serial4*)
  118-197; (*aggregation-133,Serial5 --> edge-139,Serial5*)
  225-270; (*aggregation-212,Serial5 --> core-37,Serial7*)
  267-120; (*core-39,Serial3 --> aggregation-132,Serial7*)
  247-189; (*core-46,Serial2 --> aggregation-229,Serial12*)
  11-26; (*aggregation-198,Serial3 --> edge-203,Serial6*)
  27-200; (*aggregation-294,Serial12 --> core-52,Serial3*)
  67-188; (*edge-107,Serial6 --> aggregation-102,Serial3*)
  6-37; (*aggregation-194,Serial0 --> edge-200,Serial2*)
  137-185; (*edge-238,Serial1 --> aggregation-225,Serial8*)
  292-46; (*core-27,Serial4 --> aggregation-163,Serial11*)
  296-191; (*core-25,Serial6 --> aggregation-227,Serial9*)
  138-184; (*edge-237,Serial2 --> aggregation-226,Serial5*)
  189-234; (*aggregation-229,Serial10 --> core-40,Serial2*)
  132-317; (*aggregation-129,Serial11 --> core-13,Serial0*)
  170-159; (*core-61,Serial9 --> aggregation-119,Serial3*)
  215-61; (*core-59,Serial1 --> aggregation-279,Serial0*)
  229-83; (*core-43,Serial6 --> aggregation-277,Serial3*)
  198-175; (*core-53,Serial6 --> aggregation-230,Serial13*)
  293-14; (*edge-191,Serial6 --> aggregation-182,Serial15*)
  55-246; (*aggregation-166,Serial3 --> edge-171,Serial6*)
  236-96; (*aggregation-210,Serial11 --> edge-219,Serial2*)
  307-53; (*edge-299,Serial0 --> aggregation-288,Serial11*)
  259-92; (*core-30,Serial13 --> aggregation-147,Serial6*)
  145-285; (*aggregation-242,Serial4 --> core-20,Serial7*)
  58-240; (*aggregation-291,Serial0 --> edge-296,Serial3*)
  242-50; (*edge-174,Serial1 --> aggregation-161,Serial8*)
  187-80; (*aggregation-103,Serial1 --> edge-105,Serial7*)
  184-139; (*aggregation-226,Serial0 --> edge-232,Serial2*)
  107-176; (*aggregation-256,Serial15 --> edge-271,Serial0*)
  229-161; (*core-43,Serial15 --> aggregation-117,Serial3*)
  271-100; (*core-36,Serial15 --> aggregation-260,Serial4*)
  179-106; (*edge-270,Serial1 --> aggregation-257,Serial8*)
  226-130; (*core-45,Serial8 --> aggregation-245,Serial5*)
  76-261; (*aggregation-151,Serial10 --> edge-154,Serial7*)
  304-185; (*core-10,Serial8 --> aggregation-225,Serial15*)
  68-135; (*core-5,Serial2 --> aggregation-240,Serial5*)
  311-33; (*edge-186,Serial2 --> aggregation-178,Serial10*)
  6-24; (*aggregation-194,Serial6 --> edge-206,Serial2*)
  314-208; (*core-16,Serial2 --> aggregation-82,Serial0*)
  30-6; (*edge-205,Serial2 --> aggregation-194,Serial5*)
  103-162; (*aggregation-263,Serial5 --> edge-269,Serial7*)
  253-141; (*core-34,Serial13 --> aggregation-244,Serial2*)
  238-56; (*edge-173,Serial5 --> aggregation-165,Serial7*)
  32-2; (*aggregation-292,Serial14 --> edge-302,Serial4*)
  227-318; (*core-44,Serial9 --> aggregation-309,Serial4*)
  65-202; (*edge-108,Serial5 --> aggregation-101,Serial6*)
  180-316; (*aggregation-97,Serial12 --> core-14,Serial2*)
  20-216; (*edge-93,Serial0 --> aggregation-80,Serial13*)
  190-267; (*aggregation-228,Serial7 --> core-39,Serial4*)
  196-14; (*core-54,Serial2 --> aggregation-182,Serial6*)
  199-133; (*edge-138,Serial0 --> aggregation-128,Serial10*)
  20-219; (*edge-93,Serial6 --> aggregation-86,Serial13*)
  29-2; (*aggregation-293,Serial8 --> edge-302,Serial5*)
  19-211; (*edge-92,Serial4 --> aggregation-84,Serial12*)
  94-222; (*edge-218,Serial7 --> aggregation-215,Serial10*)
  158-316; (*aggregation-113,Serial6 --> core-14,Serial12*)
  220-227; (*aggregation-85,Serial4 --> core-44,Serial11*)
  274-85; (*edge-280,Serial4 --> aggregation-276,Serial8*)
  206-219; (*core-50,Serial13 --> aggregation-86,Serial2*)
  97-171; (*aggregation-262,Serial3 --> edge-267,Serial6*)
  110-284; (*aggregation-130,Serial13 --> core-21,Serial3*)
  194-63; (*core-55,Serial10 --> aggregation-278,Serial7*)
  56-229; (*aggregation-165,Serial1 --> core-43,Serial0*)
  155-255; (*aggregation-116,Serial1 --> core-33,Serial5*)
  304-154; (*core-10,Serial4 --> aggregation-65,Serial15*)
  83-272; (*aggregation-277,Serial9 --> edge-281,Serial5*)
  148-166; (*edge-120,Serial0 --> aggregation-112,Serial8*)
  64-107; (*core-3,Serial4 --> aggregation-256,Serial3*)
  165-104; (*edge-265,Serial3 --> aggregation-259,Serial1*)
  226-12; (*core-45,Serial1 --> aggregation-197,Serial0*)
  49-298; (*edge-315,Serial3 --> aggregation-307,Serial11*)
  119-161; (*edge-124,Serial5 --> aggregation-117,Serial12*)
  262-90; (*edge-157,Serial5 --> aggregation-149,Serial13*)
  50-78; (*aggregation-161,Serial13 --> core-9,Serial6*)
  139-191; (*edge-232,Serial3 --> aggregation-227,Serial0*)
  317-132; (*core-13,Serial0 --> aggregation-129,Serial11*)
  177-102; (*edge-153,Serial0 --> aggregation-144,Serial9*)
  48-242; (*aggregation-162,Serial6 --> edge-174,Serial2*)
  55-194; (*aggregation-166,Serial15 --> core-55,Serial15*)
  236-314; (*aggregation-210,Serial0 --> core-16,Serial6*)
  37-5; (*edge-200,Serial3 --> aggregation-195,Serial0*)
  70-180; (*edge-109,Serial1 --> aggregation-97,Serial7*)
  31-11; (*edge-204,Serial6 --> aggregation-198,Serial4*)
  277-151; (*edge-75,Serial5 --> aggregation-69,Serial5*)
  258-122; (*core-31,Serial3 --> aggregation-131,Serial15*)
  316-158; (*core-14,Serial12 --> aggregation-113,Serial6*)
  154-316; (*aggregation-65,Serial12 --> core-14,Serial4*)
  25-218; (*aggregation-295,Serial9 --> core-57,Serial3*)
  46-291; (*aggregation-163,Serial12 --> core-28,Serial4*)
  42-318; (*edge-316,Serial5 --> aggregation-309,Serial12*)
  111-159; (*edge-125,Serial7 --> aggregation-119,Serial13*)
  133-169; (*aggregation-128,Serial15 --> edge-143,Serial0*)
  104-291; (*aggregation-259,Serial12 --> core-28,Serial7*)
  218-159; (*core-57,Serial9 --> aggregation-119,Serial7*)
  68-16; (*core-5,Serial8 --> aggregation-192,Serial5*)
  160-194; (*aggregation-118,Serial7 --> core-55,Serial9*)
  182-67; (*aggregation-99,Serial3 --> edge-107,Serial3*)
  302-210; (*core-12,Serial3 --> aggregation-81,Serial4*)
  93-254; (*aggregation-146,Serial15 --> edge-159,Serial2*)
  224-226; (*aggregation-213,Serial5 --> core-45,Serial7*)
  252-152; (*edge-79,Serial0 --> aggregation-64,Serial15*)
  314-184; (*core-16,Serial10 --> aggregation-226,Serial8*)
  58-258; (*aggregation-291,Serial15 --> core-31,Serial11*)
  61-235; (*aggregation-279,Serial14 --> edge-286,Serial7*)
  138-189; (*edge-237,Serial5 --> aggregation-229,Serial7*)
  209-110; (*edge-140,Serial2 --> aggregation-130,Serial4*)
  257-211; (*core-32,Serial10 --> aggregation-84,Serial0*)
  53-60; (*aggregation-288,Serial1 --> core-1,Serial11*)
  281-192; (*aggregation-311,Serial4 --> core-56,Serial10*)
  175-138; (*aggregation-230,Serial5 --> edge-237,Serial6*)
  244-125; (*core-48,Serial7 --> aggregation-70,Serial8*)
  319-45; (*aggregation-308,Serial9 --> edge-313,Serial4*)
  123-167; (*aggregation-71,Serial14 --> core-62,Serial11*)
  151-230; (*aggregation-69,Serial15 --> core-42,Serial14*)
  86-236; (*edge-223,Serial2 --> aggregation-210,Serial15*)
  141-124; (*aggregation-244,Serial10 --> edge-250,Serial4*)
  221-167; (*aggregation-87,Serial6 --> core-62,Serial5*)
  59-285; (*aggregation-290,Serial12 --> core-20,Serial8*)
  38-229; (*aggregation-181,Serial3 --> core-43,Serial5*)
  40-7; (*aggregation-289,Serial9 --> edge-303,Serial1*)
  168-107; (*edge-264,Serial0 --> aggregation-256,Serial8*)
  159-170; (*aggregation-119,Serial3 --> core-61,Serial9*)
  174-107; (*edge-266,Serial0 --> aggregation-256,Serial10*)
  245-90; (*core-47,Serial10 --> aggregation-149,Serial7*)
  222-217; (*aggregation-215,Serial5 --> core-58,Serial8*)
  16-68; (*aggregation-192,Serial5 --> core-5,Serial8*)
  225-84; (*aggregation-212,Serial12 --> edge-220,Serial4*)
  90-177; (*aggregation-149,Serial9 --> edge-153,Serial5*)
  215-172; (*core-59,Serial15 --> aggregation-231,Serial11*)
  312-105; (*core-17,Serial12 --> aggregation-258,Serial9*)
  155-267; (*aggregation-116,Serial7 --> core-39,Serial5*)
  181-62; (*aggregation-96,Serial0 --> core-0,Serial5*)
  314-48; (*core-16,Serial4 --> aggregation-162,Serial8*)
  176-100; (*edge-271,Serial4 --> aggregation-260,Serial15*)
  310-93; (*core-18,Serial15 --> aggregation-146,Serial2*)
  245-56; (*core-47,Serial0 --> aggregation-165,Serial14*)
  270-85; (*core-37,Serial14 --> aggregation-276,Serial5*)
  79-206; (*aggregation-150,Serial2 --> core-50,Serial4*)
  140-181; (*edge-111,Serial0 --> aggregation-96,Serial15*)
  34-315; (*aggregation-177,Serial7 --> core-15,Serial11*)
  54-238; (*aggregation-167,Serial5 --> edge-173,Serial7*)
  293-22; (*edge-191,Serial3 --> aggregation-179,Serial15*)
  24-8; (*edge-206,Serial1 --> aggregation-193,Serial8*)
  55-237; (*aggregation-166,Serial0 --> edge-168,Serial6*)
  257-319; (*core-32,Serial9 --> aggregation-308,Serial0*)
  192-61; (*core-56,Serial1 --> aggregation-279,Serial4*)
  300-43; (*aggregation-305,Serial15 --> edge-319,Serial1*)
  318-231; (*aggregation-309,Serial1 --> core-41,Serial9*)
  9-30; (*aggregation-199,Serial5 --> edge-205,Serial7*)
  141-257; (*aggregation-244,Serial0 --> core-32,Serial13*)
  151-229; (*aggregation-69,Serial1 --> core-43,Serial14*)
  38-313; (*aggregation-181,Serial9 --> edge-185,Serial5*)
  251-150; (*edge-78,Serial3 --> aggregation-67,Serial6*)
  111-158; (*edge-125,Serial1 --> aggregation-113,Serial13*)
  256-101; (*edge-158,Serial1 --> aggregation-145,Serial14*)
  30-12; (*edge-205,Serial5 --> aggregation-197,Serial7*)
  120-197; (*aggregation-132,Serial11 --> edge-139,Serial4*)
  207-135; (*edge-255,Serial0 --> aggregation-240,Serial15*)
  216-19; (*aggregation-80,Serial12 --> edge-92,Serial0*)
  238-48; (*edge-173,Serial2 --> aggregation-162,Serial5*)
  207-129; (*edge-255,Serial6 --> aggregation-246,Serial15*)
  57-253; (*aggregation-164,Serial2 --> core-34,Serial2*)
  240-25; (*edge-296,Serial7 --> aggregation-295,Serial0*)
  179-103; (*edge-270,Serial7 --> aggregation-263,Serial6*)
  236-88; (*aggregation-210,Serial14 --> edge-222,Serial2*)
  52-241; (*aggregation-160,Serial15 --> edge-175,Serial0*)
  64-250; (*core-3,Serial15 --> aggregation-208,Serial3*)
  158-114; (*aggregation-113,Serial15 --> edge-127,Serial1*)
  181-80; (*aggregation-96,Serial9 --> edge-105,Serial0*)
  301-41; (*aggregation-304,Serial13 --> edge-317,Serial0*)
  187-170; (*aggregation-103,Serial13 --> core-61,Serial4*)
  273-39; (*core-35,Serial8 --> aggregation-180,Serial3*)
  110-195; (*aggregation-130,Serial0 --> edge-136,Serial2*)
  269-205; (*core-38,Serial1 --> aggregation-100,Serial6*)
  66-216; (*core-2,Serial12 --> aggregation-80,Serial2*)
  74-301; (*core-6,Serial13 --> aggregation-304,Serial6*)
  29-306; (*aggregation-293,Serial3 --> edge-297,Serial5*)
  150-252; (*aggregation-67,Serial7 --> edge-79,Serial3*)
  258-92; (*core-31,Serial13 --> aggregation-147,Serial1*)
  273-149; (*core-35,Serial0 --> aggregation-68,Serial3*)
  106-303; (*aggregation-257,Serial0 --> core-11,Serial9*)
  14-206; (*aggregation-182,Serial2 --> core-50,Serial2*)
  150-275; (*aggregation-67,Serial1 --> edge-73,Serial3*)
  210-21; (*aggregation-81,Serial14 --> edge-94,Serial1*)
  219-194; (*aggregation-86,Serial7 --> core-55,Serial13*)
  306-58; (*edge-297,Serial3 --> aggregation-291,Serial1*)
  22-258; (*aggregation-179,Serial1 --> core-31,Serial14*)
  164-76; (*core-63,Serial6 --> aggregation-151,Serial2*)
  122-199; (*aggregation-131,Serial2 --> edge-138,Serial3*)
  239-55; (*edge-172,Serial6 --> aggregation-166,Serial4*)
  166-111; (*aggregation-112,Serial13 --> edge-125,Serial0*)
  98-171; (*aggregation-261,Serial5 --> edge-267,Serial5*)
  101-316; (*aggregation-145,Serial6 --> core-14,Serial5*)
  89-283; (*aggregation-274,Serial6 --> core-22,Serial0*)
  91-262; (*aggregation-148,Serial13 --> edge-157,Serial4*)
  273-57; (*core-35,Serial2 --> aggregation-164,Serial3*)
  156-111; (*aggregation-115,Serial13 --> edge-125,Serial3*)
  299-49; (*aggregation-306,Serial11 --> edge-315,Serial2*)
  269-57; (*core-38,Serial2 --> aggregation-164,Serial6*)
  153-249; (*aggregation-66,Serial5 --> edge-77,Serial2*)
  183-285; (*aggregation-98,Serial12 --> core-20,Serial9*)
  304-132; (*core-10,Serial0 --> aggregation-129,Serial15*)
  186-139; (*aggregation-224,Serial8 --> edge-232,Serial0*)
  0-213; (*edge-88,Serial3 --> aggregation-83,Serial8*)
  39-289; (*aggregation-180,Serial13 --> edge-189,Serial4*)
  315-34; (*core-15,Serial11 --> aggregation-177,Serial7*)
  181-74; (*aggregation-96,Serial6 --> core-6,Serial5*)
  81-147; (*core-8,Serial14 --> aggregation-241,Serial0*)
  120-271; (*aggregation-132,Serial4 --> core-36,Serial3*)
  316-180; (*core-14,Serial2 --> aggregation-97,Serial12*)
  217-61; (*core-58,Serial1 --> aggregation-279,Serial5*)
  82-225; (*edge-221,Serial4 --> aggregation-212,Serial13*)
  189-136; (*aggregation-229,Serial5 --> edge-235,Serial5*)
  50-303; (*aggregation-161,Serial0 --> core-11,Serial6*)
  201-141; (*edge-254,Serial4 --> aggregation-244,Serial14*)
  317-154; (*core-13,Serial4 --> aggregation-65,Serial11*)
  258-182; (*core-31,Serial2 --> aggregation-99,Serial15*)
  288-13; (*edge-187,Serial7 --> aggregation-183,Serial11*)
  56-230; (*aggregation-165,Serial15 --> core-42,Serial0*)
  92-260; (*aggregation-147,Serial11 --> edge-155,Serial3*)
  103-167; (*aggregation-263,Serial14 --> core-62,Serial0*)
  141-269; (*aggregation-244,Serial6 --> core-38,Serial13*)
  157-121; (*aggregation-114,Serial11 --> edge-123,Serial2*)
  311-38; (*edge-186,Serial5 --> aggregation-181,Serial10*)
  1-220; (*edge-89,Serial5 --> aggregation-85,Serial9*)
  145-312; (*aggregation-242,Serial1 --> core-17,Serial7*)
  240-40; (*edge-296,Serial1 --> aggregation-289,Serial2*)
  75-66; (*aggregation-272,Serial2 --> core-2,Serial3*)
  165-97; (*edge-265,Serial6 --> aggregation-262,Serial1*)
  292-104; (*core-27,Serial7 --> aggregation-259,Serial11*)
  178-76; (*edge-152,Serial7 --> aggregation-151,Serial8*)
  118-231; (*aggregation-133,Serial13 --> core-41,Serial4*)
  282-208; (*core-23,Serial2 --> aggregation-82,Serial7*)
  257-39; (*core-32,Serial8 --> aggregation-180,Serial0*)
  102-261; (*aggregation-144,Serial10 --> edge-154,Serial0*)
  103-215; (*aggregation-263,Serial11 --> core-59,Serial0*)
  164-221; (*core-63,Serial5 --> aggregation-87,Serial2*)
  212-108; (*edge-142,Serial7 --> aggregation-135,Serial6*)
  296-122; (*core-25,Serial3 --> aggregation-131,Serial9*)
  220-0; (*aggregation-85,Serial8 --> edge-88,Serial5*)
  235-73; (*edge-286,Serial1 --> aggregation-273,Serial14*)
  8-304; (*aggregation-193,Serial15 --> core-10,Serial7*)
  76-173; (*aggregation-151,Serial1 --> core-60,Serial6*)
  46-248; (*aggregation-163,Serial2 --> edge-170,Serial3*)
  97-179; (*aggregation-262,Serial6 --> edge-270,Serial6*)
  10-32; (*edge-300,Serial4 --> aggregation-292,Serial12*)
  224-88; (*aggregation-213,Serial14 --> edge-222,Serial5*)
  130-234; (*aggregation-245,Serial0 --> core-40,Serial8*)
  110-310; (*aggregation-130,Serial10 --> core-18,Serial3*)
  97-194; (*aggregation-262,Serial15 --> core-55,Serial12*)
  14-295; (*aggregation-182,Serial14 --> edge-190,Serial6*)
  46-258; (*aggregation-163,Serial15 --> core-31,Serial4*)
  147-315; (*aggregation-241,Serial7 --> core-15,Serial14*)
  89-309; (*aggregation-274,Serial3 --> core-19,Serial0*)
  108-192; (*aggregation-135,Serial8 --> core-56,Serial12*)
  150-290; (*aggregation-67,Serial13 --> core-29,Serial10*)
  56-228; (*aggregation-165,Serial3 --> edge-169,Serial5*)
  100-171; (*aggregation-260,Serial11 --> edge-267,Serial4*)
  263-92; (*edge-156,Serial3 --> aggregation-147,Serial12*)
  101-262; (*aggregation-145,Serial13 --> edge-157,Serial1*)
  44-319; (*edge-318,Serial4 --> aggregation-308,Serial14*)
  114-160; (*edge-127,Serial6 --> aggregation-118,Serial15*)
  289-38; (*edge-189,Serial5 --> aggregation-181,Serial13*)
  172-146; (*aggregation-231,Serial1 --> edge-233,Serial7*)
  216-74; (*aggregation-80,Serial6 --> core-6,Serial12*)
  173-9; (*core-60,Serial14 --> aggregation-199,Serial12*)
  213-258; (*aggregation-83,Serial1 --> core-31,Serial1*)
  315-73; (*core-15,Serial15 --> aggregation-273,Serial7*)
  27-243; (*aggregation-294,Serial9 --> core-49,Serial3*)
  184-282; (*aggregation-226,Serial15 --> core-23,Serial10*)
  116-197; (*aggregation-134,Serial3 --> edge-139,Serial6*)
  87-264; (*aggregation-275,Serial11 --> edge-283,Serial3*)
  167-54; (*core-62,Serial13 --> aggregation-167,Serial14*)
  45-298; (*edge-313,Serial3 --> aggregation-307,Serial9*)
  105-176; (*aggregation-258,Serial7 --> edge-271,Serial2*)
  260-102; (*edge-155,Serial0 --> aggregation-144,Serial11*)
  143-292; (*aggregation-243,Serial0 --> core-27,Serial9*)
  145-127; (*aggregation-242,Serial11 --> edge-251,Serial2*)
  273-205; (*core-35,Serial1 --> aggregation-100,Serial3*)
  287-36; (*edge-188,Serial0 --> aggregation-176,Serial12*)
  278-152; (*edge-76,Serial0 --> aggregation-64,Serial12*)
  72-186; (*core-7,Serial9 --> aggregation-224,Serial7*)
  72-133; (*core-7,Serial1 --> aggregation-128,Serial7*)
  180-80; (*aggregation-97,Serial3 --> edge-105,Serial1*)
  80-181; (*edge-105,Serial0 --> aggregation-96,Serial9*)
  83-234; (*aggregation-277,Serial0 --> core-40,Serial6*)
  52-69; (*aggregation-160,Serial4 --> core-4,Serial7*)
  286-51; (*aggregation-310,Serial10 --> edge-314,Serial6*)
  186-131; (*aggregation-224,Serial15 --> edge-239,Serial0*)
  5-28; (*aggregation-195,Serial2 --> edge-202,Serial3*)
  87-259; (*aggregation-275,Serial6 --> core-30,Serial8*)
  283-184; (*core-22,Serial10 --> aggregation-226,Serial14*)
  296-87; (*core-25,Serial8 --> aggregation-275,Serial7*)
  313-13; (*edge-185,Serial7 --> aggregation-183,Serial9*)
  85-255; (*aggregation-276,Serial1 --> core-33,Serial14*)
  272-75; (*edge-281,Serial0 --> aggregation-272,Serial9*)
  284-89; (*core-21,Serial0 --> aggregation-274,Serial5*)
  154-78; (*aggregation-65,Serial13 --> core-9,Serial4*)
  290-156; (*core-29,Serial12 --> aggregation-115,Serial3*)
  28-9; (*edge-202,Serial7 --> aggregation-199,Serial2*)
  298-259; (*aggregation-307,Serial6 --> core-30,Serial0*)
  118-212; (*aggregation-133,Serial8 --> edge-142,Serial5*)
  33-293; (*aggregation-178,Serial15 --> edge-191,Serial2*)
  115-158; (*edge-122,Serial1 --> aggregation-113,Serial10*)
  15-220; (*edge-90,Serial5 --> aggregation-85,Serial10*)
  112-135; (*edge-249,Serial0 --> aggregation-240,Serial9*)
  8-302; (*aggregation-193,Serial1 --> core-12,Serial7*)
  187-217; (*aggregation-103,Serial10 --> core-58,Serial4*)
  296-46; (*core-25,Serial4 --> aggregation-163,Serial9*)
  308-33; (*edge-184,Serial2 --> aggregation-178,Serial8*)
  247-56; (*core-46,Serial0 --> aggregation-165,Serial12*)
  129-201; (*aggregation-246,Serial14 --> edge-254,Serial6*)
  8-37; (*aggregation-193,Serial2 --> edge-200,Serial1*)
  230-224; (*core-42,Serial7 --> aggregation-213,Serial2*)
  83-265; (*aggregation-277,Serial10 --> edge-282,Serial5*)
  105-283; (*aggregation-258,Serial14 --> core-22,Serial12*)
  76-177; (*aggregation-151,Serial9 --> edge-153,Serial7*)
  89-272; (*aggregation-274,Serial9 --> edge-281,Serial2*)
  122-214; (*aggregation-131,Serial5 --> edge-141,Serial3*)
  222-164; (*aggregation-215,Serial2 --> core-63,Serial8*)
  319-255; (*aggregation-308,Serial1 --> core-33,Serial9*)
  213-296; (*aggregation-83,Serial7 --> core-25,Serial1*)
  51-300; (*edge-314,Serial1 --> aggregation-305,Serial10*)
  121-156; (*edge-123,Serial3 --> aggregation-115,Serial11*)
  89-274; (*aggregation-274,Serial8 --> edge-280,Serial2*)
  289-22; (*edge-189,Serial3 --> aggregation-179,Serial13*)
  162-98; (*edge-269,Serial5 --> aggregation-261,Serial7*)
  60-166; (*core-1,Serial10 --> aggregation-112,Serial1*)
  198-79; (*core-53,Serial4 --> aggregation-150,Serial5*)
  74-36; (*core-6,Serial14 --> aggregation-176,Serial6*)
  154-249; (*aggregation-65,Serial7 --> edge-77,Serial1*)
  275-154; (*edge-73,Serial1 --> aggregation-65,Serial3*)
  50-316; (*aggregation-161,Serial12 --> core-14,Serial6*)
  62-107; (*core-0,Serial4 --> aggregation-256,Serial0*)
  95-233; (*edge-217,Serial3 --> aggregation-211,Serial9*)
  186-66; (*aggregation-224,Serial2 --> core-2,Serial9*)
  180-78; (*aggregation-97,Serial13 --> core-9,Serial2*)
  122-292; (*aggregation-131,Serial11 --> core-27,Serial3*)
  123-280; (*aggregation-71,Serial0 --> edge-72,Serial7*)
  91-255; (*aggregation-148,Serial1 --> core-33,Serial12*)
  152-251; (*aggregation-64,Serial14 --> edge-78,Serial0*)
  18-208; (*edge-91,Serial2 --> aggregation-82,Serial11*)
  15-216; (*edge-90,Serial0 --> aggregation-80,Serial10*)
  182-297; (*aggregation-99,Serial8 --> core-24,Serial2*)
  107-72; (*aggregation-256,Serial7 --> core-7,Serial4*)
  11-206; (*aggregation-198,Serial10 --> core-50,Serial0*)
  58-291; (*aggregation-291,Serial12 --> core-28,Serial11*)
  318-245; (*aggregation-309,Serial7 --> core-47,Serial9*)
  9-215; (*aggregation-199,Serial11 --> core-59,Serial14*)
  173-159; (*core-60,Serial9 --> aggregation-119,Serial1*)
  231-318; (*core-41,Serial9 --> aggregation-309,Serial1*)
  209-133; (*edge-140,Serial0 --> aggregation-128,Serial12*)
  13-167; (*aggregation-183,Serial6 --> core-62,Serial7*)
  13-215; (*aggregation-183,Serial0 --> core-59,Serial7*)
  216-1; (*aggregation-80,Serial9 --> edge-89,Serial0*)
  257-155; (*core-32,Serial5 --> aggregation-116,Serial0*)
  73-304; (*aggregation-273,Serial2 --> core-10,Serial15*)
  159-115; (*aggregation-119,Serial10 --> edge-122,Serial7*)
  39-271; (*aggregation-180,Serial4 --> core-36,Serial8*)
  267-100; (*core-39,Serial15 --> aggregation-260,Serial7*)
  289-33; (*edge-189,Serial2 --> aggregation-178,Serial13*)
  152-249; (*aggregation-64,Serial13 --> edge-77,Serial0*)
  319-269; (*aggregation-308,Serial6 --> core-38,Serial9*)
  100-179; (*aggregation-260,Serial14 --> edge-270,Serial4*)
  199-132; (*edge-138,Serial1 --> aggregation-129,Serial4*)
  263-79; (*edge-156,Serial6 --> aggregation-150,Serial12*)
  312-145; (*core-17,Serial7 --> aggregation-242,Serial1*)
  81-210; (*core-8,Serial3 --> aggregation-81,Serial0*)
  124-128; (*edge-250,Serial7 --> aggregation-247,Serial10*)
  216-68; (*aggregation-80,Serial5 --> core-5,Serial12*)
  128-217; (*aggregation-247,Serial5 --> core-58,Serial2*)
  65-205; (*edge-108,Serial4 --> aggregation-100,Serial12*)
  278-123; (*edge-76,Serial7 --> aggregation-71,Serial4*)
  101-263; (*aggregation-145,Serial12 --> edge-156,Serial1*)
  106-163; (*aggregation-257,Serial6 --> edge-268,Serial1*)
  186-64; (*aggregation-224,Serial3 --> core-3,Serial9*)
  316-101; (*core-14,Serial5 --> aggregation-145,Serial6*)
  167-25; (*core-62,Serial3 --> aggregation-295,Serial14*)
  28-6; (*edge-202,Serial2 --> aggregation-194,Serial2*)
  63-206; (*aggregation-278,Serial2 --> core-50,Serial10*)
  8-35; (*aggregation-193,Serial3 --> edge-201,Serial1*)
  135-72; (*aggregation-240,Serial7 --> core-7,Serial2*)
  224-96; (*aggregation-213,Serial11 --> edge-219,Serial5*)
  78-279; (*core-9,Serial13 --> aggregation-209,Serial1*)
  286-198; (*aggregation-310,Serial5 --> core-53,Serial8*)
  74-152; (*core-6,Serial0 --> aggregation-64,Serial6*)
  219-20; (*aggregation-86,Serial13 --> edge-93,Serial6*)
  237-54; (*edge-168,Serial7 --> aggregation-167,Serial0*)
  106-78; (*aggregation-257,Serial13 --> core-9,Serial9*)
  192-9; (*core-56,Serial14 --> aggregation-199,Serial8*)
  192-54; (*core-56,Serial13 --> aggregation-167,Serial8*)
  205-271; (*aggregation-100,Serial4 --> core-36,Serial1*)
  32-306; (*aggregation-292,Serial9 --> edge-297,Serial4*)
  304-279; (*core-10,Serial13 --> aggregation-209,Serial2*)
  148-160; (*edge-120,Serial6 --> aggregation-118,Serial8*)
  41-299; (*edge-317,Serial2 --> aggregation-306,Serial13*)
  109-155; (*edge-126,Serial4 --> aggregation-116,Serial14*)
  303-180; (*core-11,Serial2 --> aggregation-97,Serial0*)
  147-316; (*aggregation-241,Serial6 --> core-14,Serial14*)
  250-60; (*aggregation-208,Serial1 --> core-1,Serial15*)
  16-24; (*aggregation-192,Serial14 --> edge-206,Serial0*)
  115-166; (*edge-122,Serial0 --> aggregation-112,Serial10*)
  281-42; (*aggregation-311,Serial12 --> edge-316,Serial7*)
  154-280; (*aggregation-65,Serial2 --> edge-72,Serial1*)
  264-61; (*edge-283,Serial7 --> aggregation-279,Serial11*)
  277-153; (*edge-75,Serial2 --> aggregation-66,Serial3*)
  73-81; (*aggregation-273,Serial0 --> core-8,Serial15*)
  247-161; (*core-46,Serial15 --> aggregation-117,Serial6*)
  301-74; (*aggregation-304,Serial6 --> core-6,Serial13*)
  226-220; (*core-45,Serial11 --> aggregation-85,Serial5*)
  175-198; (*aggregation-230,Serial13 --> core-53,Serial6*)
  105-163; (*aggregation-258,Serial4 --> edge-268,Serial2*)
  100-273; (*aggregation-260,Serial3 --> core-35,Serial15*)
  202-70; (*aggregation-101,Serial7 --> edge-109,Serial5*)
  243-27; (*core-49,Serial3 --> aggregation-294,Serial9*)
  29-234; (*aggregation-293,Serial10 --> core-40,Serial12*)
  85-266; (*aggregation-276,Serial13 --> edge-285,Serial4*)
  224-86; (*aggregation-213,Serial15 --> edge-223,Serial5*)
  80-202; (*edge-105,Serial5 --> aggregation-101,Serial3*)
  160-148; (*aggregation-118,Serial8 --> edge-120,Serial6*)
  178-91; (*edge-152,Serial4 --> aggregation-148,Serial8*)
  229-98; (*core-43,Serial3 --> aggregation-261,Serial1*)
  265-83; (*edge-282,Serial5 --> aggregation-277,Serial10*)
  309-236; (*core-19,Serial6 --> aggregation-210,Serial3*)
  143-204; (*aggregation-243,Serial13 --> edge-253,Serial3*)
  172-215; (*aggregation-231,Serial11 --> core-59,Serial15*)
  270-32; (*core-37,Serial6 --> aggregation-292,Serial5*)
  32-269; (*aggregation-292,Serial6 --> core-38,Serial6*)
  102-74; (*aggregation-144,Serial6 --> core-6,Serial6*)
  161-226; (*aggregation-117,Serial5 --> core-45,Serial15*)
  305-27; (*edge-298,Serial6 --> aggregation-294,Serial2*)
  153-282; (*aggregation-66,Serial15 --> core-23,Serial1*)
  227-29; (*core-44,Serial12 --> aggregation-293,Serial11*)
  184-137; (*aggregation-226,Serial6 --> edge-238,Serial2*)
  34-311; (*aggregation-177,Serial10 --> edge-186,Serial1*)
  204-147; (*edge-253,Serial1 --> aggregation-241,Serial13*)
  246-46; (*edge-171,Serial3 --> aggregation-163,Serial3*)
  110-309; (*aggregation-130,Serial11 --> core-19,Serial3*)
  310-157; (*core-18,Serial11 --> aggregation-114,Serial2*)
  289-14; (*edge-189,Serial6 --> aggregation-182,Serial13*)
  303-300; (*core-11,Serial1 --> aggregation-305,Serial3*)
  185-137; (*aggregation-225,Serial8 --> edge-238,Serial1*)
  241-54; (*edge-175,Serial7 --> aggregation-167,Serial7*)
  156-109; (*aggregation-115,Serial14 --> edge-126,Serial3*)
  299-42; (*aggregation-306,Serial12 --> edge-316,Serial2*)
  68-53; (*core-5,Serial11 --> aggregation-288,Serial5*)
  107-162; (*aggregation-256,Serial13 --> edge-269,Serial0*)
  292-191; (*core-27,Serial6 --> aggregation-227,Serial11*)
  283-183; (*core-22,Serial9 --> aggregation-98,Serial14*)
  303-8; (*core-11,Serial7 --> aggregation-193,Serial0*)
  74-102; (*core-6,Serial6 --> aggregation-144,Serial6*)
  33-287; (*aggregation-178,Serial12 --> edge-188,Serial2*)
  195-133; (*edge-136,Serial0 --> aggregation-128,Serial8*)
  2-25; (*edge-302,Serial7 --> aggregation-295,Serial6*)
  181-77; (*aggregation-96,Serial10 --> edge-106,Serial0*)
  190-255; (*aggregation-228,Serial1 --> core-33,Serial4*)
  34-81; (*aggregation-177,Serial0 --> core-8,Serial11*)
  142-183; (*edge-110,Serial2 --> aggregation-98,Serial6*)
  166-119; (*aggregation-112,Serial12 --> edge-124,Serial0*)
  185-317; (*aggregation-225,Serial11 --> core-13,Serial8*)
  69-181; (*core-4,Serial5 --> aggregation-96,Serial4*)
  188-70; (*aggregation-102,Serial5 --> edge-109,Serial6*)
  298-44; (*aggregation-307,Serial14 --> edge-318,Serial3*)
  221-17; (*aggregation-87,Serial15 --> edge-95,Serial7*)
  231-98; (*core-41,Serial3 --> aggregation-261,Serial13*)
  309-105; (*core-19,Serial12 --> aggregation-258,Serial11*)
  40-305; (*aggregation-289,Serial4 --> edge-298,Serial1*)
  9-167; (*aggregation-199,Serial14 --> core-62,Serial14*)
  225-95; (*aggregation-212,Serial9 --> edge-217,Serial4*)
  130-112; (*aggregation-245,Serial9 --> edge-249,Serial5*)
  209-120; (*edge-140,Serial4 --> aggregation-132,Serial12*)
  145-310; (*aggregation-242,Serial2 --> core-18,Serial7*)
  10-58; (*edge-300,Serial3 --> aggregation-291,Serial4*)
  166-69; (*aggregation-112,Serial4 --> core-4,Serial10*)
  311-39; (*edge-186,Serial4 --> aggregation-180,Serial10*)
  217-103; (*core-58,Serial0 --> aggregation-263,Serial10*)
  255-225; (*core-33,Serial7 --> aggregation-212,Serial1*)
  190-136; (*aggregation-228,Serial11 --> edge-235,Serial4*)
  257-190; (*core-32,Serial4 --> aggregation-228,Serial0*)
  310-48; (*core-18,Serial4 --> aggregation-162,Serial10*)
  27-4; (*aggregation-294,Serial5 --> edge-301,Serial6*)
  116-196; (*aggregation-134,Serial14 --> core-54,Serial14*)
  48-283; (*aggregation-162,Serial14 --> core-22,Serial4*)
  49-286; (*edge-315,Serial6 --> aggregation-310,Serial11*)
  68-52; (*core-5,Serial7 --> aggregation-160,Serial5*)
  201-143; (*edge-254,Serial3 --> aggregation-243,Serial14*)
  36-308; (*aggregation-176,Serial8 --> edge-184,Serial0*)
  247-29; (*core-46,Serial12 --> aggregation-293,Serial12*)
  203-286; (*core-51,Serial8 --> aggregation-310,Serial3*)
  182-142; (*aggregation-99,Serial6 --> edge-110,Serial3*)
  139-175; (*edge-232,Serial6 --> aggregation-230,Serial0*)
  232-87; (*edge-287,Serial3 --> aggregation-275,Serial15*)
  38-308; (*aggregation-181,Serial8 --> edge-184,Serial5*)
  164-13; (*core-63,Serial7 --> aggregation-183,Serial2*)
  83-232; (*aggregation-277,Serial15 --> edge-287,Serial5*)
  172-167; (*aggregation-231,Serial14 --> core-62,Serial15*)
  233-291; (*aggregation-211,Serial2 --> core-28,Serial15*)
  234-161; (*core-40,Serial15 --> aggregation-117,Serial0*)
  126-129; (*edge-252,Serial6 --> aggregation-246,Serial12*)
  22-297; (*aggregation-179,Serial4 --> core-24,Serial14*)
  248-56; (*edge-170,Serial5 --> aggregation-165,Serial4*)
  188-200; (*aggregation-102,Serial12 --> core-52,Serial5*)
  202-234; (*aggregation-101,Serial10 --> core-40,Serial13*)
  245-318; (*core-47,Serial9 --> aggregation-309,Serial7*)
  11-194; (*aggregation-198,Serial15 --> core-55,Serial0*)
  280-152; (*edge-72,Serial0 --> aggregation-64,Serial8*)
  139-189; (*edge-232,Serial5 --> aggregation-229,Serial2*)
  171-105; (*edge-267,Serial2 --> aggregation-258,Serial3*)
  283-93; (*core-22,Serial15 --> aggregation-146,Serial6*)
  163-103; (*edge-268,Serial7 --> aggregation-263,Serial4*)
  66-135; (*core-2,Serial2 --> aggregation-240,Serial2*)
  74-107; (*core-6,Serial4 --> aggregation-256,Serial6*)
  62-16; (*core-0,Serial8 --> aggregation-192,Serial0*)
  38-227; (*aggregation-181,Serial4 --> core-44,Serial5*)
  118-214; (*aggregation-133,Serial7 --> edge-141,Serial5*)
  32-270; (*aggregation-292,Serial5 --> core-37,Serial6*)
  77-180; (*edge-106,Serial1 --> aggregation-97,Serial4*)
  243-286; (*core-49,Serial8 --> aggregation-310,Serial1*)
  307-40; (*edge-299,Serial1 --> aggregation-289,Serial5*)
  246-57; (*edge-171,Serial4 --> aggregation-164,Serial11*)
  291-58; (*core-28,Serial11 --> aggregation-291,Serial12*)
  161-121; (*aggregation-117,Serial11 --> edge-123,Serial5*)
  165-100; (*edge-265,Serial4 --> aggregation-260,Serial9*)
  286-45; (*aggregation-310,Serial9 --> edge-313,Serial6*)
  290-122; (*core-29,Serial3 --> aggregation-131,Serial13*)
  245-12; (*core-47,Serial1 --> aggregation-197,Serial14*)
  282-6; (*core-23,Serial5 --> aggregation-194,Serial15*)
  257-85; (*core-32,Serial14 --> aggregation-276,Serial0*)
  277-150; (*edge-75,Serial3 --> aggregation-67,Serial3*)
  237-55; (*edge-168,Serial6 --> aggregation-166,Serial0*)
  318-234; (*aggregation-309,Serial0 --> core-40,Serial9*)
  191-136; (*aggregation-227,Serial3 --> edge-235,Serial3*)
  267-91; (*core-39,Serial12 --> aggregation-148,Serial7*)
  205-270; (*aggregation-100,Serial5 --> core-37,Serial1*)
  42-301; (*edge-316,Serial0 --> aggregation-304,Serial12*)
  53-62; (*aggregation-288,Serial0 --> core-0,Serial11*)
  111-157; (*edge-125,Serial2 --> aggregation-114,Serial13*)
  270-141; (*core-37,Serial13 --> aggregation-244,Serial5*)
  91-269; (*aggregation-148,Serial6 --> core-38,Serial12*)
  162-107; (*edge-269,Serial0 --> aggregation-256,Serial13*)
  87-268; (*aggregation-275,Serial12 --> edge-284,Serial3*)
  296-104; (*core-25,Serial7 --> aggregation-259,Serial9*)
  245-29; (*core-47,Serial12 --> aggregation-293,Serial14*)
  75-265; (*aggregation-272,Serial10 --> edge-282,Serial0*)
  199-118; (*edge-138,Serial5 --> aggregation-133,Serial4*)
  55-228; (*aggregation-166,Serial1 --> edge-169,Serial6*)
  76-164; (*aggregation-151,Serial2 --> core-63,Serial6*)
  292-150; (*core-27,Serial10 --> aggregation-67,Serial11*)
  29-247; (*aggregation-293,Serial12 --> core-46,Serial12*)
  257-32; (*core-32,Serial6 --> aggregation-292,Serial0*)
  188-140; (*aggregation-102,Serial7 --> edge-111,Serial6*)
  251-151; (*edge-78,Serial5 --> aggregation-69,Serial8*)
  152-74; (*aggregation-64,Serial6 --> core-6,Serial0*)
  219-1; (*aggregation-86,Serial9 --> edge-89,Serial6*)
  85-253; (*aggregation-276,Serial2 --> core-34,Serial14*)
  292-233; (*core-27,Serial15 --> aggregation-211,Serial0*)
  225-82; (*aggregation-212,Serial13 --> edge-221,Serial4*)
  245-202; (*core-47,Serial13 --> aggregation-101,Serial14*)
  47-300; (*edge-312,Serial1 --> aggregation-305,Serial8*)
  96-279; (*edge-219,Serial1 --> aggregation-209,Serial11*)
  117-156; (*edge-121,Serial3 --> aggregation-115,Serial9*)
  39-270; (*aggregation-180,Serial5 --> core-37,Serial8*)
  281-164; (*aggregation-311,Serial2 --> core-63,Serial10*)
  144-184; (*edge-234,Serial2 --> aggregation-226,Serial2*)
  36-64; (*aggregation-176,Serial3 --> core-3,Serial14*)
  303-158; (*core-11,Serial12 --> aggregation-113,Serial3*)
  61-192; (*aggregation-279,Serial4 --> core-56,Serial1*)
  226-118; (*core-45,Serial4 --> aggregation-133,Serial0*)
  78-34; (*core-9,Serial11 --> aggregation-177,Serial1*)
  262-91; (*edge-157,Serial4 --> aggregation-148,Serial13*)
  196-11; (*core-54,Serial0 --> aggregation-198,Serial14*)
  273-155; (*core-35,Serial5 --> aggregation-116,Serial3*)
  105-282; (*aggregation-258,Serial15 --> core-23,Serial12*)
  283-157; (*core-22,Serial11 --> aggregation-114,Serial6*)
  175-139; (*aggregation-230,Serial0 --> edge-232,Serial6*)
  63-203; (*aggregation-278,Serial3 --> core-51,Serial10*)
  20-210; (*edge-93,Serial1 --> aggregation-81,Serial13*)
  317-279; (*core-13,Serial13 --> aggregation-209,Serial5*)
  203-188; (*core-51,Serial5 --> aggregation-102,Serial11*)
  302-147; (*core-12,Serial14 --> aggregation-241,Serial4*)
  136-190; (*edge-235,Serial4 --> aggregation-228,Serial11*)
  167-187; (*core-62,Serial4 --> aggregation-103,Serial14*)
  250-96; (*aggregation-208,Serial11 --> edge-219,Serial0*)
  164-281; (*core-63,Serial10 --> aggregation-311,Serial2*)
  26-3; (*edge-203,Serial4 --> aggregation-196,Serial11*)
  104-259; (*aggregation-259,Serial14 --> core-30,Serial7*)
  244-219; (*core-48,Serial13 --> aggregation-86,Serial0*)
  19-213; (*edge-92,Serial3 --> aggregation-83,Serial12*)
  25-217; (*aggregation-295,Serial10 --> core-58,Serial3*)
  183-312; (*aggregation-98,Serial9 --> core-17,Serial9*)
  122-294; (*aggregation-131,Serial10 --> core-26,Serial3*)
  6-282; (*aggregation-194,Serial15 --> core-23,Serial5*)
  2-53; (*edge-302,Serial0 --> aggregation-288,Serial14*)
  255-100; (*core-33,Serial15 --> aggregation-260,Serial1*)
  39-293; (*aggregation-180,Serial15 --> edge-191,Serial4*)
  53-74; (*aggregation-288,Serial6 --> core-6,Serial11*)
  48-309; (*aggregation-162,Serial11 --> core-19,Serial4*)
  25-305; (*aggregation-295,Serial2 --> edge-298,Serial7*)
  55-196; (*aggregation-166,Serial14 --> core-54,Serial15*)
  91-257; (*aggregation-148,Serial0 --> core-32,Serial12*)
  97-174; (*aggregation-262,Serial2 --> edge-266,Serial6*)
  210-302; (*aggregation-81,Serial4 --> core-12,Serial3*)
  272-61; (*edge-281,Serial7 --> aggregation-279,Serial9*)
  203-14; (*core-51,Serial2 --> aggregation-182,Serial3*)
  296-182; (*core-25,Serial2 --> aggregation-99,Serial9*)
  14-288; (*aggregation-182,Serial11 --> edge-187,Serial6*)
  108-169; (*aggregation-135,Serial7 --> edge-143,Serial7*)
  116-199; (*aggregation-134,Serial2 --> edge-138,Serial6*)
  83-245; (*aggregation-277,Serial7 --> core-47,Serial6*)
  275-125; (*edge-73,Serial6 --> aggregation-70,Serial1*)
  309-208; (*core-19,Serial2 --> aggregation-82,Serial3*)
  166-68; (*aggregation-112,Serial5 --> core-5,Serial10*)
  146-185; (*edge-233,Serial1 --> aggregation-225,Serial3*)
  213-291; (*aggregation-83,Serial2 --> core-28,Serial1*)
  236-94; (*aggregation-210,Serial10 --> edge-218,Serial2*)
  231-151; (*core-41,Serial14 --> aggregation-69,Serial13*)
  198-129; (*core-53,Serial11 --> aggregation-246,Serial5*)
  99-223; (*edge-216,Serial6 --> aggregation-214,Serial8*)
  257-149; (*core-32,Serial0 --> aggregation-68,Serial0*)
  74-250; (*core-6,Serial15 --> aggregation-208,Serial6*)
  79-244; (*aggregation-150,Serial0 --> core-48,Serial4*)
  155-114; (*aggregation-116,Serial15 --> edge-127,Serial4*)
  298-41; (*aggregation-307,Serial13 --> edge-317,Serial3*)
  308-34; (*edge-184,Serial1 --> aggregation-177,Serial8*)
  206-175; (*core-50,Serial6 --> aggregation-230,Serial10*)
  220-226; (*aggregation-85,Serial5 --> core-45,Serial11*)
  299-312; (*aggregation-306,Serial1 --> core-17,Serial14*)
  200-63; (*core-52,Serial10 --> aggregation-278,Serial4*)
  311-34; (*edge-186,Serial1 --> aggregation-177,Serial10*)
  57-246; (*aggregation-164,Serial11 --> edge-171,Serial4*)
  50-242; (*aggregation-161,Serial8 --> edge-174,Serial1*)
  46-259; (*aggregation-163,Serial14 --> core-30,Serial4*)
  33-310; (*aggregation-178,Serial2 --> core-18,Serial13*)
  103-168; (*aggregation-263,Serial0 --> edge-264,Serial7*)
  130-245; (*aggregation-245,Serial7 --> core-47,Serial8*)
  191-294; (*aggregation-227,Serial10 --> core-26,Serial6*)
  247-83; (*core-46,Serial6 --> aggregation-277,Serial6*)
  254-76; (*edge-159,Serial7 --> aggregation-151,Serial15*)
  286-49; (*aggregation-310,Serial11 --> edge-315,Serial6*)
  138-191; (*edge-237,Serial3 --> aggregation-227,Serial5*)
  293-38; (*edge-191,Serial5 --> aggregation-181,Serial15*)
  297-233; (*core-24,Serial15 --> aggregation-211,Serial4*)
  30-5; (*edge-205,Serial3 --> aggregation-195,Serial5*)
  14-244; (*aggregation-182,Serial0 --> core-48,Serial2*)
  44-298; (*edge-318,Serial3 --> aggregation-307,Serial14*)
  114-161; (*edge-127,Serial5 --> aggregation-117,Serial15*)
  265-85; (*edge-282,Serial4 --> aggregation-276,Serial10*)
  92-292; (*aggregation-147,Serial0 --> core-27,Serial13*)
  12-245; (*aggregation-197,Serial14 --> core-47,Serial1*)
  69-133; (*core-4,Serial1 --> aggregation-128,Serial4*)
  230-38; (*core-42,Serial5 --> aggregation-181,Serial2*)
  104-162; (*aggregation-259,Serial5 --> edge-269,Serial3*)
  244-55; (*core-48,Serial15 --> aggregation-166,Serial8*)
  293-13; (*edge-191,Serial7 --> aggregation-183,Serial15*)
  15-219; (*edge-90,Serial6 --> aggregation-86,Serial10*)
  52-64; (*aggregation-160,Serial3 --> core-3,Serial7*)
  93-310; (*aggregation-146,Serial2 --> core-18,Serial15*)
  186-137; (*aggregation-224,Serial14 --> edge-238,Serial0*)
  221-15; (*aggregation-87,Serial10 --> edge-90,Serial7*)
  178-79; (*edge-152,Serial6 --> aggregation-150,Serial8*)
  134-186; (*edge-236,Serial0 --> aggregation-224,Serial12*)
  313-14; (*edge-185,Serial6 --> aggregation-182,Serial9*)
  45-286; (*edge-313,Serial6 --> aggregation-310,Serial9*)
  230-130; (*core-42,Serial8 --> aggregation-245,Serial2*)
  167-108; (*core-62,Serial12 --> aggregation-135,Serial14*)
  105-179; (*aggregation-258,Serial6 --> edge-270,Serial2*)
  309-33; (*core-19,Serial13 --> aggregation-178,Serial3*)
  185-131; (*aggregation-225,Serial9 --> edge-239,Serial1*)
  227-90; (*core-44,Serial10 --> aggregation-149,Serial4*)
  6-35; (*aggregation-194,Serial1 --> edge-201,Serial2*)
  145-126; (*aggregation-242,Serial12 --> edge-252,Serial2*)
  317-50; (*core-13,Serial6 --> aggregation-161,Serial11*)
  68-216; (*core-5,Serial12 --> aggregation-80,Serial5*)
  194-219; (*core-55,Serial13 --> aggregation-86,Serial7*)
  132-212; (*aggregation-129,Serial8 --> edge-142,Serial1*)
  174-106; (*edge-266,Serial1 --> aggregation-257,Serial4*)
  301-68; (*aggregation-304,Serial5 --> core-5,Serial13*)
  35-8; (*edge-201,Serial1 --> aggregation-193,Serial3*)
  152-280; (*aggregation-64,Serial8 --> edge-72,Serial0*)
  169-110; (*edge-143,Serial2 --> aggregation-130,Serial7*)
  182-140; (*aggregation-99,Serial7 --> edge-111,Serial3*)
  8-24; (*aggregation-193,Serial8 --> edge-206,Serial1*)
  136-172; (*edge-235,Serial7 --> aggregation-231,Serial3*)
  266-63; (*edge-285,Serial6 --> aggregation-278,Serial13*)
  90-254; (*aggregation-149,Serial15 --> edge-159,Serial5*)
  56-237; (*aggregation-165,Serial2 --> edge-168,Serial5*)
  5-258; (*aggregation-195,Serial15 --> core-31,Serial5*)
  123-252; (*aggregation-71,Serial7 --> edge-79,Serial7*)
  125-244; (*aggregation-70,Serial8 --> core-48,Serial7*)
  37-12; (*edge-200,Serial5 --> aggregation-197,Serial2*)
  286-200; (*aggregation-310,Serial4 --> core-52,Serial8*)
  319-267; (*aggregation-308,Serial7 --> core-39,Serial9*)
  149-276; (*aggregation-68,Serial10 --> edge-74,Serial4*)
  315-210; (*core-15,Serial3 --> aggregation-81,Serial7*)
  33-289; (*aggregation-178,Serial13 --> edge-189,Serial2*)
  245-224; (*core-47,Serial7 --> aggregation-213,Serial7*)
  228-48; (*edge-169,Serial2 --> aggregation-162,Serial1*)
  57-228; (*aggregation-164,Serial9 --> edge-169,Serial4*)
  77-182; (*edge-106,Serial3 --> aggregation-99,Serial2*)
  64-75; (*core-3,Serial3 --> aggregation-272,Serial3*)
  220-19; (*aggregation-85,Serial12 --> edge-92,Serial5*)
  70-182; (*edge-109,Serial3 --> aggregation-99,Serial5*)
  158-117; (*aggregation-113,Serial9 --> edge-121,Serial1*)
  100-163; (*aggregation-260,Serial12 --> edge-268,Serial4*)
  149-267; (*aggregation-68,Serial7 --> core-39,Serial0*)
  185-316; (*aggregation-225,Serial12 --> core-14,Serial8*)
  22-291; (*aggregation-179,Serial2 --> core-28,Serial14*)
  216-62; (*aggregation-80,Serial0 --> core-0,Serial12*)
  76-254; (*aggregation-151,Serial15 --> edge-159,Serial7*)
  302-34; (*core-12,Serial11 --> aggregation-177,Serial4*)
  212-116; (*edge-142,Serial6 --> aggregation-134,Serial6*)
  70-181; (*edge-109,Serial0 --> aggregation-96,Serial13*)
  2-59; (*edge-302,Serial2 --> aggregation-290,Serial6*)
  116-198; (*aggregation-134,Serial13 --> core-53,Serial14*)
  164-172; (*core-63,Serial15 --> aggregation-231,Serial15*)
  217-54; (*core-58,Serial13 --> aggregation-167,Serial10*)
  66-186; (*core-2,Serial9 --> aggregation-224,Serial2*)
  190-257; (*aggregation-228,Serial0 --> core-32,Serial4*)
  269-319; (*core-38,Serial9 --> aggregation-308,Serial6*)
  22-313; (*aggregation-179,Serial9 --> edge-185,Serial3*)
  54-217; (*aggregation-167,Serial10 --> core-58,Serial13*)
  226-56; (*core-45,Serial0 --> aggregation-165,Serial0*)
  93-177; (*aggregation-146,Serial9 --> edge-153,Serial2*)
  211-21; (*aggregation-84,Serial14 --> edge-94,Serial4*)
  211-257; (*aggregation-84,Serial0 --> core-32,Serial10*)
  108-173; (*aggregation-135,Serial12 --> core-60,Serial12*)
  283-59; (*core-22,Serial8 --> aggregation-290,Serial14*)
  9-170; (*aggregation-199,Serial13 --> core-61,Serial14*)
  124-135; (*edge-250,Serial0 --> aggregation-240,Serial10*)
  185-146; (*aggregation-225,Serial3 --> edge-233,Serial1*)
  84-236; (*edge-220,Serial2 --> aggregation-210,Serial12*)
  190-134; (*aggregation-228,Serial12 --> edge-236,Serial4*)
  241-55; (*edge-175,Serial6 --> aggregation-166,Serial7*)
  64-301; (*core-3,Serial13 --> aggregation-304,Serial3*)
  129-244; (*aggregation-246,Serial0 --> core-48,Serial11*)
  264-75; (*edge-283,Serial0 --> aggregation-272,Serial11*)
  204-145; (*edge-253,Serial2 --> aggregation-242,Serial13*)
  42-281; (*edge-316,Serial7 --> aggregation-311,Serial12*)
  128-113; (*aggregation-247,Serial8 --> edge-248,Serial7*)
  235-89; (*edge-286,Serial2 --> aggregation-274,Serial14*)
  223-99; (*aggregation-214,Serial8 --> edge-216,Serial6*)
  284-145; (*core-21,Serial7 --> aggregation-242,Serial5*)
  123-215; (*aggregation-71,Serial11 --> core-59,Serial11*)
  202-230; (*aggregation-101,Serial15 --> core-42,Serial13*)
  210-20; (*aggregation-81,Serial13 --> edge-93,Serial1*)
  47-301; (*edge-312,Serial0 --> aggregation-304,Serial8*)
  117-157; (*edge-121,Serial2 --> aggregation-114,Serial9*)
  218-123; (*core-57,Serial11 --> aggregation-71,Serial9*)
  92-296; (*aggregation-147,Serial7 --> core-25,Serial13*)
  125-200; (*aggregation-70,Serial12 --> core-52,Serial7*)
  46-237; (*aggregation-163,Serial0 --> edge-168,Serial3*)
  97-163; (*aggregation-262,Serial4 --> edge-268,Serial6*)
  196-125; (*core-54,Serial7 --> aggregation-70,Serial14*)
  290-182; (*core-29,Serial2 --> aggregation-99,Serial13*)
  236-285; (*aggregation-210,Serial4 --> core-20,Serial6*)
  157-115; (*aggregation-114,Serial10 --> edge-122,Serial2*)
  317-40; (*core-13,Serial10 --> aggregation-289,Serial11*)
  130-247; (*aggregation-245,Serial6 --> core-46,Serial8*)
  231-118; (*core-41,Serial4 --> aggregation-133,Serial13*)
  89-235; (*aggregation-274,Serial14 --> edge-286,Serial2*)
  16-74; (*aggregation-192,Serial6 --> core-6,Serial8*)
  107-179; (*aggregation-256,Serial14 --> edge-270,Serial0*)
  173-103; (*core-60,Serial0 --> aggregation-263,Serial12*)
  9-218; (*aggregation-199,Serial9 --> core-57,Serial14*)
  89-284; (*aggregation-274,Serial5 --> core-21,Serial0*)
  136-175; (*edge-235,Serial6 --> aggregation-230,Serial3*)
  156-259; (*aggregation-115,Serial6 --> core-30,Serial12*)
  208-15; (*aggregation-82,Serial10 --> edge-90,Serial2*)
  207-128; (*edge-255,Serial7 --> aggregation-247,Serial15*)
  96-250; (*edge-219,Serial0 --> aggregation-208,Serial11*)
  150-258; (*aggregation-67,Serial15 --> core-31,Serial10*)
  172-131; (*aggregation-231,Serial7 --> edge-239,Serial7*)
  120-199; (*aggregation-132,Serial10 --> edge-138,Serial4*)
  244-63; (*core-48,Serial10 --> aggregation-278,Serial0*)
  43-300; (*edge-319,Serial1 --> aggregation-305,Serial15*)
  93-312; (*aggregation-146,Serial1 --> core-17,Serial15*)
  64-36; (*core-3,Serial14 --> aggregation-176,Serial3*)
  75-264; (*aggregation-272,Serial11 --> edge-283,Serial0*)
  57-239; (*aggregation-164,Serial12 --> edge-172,Serial4*)
  111-166; (*edge-125,Serial0 --> aggregation-112,Serial13*)
  73-303; (*aggregation-273,Serial3 --> core-11,Serial15*)
  94-236; (*edge-218,Serial2 --> aggregation-210,Serial10*)
  256-102; (*edge-158,Serial0 --> aggregation-144,Serial14*)
  32-4; (*aggregation-292,Serial13 --> edge-301,Serial4*)
  192-159; (*core-56,Serial9 --> aggregation-119,Serial4*)
  123-251; (*aggregation-71,Serial6 --> edge-78,Serial7*)
  152-68; (*aggregation-64,Serial5 --> core-5,Serial0*)
  304-50; (*core-10,Serial6 --> aggregation-161,Serial15*)
  91-267; (*aggregation-148,Serial7 --> core-39,Serial12*)
  283-299; (*core-22,Serial14 --> aggregation-306,Serial6*)
  248-54; (*edge-170,Serial7 --> aggregation-167,Serial2*)
  164-128; (*core-63,Serial2 --> aggregation-247,Serial2*)
  178-90; (*edge-152,Serial5 --> aggregation-149,Serial8*)
  251-125; (*edge-78,Serial6 --> aggregation-70,Serial6*)
  205-140; (*aggregation-100,Serial15 --> edge-111,Serial4*)
  196-116; (*core-54,Serial14 --> aggregation-134,Serial14*)
  155-269; (*aggregation-116,Serial6 --> core-38,Serial5*)
  196-55; (*core-54,Serial15 --> aggregation-166,Serial14*)
  102-62; (*aggregation-144,Serial0 --> core-0,Serial6*)
  208-0; (*aggregation-82,Serial8 --> edge-88,Serial2*)
  3-26; (*aggregation-196,Serial11 --> edge-203,Serial4*)
  154-317; (*aggregation-65,Serial11 --> core-13,Serial4*)
  217-159; (*core-58,Serial9 --> aggregation-119,Serial5*)
  116-243; (*aggregation-134,Serial9 --> core-49,Serial14*)
  183-314; (*aggregation-98,Serial8 --> core-16,Serial9*)
  115-159; (*edge-122,Serial7 --> aggregation-119,Serial10*)
  10-29; (*edge-300,Serial5 --> aggregation-293,Serial6*)
  183-77; (*aggregation-98,Serial2 --> edge-106,Serial2*)
  112-129; (*edge-249,Serial6 --> aggregation-246,Serial9*)
  17-220; (*edge-95,Serial5 --> aggregation-85,Serial15*)
  95-225; (*edge-217,Serial4 --> aggregation-212,Serial9*)
  23-9; (*edge-207,Serial7 --> aggregation-199,Serial7*)
  210-316; (*aggregation-81,Serial6 --> core-14,Serial3*)
  273-211; (*core-35,Serial10 --> aggregation-84,Serial3*)
  53-2; (*aggregation-288,Serial14 --> edge-302,Serial0*)
  99-222; (*edge-216,Serial7 --> aggregation-215,Serial8*)
  206-160; (*core-50,Serial9 --> aggregation-118,Serial2*)
  250-94; (*aggregation-208,Serial10 --> edge-218,Serial0*)
  191-144; (*aggregation-227,Serial2 --> edge-234,Serial3*)
  180-317; (*aggregation-97,Serial11 --> core-13,Serial2*)
  294-233; (*core-26,Serial15 --> aggregation-211,Serial5*)
  221-215; (*aggregation-87,Serial0 --> core-59,Serial5*)
  312-6; (*core-17,Serial5 --> aggregation-194,Serial9*)
  61-266; (*aggregation-279,Serial13 --> edge-285,Serial7*)
  311-22; (*edge-186,Serial3 --> aggregation-179,Serial10*)
  98-176; (*aggregation-261,Serial9 --> edge-271,Serial5*)
  29-7; (*aggregation-293,Serial9 --> edge-303,Serial5*)
  218-172; (*core-57,Serial15 --> aggregation-231,Serial9*)
  128-127; (*aggregation-247,Serial11 --> edge-251,Serial7*)
  36-69; (*aggregation-176,Serial4 --> core-4,Serial14*)
  169-133; (*edge-143,Serial0 --> aggregation-128,Serial15*)
  200-97; (*core-52,Serial12 --> aggregation-262,Serial12*)
  200-223; (*core-52,Serial1 --> aggregation-214,Serial4*)
  206-79; (*core-50,Serial4 --> aggregation-150,Serial2*)
  33-295; (*aggregation-178,Serial14 --> edge-190,Serial2*)
  191-292; (*aggregation-227,Serial11 --> core-27,Serial6*)
  61-274; (*aggregation-279,Serial8 --> edge-280,Serial7*)
  250-72; (*aggregation-208,Serial7 --> core-7,Serial15*)
  51-299; (*edge-314,Serial2 --> aggregation-306,Serial10*)
  121-155; (*edge-123,Serial4 --> aggregation-116,Serial11*)
  115-160; (*edge-122,Serial6 --> aggregation-118,Serial10*)
  104-258; (*aggregation-259,Serial15 --> core-31,Serial7*)
  168-98; (*edge-264,Serial5 --> aggregation-261,Serial2*)
  306-29; (*edge-297,Serial5 --> aggregation-293,Serial3*)
  296-92; (*core-25,Serial13 --> aggregation-147,Serial7*)
  107-74; (*aggregation-256,Serial6 --> core-6,Serial4*)
  210-317; (*aggregation-81,Serial5 --> core-13,Serial3*)
  128-124; (*aggregation-247,Serial10 --> edge-250,Serial7*)
  275-123; (*edge-73,Serial7 --> aggregation-71,Serial1*)
  272-63; (*edge-281,Serial6 --> aggregation-278,Serial9*)
  17-211; (*edge-95,Serial4 --> aggregation-84,Serial15*)
  25-307; (*aggregation-295,Serial3 --> edge-299,Serial7*)
  147-207; (*aggregation-241,Serial15 --> edge-255,Serial1*)
  11-28; (*aggregation-198,Serial2 --> edge-202,Serial6*)
  145-314; (*aggregation-242,Serial0 --> core-16,Serial7*)
  180-303; (*aggregation-97,Serial0 --> core-11,Serial2*)
  319-257; (*aggregation-308,Serial0 --> core-32,Serial9*)
  122-212; (*aggregation-131,Serial6 --> edge-142,Serial3*)
  57-237; (*aggregation-164,Serial8 --> edge-168,Serial4*)
  164-123; (*core-63,Serial11 --> aggregation-71,Serial15*)
  158-317; (*aggregation-113,Serial5 --> core-13,Serial12*)
  166-109; (*aggregation-112,Serial14 --> edge-126,Serial0*)
  53-72; (*aggregation-288,Serial7 --> core-7,Serial11*)
  124-147; (*edge-250,Serial1 --> aggregation-241,Serial10*)
  200-27; (*core-52,Serial3 --> aggregation-294,Serial12*)
  154-251; (*aggregation-65,Serial8 --> edge-78,Serial1*)
  43-301; (*edge-319,Serial0 --> aggregation-304,Serial15*)
  149-277; (*aggregation-68,Serial11 --> edge-75,Serial4*)
  263-91; (*edge-156,Serial4 --> aggregation-148,Serial12*)
  108-218; (*aggregation-135,Serial9 --> core-57,Serial12*)
  316-300; (*core-14,Serial1 --> aggregation-305,Serial6*)
  93-178; (*aggregation-146,Serial8 --> edge-152,Serial2*)
  217-9; (*core-58,Serial14 --> aggregation-199,Serial10*)
  3-31; (*aggregation-196,Serial12 --> edge-204,Serial4*)
  21-221; (*edge-94,Serial7 --> aggregation-87,Serial14*)
  112-128; (*edge-249,Serial7 --> aggregation-247,Serial9*)
  266-83; (*edge-285,Serial5 --> aggregation-277,Serial13*)
  23-11; (*edge-207,Serial6 --> aggregation-198,Serial7*)
  78-40; (*core-9,Serial10 --> aggregation-289,Serial13*)
  8-23; (*aggregation-193,Serial9 --> edge-207,Serial1*)
  110-193; (*aggregation-130,Serial1 --> edge-137,Serial2*)
  169-122; (*edge-143,Serial3 --> aggregation-131,Serial7*)
  304-40; (*core-10,Serial10 --> aggregation-289,Serial15*)
  285-48; (*core-20,Serial4 --> aggregation-162,Serial12*)
  129-204; (*aggregation-246,Serial13 --> edge-253,Serial6*)
  289-39; (*edge-189,Serial4 --> aggregation-180,Serial13*)
  224-82; (*aggregation-213,Serial13 --> edge-221,Serial5*)
  223-200; (*aggregation-214,Serial4 --> core-52,Serial1*)
  125-198; (*aggregation-70,Serial13 --> core-53,Serial7*)
  188-203; (*aggregation-102,Serial11 --> core-51,Serial5*)
  279-78; (*aggregation-209,Serial1 --> core-9,Serial13*)
  278-154; (*edge-76,Serial1 --> aggregation-65,Serial6*)
  150-280; (*aggregation-67,Serial0 --> edge-72,Serial3*)
  233-258; (*aggregation-211,Serial1 --> core-31,Serial15*)
  145-112; (*aggregation-242,Serial9 --> edge-249,Serial2*)
  271-91; (*core-36,Serial12 --> aggregation-148,Serial4*)
  80-187; (*edge-105,Serial7 --> aggregation-103,Serial1*)
  199-108; (*edge-138,Serial7 --> aggregation-135,Serial2*)
  227-202; (*core-44,Serial13 --> aggregation-101,Serial11*)
  244-116; (*core-48,Serial14 --> aggregation-134,Serial8*)
  307-59; (*edge-299,Serial2 --> aggregation-290,Serial3*)
  176-98; (*edge-271,Serial5 --> aggregation-261,Serial9*)
  152-69; (*aggregation-64,Serial4 --> core-4,Serial0*)
  199-120; (*edge-138,Serial4 --> aggregation-132,Serial10*)
  74-166; (*core-6,Serial10 --> aggregation-112,Serial6*)
  57-255; (*aggregation-164,Serial1 --> core-33,Serial2*)
  181-71; (*aggregation-96,Serial8 --> edge-104,Serial0*)
  172-170; (*aggregation-231,Serial13 --> core-61,Serial15*)
  188-244; (*aggregation-102,Serial8 --> core-48,Serial5*)
  181-72; (*aggregation-96,Serial7 --> core-7,Serial5*)
  281-217; (*aggregation-311,Serial5 --> core-58,Serial10*)
  89-232; (*aggregation-274,Serial15 --> edge-287,Serial2*)
  185-139; (*aggregation-225,Serial2 --> edge-232,Serial1*)
  254-79; (*edge-159,Serial6 --> aggregation-150,Serial15*)
  284-236; (*core-21,Serial6 --> aggregation-210,Serial5*)
  7-32; (*edge-303,Serial4 --> aggregation-292,Serial15*)
  237-56; (*edge-168,Serial5 --> aggregation-165,Serial2*)
  9-37; (*aggregation-199,Serial0 --> edge-200,Serial7*)
  101-256; (*aggregation-145,Serial14 --> edge-158,Serial1*)
  258-191; (*core-31,Serial6 --> aggregation-227,Serial15*)
  168-100; (*edge-264,Serial4 --> aggregation-260,Serial8*)
  55-248; (*aggregation-166,Serial2 --> edge-170,Serial6*)
  279-86; (*aggregation-209,Serial15 --> edge-223,Serial1*)
  33-312; (*aggregation-178,Serial1 --> core-17,Serial13*)
  92-261; (*aggregation-147,Serial10 --> edge-154,Serial3*)
  208-18; (*aggregation-82,Serial11 --> edge-91,Serial2*)
  195-122; (*edge-136,Serial3 --> aggregation-131,Serial0*)
  77-183; (*edge-106,Serial2 --> aggregation-98,Serial2*)
  248-57; (*edge-170,Serial4 --> aggregation-164,Serial10*)
  298-47; (*aggregation-307,Serial8 --> edge-312,Serial3*)
  22-308; (*aggregation-179,Serial8 --> edge-184,Serial3*)
  271-120; (*core-36,Serial3 --> aggregation-132,Serial4*)
  255-3; (*core-33,Serial11 --> aggregation-196,Serial1*)
  172-218; (*aggregation-231,Serial9 --> core-57,Serial15*)
  269-225; (*core-38,Serial7 --> aggregation-212,Serial6*)
  284-105; (*core-21,Serial12 --> aggregation-258,Serial13*)
  126-130; (*edge-252,Serial5 --> aggregation-245,Serial12*)
  14-287; (*aggregation-182,Serial12 --> edge-188,Serial6*)
  280-153; (*edge-72,Serial2 --> aggregation-66,Serial0*)
  158-148; (*aggregation-113,Serial8 --> edge-120,Serial1*)
  46-228; (*aggregation-163,Serial1 --> edge-169,Serial3*)
  46-290; (*aggregation-163,Serial13 --> core-29,Serial4*)
  50-241; (*aggregation-161,Serial9 --> edge-175,Serial1*)
  88-222; (*edge-222,Serial7 --> aggregation-215,Serial14*)
  208-309; (*aggregation-82,Serial3 --> core-19,Serial2*)
  29-305; (*aggregation-293,Serial4 --> edge-298,Serial5*)
  295-36; (*edge-190,Serial0 --> aggregation-176,Serial14*)
  163-97; (*edge-268,Serial6 --> aggregation-262,Serial4*)
  270-190; (*core-37,Serial4 --> aggregation-228,Serial5*)
  160-196; (*aggregation-118,Serial6 --> core-54,Serial9*)
  219-196; (*aggregation-86,Serial6 --> core-54,Serial13*)
  290-213; (*core-29,Serial1 --> aggregation-83,Serial3*)
  141-267; (*aggregation-244,Serial7 --> core-39,Serial13*)
  90-256; (*aggregation-149,Serial14 --> edge-158,Serial5*)
  211-17; (*aggregation-84,Serial15 --> edge-95,Serial4*)
  154-303; (*aggregation-65,Serial0 --> core-11,Serial4*)
  161-119; (*aggregation-117,Serial12 --> edge-124,Serial5*)
  319-51; (*aggregation-308,Serial10 --> edge-314,Serial4*)
  60-152; (*core-1,Serial0 --> aggregation-64,Serial1*)
  2-40; (*edge-302,Serial1 --> aggregation-289,Serial8*)
  1-211; (*edge-89,Serial4 --> aggregation-84,Serial9*)
  302-73; (*core-12,Serial15 --> aggregation-273,Serial4*)
  12-229; (*aggregation-197,Serial1 --> core-43,Serial1*)
  103-170; (*aggregation-263,Serial13 --> core-61,Serial0*)
  239-52; (*edge-172,Serial0 --> aggregation-160,Serial12*)
  244-11; (*core-48,Serial0 --> aggregation-198,Serial8*)
  247-202; (*core-46,Serial13 --> aggregation-101,Serial12*)
  299-314; (*aggregation-306,Serial0 --> core-16,Serial14*)
  300-78; (*aggregation-305,Serial1 --> core-9,Serial1*)
  264-73; (*edge-283,Serial1 --> aggregation-273,Serial11*)
  296-22; (*core-25,Serial14 --> aggregation-179,Serial7*)
  84-233; (*edge-220,Serial3 --> aggregation-211,Serial12*)
  120-270; (*aggregation-132,Serial5 --> core-37,Serial3*)
  186-146; (*aggregation-224,Serial9 --> edge-233,Serial0*)
  132-169; (*aggregation-129,Serial9 --> edge-143,Serial1*)
  62-53; (*core-0,Serial11 --> aggregation-288,Serial0*)
  242-57; (*edge-174,Serial4 --> aggregation-164,Serial14*)
  209-132; (*edge-140,Serial1 --> aggregation-129,Serial6*)
  64-102; (*core-3,Serial6 --> aggregation-144,Serial3*)
  312-89; (*core-17,Serial0 --> aggregation-274,Serial1*)
  72-181; (*core-7,Serial5 --> aggregation-96,Serial7*)
  83-247; (*aggregation-277,Serial6 --> core-46,Serial6*)
  78-8; (*core-9,Serial7 --> aggregation-193,Serial13*)
  10-25; (*edge-300,Serial7 --> aggregation-295,Serial4*)
  296-233; (*core-25,Serial15 --> aggregation-211,Serial7*)
  314-59; (*core-16,Serial8 --> aggregation-290,Serial8*)
  69-301; (*core-4,Serial13 --> aggregation-304,Serial4*)
  33-288; (*aggregation-178,Serial11 --> edge-187,Serial2*)
  190-138; (*aggregation-228,Serial13 --> edge-237,Serial4*)
  120-193; (*aggregation-132,Serial9 --> edge-137,Serial4*)
  190-144; (*aggregation-228,Serial10 --> edge-234,Serial4*)
  22-290; (*aggregation-179,Serial3 --> core-29,Serial14*)
  303-50; (*core-11,Serial6 --> aggregation-161,Serial0*)
  9-164; (*aggregation-199,Serial15 --> core-63,Serial14*)
  231-83; (*core-41,Serial6 --> aggregation-277,Serial1*)
  194-175; (*core-55,Serial6 --> aggregation-230,Serial15*)
  283-6; (*core-22,Serial5 --> aggregation-194,Serial14*)
  9-173; (*aggregation-199,Serial12 --> core-60,Serial14*)
  241-48; (*edge-175,Serial2 --> aggregation-162,Serial7*)
  204-135; (*edge-253,Serial0 --> aggregation-240,Serial13*)
  110-209; (*aggregation-130,Serial4 --> edge-140,Serial2*)
  128-167; (*aggregation-247,Serial6 --> core-62,Serial2*)
  157-117; (*aggregation-114,Serial9 --> edge-121,Serial2*)
  185-138; (*aggregation-225,Serial7 --> edge-237,Serial1*)
  162-103; (*edge-269,Serial7 --> aggregation-263,Serial5*)
  217-25; (*core-58,Serial3 --> aggregation-295,Serial10*)
  76-178; (*aggregation-151,Serial8 --> edge-152,Serial7*)
  64-135; (*core-3,Serial2 --> aggregation-240,Serial3*)
  12-226; (*aggregation-197,Serial0 --> core-45,Serial1*)
  279-96; (*aggregation-209,Serial11 --> edge-219,Serial1*)
  170-187; (*core-61,Serial4 --> aggregation-103,Serial13*)
  176-107; (*edge-271,Serial0 --> aggregation-256,Serial15*)
  199-122; (*edge-138,Serial3 --> aggregation-131,Serial2*)
  13-218; (*aggregation-183,Serial7 --> core-57,Serial7*)
  181-67; (*aggregation-96,Serial11 --> edge-107,Serial0*)
  23-6; (*edge-207,Serial2 --> aggregation-194,Serial7*)
  199-116; (*edge-138,Serial6 --> aggregation-134,Serial2*)
  8-28; (*aggregation-193,Serial4 --> edge-202,Serial1*)
  8-30; (*aggregation-193,Serial7 --> edge-205,Serial1*)
  234-151; (*core-40,Serial14 --> aggregation-69,Serial10*)
  213-294; (*aggregation-83,Serial5 --> core-26,Serial1*)
  247-318; (*core-46,Serial9 --> aggregation-309,Serial6*)
  308-22; (*edge-184,Serial3 --> aggregation-179,Serial8*)
  298-43; (*aggregation-307,Serial15 --> edge-319,Serial3*)
  313-38; (*edge-185,Serial5 --> aggregation-181,Serial9*)
  15-221; (*edge-90,Serial7 --> aggregation-87,Serial10*)
  50-238; (*aggregation-161,Serial7 --> edge-173,Serial1*)
  50-248; (*aggregation-161,Serial4 --> edge-170,Serial1*)
  301-72; (*aggregation-304,Serial7 --> core-7,Serial13*)
  108-212; (*aggregation-135,Serial6 --> edge-142,Serial7*)
  85-268; (*aggregation-276,Serial12 --> edge-284,Serial4*)
  131-184; (*edge-239,Serial2 --> aggregation-226,Serial7*)
  248-55; (*edge-170,Serial6 --> aggregation-166,Serial2*)
  85-232; (*aggregation-276,Serial15 --> edge-287,Serial4*)
  166-62; (*aggregation-112,Serial0 --> core-0,Serial10*)
  173-281; (*core-60,Serial10 --> aggregation-311,Serial1*)
  269-155; (*core-38,Serial5 --> aggregation-116,Serial6*)
  310-33; (*core-18,Serial13 --> aggregation-178,Serial2*)
  265-63; (*edge-282,Serial6 --> aggregation-278,Serial10*)
  289-34; (*edge-189,Serial1 --> aggregation-177,Serial13*)
  149-278; (*aggregation-68,Serial12 --> edge-76,Serial4*)
  54-228; (*aggregation-167,Serial1 --> edge-169,Serial7*)
  116-193; (*aggregation-134,Serial1 --> edge-137,Serial6*)
  93-309; (*aggregation-146,Serial3 --> core-19,Serial15*)
  14-311; (*aggregation-182,Serial10 --> edge-186,Serial6*)
  221-18; (*aggregation-87,Serial11 --> edge-91,Serial7*)
  292-213; (*core-27,Serial1 --> aggregation-83,Serial0*)
  186-72; (*aggregation-224,Serial7 --> core-7,Serial9*)
  271-141; (*core-36,Serial13 --> aggregation-244,Serial4*)
  212-118; (*edge-142,Serial5 --> aggregation-133,Serial8*)
  25-215; (*aggregation-295,Serial11 --> core-59,Serial3*)
  159-148; (*aggregation-119,Serial8 --> edge-120,Serial7*)
  191-291; (*aggregation-227,Serial12 --> core-28,Serial6*)
  28-3; (*edge-202,Serial4 --> aggregation-196,Serial10*)
  147-317; (*aggregation-241,Serial5 --> core-13,Serial14*)
  6-283; (*aggregation-194,Serial14 --> core-22,Serial5*)
  145-124; (*aggregation-242,Serial10 --> edge-250,Serial2*)
  297-182; (*core-24,Serial2 --> aggregation-99,Serial8*)
  160-244; (*aggregation-118,Serial0 --> core-48,Serial9*)
  12-230; (*aggregation-197,Serial15 --> core-42,Serial1*)
  211-267; (*aggregation-84,Serial7 --> core-39,Serial10*)
  282-157; (*core-23,Serial11 --> aggregation-114,Serial7*)
  150-259; (*aggregation-67,Serial14 --> core-30,Serial10*)
  312-236; (*core-17,Serial6 --> aggregation-210,Serial1*)
  275-151; (*edge-73,Serial5 --> aggregation-69,Serial3*)
  283-153; (*core-22,Serial1 --> aggregation-66,Serial14*)
  302-158; (*core-12,Serial12 --> aggregation-113,Serial4*)
  44-318; (*edge-318,Serial5 --> aggregation-309,Serial14*)
  57-248; (*aggregation-164,Serial10 --> edge-170,Serial4*)
  114-159; (*edge-127,Serial7 --> aggregation-119,Serial15*)
  198-63; (*core-53,Serial10 --> aggregation-278,Serial5*)
  172-139; (*aggregation-231,Serial0 --> edge-232,Serial7*)
  172-137; (*aggregation-231,Serial6 --> edge-238,Serial7*)
  35-16; (*edge-201,Serial0 --> aggregation-192,Serial9*)
  75-72; (*aggregation-272,Serial7 --> core-7,Serial3*)
  149-252; (*aggregation-68,Serial15 --> edge-79,Serial4*)
  75-60; (*aggregation-272,Serial1 --> core-1,Serial3*)
  68-107; (*core-5,Serial4 --> aggregation-256,Serial5*)
  100-267; (*aggregation-260,Serial7 --> core-39,Serial15*)
  46-239; (*aggregation-163,Serial4 --> edge-172,Serial3*)
  41-298; (*edge-317,Serial3 --> aggregation-307,Serial13*)
  109-161; (*edge-126,Serial5 --> aggregation-117,Serial14*)
  122-291; (*aggregation-131,Serial12 --> core-28,Serial3*)
  226-29; (*core-45,Serial12 --> aggregation-293,Serial0*)
  125-203; (*aggregation-70,Serial11 --> core-51,Serial7*)
  186-69; (*aggregation-224,Serial4 --> core-4,Serial9*)
  99-236; (*edge-216,Serial2 --> aggregation-210,Serial8*)
  99-224; (*edge-216,Serial5 --> aggregation-213,Serial8*)
  112-145; (*edge-249,Serial2 --> aggregation-242,Serial9*)
  272-83; (*edge-281,Serial5 --> aggregation-277,Serial9*)
  144-191; (*edge-234,Serial3 --> aggregation-227,Serial2*)
  245-118; (*core-47,Serial4 --> aggregation-133,Serial14*)
  17-219; (*edge-95,Serial6 --> aggregation-86,Serial15*)
  202-247; (*aggregation-101,Serial12 --> core-46,Serial13*)
  308-36; (*edge-184,Serial0 --> aggregation-176,Serial8*)
  5-297; (*aggregation-195,Serial8 --> core-24,Serial5*)
  168-105; (*edge-264,Serial2 --> aggregation-258,Serial0*)
  28-5; (*edge-202,Serial3 --> aggregation-195,Serial2*)
  28-11; (*edge-202,Serial6 --> aggregation-198,Serial2*)
  217-108; (*core-58,Serial12 --> aggregation-135,Serial10*)
  67-180; (*edge-107,Serial1 --> aggregation-97,Serial5*)
  316-106; (*core-14,Serial9 --> aggregation-257,Serial12*)
  85-269; (*aggregation-276,Serial6 --> core-38,Serial14*)
  172-136; (*aggregation-231,Serial3 --> edge-235,Serial7*)
  110-169; (*aggregation-130,Serial7 --> edge-143,Serial2*)
  64-16; (*core-3,Serial8 --> aggregation-192,Serial3*)
  75-69; (*aggregation-272,Serial4 --> core-4,Serial3*)
  243-219; (*core-49,Serial13 --> aggregation-86,Serial1*)
  18-216; (*edge-91,Serial0 --> aggregation-80,Serial11*)
  46-241; (*aggregation-163,Serial7 --> edge-175,Serial3*)
  154-252; (*aggregation-65,Serial9 --> edge-79,Serial1*)
  3-28; (*aggregation-196,Serial10 --> edge-202,Serial4*)
  158-81; (*aggregation-113,Serial0 --> core-8,Serial12*)
  303-147; (*core-11,Serial14 --> aggregation-241,Serial3*)
  186-60; (*aggregation-224,Serial1 --> core-1,Serial9*)
  7-53; (*edge-303,Serial0 --> aggregation-288,Serial15*)
  112-130; (*edge-249,Serial5 --> aggregation-245,Serial9*)
  144-190; (*edge-234,Serial4 --> aggregation-228,Serial10*)
  310-208; (*core-18,Serial2 --> aggregation-82,Serial2*)
  58-292; (*aggregation-291,Serial11 --> core-27,Serial11*)
  27-305; (*aggregation-294,Serial2 --> edge-298,Serial6*)
  267-85; (*core-39,Serial14 --> aggregation-276,Serial7*)
  254-90; (*edge-159,Serial5 --> aggregation-149,Serial15*)
  258-104; (*core-31,Serial7 --> aggregation-259,Serial15*)
  87-265; (*aggregation-275,Serial10 --> edge-282,Serial3*)
  91-253; (*aggregation-148,Serial2 --> core-34,Serial12*)
  61-170; (*aggregation-279,Serial3 --> core-61,Serial1*)
  191-131; (*aggregation-227,Serial7 --> edge-239,Serial3*)
  201-145; (*edge-254,Serial2 --> aggregation-242,Serial14*)
  21-210; (*edge-94,Serial1 --> aggregation-81,Serial14*)
  21-211; (*edge-94,Serial4 --> aggregation-84,Serial14*)
  250-99; (*aggregation-208,Serial8 --> edge-216,Serial0*)
  294-213; (*core-26,Serial1 --> aggregation-83,Serial5*)
  146-186; (*edge-233,Serial0 --> aggregation-224,Serial9*)
  59-10; (*aggregation-290,Serial4 --> edge-300,Serial2*)
  166-121; (*aggregation-112,Serial11 --> edge-123,Serial0*)
  52-238; (*aggregation-160,Serial13 --> edge-173,Serial0*)
  153-283; (*aggregation-66,Serial14 --> core-22,Serial1*)
  22-289; (*aggregation-179,Serial13 --> edge-189,Serial3*)
  181-142; (*aggregation-96,Serial14 --> edge-110,Serial0*)
  85-273; (*aggregation-276,Serial3 --> core-35,Serial14*)
  226-38; (*core-45,Serial5 --> aggregation-181,Serial5*)
  151-275; (*aggregation-69,Serial3 --> edge-73,Serial5*)
  189-231; (*aggregation-229,Serial13 --> core-41,Serial2*)
  229-189; (*core-43,Serial2 --> aggregation-229,Serial1*)
  118-209; (*aggregation-133,Serial6 --> edge-140,Serial5*)
  171-104; (*edge-267,Serial3 --> aggregation-259,Serial3*)
  152-60; (*aggregation-64,Serial1 --> core-1,Serial0*)
  156-114; (*aggregation-115,Serial15 --> edge-127,Serial3*)
  299-41; (*aggregation-306,Serial13 --> edge-317,Serial2*)
  29-229; (*aggregation-293,Serial1 --> core-43,Serial12*)
  89-310; (*aggregation-274,Serial2 --> core-18,Serial0*)
  29-230; (*aggregation-293,Serial15 --> core-42,Serial12*)
  3-257; (*aggregation-196,Serial0 --> core-32,Serial11*)
  92-259; (*aggregation-147,Serial6 --> core-30,Serial13*)
  304-8; (*core-10,Serial7 --> aggregation-193,Serial15*)
  307-58; (*edge-299,Serial3 --> aggregation-291,Serial3*)
  47-318; (*edge-312,Serial5 --> aggregation-309,Serial8*)
  117-159; (*edge-121,Serial7 --> aggregation-119,Serial9*)
  121-158; (*edge-123,Serial1 --> aggregation-113,Serial11*)
  52-68; (*aggregation-160,Serial5 --> core-5,Serial7*)
  263-101; (*edge-156,Serial1 --> aggregation-145,Serial12*)
  143-126; (*aggregation-243,Serial12 --> edge-252,Serial3*)
  182-290; (*aggregation-99,Serial13 --> core-29,Serial2*)
  297-213; (*core-24,Serial1 --> aggregation-83,Serial4*)
  5-35; (*aggregation-195,Serial1 --> edge-201,Serial3*)
  226-90; (*core-45,Serial10 --> aggregation-149,Serial5*)
  244-79; (*core-48,Serial4 --> aggregation-150,Serial0*)
  132-303; (*aggregation-129,Serial0 --> core-11,Serial0*)
  236-84; (*aggregation-210,Serial12 --> edge-220,Serial2*)
  317-8; (*core-13,Serial7 --> aggregation-193,Serial11*)
  16-30; (*aggregation-192,Serial13 --> edge-205,Serial0*)
  281-49; (*aggregation-311,Serial11 --> edge-315,Serial7*)
  23-16; (*edge-207,Serial0 --> aggregation-192,Serial15*)
  107-62; (*aggregation-256,Serial0 --> core-0,Serial4*)
  32-267; (*aggregation-292,Serial7 --> core-39,Serial6*)
  173-54; (*core-60,Serial13 --> aggregation-167,Serial12*)
  249-152; (*edge-77,Serial0 --> aggregation-64,Serial13*)
  80-188; (*edge-105,Serial6 --> aggregation-102,Serial1*)
  295-38; (*edge-190,Serial5 --> aggregation-181,Serial14*)
  98-247; (*aggregation-261,Serial12 --> core-46,Serial3*)
  229-130; (*core-43,Serial8 --> aggregation-245,Serial3*)
  133-60; (*aggregation-128,Serial1 --> core-1,Serial1*)
  135-113; (*aggregation-240,Serial8 --> edge-248,Serial0*)
  86-225; (*edge-223,Serial4 --> aggregation-212,Serial15*)
  82-236; (*edge-221,Serial2 --> aggregation-210,Serial13*)
  116-194; (*aggregation-134,Serial15 --> core-55,Serial14*)
  222-99; (*aggregation-215,Serial8 --> edge-216,Serial7*)
  80-182; (*edge-105,Serial3 --> aggregation-99,Serial1*)
  37-3; (*edge-200,Serial4 --> aggregation-196,Serial8*)
  211-271; (*aggregation-84,Serial4 --> core-36,Serial10*)
  101-260; (*aggregation-145,Serial11 --> edge-155,Serial1*)
  232-89; (*edge-287,Serial2 --> aggregation-274,Serial15*)
  211-15; (*aggregation-84,Serial10 --> edge-90,Serial4*)
  213-292; (*aggregation-83,Serial0 --> core-27,Serial1*)
  13-173; (*aggregation-183,Serial1 --> core-60,Serial7*)
  98-245; (*aggregation-261,Serial14 --> core-47,Serial3*)
  50-81; (*aggregation-161,Serial10 --> core-8,Serial6*)
  131-186; (*edge-239,Serial0 --> aggregation-224,Serial15*)
  316-210; (*core-14,Serial3 --> aggregation-81,Serial6*)
  224-94; (*aggregation-213,Serial10 --> edge-218,Serial5*)
  237-48; (*edge-168,Serial2 --> aggregation-162,Serial0*)
  43-281; (*edge-319,Serial7 --> aggregation-311,Serial15*)
  212-110; (*edge-142,Serial2 --> aggregation-130,Serial6*)
  233-290; (*aggregation-211,Serial3 --> core-29,Serial15*)
  133-214; (*aggregation-128,Serial13 --> edge-141,Serial0*)
  92-177; (*aggregation-147,Serial9 --> edge-153,Serial3*)
  93-262; (*aggregation-146,Serial13 --> edge-157,Serial2*)
  235-75; (*edge-286,Serial0 --> aggregation-272,Serial14*)
  91-254; (*aggregation-148,Serial15 --> edge-159,Serial4*)
  100-174; (*aggregation-260,Serial10 --> edge-266,Serial4*)
  66-75; (*core-2,Serial3 --> aggregation-272,Serial2*)
  134-172; (*edge-236,Serial7 --> aggregation-231,Serial4*)
  34-303; (*aggregation-177,Serial3 --> core-11,Serial11*)
  221-21; (*aggregation-87,Serial14 --> edge-94,Serial7*)
  290-46; (*core-29,Serial4 --> aggregation-163,Serial13*)
  166-64; (*aggregation-112,Serial3 --> core-3,Serial10*)
  62-216; (*core-0,Serial12 --> aggregation-80,Serial0*)
  87-290; (*aggregation-275,Serial3 --> core-29,Serial8*)
  297-122; (*core-24,Serial3 --> aggregation-131,Serial8*)
  305-40; (*edge-298,Serial1 --> aggregation-289,Serial4*)
  255-149; (*core-33,Serial0 --> aggregation-68,Serial1*)
  289-13; (*edge-189,Serial7 --> aggregation-183,Serial13*)
  68-250; (*core-5,Serial15 --> aggregation-208,Serial5*)
  263-76; (*edge-156,Serial7 --> aggregation-151,Serial12*)
  77-181; (*edge-106,Serial0 --> aggregation-96,Serial10*)
  300-304; (*aggregation-305,Serial2 --> core-10,Serial1*)
  280-150; (*edge-72,Serial3 --> aggregation-67,Serial0*)
  165-106; (*edge-265,Serial1 --> aggregation-257,Serial3*)
  139-185; (*edge-232,Serial1 --> aggregation-225,Serial2*)
  26-12; (*edge-203,Serial5 --> aggregation-197,Serial5*)
  76-217; (*aggregation-151,Serial5 --> core-58,Serial6*)
  53-240; (*aggregation-288,Serial8 --> edge-296,Serial0*)
  208-19; (*aggregation-82,Serial12 --> edge-92,Serial2*)
  103-179; (*aggregation-263,Serial6 --> edge-270,Serial7*)
  25-2; (*aggregation-295,Serial6 --> edge-302,Serial7*)
  76-215; (*aggregation-151,Serial0 --> core-59,Serial6*)
  229-202; (*core-43,Serial13 --> aggregation-101,Serial1*)
  255-39; (*core-33,Serial8 --> aggregation-180,Serial1*)
  317-34; (*core-13,Serial11 --> aggregation-177,Serial5*)
  136-189; (*edge-235,Serial5 --> aggregation-229,Serial5*)
  37-8; (*edge-200,Serial1 --> aggregation-193,Serial2*)
  210-15; (*aggregation-81,Serial10 --> edge-90,Serial1*)
  194-116; (*core-55,Serial14 --> aggregation-134,Serial15*)
  270-319; (*core-37,Serial9 --> aggregation-308,Serial5*)
  3-35; (*aggregation-196,Serial9 --> edge-201,Serial4*)
  74-181; (*core-6,Serial5 --> aggregation-96,Serial6*)
  290-92; (*core-29,Serial13 --> aggregation-147,Serial3*)
  188-243; (*aggregation-102,Serial9 --> core-49,Serial5*)
  276-152; (*edge-74,Serial0 --> aggregation-64,Serial10*)
  288-22; (*edge-187,Serial3 --> aggregation-179,Serial11*)
  79-254; (*aggregation-150,Serial15 --> edge-159,Serial6*)
  79-263; (*aggregation-150,Serial12 --> edge-156,Serial6*)
  141-270; (*aggregation-244,Serial5 --> core-37,Serial13*)
  153-309; (*aggregation-66,Serial11 --> core-19,Serial1*)
  307-32; (*edge-299,Serial4 --> aggregation-292,Serial11*)
  310-59; (*core-18,Serial8 --> aggregation-290,Serial10*)
  120-253; (*aggregation-132,Serial2 --> core-34,Serial3*)
  96-236; (*edge-219,Serial2 --> aggregation-210,Serial11*)
  118-247; (*aggregation-133,Serial12 --> core-46,Serial4*)
  184-144; (*aggregation-226,Serial2 --> edge-234,Serial2*)
  19-220; (*edge-92,Serial5 --> aggregation-85,Serial12*)
  125-251; (*aggregation-70,Serial6 --> edge-78,Serial6*)
  181-68; (*aggregation-96,Serial5 --> core-5,Serial5*)
  234-224; (*core-40,Serial7 --> aggregation-213,Serial0*)
  281-173; (*aggregation-311,Serial1 --> core-60,Serial10*)
  145-283; (*aggregation-242,Serial6 --> core-22,Serial7*)
  267-225; (*core-39,Serial7 --> aggregation-212,Serial7*)
  69-36; (*core-4,Serial14 --> aggregation-176,Serial4*)
  202-227; (*aggregation-101,Serial11 --> core-44,Serial13*)
  120-209; (*aggregation-132,Serial12 --> edge-140,Serial4*)
  148-157; (*edge-120,Serial2 --> aggregation-114,Serial8*)
  238-54; (*edge-173,Serial7 --> aggregation-167,Serial5*)
  73-235; (*aggregation-273,Serial14 --> edge-286,Serial1*)
  270-205; (*core-37,Serial1 --> aggregation-100,Serial5*)
  266-61; (*edge-285,Serial7 --> aggregation-279,Serial13*)
  132-316; (*aggregation-129,Serial12 --> core-14,Serial0*)
  311-36; (*edge-186,Serial0 --> aggregation-176,Serial10*)
  68-186; (*core-5,Serial9 --> aggregation-224,Serial5*)
  192-108; (*core-56,Serial12 --> aggregation-135,Serial8*)
  1-210; (*edge-89,Serial1 --> aggregation-81,Serial9*)
  75-272; (*aggregation-272,Serial9 --> edge-281,Serial0*)
  224-227; (*aggregation-213,Serial4 --> core-44,Serial7*)
  122-195; (*aggregation-131,Serial0 --> edge-136,Serial3*)
  319-47; (*aggregation-308,Serial8 --> edge-312,Serial4*)
  298-290; (*aggregation-307,Serial3 --> core-29,Serial0*)
  73-316; (*aggregation-273,Serial6 --> core-14,Serial15*)
  303-106; (*core-11,Serial9 --> aggregation-257,Serial0*)
  286-196; (*aggregation-310,Serial6 --> core-54,Serial8*)
  36-289; (*aggregation-176,Serial13 --> edge-189,Serial0*)
  39-257; (*aggregation-180,Serial0 --> core-32,Serial8*)
  282-89; (*core-23,Serial0 --> aggregation-274,Serial7*)
  96-224; (*edge-219,Serial5 --> aggregation-213,Serial11*)
  269-211; (*core-38,Serial10 --> aggregation-84,Serial6*)
  11-31; (*aggregation-198,Serial4 --> edge-204,Serial6*)
  184-310; (*aggregation-226,Serial10 --> core-18,Serial10*)
  34-316; (*aggregation-177,Serial6 --> core-14,Serial11*)
  221-0; (*aggregation-87,Serial8 --> edge-88,Serial7*)
  12-231; (*aggregation-197,Serial13 --> core-41,Serial1*)
  139-190; (*edge-232,Serial4 --> aggregation-228,Serial8*)
  187-67; (*aggregation-103,Serial3 --> edge-107,Serial7*)
  73-264; (*aggregation-273,Serial11 --> edge-283,Serial1*)
  120-169; (*aggregation-132,Serial15 --> edge-143,Serial4*)
  252-125; (*edge-79,Serial6 --> aggregation-70,Serial7*)
  14-313; (*aggregation-182,Serial9 --> edge-185,Serial6*)
  133-197; (*aggregation-128,Serial11 --> edge-139,Serial0*)
  31-16; (*edge-204,Serial0 --> aggregation-192,Serial12*)
  194-55; (*core-55,Serial15 --> aggregation-166,Serial15*)
  276-123; (*edge-74,Serial7 --> aggregation-71,Serial2*)
  68-133; (*core-5,Serial1 --> aggregation-128,Serial5*)
  92-297; (*aggregation-147,Serial4 --> core-24,Serial13*)
  261-92; (*edge-154,Serial3 --> aggregation-147,Serial10*)
  290-22; (*core-29,Serial14 --> aggregation-179,Serial3*)
  220-234; (*aggregation-85,Serial0 --> core-40,Serial11*)
  63-266; (*aggregation-278,Serial13 --> edge-285,Serial6*)
  55-238; (*aggregation-166,Serial5 --> edge-173,Serial6*)
  183-70; (*aggregation-98,Serial5 --> edge-109,Serial2*)
  95-222; (*edge-217,Serial7 --> aggregation-215,Serial9*)
  286-243; (*aggregation-310,Serial1 --> core-49,Serial8*)
  38-230; (*aggregation-181,Serial2 --> core-42,Serial5*)
  220-229; (*aggregation-85,Serial3 --> core-43,Serial11*)
  64-166; (*core-3,Serial10 --> aggregation-112,Serial3*)
  104-174; (*aggregation-259,Serial2 --> edge-266,Serial3*)
  188-198; (*aggregation-102,Serial13 --> core-53,Serial5*)
  225-273; (*aggregation-212,Serial3 --> core-35,Serial7*)
  215-159; (*core-59,Serial9 --> aggregation-119,Serial0*)
  194-79; (*core-55,Serial4 --> aggregation-150,Serial7*)
  203-223; (*core-51,Serial1 --> aggregation-214,Serial3*)
  314-183; (*core-16,Serial9 --> aggregation-98,Serial8*)
  277-154; (*edge-75,Serial1 --> aggregation-65,Serial5*)
  228-50; (*edge-169,Serial1 --> aggregation-161,Serial3*)
  63-194; (*aggregation-278,Serial7 --> core-55,Serial10*)
  11-196; (*aggregation-198,Serial14 --> core-54,Serial0*)
  149-257; (*aggregation-68,Serial0 --> core-32,Serial0*)
  215-9; (*core-59,Serial14 --> aggregation-199,Serial11*)
  107-64; (*aggregation-256,Serial3 --> core-3,Serial4*)
  222-88; (*aggregation-215,Serial14 --> edge-222,Serial7*)
  177-93; (*edge-153,Serial2 --> aggregation-146,Serial9*)
  167-61; (*core-62,Serial1 --> aggregation-279,Serial6*)
  104-165; (*aggregation-259,Serial1 --> edge-265,Serial3*)
  222-167; (*aggregation-215,Serial6 --> core-62,Serial8*)
  205-273; (*aggregation-100,Serial3 --> core-35,Serial1*)
  196-129; (*core-54,Serial11 --> aggregation-246,Serial6*)
  203-160; (*core-51,Serial9 --> aggregation-118,Serial3*)
  54-167; (*aggregation-167,Serial14 --> core-62,Serial13*)
  230-151; (*core-42,Serial14 --> aggregation-69,Serial15*)
  283-145; (*core-22,Serial7 --> aggregation-242,Serial6*)
  258-5; (*core-31,Serial5 --> aggregation-195,Serial15*)
  58-306; (*aggregation-291,Serial1 --> edge-297,Serial3*)
  208-1; (*aggregation-82,Serial9 --> edge-89,Serial2*)
  171-98; (*edge-267,Serial5 --> aggregation-261,Serial5*)
  226-151; (*core-45,Serial14 --> aggregation-69,Serial0*)
  290-104; (*core-29,Serial7 --> aggregation-259,Serial13*)
  228-57; (*edge-169,Serial4 --> aggregation-164,Serial9*)
  5-23; (*aggregation-195,Serial7 --> edge-207,Serial3*)
  82-250; (*edge-221,Serial0 --> aggregation-208,Serial13*)
  195-120; (*edge-136,Serial4 --> aggregation-132,Serial8*)
  310-110; (*core-18,Serial3 --> aggregation-130,Serial10*)
  205-257; (*aggregation-100,Serial0 --> core-32,Serial1*)
  81-300; (*core-8,Serial1 --> aggregation-305,Serial0*)
  194-97; (*core-55,Serial12 --> aggregation-262,Serial15*)
  189-226; (*aggregation-229,Serial0 --> core-45,Serial2*)
  36-287; (*aggregation-176,Serial12 --> edge-188,Serial0*)
  7-29; (*edge-303,Serial5 --> aggregation-293,Serial9*)
  34-295; (*aggregation-177,Serial14 --> edge-190,Serial1*)
  91-270; (*aggregation-148,Serial5 --> core-37,Serial12*)
  225-271; (*aggregation-212,Serial4 --> core-36,Serial7*)
  303-40; (*core-11,Serial10 --> aggregation-289,Serial0*)
  191-134; (*aggregation-227,Serial4 --> edge-236,Serial3*)
  158-304; (*aggregation-113,Serial2 --> core-10,Serial12*)
  302-279; (*core-12,Serial13 --> aggregation-209,Serial4*)
  130-204; (*aggregation-245,Serial13 --> edge-253,Serial5*)
  155-148; (*aggregation-116,Serial8 --> edge-120,Serial4*)
  273-32; (*core-35,Serial6 --> aggregation-292,Serial3*)
  268-83; (*edge-284,Serial5 --> aggregation-277,Serial12*)
  29-4; (*aggregation-293,Serial7 --> edge-301,Serial5*)
  259-156; (*core-30,Serial12 --> aggregation-115,Serial6*)
  122-193; (*aggregation-131,Serial1 --> edge-137,Serial3*)
  236-95; (*aggregation-210,Serial9 --> edge-217,Serial2*)
  221-173; (*aggregation-87,Serial1 --> core-60,Serial5*)
  314-157; (*core-16,Serial11 --> aggregation-114,Serial0*)
  49-299; (*edge-315,Serial2 --> aggregation-306,Serial11*)
  119-155; (*edge-124,Serial4 --> aggregation-116,Serial12*)
  36-66; (*aggregation-176,Serial2 --> core-2,Serial14*)
  133-199; (*aggregation-128,Serial10 --> edge-138,Serial0*)
  198-27; (*core-53,Serial3 --> aggregation-294,Serial13*)
  18-213; (*edge-91,Serial3 --> aggregation-83,Serial11*)
  58-305; (*aggregation-291,Serial2 --> edge-298,Serial3*)
  132-214; (*aggregation-129,Serial7 --> edge-141,Serial1*)
  240-59; (*edge-296,Serial2 --> aggregation-290,Serial0*)
  29-227; (*aggregation-293,Serial11 --> core-44,Serial12*)
  317-300; (*core-13,Serial1 --> aggregation-305,Serial5*)
  64-53; (*core-3,Serial11 --> aggregation-288,Serial3*)
  59-310; (*aggregation-290,Serial10 --> core-18,Serial8*)
  158-109; (*aggregation-113,Serial14 --> edge-126,Serial1*)
  301-42; (*aggregation-304,Serial12 --> edge-316,Serial0*)
  249-153; (*edge-77,Serial2 --> aggregation-66,Serial5*)
  126-128; (*edge-252,Serial7 --> aggregation-247,Serial12*)
  143-296; (*aggregation-243,Serial7 --> core-25,Serial9*)
  149-271; (*aggregation-68,Serial4 --> core-36,Serial0*)
  141-255; (*aggregation-244,Serial1 --> core-33,Serial13*)
  197-120; (*edge-139,Serial4 --> aggregation-132,Serial11*)
  77-205; (*edge-106,Serial4 --> aggregation-100,Serial10*)
  31-6; (*edge-204,Serial2 --> aggregation-194,Serial4*)
  241-52; (*edge-175,Serial0 --> aggregation-160,Serial15*)
  167-159; (*core-62,Serial9 --> aggregation-119,Serial6*)
  47-299; (*edge-312,Serial2 --> aggregation-306,Serial8*)
  117-155; (*edge-121,Serial4 --> aggregation-116,Serial9*)
  157-314; (*aggregation-114,Serial0 --> core-16,Serial11*)
  65-187; (*edge-108,Serial7 --> aggregation-103,Serial4*)
  258-58; (*core-31,Serial11 --> aggregation-291,Serial15*)
  125-276; (*aggregation-70,Serial2 --> edge-74,Serial6*)
  37-9; (*edge-200,Serial7 --> aggregation-199,Serial0*)
  161-231; (*aggregation-117,Serial1 --> core-41,Serial15*)
  55-239; (*aggregation-166,Serial4 --> edge-172,Serial6*)
  137-175; (*edge-238,Serial6 --> aggregation-230,Serial6*)
  219-244; (*aggregation-86,Serial0 --> core-48,Serial13*)
  231-12; (*core-41,Serial1 --> aggregation-197,Serial13*)
  151-278; (*aggregation-69,Serial6 --> edge-76,Serial5*)
  187-142; (*aggregation-103,Serial6 --> edge-110,Serial7*)
  233-96; (*aggregation-211,Serial11 --> edge-219,Serial3*)
  184-309; (*aggregation-226,Serial11 --> core-19,Serial10*)
  284-299; (*core-21,Serial14 --> aggregation-306,Serial5*)
  302-101; (*core-12,Serial5 --> aggregation-145,Serial4*)
  61-217; (*aggregation-279,Serial5 --> core-58,Serial1*)
  179-97; (*edge-270,Serial6 --> aggregation-262,Serial6*)
  152-275; (*aggregation-64,Serial9 --> edge-73,Serial0*)
  135-127; (*aggregation-240,Serial11 --> edge-251,Serial0*)
  133-69; (*aggregation-128,Serial4 --> core-4,Serial1*)
  160-119; (*aggregation-118,Serial12 --> edge-124,Serial6*)
  318-51; (*aggregation-309,Serial10 --> edge-314,Serial5*)
  147-304; (*aggregation-241,Serial2 --> core-10,Serial14*)
  299-310; (*aggregation-306,Serial2 --> core-18,Serial14*)
  81-158; (*core-8,Serial12 --> aggregation-113,Serial0*)
  203-125; (*core-51,Serial7 --> aggregation-70,Serial11*)
  270-91; (*core-37,Serial12 --> aggregation-148,Serial5*)
  89-282; (*aggregation-274,Serial7 --> core-23,Serial0*)
  90-178; (*aggregation-149,Serial8 --> edge-152,Serial5*)
  221-170; (*aggregation-87,Serial3 --> core-61,Serial5*)
  98-227; (*aggregation-261,Serial11 --> core-44,Serial3*)
  211-20; (*aggregation-84,Serial13 --> edge-93,Serial4*)
  127-130; (*edge-251,Serial5 --> aggregation-245,Serial11*)
  13-287; (*aggregation-183,Serial12 --> edge-188,Serial7*)
  164-103; (*core-63,Serial0 --> aggregation-263,Serial15*)
  103-176; (*aggregation-263,Serial7 --> edge-271,Serial7*)
  143-112; (*aggregation-243,Serial9 --> edge-249,Serial3*)
  173-123; (*core-60,Serial11 --> aggregation-71,Serial12*)
  172-164; (*aggregation-231,Serial15 --> core-63,Serial15*)
  161-115; (*aggregation-117,Serial10 --> edge-122,Serial5*)
  87-291; (*aggregation-275,Serial2 --> core-28,Serial8*)
  5-259; (*aggregation-195,Serial14 --> core-30,Serial5*)
  209-108; (*edge-140,Serial7 --> aggregation-135,Serial4*)
  256-79; (*edge-158,Serial6 --> aggregation-150,Serial14*)
  120-273; (*aggregation-132,Serial3 --> core-35,Serial3*)
  253-3; (*core-34,Serial11 --> aggregation-196,Serial2*)
  223-206; (*aggregation-214,Serial2 --> core-50,Serial1*)
  173-61; (*core-60,Serial1 --> aggregation-279,Serial1*)
  287-39; (*edge-188,Serial4 --> aggregation-180,Serial12*)
  62-102; (*core-0,Serial6 --> aggregation-144,Serial0*)
  2-32; (*edge-302,Serial4 --> aggregation-292,Serial14*)
  93-256; (*aggregation-146,Serial14 --> edge-158,Serial2*)
  309-183; (*core-19,Serial9 --> aggregation-98,Serial11*)
  33-309; (*aggregation-178,Serial3 --> core-19,Serial13*)
  253-190; (*core-34,Serial4 --> aggregation-228,Serial2*)
  34-304; (*aggregation-177,Serial2 --> core-10,Serial11*)
  106-168; (*aggregation-257,Serial2 --> edge-264,Serial1*)
  41-286; (*edge-317,Serial6 --> aggregation-310,Serial13*)
  64-52; (*core-3,Serial7 --> aggregation-160,Serial3*)
  73-232; (*aggregation-273,Serial15 --> edge-287,Serial1*)
  300-317; (*aggregation-305,Serial5 --> core-13,Serial1*)
  316-185; (*core-14,Serial8 --> aggregation-225,Serial12*)
  135-207; (*aggregation-240,Serial15 --> edge-255,Serial0*)
  238-57; (*edge-173,Serial4 --> aggregation-164,Serial13*)
  318-44; (*aggregation-309,Serial14 --> edge-318,Serial5*)
  86-233; (*edge-223,Serial3 --> aggregation-211,Serial15*)
  292-298; (*core-27,Serial0 --> aggregation-307,Serial0*)
  202-80; (*aggregation-101,Serial3 --> edge-105,Serial5*)
  133-212; (*aggregation-128,Serial14 --> edge-142,Serial0*)
  135-64; (*aggregation-240,Serial3 --> core-3,Serial2*)
  105-310; (*aggregation-258,Serial10 --> core-18,Serial12*)
  314-89; (*core-16,Serial0 --> aggregation-274,Serial0*)
  240-27; (*edge-296,Serial6 --> aggregation-294,Serial0*)
  220-17; (*aggregation-85,Serial15 --> edge-95,Serial5*)
  69-152; (*core-4,Serial0 --> aggregation-64,Serial4*)
  238-46; (*edge-173,Serial3 --> aggregation-163,Serial5*)
  185-144; (*aggregation-225,Serial4 --> edge-234,Serial1*)
  170-281; (*core-61,Serial10 --> aggregation-311,Serial3*)
  127-147; (*edge-251,Serial1 --> aggregation-241,Serial11*)
  209-122; (*edge-140,Serial3 --> aggregation-131,Serial4*)
  82-233; (*edge-221,Serial3 --> aggregation-211,Serial13*)
  170-13; (*core-61,Serial7 --> aggregation-183,Serial3*)
  291-213; (*core-28,Serial1 --> aggregation-83,Serial2*)
  268-73; (*edge-284,Serial1 --> aggregation-273,Serial12*)
  76-192; (*aggregation-151,Serial4 --> core-56,Serial6*)
  250-86; (*aggregation-208,Serial15 --> edge-223,Serial0*)
  290-191; (*core-29,Serial6 --> aggregation-227,Serial13*)
  101-317; (*aggregation-145,Serial5 --> core-13,Serial5*)
  285-183; (*core-20,Serial9 --> aggregation-98,Serial12*)
  2-58; (*edge-302,Serial3 --> aggregation-291,Serial6*)
  153-278; (*aggregation-66,Serial4 --> edge-76,Serial2*)
  255-120; (*core-33,Serial3 --> aggregation-132,Serial1*)
  25-7; (*aggregation-295,Serial7 --> edge-303,Serial7*)
  307-25; (*edge-299,Serial7 --> aggregation-295,Serial3*)
  297-298; (*core-24,Serial0 --> aggregation-307,Serial4*)
  269-120; (*core-38,Serial3 --> aggregation-132,Serial6*)
  51-301; (*edge-314,Serial0 --> aggregation-304,Serial10*)
  121-157; (*edge-123,Serial2 --> aggregation-114,Serial11*)
  202-71; (*aggregation-101,Serial2 --> edge-104,Serial5*)
  107-66; (*aggregation-256,Serial2 --> core-2,Serial4*)
  76-167; (*aggregation-151,Serial6 --> core-62,Serial6*)
  30-3; (*edge-205,Serial4 --> aggregation-196,Serial13*)
  91-256; (*aggregation-148,Serial14 --> edge-158,Serial4*)
  188-194; (*aggregation-102,Serial15 --> core-55,Serial5*)
  285-157; (*core-20,Serial11 --> aggregation-114,Serial4*)
  225-99; (*aggregation-212,Serial8 --> edge-216,Serial4*)
  11-200; (*aggregation-198,Serial12 --> core-52,Serial0*)
  61-215; (*aggregation-279,Serial0 --> core-59,Serial1*)
  91-260; (*aggregation-148,Serial11 --> edge-155,Serial4*)
  4-40; (*edge-301,Serial1 --> aggregation-289,Serial7*)
  211-18; (*aggregation-84,Serial11 --> edge-91,Serial4*)
  13-289; (*aggregation-183,Serial13 --> edge-189,Serial7*)
  205-253; (*aggregation-100,Serial2 --> core-34,Serial1*)
  258-156; (*core-31,Serial12 --> aggregation-115,Serial1*)
  65-188; (*edge-108,Serial6 --> aggregation-102,Serial4*)
  82-223; (*edge-221,Serial6 --> aggregation-214,Serial13*)
  80-183; (*edge-105,Serial2 --> aggregation-98,Serial1*)
  52-242; (*aggregation-160,Serial14 --> edge-174,Serial0*)
  25-10; (*aggregation-295,Serial4 --> edge-300,Serial7*)
  157-285; (*aggregation-114,Serial4 --> core-20,Serial11*)
  187-215; (*aggregation-103,Serial11 --> core-59,Serial4*)
  229-220; (*core-43,Serial11 --> aggregation-85,Serial3*)
  134-184; (*edge-236,Serial2 --> aggregation-226,Serial4*)
  234-38; (*core-40,Serial5 --> aggregation-181,Serial0*)
  281-170; (*aggregation-311,Serial3 --> core-61,Serial10*)
  71-180; (*edge-104,Serial1 --> aggregation-97,Serial2*)
  250-95; (*aggregation-208,Serial9 --> edge-217,Serial0*)
  293-34; (*edge-191,Serial1 --> aggregation-177,Serial15*)
  5-26; (*aggregation-195,Serial3 --> edge-203,Serial3*)
  162-105; (*edge-269,Serial2 --> aggregation-258,Serial5*)
  237-46; (*edge-168,Serial3 --> aggregation-163,Serial0*)
  34-317; (*aggregation-177,Serial5 --> core-13,Serial11*)
  254-101; (*edge-159,Serial1 --> aggregation-145,Serial15*)
  198-219; (*core-53,Serial13 --> aggregation-86,Serial5*)
  294-298; (*core-26,Serial0 --> aggregation-307,Serial5*)
  133-62; (*aggregation-128,Serial0 --> core-0,Serial1*)
  163-106; (*edge-268,Serial1 --> aggregation-257,Serial6*)
  200-11; (*core-52,Serial0 --> aggregation-198,Serial12*)
  254-91; (*edge-159,Serial4 --> aggregation-148,Serial15*)
  151-277; (*aggregation-69,Serial5 --> edge-75,Serial5*)
  191-137; (*aggregation-227,Serial6 --> edge-238,Serial3*)
  290-150; (*core-29,Serial10 --> aggregation-67,Serial13*)
  39-253; (*aggregation-180,Serial2 --> core-34,Serial8*)
  283-48; (*core-22,Serial4 --> aggregation-162,Serial14*)
  173-76; (*core-60,Serial6 --> aggregation-151,Serial1*)
  240-32; (*edge-296,Serial4 --> aggregation-292,Serial8*)
  36-62; (*aggregation-176,Serial0 --> core-0,Serial14*)
  253-85; (*core-34,Serial14 --> aggregation-276,Serial2*)
  61-232; (*aggregation-279,Serial15 --> edge-287,Serial7*)
  161-234; (*aggregation-117,Serial0 --> core-40,Serial15*)
  292-122; (*core-27,Serial3 --> aggregation-131,Serial11*)
  129-207; (*aggregation-246,Serial15 --> edge-255,Serial6*)
  286-194; (*aggregation-310,Serial7 --> core-55,Serial8*)
  290-143; (*core-29,Serial9 --> aggregation-243,Serial3*)
  74-53; (*core-6,Serial11 --> aggregation-288,Serial6*)
  269-149; (*core-38,Serial0 --> aggregation-68,Serial6*)
  132-302; (*aggregation-129,Serial1 --> core-12,Serial0*)
  236-82; (*aggregation-210,Serial13 --> edge-221,Serial2*)
  14-293; (*aggregation-182,Serial15 --> edge-191,Serial6*)
  62-135; (*core-0,Serial2 --> aggregation-240,Serial0*)
  181-60; (*aggregation-96,Serial1 --> core-1,Serial5*)
  200-175; (*core-52,Serial6 --> aggregation-230,Serial12*)
  10-53; (*edge-300,Serial0 --> aggregation-288,Serial12*)
  40-2; (*aggregation-289,Serial8 --> edge-302,Serial1*)
  227-83; (*core-44,Serial6 --> aggregation-277,Serial4*)
  263-93; (*edge-156,Serial2 --> aggregation-146,Serial12*)
  118-245; (*aggregation-133,Serial14 --> core-47,Serial4*)
  148-158; (*edge-120,Serial1 --> aggregation-113,Serial8*)
  310-153; (*core-18,Serial1 --> aggregation-66,Serial10*)
  118-227; (*aggregation-133,Serial11 --> core-44,Serial4*)
  267-3; (*core-39,Serial11 --> aggregation-196,Serial7*)
  182-291; (*aggregation-99,Serial12 --> core-28,Serial2*)
  272-85; (*edge-281,Serial4 --> aggregation-276,Serial9*)
  85-270; (*aggregation-276,Serial5 --> core-37,Serial14*)
  194-27; (*core-55,Serial3 --> aggregation-294,Serial15*)
  252-123; (*edge-79,Serial7 --> aggregation-71,Serial7*)
  272-73; (*edge-281,Serial1 --> aggregation-273,Serial9*)
  67-202; (*edge-107,Serial5 --> aggregation-101,Serial5*)
  98-229; (*aggregation-261,Serial1 --> core-43,Serial3*)
  158-78; (*aggregation-113,Serial1 --> core-9,Serial12*)
  198-14; (*core-53,Serial2 --> aggregation-182,Serial5*)
  52-74; (*aggregation-160,Serial6 --> core-6,Serial7*)
  224-247; (*aggregation-213,Serial6 --> core-46,Serial7*)
  138-190; (*edge-237,Serial4 --> aggregation-228,Serial13*)
  230-161; (*core-42,Serial15 --> aggregation-117,Serial2*)
  239-56; (*edge-172,Serial5 --> aggregation-165,Serial6*)
  91-273; (*aggregation-148,Serial3 --> core-35,Serial12*)
  9-217; (*aggregation-199,Serial10 --> core-58,Serial14*)
  273-100; (*core-35,Serial15 --> aggregation-260,Serial3*)
  58-290; (*aggregation-291,Serial13 --> core-29,Serial11*)
  30-11; (*edge-205,Serial6 --> aggregation-198,Serial5*)
  128-192; (*aggregation-247,Serial4 --> core-56,Serial2*)
  203-129; (*core-51,Serial11 --> aggregation-246,Serial3*)
  46-242; (*aggregation-163,Serial6 --> edge-174,Serial3*)
  5-24; (*aggregation-195,Serial6 --> edge-206,Serial3*)
  125-206; (*aggregation-70,Serial10 --> core-50,Serial7*)
  172-144; (*aggregation-231,Serial2 --> edge-234,Serial7*)
  222-84; (*aggregation-215,Serial12 --> edge-220,Serial7*)
  172-138; (*aggregation-231,Serial5 --> edge-237,Serial7*)
  75-64; (*aggregation-272,Serial3 --> core-3,Serial3*)
  75-74; (*aggregation-272,Serial6 --> core-6,Serial3*)
  81-73; (*core-8,Serial15 --> aggregation-273,Serial0*)
  315-279; (*core-15,Serial13 --> aggregation-209,Serial7*)
  18-210; (*edge-91,Serial1 --> aggregation-81,Serial11*)
  198-160; (*core-53,Serial9 --> aggregation-118,Serial5*)
  198-188; (*core-53,Serial5 --> aggregation-102,Serial13*)
  38-231; (*aggregation-181,Serial1 --> core-41,Serial5*)
  278-151; (*edge-76,Serial5 --> aggregation-69,Serial6*)
  261-90; (*edge-154,Serial5 --> aggregation-149,Serial10*)
  100-176; (*aggregation-260,Serial15 --> edge-271,Serial4*)
  157-282; (*aggregation-114,Serial7 --> core-23,Serial11*)
  132-315; (*aggregation-129,Serial14 --> core-15,Serial0*)
  54-170; (*aggregation-167,Serial13 --> core-61,Serial13*)
  168-104; (*edge-264,Serial3 --> aggregation-259,Serial0*)
  312-299; (*core-17,Serial14 --> aggregation-306,Serial1*)
  316-132; (*core-14,Serial0 --> aggregation-129,Serial12*)
  21-213; (*edge-94,Serial3 --> aggregation-83,Serial14*)
  11-30; (*aggregation-198,Serial5 --> edge-205,Serial6*)
  315-180; (*core-15,Serial2 --> aggregation-97,Serial14*)
  177-101; (*edge-153,Serial1 --> aggregation-145,Serial9*)
  285-33; (*core-20,Serial13 --> aggregation-178,Serial4*)
  255-155; (*core-33,Serial5 --> aggregation-116,Serial1*)
  226-224; (*core-45,Serial7 --> aggregation-213,Serial5*)
  248-48; (*edge-170,Serial2 --> aggregation-162,Serial2*)
  312-93; (*core-17,Serial15 --> aggregation-146,Serial1*)
  32-7; (*aggregation-292,Serial15 --> edge-303,Serial4*)
  225-267; (*aggregation-212,Serial7 --> core-39,Serial7*)
  27-240; (*aggregation-294,Serial0 --> edge-296,Serial6*)
  27-307; (*aggregation-294,Serial3 --> edge-299,Serial6*)
  63-244; (*aggregation-278,Serial0 --> core-48,Serial10*)
  250-88; (*aggregation-208,Serial14 --> edge-222,Serial0*)
  130-124; (*aggregation-245,Serial10 --> edge-250,Serial5*)
  269-39; (*core-38,Serial8 --> aggregation-180,Serial6*)
  149-251; (*aggregation-68,Serial14 --> edge-78,Serial4*)
  244-188; (*core-48,Serial5 --> aggregation-102,Serial8*)
  99-233; (*edge-216,Serial3 --> aggregation-211,Serial8*)
  268-89; (*edge-284,Serial2 --> aggregation-274,Serial12*)
  6-285; (*aggregation-194,Serial12 --> core-20,Serial5*)
  122-290; (*aggregation-131,Serial13 --> core-29,Serial3*)
  310-6; (*core-18,Serial5 --> aggregation-194,Serial10*)
  145-284; (*aggregation-242,Serial5 --> core-21,Serial7*)
  185-315; (*aggregation-225,Serial14 --> core-15,Serial8*)
  238-55; (*edge-173,Serial6 --> aggregation-166,Serial5*)
  159-218; (*aggregation-119,Serial7 --> core-57,Serial9*)
  127-141; (*edge-251,Serial4 --> aggregation-244,Serial11*)
  112-147; (*edge-249,Serial1 --> aggregation-241,Serial9*)
  55-241; (*aggregation-166,Serial7 --> edge-175,Serial6*)
  304-34; (*core-10,Serial11 --> aggregation-177,Serial2*)
  66-133; (*core-2,Serial1 --> aggregation-128,Serial2*)
  205-67; (*aggregation-100,Serial11 --> edge-107,Serial4*)
  184-146; (*aggregation-226,Serial1 --> edge-233,Serial2*)
  26-8; (*edge-203,Serial1 --> aggregation-193,Serial5*)
  2-27; (*edge-302,Serial6 --> aggregation-294,Serial6*)
  197-133; (*edge-139,Serial0 --> aggregation-128,Serial11*)
  232-63; (*edge-287,Serial6 --> aggregation-278,Serial15*)
  46-246; (*aggregation-163,Serial3 --> edge-171,Serial3*)
  301-43; (*aggregation-304,Serial15 --> edge-319,Serial0*)
  149-255; (*aggregation-68,Serial1 --> core-33,Serial0*)
  144-186; (*edge-234,Serial0 --> aggregation-224,Serial10*)
  243-125; (*core-49,Serial7 --> aggregation-70,Serial9*)
  109-158; (*edge-126,Serial1 --> aggregation-113,Serial14*)
  73-268; (*aggregation-273,Serial12 --> edge-284,Serial1*)
  64-216; (*core-3,Serial12 --> aggregation-80,Serial3*)
  171-107; (*edge-267,Serial0 --> aggregation-256,Serial11*)
  202-142; (*aggregation-101,Serial8 --> edge-110,Serial5*)
  217-221; (*core-58,Serial5 --> aggregation-87,Serial5*)
  257-57; (*core-32,Serial2 --> aggregation-164,Serial0*)
  173-108; (*core-60,Serial12 --> aggregation-135,Serial12*)
  206-63; (*core-50,Serial10 --> aggregation-278,Serial2*)
  292-5; (*core-27,Serial5 --> aggregation-195,Serial11*)
  120-214; (*aggregation-132,Serial13 --> edge-141,Serial4*)
  147-303; (*aggregation-241,Serial3 --> core-11,Serial14*)
  135-126; (*aggregation-240,Serial12 --> edge-252,Serial0*)
  19-219; (*edge-92,Serial6 --> aggregation-86,Serial12*)
  125-249; (*aggregation-70,Serial5 --> edge-77,Serial6*)
  187-77; (*aggregation-103,Serial2 --> edge-106,Serial7*)
  186-74; (*aggregation-224,Serial6 --> core-6,Serial9*)
  32-240; (*aggregation-292,Serial8 --> edge-296,Serial4*)
  192-103; (*core-56,Serial0 --> aggregation-263,Serial8*)
  23-5; (*edge-207,Serial3 --> aggregation-195,Serial7*)
  211-269; (*aggregation-84,Serial6 --> core-38,Serial10*)
  8-31; (*aggregation-193,Serial6 --> edge-204,Serial1*)
  249-150; (*edge-77,Serial3 --> aggregation-67,Serial5*)
  149-273; (*aggregation-68,Serial3 --> core-35,Serial0*)
  159-117; (*aggregation-119,Serial9 --> edge-121,Serial7*)
  181-69; (*aggregation-96,Serial4 --> core-4,Serial5*)
  135-201; (*aggregation-240,Serial14 --> edge-254,Serial0*)
  208-21; (*aggregation-82,Serial14 --> edge-94,Serial2*)
  160-114; (*aggregation-118,Serial15 --> edge-127,Serial6*)
  318-41; (*aggregation-309,Serial13 --> edge-317,Serial5*)
  135-66; (*aggregation-240,Serial2 --> core-2,Serial2*)
  174-100; (*edge-266,Serial4 --> aggregation-260,Serial10*)
  172-217; (*aggregation-231,Serial10 --> core-58,Serial15*)
  234-130; (*core-40,Serial8 --> aggregation-245,Serial0*)
  291-87; (*core-28,Serial8 --> aggregation-275,Serial2*)
  96-225; (*edge-219,Serial4 --> aggregation-212,Serial11*)
  226-98; (*core-45,Serial3 --> aggregation-261,Serial0*)
  267-141; (*core-39,Serial13 --> aggregation-244,Serial7*)
  277-149; (*edge-75,Serial4 --> aggregation-68,Serial11*)
  85-235; (*aggregation-276,Serial14 --> edge-286,Serial4*)
  120-257; (*aggregation-132,Serial0 --> core-32,Serial3*)
  285-59; (*core-20,Serial8 --> aggregation-290,Serial12*)
  159-173; (*aggregation-119,Serial1 --> core-60,Serial9*)
  199-110; (*edge-138,Serial2 --> aggregation-130,Serial2*)
  22-296; (*aggregation-179,Serial7 --> core-25,Serial14*)
  150-297; (*aggregation-67,Serial8 --> core-24,Serial10*)
  130-113; (*aggregation-245,Serial8 --> edge-248,Serial5*)
  185-136; (*aggregation-225,Serial5 --> edge-235,Serial1*)
  60-301; (*core-1,Serial13 --> aggregation-304,Serial1*)
  127-145; (*edge-251,Serial2 --> aggregation-242,Serial11*)
  107-174; (*aggregation-256,Serial10 --> edge-266,Serial0*)
  51-281; (*edge-314,Serial7 --> aggregation-311,Serial10*)
  297-58; (*core-24,Serial11 --> aggregation-291,Serial8*)
  79-262; (*aggregation-150,Serial13 --> edge-157,Serial6*)
  273-319; (*core-35,Serial9 --> aggregation-308,Serial3*)
  136-185; (*edge-235,Serial1 --> aggregation-225,Serial5*)
  230-90; (*core-42,Serial10 --> aggregation-149,Serial2*)
  73-315; (*aggregation-273,Serial7 --> core-15,Serial15*)
  142-181; (*edge-110,Serial0 --> aggregation-96,Serial14*)
  209-116; (*edge-140,Serial6 --> aggregation-134,Serial4*)
  268-85; (*edge-284,Serial4 --> aggregation-276,Serial12*)
  142-182; (*edge-110,Serial3 --> aggregation-99,Serial6*)
  250-84; (*aggregation-208,Serial12 --> edge-220,Serial0*)
  153-252; (*aggregation-66,Serial7 --> edge-79,Serial2*)
  54-192; (*aggregation-167,Serial8 --> core-56,Serial13*)
  201-129; (*edge-254,Serial6 --> aggregation-246,Serial14*)
  39-288; (*aggregation-180,Serial11 --> edge-187,Serial4*)
  141-271; (*aggregation-244,Serial4 --> core-36,Serial13*)
  202-229; (*aggregation-101,Serial1 --> core-43,Serial13*)
  137-189; (*edge-238,Serial5 --> aggregation-229,Serial8*)
  105-309; (*aggregation-258,Serial11 --> core-19,Serial12*)
  298-296; (*aggregation-307,Serial7 --> core-25,Serial0*)
  24-12; (*edge-206,Serial5 --> aggregation-197,Serial8*)
  160-111; (*aggregation-118,Serial13 --> edge-125,Serial6*)
  318-49; (*aggregation-309,Serial11 --> edge-315,Serial5*)
  133-64; (*aggregation-128,Serial3 --> core-3,Serial1*)
  281-43; (*aggregation-311,Serial15 --> edge-319,Serial7*)
  311-13; (*edge-186,Serial7 --> aggregation-183,Serial10*)
  156-121; (*aggregation-115,Serial11 --> edge-123,Serial3*)
  68-75; (*core-5,Serial3 --> aggregation-272,Serial5*)
  232-85; (*edge-287,Serial4 --> aggregation-276,Serial15*)
  300-302; (*aggregation-305,Serial4 --> core-12,Serial1*)
  258-46; (*core-31,Serial4 --> aggregation-163,Serial15*)
  62-186; (*core-0,Serial9 --> aggregation-224,Serial0*)
  170-172; (*core-61,Serial15 --> aggregation-231,Serial13*)
  218-187; (*core-57,Serial4 --> aggregation-103,Serial9*)
  259-22; (*core-30,Serial14 --> aggregation-179,Serial6*)
  300-45; (*aggregation-305,Serial9 --> edge-313,Serial1*)
  34-313; (*aggregation-177,Serial9 --> edge-185,Serial1*)
  146-172; (*edge-233,Serial7 --> aggregation-231,Serial1*)
  270-57; (*core-37,Serial2 --> aggregation-164,Serial5*)
  102-177; (*aggregation-144,Serial9 --> edge-153,Serial0*)
  174-103; (*edge-266,Serial7 --> aggregation-263,Serial2*)
  212-132; (*edge-142,Serial1 --> aggregation-129,Serial8*)
  39-287; (*aggregation-180,Serial12 --> edge-188,Serial4*)
  47-298; (*edge-312,Serial3 --> aggregation-307,Serial8*)
  117-161; (*edge-121,Serial5 --> aggregation-117,Serial9*)
  11-243; (*aggregation-198,Serial9 --> core-49,Serial0*)
  143-201; (*aggregation-243,Serial14 --> edge-254,Serial3*)
  65-182; (*edge-108,Serial3 --> aggregation-99,Serial4*)
  46-238; (*aggregation-163,Serial5 --> edge-173,Serial3*)
  141-253; (*aggregation-244,Serial2 --> core-34,Serial13*)
  200-55; (*core-52,Serial15 --> aggregation-166,Serial12*)
  206-97; (*core-50,Serial12 --> aggregation-262,Serial10*)
  211-0; (*aggregation-84,Serial8 --> edge-88,Serial4*)
  240-58; (*edge-296,Serial3 --> aggregation-291,Serial0*)
  62-250; (*core-0,Serial15 --> aggregation-208,Serial0*)
  16-23; (*aggregation-192,Serial15 --> edge-207,Serial0*)
  281-41; (*aggregation-311,Serial13 --> edge-317,Serial7*)
  164-54; (*core-63,Serial13 --> aggregation-167,Serial15*)
  92-258; (*aggregation-147,Serial1 --> core-31,Serial13*)
  162-100; (*edge-269,Serial4 --> aggregation-260,Serial13*)
  52-72; (*aggregation-160,Serial7 --> core-7,Serial7*)
  215-221; (*core-59,Serial5 --> aggregation-87,Serial0*)
  187-71; (*aggregation-103,Serial0 --> edge-104,Serial7*)
  272-87; (*edge-281,Serial3 --> aggregation-275,Serial9*)
  221-19; (*aggregation-87,Serial12 --> edge-92,Serial7*)
  107-163; (*aggregation-256,Serial12 --> edge-268,Serial0*)
  29-10; (*aggregation-293,Serial6 --> edge-300,Serial5*)
  36-60; (*aggregation-176,Serial1 --> core-1,Serial14*)
  186-68; (*aggregation-224,Serial5 --> core-5,Serial9*)
  284-184; (*core-21,Serial10 --> aggregation-226,Serial13*)
  292-87; (*core-27,Serial8 --> aggregation-275,Serial0*)
  176-104; (*edge-271,Serial3 --> aggregation-259,Serial7*)
  166-60; (*aggregation-112,Serial1 --> core-1,Serial10*)
  302-154; (*core-12,Serial4 --> aggregation-65,Serial1*)
  135-62; (*aggregation-240,Serial0 --> core-0,Serial2*)
  39-273; (*aggregation-180,Serial3 --> core-35,Serial8*)
  78-154; (*core-9,Serial4 --> aggregation-65,Serial13*)
  131-191; (*edge-239,Serial3 --> aggregation-227,Serial7*)
  225-269; (*aggregation-212,Serial6 --> core-38,Serial7*)
  243-160; (*core-49,Serial9 --> aggregation-118,Serial1*)
  125-277; (*aggregation-70,Serial3 --> edge-75,Serial6*)
  41-300; (*edge-317,Serial1 --> aggregation-305,Serial13*)
  109-156; (*edge-126,Serial3 --> aggregation-115,Serial14*)
  254-92; (*edge-159,Serial3 --> aggregation-147,Serial15*)
  205-70; (*aggregation-100,Serial13 --> edge-109,Serial4*)
  181-66; (*aggregation-96,Serial2 --> core-2,Serial5*)
  213-297; (*aggregation-83,Serial4 --> core-24,Serial1*)
  269-32; (*core-38,Serial6 --> aggregation-292,Serial6*)
  301-44; (*aggregation-304,Serial14 --> edge-318,Serial0*)
  147-302; (*aggregation-241,Serial4 --> core-12,Serial14*)
  177-76; (*edge-153,Serial7 --> aggregation-151,Serial9*)
  191-138; (*aggregation-227,Serial5 --> edge-237,Serial3*)
  263-102; (*edge-156,Serial0 --> aggregation-144,Serial12*)
  202-245; (*aggregation-101,Serial14 --> core-47,Serial13*)
  292-182; (*core-27,Serial2 --> aggregation-99,Serial11*)
  289-36; (*edge-189,Serial0 --> aggregation-176,Serial13*)
  211-270; (*aggregation-84,Serial5 --> core-37,Serial10*)
  49-319; (*edge-315,Serial4 --> aggregation-308,Serial11*)
  119-160; (*edge-124,Serial6 --> aggregation-118,Serial12*)
  157-283; (*aggregation-114,Serial6 --> core-22,Serial11*)
  234-98; (*core-40,Serial3 --> aggregation-261,Serial10*)
  282-33; (*core-23,Serial13 --> aggregation-178,Serial7*)
  243-14; (*core-49,Serial2 --> aggregation-182,Serial1*)
  285-208; (*core-20,Serial2 --> aggregation-82,Serial4*)
  104-176; (*aggregation-259,Serial7 --> edge-271,Serial3*)
  306-53; (*edge-297,Serial0 --> aggregation-288,Serial9*)
  202-140; (*aggregation-101,Serial9 --> edge-111,Serial5*)
  112-143; (*edge-249,Serial3 --> aggregation-243,Serial9*)
  63-265; (*aggregation-278,Serial10 --> edge-282,Serial6*)
  181-140; (*aggregation-96,Serial15 --> edge-111,Serial0*)
  200-116; (*core-52,Serial14 --> aggregation-134,Serial12*)
  59-309; (*aggregation-290,Serial11 --> core-19,Serial8*)
  315-106; (*core-15,Serial9 --> aggregation-257,Serial14*)
  8-26; (*aggregation-193,Serial5 --> edge-203,Serial1*)
  54-173; (*aggregation-167,Serial12 --> core-60,Serial13*)
  110-214; (*aggregation-130,Serial5 --> edge-141,Serial2*)
  192-221; (*core-56,Serial5 --> aggregation-87,Serial4*)
  130-127; (*aggregation-245,Serial11 --> edge-251,Serial5*)
  11-23; (*aggregation-198,Serial7 --> edge-207,Serial6*)
  278-125; (*edge-76,Serial6 --> aggregation-70,Serial4*)
  5-30; (*aggregation-195,Serial5 --> edge-205,Serial3*)
  257-205; (*core-32,Serial1 --> aggregation-100,Serial0*)
  222-192; (*aggregation-215,Serial4 --> core-56,Serial8*)
  297-46; (*core-24,Serial4 --> aggregation-163,Serial8*)
  21-208; (*edge-94,Serial2 --> aggregation-82,Serial14*)
  92-291; (*aggregation-147,Serial2 --> core-28,Serial13*)
  205-255; (*aggregation-100,Serial1 --> core-33,Serial1*)
  255-211; (*core-33,Serial10 --> aggregation-84,Serial1*)
  224-245; (*aggregation-213,Serial7 --> core-47,Serial7*)
  294-104; (*core-26,Serial7 --> aggregation-259,Serial10*)
  74-16; (*core-6,Serial8 --> aggregation-192,Serial6*)
  214-133; (*edge-141,Serial0 --> aggregation-128,Serial13*)
  286-244; (*aggregation-310,Serial0 --> core-48,Serial8*)
  27-306; (*aggregation-294,Serial1 --> edge-297,Serial6*)
  5-296; (*aggregation-195,Serial9 --> core-25,Serial5*)
  40-4; (*aggregation-289,Serial7 --> edge-301,Serial1*)
  220-230; (*aggregation-85,Serial2 --> core-42,Serial11*)
  49-318; (*edge-315,Serial5 --> aggregation-309,Serial11*)
  119-159; (*edge-124,Serial7 --> aggregation-119,Serial12*)
  196-223; (*core-54,Serial1 --> aggregation-214,Serial6*)
  62-166; (*core-0,Serial10 --> aggregation-112,Serial0*)
  11-203; (*aggregation-198,Serial11 --> core-51,Serial0*)
  200-79; (*core-52,Serial4 --> aggregation-150,Serial4*)
  246-50; (*edge-171,Serial1 --> aggregation-161,Serial5*)
  38-234; (*aggregation-181,Serial0 --> core-40,Serial5*)
  30-9; (*edge-205,Serial7 --> aggregation-199,Serial5*)
  172-134; (*aggregation-231,Serial4 --> edge-236,Serial7*)
  29-245; (*aggregation-293,Serial14 --> core-47,Serial12*)
  167-13; (*core-62,Serial7 --> aggregation-183,Serial6*)
  75-68; (*aggregation-272,Serial5 --> core-5,Serial3*)
  261-91; (*edge-154,Serial4 --> aggregation-148,Serial10*)
  167-281; (*core-62,Serial10 --> aggregation-311,Serial6*)
  85-267; (*aggregation-276,Serial7 --> core-39,Serial14*)
  206-27; (*core-50,Serial3 --> aggregation-294,Serial10*)
  83-274; (*aggregation-277,Serial8 --> edge-280,Serial5*)
  34-308; (*aggregation-177,Serial8 --> edge-184,Serial1*)
  166-66; (*aggregation-112,Serial2 --> core-2,Serial10*)
  297-87; (*core-24,Serial8 --> aggregation-275,Serial4*)
  201-130; (*edge-254,Serial5 --> aggregation-245,Serial14*)
  315-185; (*core-15,Serial8 --> aggregation-225,Serial14*)
  101-315; (*aggregation-145,Serial7 --> core-15,Serial5*)
  59-7; (*aggregation-290,Serial7 --> edge-303,Serial2*)
  5-31; (*aggregation-195,Serial4 --> edge-204,Serial3*)
  153-251; (*aggregation-66,Serial6 --> edge-78,Serial2*)
  34-302; (*aggregation-177,Serial4 --> core-12,Serial11*)
  63-264; (*aggregation-278,Serial11 --> edge-283,Serial6*)
  236-86; (*aggregation-210,Serial15 --> edge-223,Serial2*)
  222-82; (*aggregation-215,Serial13 --> edge-221,Serial7*)
  208-17; (*aggregation-82,Serial15 --> edge-95,Serial2*)
  157-284; (*aggregation-114,Serial5 --> core-21,Serial11*)
  309-89; (*core-19,Serial0 --> aggregation-274,Serial3*)
  221-20; (*aggregation-87,Serial13 --> edge-93,Serial7*)
  316-147; (*core-14,Serial14 --> aggregation-241,Serial6*)
  106-179; (*aggregation-257,Serial8 --> edge-270,Serial1*)
  185-134; (*aggregation-225,Serial6 --> edge-236,Serial1*)
  135-60; (*aggregation-240,Serial1 --> core-1,Serial2*)
  170-61; (*core-61,Serial1 --> aggregation-279,Serial3*)
  107-171; (*aggregation-256,Serial11 --> edge-267,Serial0*)
  176-105; (*edge-271,Serial2 --> aggregation-258,Serial7*)
  253-91; (*core-34,Serial12 --> aggregation-148,Serial2*)
  206-188; (*core-50,Serial5 --> aggregation-102,Serial10*)
  133-66; (*aggregation-128,Serial2 --> core-2,Serial1*)
  315-101; (*core-15,Serial5 --> aggregation-145,Serial7*)
  294-87; (*core-26,Serial8 --> aggregation-275,Serial5*)
  269-100; (*core-38,Serial15 --> aggregation-260,Serial6*)
  272-89; (*edge-281,Serial2 --> aggregation-274,Serial9*)
  195-108; (*edge-136,Serial7 --> aggregation-135,Serial0*)
  140-180; (*edge-111,Serial1 --> aggregation-97,Serial9*)
  228-54; (*edge-169,Serial7 --> aggregation-167,Serial1*)
  276-153; (*edge-74,Serial2 --> aggregation-66,Serial2*)
  295-39; (*edge-190,Serial4 --> aggregation-180,Serial14*)
  265-61; (*edge-282,Serial7 --> aggregation-279,Serial10*)
  96-233; (*edge-219,Serial3 --> aggregation-211,Serial11*)
  181-64; (*aggregation-96,Serial3 --> core-3,Serial5*)
  258-22; (*core-31,Serial14 --> aggregation-179,Serial1*)
  120-255; (*aggregation-132,Serial1 --> core-33,Serial3*)
  218-61; (*core-57,Serial1 --> aggregation-279,Serial7*)
  1-216; (*edge-89,Serial0 --> aggregation-80,Serial9*)
  47-319; (*edge-312,Serial4 --> aggregation-308,Serial8*)
  117-160; (*edge-121,Serial6 --> aggregation-118,Serial9*)
  60-181; (*core-1,Serial5 --> aggregation-96,Serial1*)
  107-60; (*aggregation-256,Serial1 --> core-1,Serial4*)
  71-202; (*edge-104,Serial5 --> aggregation-101,Serial2*)
  13-164; (*aggregation-183,Serial2 --> core-63,Serial7*)
  193-118; (*edge-137,Serial5 --> aggregation-133,Serial3*)
  280-149; (*edge-72,Serial4 --> aggregation-68,Serial8*)
  120-212; (*aggregation-132,Serial14 --> edge-142,Serial4*)
  60-36; (*core-1,Serial14 --> aggregation-176,Serial1*)
  121-166; (*edge-123,Serial0 --> aggregation-112,Serial11*)
  125-278; (*aggregation-70,Serial4 --> edge-76,Serial6*)
  156-294; (*aggregation-115,Serial5 --> core-26,Serial12*)
  19-221; (*edge-92,Serial7 --> aggregation-87,Serial12*)
  50-246; (*aggregation-161,Serial5 --> edge-171,Serial1*)
  251-154; (*edge-78,Serial1 --> aggregation-65,Serial8*)
  135-112; (*aggregation-240,Serial9 --> edge-249,Serial0*)
  173-13; (*core-60,Serial7 --> aggregation-183,Serial1*)
  226-161; (*core-45,Serial15 --> aggregation-117,Serial5*)
  89-312; (*aggregation-274,Serial1 --> core-17,Serial0*)
  41-301; (*edge-317,Serial0 --> aggregation-304,Serial13*)
  109-157; (*edge-126,Serial2 --> aggregation-114,Serial14*)
  254-93; (*edge-159,Serial2 --> aggregation-146,Serial15*)
  283-110; (*core-22,Serial3 --> aggregation-130,Serial14*)
  152-62; (*aggregation-64,Serial0 --> core-0,Serial0*)
  230-98; (*core-42,Serial3 --> aggregation-261,Serial15*)
  151-276; (*aggregation-69,Serial4 --> edge-74,Serial5*)
  102-178; (*aggregation-144,Serial8 --> edge-152,Serial0*)
  82-224; (*edge-221,Serial5 --> aggregation-213,Serial13*)
  215-103; (*core-59,Serial0 --> aggregation-263,Serial11*)
  232-83; (*edge-287,Serial5 --> aggregation-277,Serial15*)
  6-314; (*aggregation-194,Serial8 --> core-16,Serial5*)
  244-27; (*core-48,Serial3 --> aggregation-294,Serial8*)
  291-298; (*core-28,Serial0 --> aggregation-307,Serial2*)
  205-65; (*aggregation-100,Serial12 --> edge-108,Serial4*)
  245-189; (*core-47,Serial2 --> aggregation-229,Serial14*)
  293-33; (*edge-191,Serial2 --> aggregation-178,Serial15*)
  143-207; (*aggregation-243,Serial15 --> edge-255,Serial3*)
  156-292; (*aggregation-115,Serial0 --> core-27,Serial12*)
  72-152; (*core-7,Serial0 --> aggregation-64,Serial7*)
  73-266; (*aggregation-273,Serial13 --> edge-285,Serial1*)
  54-218; (*aggregation-167,Serial9 --> core-57,Serial13*)
  294-191; (*core-26,Serial6 --> aggregation-227,Serial10*)
  250-82; (*aggregation-208,Serial13 --> edge-221,Serial0*)
  95-279; (*edge-217,Serial1 --> aggregation-209,Serial9*)
  141-273; (*aggregation-244,Serial3 --> core-35,Serial13*)
  282-183; (*core-23,Serial9 --> aggregation-98,Serial15*)
  316-73; (*core-14,Serial15 --> aggregation-273,Serial6*)
  281-44; (*aggregation-311,Serial14 --> edge-318,Serial7*)
  164-9; (*core-63,Serial14 --> aggregation-199,Serial15*)
  196-286; (*core-54,Serial8 --> aggregation-310,Serial6*)
  234-220; (*core-40,Serial11 --> aggregation-85,Serial0*)
  127-143; (*edge-251,Serial3 --> aggregation-243,Serial11*)
  271-225; (*core-36,Serial7 --> aggregation-212,Serial4*)
  65-183; (*edge-108,Serial2 --> aggregation-98,Serial4*)
  142-205; (*edge-110,Serial4 --> aggregation-100,Serial14*)
  142-187; (*edge-110,Serial7 --> aggregation-103,Serial6*)
  166-115; (*aggregation-112,Serial10 --> edge-122,Serial0*)
  300-47; (*aggregation-305,Serial8 --> edge-312,Serial1*)
  169-120; (*edge-143,Serial4 --> aggregation-132,Serial15*)
  74-52; (*core-6,Serial7 --> aggregation-160,Serial6*)
  292-58; (*core-27,Serial11 --> aggregation-291,Serial11*)
  122-197; (*aggregation-131,Serial3 --> edge-139,Serial3*)
  268-87; (*edge-284,Serial3 --> aggregation-275,Serial12*)
  217-123; (*core-58,Serial11 --> aggregation-71,Serial10*)
  164-222; (*core-63,Serial8 --> aggregation-215,Serial2*)
  233-296; (*aggregation-211,Serial7 --> core-25,Serial15*)
  169-108; (*edge-143,Serial7 --> aggregation-135,Serial7*)
  79-256; (*aggregation-150,Serial14 --> edge-158,Serial6*)
  146-175; (*edge-233,Serial6 --> aggregation-230,Serial1*)
  258-150; (*core-31,Serial10 --> aggregation-67,Serial15*)
  25-4; (*aggregation-295,Serial5 --> edge-301,Serial7*)
  174-97; (*edge-266,Serial6 --> aggregation-262,Serial2*)
  68-102; (*core-5,Serial6 --> aggregation-144,Serial5*)
  307-27; (*edge-299,Serial6 --> aggregation-294,Serial3*)
  53-306; (*aggregation-288,Serial9 --> edge-297,Serial0*)
  248-46; (*edge-170,Serial3 --> aggregation-163,Serial2*)
  297-5; (*core-24,Serial5 --> aggregation-195,Serial8*)
  167-76; (*core-62,Serial6 --> aggregation-151,Serial6*)
  51-286; (*edge-314,Serial6 --> aggregation-310,Serial10*)
  8-78; (*aggregation-193,Serial13 --> core-9,Serial7*)
  160-109; (*aggregation-118,Serial14 --> edge-126,Serial6*)
  318-42; (*aggregation-309,Serial12 --> edge-316,Serial5*)
  63-243; (*aggregation-278,Serial1 --> core-49,Serial10*)
  311-14; (*edge-186,Serial6 --> aggregation-182,Serial10*)
  118-229; (*aggregation-133,Serial1 --> core-43,Serial4*)
  11-244; (*aggregation-198,Serial8 --> core-48,Serial0*)
  244-97; (*core-48,Serial12 --> aggregation-262,Serial8*)
  91-263; (*aggregation-148,Serial12 --> edge-156,Serial4*)
  222-95; (*aggregation-215,Serial9 --> edge-217,Serial7*)
  248-52; (*edge-170,Serial0 --> aggregation-160,Serial10*)
  33-311; (*aggregation-178,Serial10 --> edge-186,Serial2*)
  156-119; (*aggregation-115,Serial12 --> edge-124,Serial3*)
  299-51; (*aggregation-306,Serial10 --> edge-314,Serial2*)
  101-261; (*aggregation-145,Serial10 --> edge-154,Serial1*)
  300-303; (*aggregation-305,Serial3 --> core-11,Serial1*)
  258-298; (*core-31,Serial0 --> aggregation-307,Serial1*)
  244-286; (*core-48,Serial8 --> aggregation-310,Serial0*)
  71-183; (*edge-104,Serial2 --> aggregation-98,Serial0*)
  88-233; (*edge-222,Serial3 --> aggregation-211,Serial14*)
  167-172; (*core-62,Serial15 --> aggregation-231,Serial14*)
  239-57; (*edge-172,Serial4 --> aggregation-164,Serial12*)
  132-304; (*aggregation-129,Serial15 --> core-10,Serial0*)
  101-177; (*aggregation-145,Serial9 --> edge-153,Serial1*)
  72-107; (*core-7,Serial4 --> aggregation-256,Serial7*)
  296-143; (*core-25,Serial9 --> aggregation-243,Serial7*)
  281-45; (*aggregation-311,Serial9 --> edge-313,Serial7*)
  48-314; (*aggregation-162,Serial8 --> core-16,Serial4*)
  220-21; (*aggregation-85,Serial14 --> edge-94,Serial5*)
  255-85; (*core-33,Serial14 --> aggregation-276,Serial1*)
  104-171; (*aggregation-259,Serial3 --> edge-267,Serial3*)
  149-270; (*aggregation-68,Serial5 --> core-37,Serial0*)
  297-104; (*core-24,Serial7 --> aggregation-259,Serial8*)
  291-122; (*core-28,Serial3 --> aggregation-131,Serial12*)
  81-185; (*core-8,Serial8 --> aggregation-225,Serial10*)
  215-13; (*core-59,Serial7 --> aggregation-183,Serial0*)
  318-43; (*aggregation-309,Serial15 --> edge-319,Serial5*)
  92-256; (*aggregation-147,Serial14 --> edge-158,Serial3*)
  160-121; (*aggregation-118,Serial11 --> edge-123,Serial6*)
  37-11; (*edge-200,Serial6 --> aggregation-198,Serial0*)
  284-93; (*core-21,Serial15 --> aggregation-146,Serial5*)
  276-150; (*edge-74,Serial3 --> aggregation-67,Serial2*)
  80-180; (*edge-105,Serial1 --> aggregation-97,Serial3*)
  55-242; (*aggregation-166,Serial6 --> edge-174,Serial6*)
  285-153; (*core-20,Serial1 --> aggregation-66,Serial12*)
  35-12; (*edge-201,Serial5 --> aggregation-197,Serial3*)
  157-310; (*aggregation-114,Serial2 --> core-18,Serial11*)
  220-245; (*aggregation-85,Serial7 --> core-47,Serial11*)
  221-1; (*aggregation-87,Serial9 --> edge-89,Serial7*)
  127-129; (*edge-251,Serial6 --> aggregation-246,Serial11*)
  268-63; (*edge-284,Serial6 --> aggregation-278,Serial12*)
  284-157; (*core-21,Serial11 --> aggregation-114,Serial5*)
  60-135; (*core-1,Serial2 --> aggregation-240,Serial1*)
  49-301; (*edge-315,Serial0 --> aggregation-304,Serial11*)
  119-157; (*edge-124,Serial2 --> aggregation-114,Serial12*)
  299-309; (*aggregation-306,Serial3 --> core-19,Serial14*)
  11-24; (*aggregation-198,Serial6 --> edge-206,Serial6*)
  269-141; (*core-38,Serial13 --> aggregation-244,Serial6*)
  14-308; (*aggregation-182,Serial8 --> edge-184,Serial6*)
  267-190; (*core-39,Serial4 --> aggregation-228,Serial7*)
  219-200; (*aggregation-86,Serial4 --> core-52,Serial13*)
  228-55; (*edge-169,Serial6 --> aggregation-166,Serial1*)
  267-149; (*core-39,Serial0 --> aggregation-68,Serial7*)
  294-150; (*core-26,Serial10 --> aggregation-67,Serial10*)
  40-78; (*aggregation-289,Serial13 --> core-9,Serial10*)
  223-196; (*aggregation-214,Serial6 --> core-54,Serial1*)
  81-132; (*core-8,Serial0 --> aggregation-129,Serial10*)
  135-124; (*aggregation-240,Serial10 --> edge-250,Serial0*)
  190-271; (*aggregation-228,Serial4 --> core-36,Serial4*)
  233-82; (*aggregation-211,Serial13 --> edge-221,Serial3*)
  63-232; (*aggregation-278,Serial15 --> edge-287,Serial6*)
  130-207; (*aggregation-245,Serial15 --> edge-255,Serial5*)
  269-91; (*core-38,Serial12 --> aggregation-148,Serial6*)
  94-225; (*edge-218,Serial4 --> aggregation-212,Serial10*)
  234-12; (*core-40,Serial1 --> aggregation-197,Serial10*)
  196-219; (*core-54,Serial13 --> aggregation-86,Serial6*)
  256-91; (*edge-158,Serial4 --> aggregation-148,Serial14*)
  271-155; (*core-36,Serial5 --> aggregation-116,Serial4*)
  58-307; (*aggregation-291,Serial3 --> edge-299,Serial3*)
  145-282; (*aggregation-242,Serial7 --> core-23,Serial7*)
  184-136; (*aggregation-226,Serial3 --> edge-235,Serial2*)
  215-281; (*core-59,Serial10 --> aggregation-311,Serial0*)
  61-167; (*aggregation-279,Serial6 --> core-62,Serial1*)
  279-95; (*aggregation-209,Serial9 --> edge-217,Serial1*)
  134-190; (*edge-236,Serial4 --> aggregation-228,Serial12*)
  200-188; (*core-52,Serial5 --> aggregation-102,Serial12*)
  164-187; (*core-63,Serial4 --> aggregation-103,Serial15*)
  6-26; (*aggregation-194,Serial3 --> edge-203,Serial2*)
  103-174; (*aggregation-263,Serial2 --> edge-266,Serial7*)
  195-116; (*edge-136,Serial6 --> aggregation-134,Serial0*)
  196-27; (*core-54,Serial3 --> aggregation-294,Serial14*)
  252-153; (*edge-79,Serial2 --> aggregation-66,Serial7*)
  312-110; (*core-17,Serial3 --> aggregation-130,Serial9*)
  177-92; (*edge-153,Serial3 --> aggregation-147,Serial9*)
  174-105; (*edge-266,Serial2 --> aggregation-258,Serial2*)
  125-275; (*aggregation-70,Serial1 --> edge-73,Serial6*)
  236-309; (*aggregation-210,Serial3 --> core-19,Serial6*)
  252-151; (*edge-79,Serial5 --> aggregation-69,Serial9*)
  31-5; (*edge-204,Serial3 --> aggregation-195,Serial4*)
  255-32; (*core-33,Serial6 --> aggregation-292,Serial1*)
  118-230; (*aggregation-133,Serial15 --> core-42,Serial4*)
  294-156; (*core-26,Serial12 --> aggregation-115,Serial5*)
  90-231; (*aggregation-149,Serial1 --> core-41,Serial10*)
  180-140; (*aggregation-97,Serial9 --> edge-111,Serial1*)
  274-61; (*edge-280,Serial7 --> aggregation-279,Serial8*)
  194-11; (*core-55,Serial0 --> aggregation-198,Serial15*)
  97-168; (*aggregation-262,Serial0 --> edge-264,Serial6*)
  220-231; (*aggregation-85,Serial1 --> core-41,Serial11*)
  60-16; (*core-1,Serial8 --> aggregation-192,Serial1*)
  314-299; (*core-16,Serial14 --> aggregation-306,Serial0*)
  257-100; (*core-32,Serial15 --> aggregation-260,Serial0*)
  4-53; (*edge-301,Serial0 --> aggregation-288,Serial13*)
  38-293; (*aggregation-181,Serial15 --> edge-191,Serial5*)
  225-253; (*aggregation-212,Serial2 --> core-34,Serial7*)
  27-194; (*aggregation-294,Serial15 --> core-55,Serial3*)
  3-37; (*aggregation-196,Serial8 --> edge-200,Serial4*)
  175-144; (*aggregation-230,Serial2 --> edge-234,Serial6*)
  185-81; (*aggregation-225,Serial10 --> core-8,Serial8*)
  200-129; (*core-52,Serial11 --> aggregation-246,Serial4*)
  13-192; (*aggregation-183,Serial4 --> core-56,Serial7*)
  140-202; (*edge-111,Serial5 --> aggregation-101,Serial9*)
  183-65; (*aggregation-98,Serial4 --> edge-108,Serial2*)
  153-310; (*aggregation-66,Serial10 --> core-18,Serial1*)
  20-213; (*edge-93,Serial3 --> aggregation-83,Serial13*)
  297-156; (*core-24,Serial12 --> aggregation-115,Serial4*)
  316-279; (*core-14,Serial13 --> aggregation-209,Serial6*)
  40-10; (*aggregation-289,Serial6 --> edge-300,Serial1*)
  5-294; (*aggregation-195,Serial10 --> core-26,Serial5*)
  19-210; (*edge-92,Serial1 --> aggregation-81,Serial12*)
  170-76; (*core-61,Serial6 --> aggregation-151,Serial3*)
  31-3; (*edge-204,Serial4 --> aggregation-196,Serial12*)
  53-10; (*aggregation-288,Serial12 --> edge-300,Serial0*)
  158-303; (*aggregation-113,Serial3 --> core-11,Serial12*)
  25-164; (*aggregation-295,Serial15 --> core-63,Serial3*)
  147-126; (*aggregation-241,Serial12 --> edge-252,Serial1*)
  159-111; (*aggregation-119,Serial13 --> edge-125,Serial7*)
  203-63; (*core-51,Serial10 --> aggregation-278,Serial3*)
  39-267; (*aggregation-180,Serial7 --> core-39,Serial8*)
  41-281; (*edge-317,Serial7 --> aggregation-311,Serial13*)
  245-83; (*core-47,Serial6 --> aggregation-277,Serial7*)
  156-297; (*aggregation-115,Serial4 --> core-24,Serial12*)
  80-205; (*edge-105,Serial4 --> aggregation-100,Serial9*)
  58-296; (*aggregation-291,Serial9 --> core-25,Serial11*)
  134-191; (*edge-236,Serial3 --> aggregation-227,Serial4*)
  200-160; (*core-52,Serial9 --> aggregation-118,Serial4*)
  93-282; (*aggregation-146,Serial7 --> core-23,Serial15*)
  303-34; (*core-11,Serial11 --> aggregation-177,Serial3*)
  282-59; (*core-23,Serial8 --> aggregation-290,Serial15*)
  43-298; (*edge-319,Serial3 --> aggregation-307,Serial15*)
  244-175; (*core-48,Serial6 --> aggregation-230,Serial8*)
  103-192; (*aggregation-263,Serial8 --> core-56,Serial0*)
  124-141; (*edge-250,Serial4 --> aggregation-244,Serial10*)
  185-304; (*aggregation-225,Serial15 --> core-10,Serial8*)
  215-54; (*core-59,Serial13 --> aggregation-167,Serial11*)
  279-316; (*aggregation-209,Serial6 --> core-14,Serial13*)
  6-312; (*aggregation-194,Serial9 --> core-17,Serial5*)
  247-224; (*core-46,Serial7 --> aggregation-213,Serial6*)
  132-81; (*aggregation-129,Serial10 --> core-8,Serial0*)
  72-250; (*core-7,Serial15 --> aggregation-208,Serial7*)
  64-133; (*core-3,Serial1 --> aggregation-128,Serial3*)
  105-165; (*aggregation-258,Serial1 --> edge-265,Serial2*)
  48-246; (*aggregation-162,Serial3 --> edge-171,Serial2*)
  55-200; (*aggregation-166,Serial12 --> core-52,Serial15*)
  178-101; (*edge-152,Serial1 --> aggregation-145,Serial8*)
  210-304; (*aggregation-81,Serial2 --> core-10,Serial3*)
  144-185; (*edge-234,Serial1 --> aggregation-225,Serial4*)
  261-101; (*edge-154,Serial1 --> aggregation-145,Serial10*)
  138-186; (*edge-237,Serial0 --> aggregation-224,Serial13*)
  205-267; (*aggregation-100,Serial7 --> core-39,Serial1*)
  233-94; (*aggregation-211,Serial10 --> edge-218,Serial3*)
  267-39; (*core-39,Serial8 --> aggregation-180,Serial7*)
  274-73; (*edge-280,Serial1 --> aggregation-273,Serial8*)
  288-34; (*edge-187,Serial1 --> aggregation-177,Serial11*)
  151-226; (*aggregation-69,Serial0 --> core-45,Serial14*)
  56-226; (*aggregation-165,Serial0 --> core-45,Serial0*)
  22-259; (*aggregation-179,Serial6 --> core-30,Serial14*)
  295-13; (*edge-190,Serial7 --> aggregation-183,Serial14*)
  79-260; (*aggregation-150,Serial11 --> edge-155,Serial6*)
  229-38; (*core-43,Serial5 --> aggregation-181,Serial3*)
  94-279; (*edge-218,Serial1 --> aggregation-209,Serial10*)
  148-156; (*edge-120,Serial3 --> aggregation-115,Serial8*)
  187-140; (*aggregation-103,Serial7 --> edge-111,Serial7*)
  96-223; (*edge-219,Serial6 --> aggregation-214,Serial11*)
  104-297; (*aggregation-259,Serial8 --> core-24,Serial7*)
  177-79; (*edge-153,Serial6 --> aggregation-150,Serial9*)
  30-16; (*edge-205,Serial0 --> aggregation-192,Serial13*)
  231-130; (*core-41,Serial8 --> aggregation-245,Serial1*)
  250-69; (*aggregation-208,Serial4 --> core-4,Serial15*)
  40-303; (*aggregation-289,Serial0 --> core-11,Serial10*)
  164-108; (*core-63,Serial12 --> aggregation-135,Serial15*)
  259-298; (*core-30,Serial0 --> aggregation-307,Serial6*)
  182-259; (*aggregation-99,Serial14 --> core-30,Serial2*)
  104-179; (*aggregation-259,Serial6 --> edge-270,Serial3*)
  294-5; (*core-26,Serial5 --> aggregation-195,Serial10*)
  73-265; (*aggregation-273,Serial10 --> edge-282,Serial1*)
  74-216; (*core-6,Serial12 --> aggregation-80,Serial6*)
  13-288; (*aggregation-183,Serial11 --> edge-187,Serial7*)
  108-199; (*aggregation-135,Serial2 --> edge-138,Serial7*)
  100-168; (*aggregation-260,Serial8 --> edge-264,Serial4*)
  39-255; (*aggregation-180,Serial1 --> core-33,Serial8*)
  188-77; (*aggregation-102,Serial2 --> edge-106,Serial6*)
  56-231; (*aggregation-165,Serial13 --> core-41,Serial0*)
  73-317; (*aggregation-273,Serial5 --> core-13,Serial15*)
  219-15; (*aggregation-86,Serial10 --> edge-90,Serial6*)
  47-286; (*edge-312,Serial6 --> aggregation-310,Serial8*)
  81-154; (*core-8,Serial4 --> aggregation-65,Serial10*)
  161-230; (*aggregation-117,Serial2 --> core-42,Serial15*)
  32-273; (*aggregation-292,Serial3 --> core-35,Serial6*)
  89-268; (*aggregation-274,Serial12 --> edge-284,Serial2*)
  34-289; (*aggregation-177,Serial13 --> edge-189,Serial1*)
  97-244; (*aggregation-262,Serial8 --> core-48,Serial12*)
  33-313; (*aggregation-178,Serial9 --> edge-185,Serial2*)
  279-84; (*aggregation-209,Serial12 --> edge-220,Serial1*)
  190-139; (*aggregation-228,Serial8 --> edge-232,Serial4*)
  192-13; (*core-56,Serial7 --> aggregation-183,Serial4*)
  187-65; (*aggregation-103,Serial4 --> edge-108,Serial7*)
  217-281; (*core-58,Serial10 --> aggregation-311,Serial5*)
  76-262; (*aggregation-151,Serial13 --> edge-157,Serial7*)
  229-90; (*core-43,Serial10 --> aggregation-149,Serial3*)
  28-8; (*edge-202,Serial1 --> aggregation-193,Serial4*)
  64-186; (*core-3,Serial9 --> aggregation-224,Serial3*)
  264-85; (*edge-283,Serial4 --> aggregation-276,Serial11*)
  171-106; (*edge-267,Serial1 --> aggregation-257,Serial5*)
  256-76; (*edge-158,Serial7 --> aggregation-151,Serial14*)
  36-288; (*aggregation-176,Serial11 --> edge-187,Serial0*)
  292-156; (*core-27,Serial12 --> aggregation-115,Serial0*)
  90-245; (*aggregation-149,Serial7 --> core-47,Serial10*)
  202-77; (*aggregation-101,Serial4 --> edge-106,Serial5*)
  125-252; (*aggregation-70,Serial7 --> edge-79,Serial6*)
  258-233; (*core-31,Serial15 --> aggregation-211,Serial1*)
  290-87; (*core-29,Serial8 --> aggregation-275,Serial3*)
  59-2; (*aggregation-290,Serial6 --> edge-302,Serial2*)
  193-110; (*edge-137,Serial2 --> aggregation-130,Serial1*)
  127-135; (*edge-251,Serial0 --> aggregation-240,Serial11*)
  268-75; (*edge-284,Serial0 --> aggregation-272,Serial12*)
  183-140; (*aggregation-98,Serial7 --> edge-111,Serial2*)
  144-172; (*edge-234,Serial7 --> aggregation-231,Serial2*)
  218-76; (*core-57,Serial6 --> aggregation-151,Serial7*)
  7-27; (*edge-303,Serial6 --> aggregation-294,Serial7*)
  69-75; (*core-4,Serial3 --> aggregation-272,Serial4*)
  75-235; (*aggregation-272,Serial14 --> edge-286,Serial0*)
  171-103; (*edge-267,Serial7 --> aggregation-263,Serial3*)
  192-281; (*core-56,Serial10 --> aggregation-311,Serial4*)
  151-231; (*aggregation-69,Serial13 --> core-41,Serial14*)
  211-19; (*aggregation-84,Serial12 --> edge-92,Serial4*)
  108-167; (*aggregation-135,Serial14 --> core-62,Serial12*)
  218-25; (*core-57,Serial3 --> aggregation-295,Serial9*)
  118-195; (*aggregation-133,Serial2 --> edge-136,Serial5*)
  161-114; (*aggregation-117,Serial15 --> edge-127,Serial5*)
  319-41; (*aggregation-308,Serial13 --> edge-317,Serial4*)
  146-189; (*edge-233,Serial5 --> aggregation-229,Serial3*)
  46-297; (*aggregation-163,Serial8 --> core-24,Serial4*)
  217-13; (*core-58,Serial7 --> aggregation-183,Serial5*)
  267-319; (*core-39,Serial9 --> aggregation-308,Serial7*)
  143-290; (*aggregation-243,Serial3 --> core-29,Serial9*)
  106-165; (*aggregation-257,Serial3 --> edge-265,Serial1*)
  66-301; (*core-2,Serial13 --> aggregation-304,Serial2*)
  128-170; (*aggregation-247,Serial3 --> core-61,Serial2*)
  301-45; (*aggregation-304,Serial9 --> edge-313,Serial0*)
  129-113; (*aggregation-246,Serial8 --> edge-248,Serial6*)
  126-145; (*edge-252,Serial2 --> aggregation-242,Serial12*)
  266-89; (*edge-285,Serial2 --> aggregation-274,Serial13*)
  224-99; (*aggregation-213,Serial8 --> edge-216,Serial5*)
  135-69; (*aggregation-240,Serial4 --> core-4,Serial2*)
  166-117; (*aggregation-112,Serial9 --> edge-121,Serial0*)
  305-32; (*edge-298,Serial4 --> aggregation-292,Serial10*)
  286-206; (*aggregation-310,Serial2 --> core-50,Serial8*)
  276-149; (*edge-74,Serial4 --> aggregation-68,Serial10*)
  169-116; (*edge-143,Serial6 --> aggregation-134,Serial7*)
  259-46; (*core-30,Serial4 --> aggregation-163,Serial14*)
  133-68; (*aggregation-128,Serial5 --> core-5,Serial1*)
  4-59; (*edge-301,Serial2 --> aggregation-290,Serial5*)
  142-188; (*edge-110,Serial6 --> aggregation-102,Serial6*)
  150-294; (*aggregation-67,Serial10 --> core-26,Serial10*)
  65-181; (*edge-108,Serial0 --> aggregation-96,Serial12*)
  198-223; (*core-53,Serial1 --> aggregation-214,Serial5*)
  52-60; (*aggregation-160,Serial1 --> core-1,Serial7*)
  63-274; (*aggregation-278,Serial8 --> edge-280,Serial6*)
  234-90; (*core-40,Serial10 --> aggregation-149,Serial0*)
  245-98; (*core-47,Serial3 --> aggregation-261,Serial14*)
  125-194; (*aggregation-70,Serial15 --> core-55,Serial7*)
  208-285; (*aggregation-82,Serial4 --> core-20,Serial2*)
  109-166; (*edge-126,Serial0 --> aggregation-112,Serial14*)
  163-100; (*edge-268,Serial4 --> aggregation-260,Serial12*)
  67-182; (*edge-107,Serial3 --> aggregation-99,Serial3*)
  205-77; (*aggregation-100,Serial10 --> edge-106,Serial4*)
  132-209; (*aggregation-129,Serial6 --> edge-140,Serial1*)
  48-237; (*aggregation-162,Serial0 --> edge-168,Serial2*)
  180-65; (*aggregation-97,Serial6 --> edge-108,Serial1*)
  232-61; (*edge-287,Serial7 --> aggregation-279,Serial15*)
  28-16; (*edge-202,Serial0 --> aggregation-192,Serial10*)
  222-215; (*aggregation-215,Serial0 --> core-59,Serial8*)
  296-58; (*core-25,Serial11 --> aggregation-291,Serial9*)
  43-286; (*edge-319,Serial6 --> aggregation-310,Serial15*)
  285-145; (*core-20,Serial7 --> aggregation-242,Serial4*)
  286-41; (*aggregation-310,Serial13 --> edge-317,Serial6*)
  170-103; (*core-61,Serial0 --> aggregation-263,Serial13*)
  38-287; (*aggregation-181,Serial12 --> edge-188,Serial5*)
  85-271; (*aggregation-276,Serial4 --> core-36,Serial14*)
  186-134; (*aggregation-224,Serial12 --> edge-236,Serial0*)
  134-175; (*edge-236,Serial6 --> aggregation-230,Serial4*)
  231-29; (*core-41,Serial12 --> aggregation-293,Serial13*)
  249-125; (*edge-77,Serial6 --> aggregation-70,Serial5*)
  246-54; (*edge-171,Serial7 --> aggregation-167,Serial3*)
  271-211; (*core-36,Serial10 --> aggregation-84,Serial4*)
  282-299; (*core-23,Serial14 --> aggregation-306,Serial7*)
  244-14; (*core-48,Serial2 --> aggregation-182,Serial0*)
  304-101; (*core-10,Serial5 --> aggregation-145,Serial2*)
  315-154; (*core-15,Serial4 --> aggregation-65,Serial14*)
  182-292; (*aggregation-99,Serial11 --> core-27,Serial2*)
  87-235; (*aggregation-275,Serial14 --> edge-286,Serial3*)
  259-233; (*core-30,Serial15 --> aggregation-211,Serial6*)
  152-66; (*aggregation-64,Serial2 --> core-2,Serial0*)
  123-277; (*aggregation-71,Serial3 --> edge-75,Serial7*)
  145-201; (*aggregation-242,Serial14 --> edge-254,Serial2*)
  192-123; (*core-56,Serial11 --> aggregation-71,Serial8*)
  91-271; (*aggregation-148,Serial4 --> core-36,Serial12*)
  130-229; (*aggregation-245,Serial3 --> core-43,Serial8*)
  198-116; (*core-53,Serial14 --> aggregation-134,Serial13*)
  291-182; (*core-28,Serial2 --> aggregation-99,Serial12*)
  89-314; (*aggregation-274,Serial0 --> core-16,Serial0*)
  198-55; (*core-53,Serial15 --> aggregation-166,Serial13*)
  50-239; (*aggregation-161,Serial6 --> edge-172,Serial1*)
  149-249; (*aggregation-68,Serial13 --> edge-77,Serial4*)
  44-300; (*edge-318,Serial1 --> aggregation-305,Serial14*)
  114-156; (*edge-127,Serial3 --> aggregation-115,Serial15*)
  108-214; (*aggregation-135,Serial5 --> edge-141,Serial7*)
  219-243; (*aggregation-86,Serial1 --> core-49,Serial13*)
  3-24; (*aggregation-196,Serial14 --> edge-206,Serial4*)
  151-249; (*aggregation-69,Serial7 --> edge-77,Serial5*)
  115-155; (*edge-122,Serial4 --> aggregation-116,Serial10*)
  162-104; (*edge-269,Serial3 --> aggregation-259,Serial5*)
  17-208; (*edge-95,Serial2 --> aggregation-82,Serial15*)
  129-127; (*aggregation-246,Serial11 --> edge-251,Serial6*)
  198-97; (*core-53,Serial12 --> aggregation-262,Serial13*)
  54-164; (*aggregation-167,Serial15 --> core-63,Serial13*)
  66-36; (*core-2,Serial14 --> aggregation-176,Serial2*)
  92-290; (*aggregation-147,Serial3 --> core-29,Serial13*)
  107-69; (*aggregation-256,Serial4 --> core-4,Serial4*)
  240-53; (*edge-296,Serial0 --> aggregation-288,Serial8*)
  4-29; (*edge-301,Serial5 --> aggregation-293,Serial7*)
  279-94; (*aggregation-209,Serial10 --> edge-218,Serial1*)
  11-198; (*aggregation-198,Serial13 --> core-53,Serial0*)
  298-258; (*aggregation-307,Serial1 --> core-31,Serial0*)
  128-204; (*aggregation-247,Serial13 --> edge-253,Serial7*)
  247-38; (*core-46,Serial5 --> aggregation-181,Serial6*)
  173-222; (*core-60,Serial8 --> aggregation-215,Serial1*)
  113-128; (*edge-248,Serial7 --> aggregation-247,Serial8*)
  33-282; (*aggregation-178,Serial7 --> core-23,Serial13*)
  95-223; (*edge-217,Serial6 --> aggregation-214,Serial9*)
  110-212; (*aggregation-130,Serial6 --> edge-142,Serial2*)
  248-50; (*edge-170,Serial1 --> aggregation-161,Serial4*)
  24-9; (*edge-206,Serial7 --> aggregation-199,Serial6*)
  21-220; (*edge-94,Serial5 --> aggregation-85,Serial14*)
  36-72; (*aggregation-176,Serial7 --> core-7,Serial14*)
  12-247; (*aggregation-197,Serial12 --> core-46,Serial1*)
  188-196; (*aggregation-102,Serial14 --> core-54,Serial5*)
  221-192; (*aggregation-87,Serial4 --> core-56,Serial5*)
  309-184; (*core-19,Serial10 --> aggregation-226,Serial11*)
  12-37; (*aggregation-197,Serial2 --> edge-200,Serial5*)
  283-33; (*core-22,Serial13 --> aggregation-178,Serial6*)
  13-217; (*aggregation-183,Serial5 --> core-58,Serial7*)
  45-301; (*edge-313,Serial0 --> aggregation-304,Serial9*)
  210-0; (*aggregation-81,Serial8 --> edge-88,Serial1*)
  6-284; (*aggregation-194,Serial13 --> core-21,Serial5*)
  22-295; (*aggregation-179,Serial14 --> edge-190,Serial3*)
  99-225; (*edge-216,Serial4 --> aggregation-212,Serial8*)
  112-141; (*edge-249,Serial4 --> aggregation-244,Serial9*)
  203-27; (*core-51,Serial3 --> aggregation-294,Serial11*)
  280-123; (*edge-72,Serial7 --> aggregation-71,Serial0*)
  51-319; (*edge-314,Serial4 --> aggregation-308,Serial10*)
  121-160; (*edge-123,Serial6 --> aggregation-118,Serial11*)
  297-150; (*core-24,Serial10 --> aggregation-67,Serial8*)
  165-98; (*edge-265,Serial5 --> aggregation-261,Serial3*)
  108-215; (*aggregation-135,Serial11 --> core-59,Serial12*)
  210-78; (*aggregation-81,Serial1 --> core-9,Serial3*)
  160-243; (*aggregation-118,Serial1 --> core-49,Serial9*)
  305-29; (*edge-298,Serial5 --> aggregation-293,Serial4*)
  303-73; (*core-11,Serial15 --> aggregation-273,Serial3*)
  108-209; (*aggregation-135,Serial4 --> edge-140,Serial7*)
  249-154; (*edge-77,Serial1 --> aggregation-65,Serial7*)
  147-112; (*aggregation-241,Serial9 --> edge-249,Serial1*)
  68-166; (*core-5,Serial10 --> aggregation-112,Serial5*)
  83-227; (*aggregation-277,Serial4 --> core-44,Serial6*)
  6-310; (*aggregation-194,Serial10 --> core-18,Serial5*)
  160-200; (*aggregation-118,Serial4 --> core-52,Serial9*)
  299-283; (*aggregation-306,Serial6 --> core-22,Serial14*)
  260-91; (*edge-155,Serial4 --> aggregation-148,Serial11*)
  61-264; (*aggregation-279,Serial11 --> edge-283,Serial7*)
  231-56; (*core-41,Serial0 --> aggregation-165,Serial13*)
  243-116; (*core-49,Serial14 --> aggregation-134,Serial9*)
  192-172; (*core-56,Serial15 --> aggregation-231,Serial8*)
  317-147; (*core-13,Serial14 --> aggregation-241,Serial5*)
  53-69; (*aggregation-288,Serial4 --> core-4,Serial11*)
  213-17; (*aggregation-83,Serial15 --> edge-95,Serial3*)
  310-89; (*core-18,Serial0 --> aggregation-274,Serial2*)
  293-36; (*edge-191,Serial0 --> aggregation-176,Serial15*)
  238-52; (*edge-173,Serial0 --> aggregation-160,Serial13*)
  273-91; (*core-35,Serial12 --> aggregation-148,Serial3*)
  211-273; (*aggregation-84,Serial3 --> core-35,Serial10*)
  87-296; (*aggregation-275,Serial7 --> core-25,Serial8*)
  271-190; (*core-36,Serial4 --> aggregation-228,Serial4*)
  9-28; (*aggregation-199,Serial2 --> edge-202,Serial7*)
  145-207; (*aggregation-242,Serial15 --> edge-255,Serial2*)
  223-82; (*aggregation-214,Serial13 --> edge-221,Serial6*)
  92-178; (*aggregation-147,Serial8 --> edge-152,Serial3*)
  192-128; (*core-56,Serial2 --> aggregation-247,Serial4*)
  241-46; (*edge-175,Serial3 --> aggregation-163,Serial7*)
  48-228; (*aggregation-162,Serial1 --> edge-169,Serial2*)
  211-1; (*aggregation-84,Serial9 --> edge-89,Serial4*)
  17-221; (*edge-95,Serial7 --> aggregation-87,Serial15*)
  204-130; (*edge-253,Serial5 --> aggregation-245,Serial13*)
  167-123; (*core-62,Serial11 --> aggregation-71,Serial14*)
  87-258; (*aggregation-275,Serial1 --> core-31,Serial8*)
  151-227; (*aggregation-69,Serial11 --> core-44,Serial14*)
  162-97; (*edge-269,Serial6 --> aggregation-262,Serial5*)
  170-221; (*core-61,Serial5 --> aggregation-87,Serial3*)
  44-301; (*edge-318,Serial0 --> aggregation-304,Serial14*)
  114-157; (*edge-127,Serial2 --> aggregation-114,Serial15*)
  132-193; (*aggregation-129,Serial3 --> edge-137,Serial1*)
  155-271; (*aggregation-116,Serial4 --> core-36,Serial5*)
  315-300; (*core-15,Serial1 --> aggregation-305,Serial7*)
  39-311; (*aggregation-180,Serial10 --> edge-186,Serial4*)
  0-216; (*edge-88,Serial0 --> aggregation-80,Serial8*)
  78-101; (*core-9,Serial5 --> aggregation-145,Serial1*)
  218-221; (*core-57,Serial5 --> aggregation-87,Serial7*)
  305-59; (*edge-298,Serial2 --> aggregation-290,Serial2*)
  82-222; (*edge-221,Serial7 --> aggregation-215,Serial13*)
  116-206; (*aggregation-134,Serial10 --> core-50,Serial14*)
  129-194; (*aggregation-246,Serial7 --> core-55,Serial11*)
  102-256; (*aggregation-144,Serial14 --> edge-158,Serial0*)
  217-128; (*core-58,Serial2 --> aggregation-247,Serial5*)
  208-312; (*aggregation-82,Serial1 --> core-17,Serial2*)
  282-110; (*core-23,Serial3 --> aggregation-130,Serial15*)
  38-247; (*aggregation-181,Serial6 --> core-46,Serial5*)
  62-181; (*core-0,Serial5 --> aggregation-96,Serial0*)
  143-294; (*aggregation-243,Serial5 --> core-26,Serial9*)
  315-132; (*core-15,Serial0 --> aggregation-129,Serial14*)
  284-48; (*core-21,Serial4 --> aggregation-162,Serial13*)
  57-242; (*aggregation-164,Serial14 --> edge-174,Serial4*)
  130-227; (*aggregation-245,Serial4 --> core-44,Serial8*)
  77-187; (*edge-106,Serial7 --> aggregation-103,Serial2*)
  50-228; (*aggregation-161,Serial3 --> edge-169,Serial1*)
  197-108; (*edge-139,Serial7 --> aggregation-135,Serial3*)
  66-152; (*core-2,Serial0 --> aggregation-64,Serial2*)
  194-286; (*core-55,Serial8 --> aggregation-310,Serial7*)
  229-12; (*core-43,Serial1 --> aggregation-197,Serial1*)
  107-165; (*aggregation-256,Serial9 --> edge-265,Serial0*)
  67-183; (*edge-107,Serial2 --> aggregation-98,Serial3*)
  299-47; (*aggregation-306,Serial8 --> edge-312,Serial2*)
  273-120; (*core-35,Serial3 --> aggregation-132,Serial3*)
  270-225; (*core-37,Serial7 --> aggregation-212,Serial5*)
  90-263; (*aggregation-149,Serial12 --> edge-156,Serial5*)
  180-70; (*aggregation-97,Serial7 --> edge-109,Serial1*)
  110-197; (*aggregation-130,Serial3 --> edge-139,Serial2*)
  52-62; (*aggregation-160,Serial0 --> core-0,Serial7*)
  54-248; (*aggregation-167,Serial2 --> edge-170,Serial7*)
  201-128; (*edge-254,Serial7 --> aggregation-247,Serial14*)
  59-4; (*aggregation-290,Serial5 --> edge-301,Serial2*)
  254-102; (*edge-159,Serial0 --> aggregation-144,Serial15*)
  186-136; (*aggregation-224,Serial11 --> edge-235,Serial0*)
  87-232; (*aggregation-275,Serial15 --> edge-287,Serial3*)
  193-122; (*edge-137,Serial3 --> aggregation-131,Serial1*)
  301-66; (*aggregation-304,Serial2 --> core-2,Serial13*)
  84-224; (*edge-220,Serial5 --> aggregation-213,Serial12*)
  190-131; (*aggregation-228,Serial15 --> edge-239,Serial4*)
  236-283; (*aggregation-210,Serial6 --> core-22,Serial6*)
  283-105; (*core-22,Serial12 --> aggregation-258,Serial14*)
  157-148; (*aggregation-114,Serial8 --> edge-120,Serial2*)
  75-266; (*aggregation-272,Serial13 --> edge-285,Serial0*)
  259-191; (*core-30,Serial6 --> aggregation-227,Serial14*)
  1-208; (*edge-89,Serial2 --> aggregation-82,Serial9*)
  56-227; (*aggregation-165,Serial11 --> core-44,Serial0*)
  99-279; (*edge-216,Serial1 --> aggregation-209,Serial8*)
  235-83; (*edge-286,Serial5 --> aggregation-277,Serial14*)
  286-44; (*aggregation-310,Serial14 --> edge-318,Serial6*)
  91-261; (*aggregation-148,Serial10 --> edge-154,Serial4*)
  150-296; (*aggregation-67,Serial9 --> core-25,Serial10*)
  212-122; (*edge-142,Serial3 --> aggregation-131,Serial6*)
  265-73; (*edge-282,Serial1 --> aggregation-273,Serial10*)
  156-115; (*aggregation-115,Serial10 --> edge-122,Serial3*)
  275-152; (*edge-73,Serial0 --> aggregation-64,Serial9*)
  257-3; (*core-32,Serial11 --> aggregation-196,Serial0*)
  283-236; (*core-22,Serial6 --> aggregation-210,Serial6*)
  11-37; (*aggregation-198,Serial0 --> edge-200,Serial6*)
  60-53; (*core-1,Serial11 --> aggregation-288,Serial1*)
  120-267; (*aggregation-132,Serial7 --> core-39,Serial3*)
  120-195; (*aggregation-132,Serial8 --> edge-136,Serial4*)
  151-280; (*aggregation-69,Serial2 --> edge-72,Serial5*)
  66-102; (*core-2,Serial6 --> aggregation-144,Serial2*)
  189-137; (*aggregation-229,Serial8 --> edge-238,Serial5*)
  243-97; (*core-49,Serial12 --> aggregation-262,Serial9*)
  159-164; (*aggregation-119,Serial2 --> core-63,Serial9*)
  155-119; (*aggregation-116,Serial12 --> edge-124,Serial4*)
  298-51; (*aggregation-307,Serial10 --> edge-314,Serial3*)
  198-125; (*core-53,Serial7 --> aggregation-70,Serial13*)
  32-253; (*aggregation-292,Serial2 --> core-34,Serial6*)
  210-18; (*aggregation-81,Serial11 --> edge-91,Serial1*)
  143-291; (*aggregation-243,Serial2 --> core-28,Serial9*)
  243-11; (*core-49,Serial0 --> aggregation-198,Serial9*)
  208-20; (*aggregation-82,Serial13 --> edge-93,Serial2*)
  212-120; (*edge-142,Serial4 --> aggregation-132,Serial14*)
  241-57; (*edge-175,Serial4 --> aggregation-164,Serial15*)
  214-132; (*edge-141,Serial1 --> aggregation-129,Serial7*)
  35-5; (*edge-201,Serial3 --> aggregation-195,Serial1*)
  229-56; (*core-43,Serial0 --> aggregation-165,Serial1*)
  202-67; (*aggregation-101,Serial5 --> edge-107,Serial5*)
  51-298; (*edge-314,Serial3 --> aggregation-307,Serial10*)
  121-161; (*edge-123,Serial5 --> aggregation-117,Serial11*)
  194-188; (*core-55,Serial5 --> aggregation-102,Serial15*)
  40-302; (*aggregation-289,Serial1 --> core-12,Serial10*)
  107-68; (*aggregation-256,Serial5 --> core-5,Serial4*)
  140-205; (*edge-111,Serial4 --> aggregation-100,Serial15*)
  197-116; (*edge-139,Serial6 --> aggregation-134,Serial3*)
  98-165; (*aggregation-261,Serial3 --> edge-265,Serial5*)
  113-145; (*edge-248,Serial2 --> aggregation-242,Serial8*)
  218-281; (*core-57,Serial10 --> aggregation-311,Serial7*)
  249-149; (*edge-77,Serial4 --> aggregation-68,Serial13*)
  261-93; (*edge-154,Serial2 --> aggregation-146,Serial10*)
  99-250; (*edge-216,Serial0 --> aggregation-208,Serial8*)
  104-296; (*aggregation-259,Serial9 --> core-25,Serial7*)
  129-126; (*aggregation-246,Serial12 --> edge-252,Serial6*)
  231-220; (*core-41,Serial11 --> aggregation-85,Serial1*)
  210-315; (*aggregation-81,Serial7 --> core-15,Serial3*)
  161-229; (*aggregation-117,Serial3 --> core-43,Serial15*)
  203-219; (*core-51,Serial13 --> aggregation-86,Serial3*)
  264-87; (*edge-283,Serial3 --> aggregation-275,Serial11*)
  70-202; (*edge-109,Serial5 --> aggregation-101,Serial7*)
  279-82; (*aggregation-209,Serial13 --> edge-221,Serial1*)
  60-52; (*core-1,Serial7 --> aggregation-160,Serial1*)
  143-113; (*aggregation-243,Serial8 --> edge-248,Serial3*)
  194-14; (*core-55,Serial2 --> aggregation-182,Serial7*)
  59-283; (*aggregation-290,Serial14 --> core-22,Serial8*)
  219-18; (*aggregation-86,Serial11 --> edge-91,Serial6*)
  5-37; (*aggregation-195,Serial0 --> edge-200,Serial3*)
  222-86; (*aggregation-215,Serial15 --> edge-223,Serial7*)
  228-52; (*edge-169,Serial0 --> aggregation-160,Serial9*)
  57-257; (*aggregation-164,Serial0 --> core-32,Serial2*)
  271-149; (*core-36,Serial0 --> aggregation-68,Serial4*)
  147-204; (*aggregation-241,Serial13 --> edge-253,Serial1*)
  151-251; (*aggregation-69,Serial8 --> edge-78,Serial5*)
  221-217; (*aggregation-87,Serial5 --> core-58,Serial5*)
  116-244; (*aggregation-134,Serial8 --> core-48,Serial14*)
  205-142; (*aggregation-100,Serial14 --> edge-110,Serial4*)
  295-14; (*edge-190,Serial6 --> aggregation-182,Serial14*)
  131-172; (*edge-239,Serial7 --> aggregation-231,Serial7*)
  50-237; (*aggregation-161,Serial2 --> edge-168,Serial1*)
  230-83; (*core-42,Serial6 --> aggregation-277,Serial2*)
  196-175; (*core-54,Serial6 --> aggregation-230,Serial14*)
  234-56; (*core-40,Serial0 --> aggregation-165,Serial10*)
  309-157; (*core-19,Serial11 --> aggregation-114,Serial3*)
  123-278; (*aggregation-71,Serial4 --> edge-76,Serial7*)
  13-311; (*aggregation-183,Serial10 --> edge-186,Serial7*)
  12-23; (*aggregation-197,Serial9 --> edge-207,Serial5*)
  270-3; (*core-37,Serial11 --> aggregation-196,Serial5*)
  166-114; (*aggregation-112,Serial15 --> edge-127,Serial0*)
  24-6; (*edge-206,Serial2 --> aggregation-194,Serial6*)
  73-302; (*aggregation-273,Serial4 --> core-12,Serial15*)
  137-184; (*edge-238,Serial2 --> aggregation-226,Serial6*)
  267-155; (*core-39,Serial5 --> aggregation-116,Serial7*)
  43-299; (*edge-319,Serial2 --> aggregation-306,Serial15*)
  52-228; (*aggregation-160,Serial9 --> edge-169,Serial0*)
  206-11; (*core-50,Serial0 --> aggregation-198,Serial10*)
  138-185; (*edge-237,Serial1 --> aggregation-225,Serial7*)
  294-46; (*core-26,Serial4 --> aggregation-163,Serial10*)
  224-229; (*aggregation-213,Serial3 --> core-43,Serial7*)
  92-294; (*aggregation-147,Serial5 --> core-26,Serial13*)
  52-246; (*aggregation-160,Serial11 --> edge-171,Serial0*)
  123-173; (*aggregation-71,Serial12 --> core-60,Serial11*)
  148-155; (*edge-120,Serial4 --> aggregation-116,Serial8*)
  47-281; (*edge-312,Serial7 --> aggregation-311,Serial8*)
  271-85; (*core-36,Serial14 --> aggregation-276,Serial4*)
  96-222; (*edge-219,Serial7 --> aggregation-215,Serial11*)
  194-160; (*core-55,Serial9 --> aggregation-118,Serial7*)
  76-170; (*aggregation-151,Serial3 --> core-61,Serial6*)
  185-302; (*aggregation-225,Serial1 --> core-12,Serial8*)
  263-90; (*edge-156,Serial5 --> aggregation-149,Serial12*)
  173-25; (*core-60,Serial3 --> aggregation-295,Serial12*)
  62-133; (*core-0,Serial1 --> aggregation-128,Serial0*)
  296-213; (*core-25,Serial1 --> aggregation-83,Serial7*)
  297-191; (*core-24,Serial6 --> aggregation-227,Serial8*)
  146-191; (*edge-233,Serial3 --> aggregation-227,Serial1*)
  245-151; (*core-47,Serial14 --> aggregation-69,Serial14*)
  3-255; (*aggregation-196,Serial1 --> core-33,Serial11*)
  106-315; (*aggregation-257,Serial14 --> core-15,Serial9*)
  247-130; (*core-46,Serial8 --> aggregation-245,Serial6*)
  18-211; (*edge-91,Serial4 --> aggregation-84,Serial11*)
  48-239; (*aggregation-162,Serial4 --> edge-172,Serial2*)
  23-3; (*edge-207,Serial4 --> aggregation-196,Serial15*)
  61-268; (*aggregation-279,Serial12 --> edge-284,Serial7*)
  97-162; (*aggregation-262,Serial5 --> edge-269,Serial6*)
  81-106; (*core-8,Serial9 --> aggregation-257,Serial10*)
  215-108; (*core-59,Serial12 --> aggregation-135,Serial11*)
  288-33; (*edge-187,Serial2 --> aggregation-178,Serial11*)
  270-100; (*core-37,Serial15 --> aggregation-260,Serial5*)
  305-53; (*edge-298,Serial0 --> aggregation-288,Serial10*)
  128-215; (*aggregation-247,Serial0 --> core-59,Serial2*)
  255-141; (*core-33,Serial13 --> aggregation-244,Serial1*)
  290-233; (*core-29,Serial15 --> aggregation-211,Serial3*)
  227-118; (*core-44,Serial4 --> aggregation-133,Serial11*)
  224-84; (*aggregation-213,Serial12 --> edge-220,Serial5*)
  21-219; (*edge-94,Serial6 --> aggregation-86,Serial14*)
  274-89; (*edge-280,Serial2 --> aggregation-274,Serial8*)
  143-124; (*aggregation-243,Serial10 --> edge-250,Serial3*)
  182-80; (*aggregation-99,Serial1 --> edge-105,Serial3*)
  21-216; (*edge-94,Serial0 --> aggregation-80,Serial14*)
  58-294; (*aggregation-291,Serial10 --> core-26,Serial11*)
  218-13; (*core-57,Serial7 --> aggregation-183,Serial7*)
  58-297; (*aggregation-291,Serial8 --> core-24,Serial11*)
  302-50; (*core-12,Serial6 --> aggregation-161,Serial1*)
  122-169; (*aggregation-131,Serial7 --> edge-143,Serial3*)
  63-196; (*aggregation-278,Serial6 --> core-54,Serial10*)
  276-151; (*edge-74,Serial5 --> aggregation-69,Serial4*)
  41-318; (*edge-317,Serial5 --> aggregation-309,Serial13*)
  109-159; (*edge-126,Serial7 --> aggregation-119,Serial14*)
  229-318; (*core-43,Serial9 --> aggregation-309,Serial3*)
  214-118; (*edge-141,Serial5 --> aggregation-133,Serial7*)
  309-299; (*core-19,Serial14 --> aggregation-306,Serial3*)
  12-227; (*aggregation-197,Serial11 --> core-44,Serial1*)
  191-139; (*aggregation-227,Serial0 --> edge-232,Serial3*)
  242-56; (*edge-174,Serial5 --> aggregation-165,Serial8*)
  238-50; (*edge-173,Serial1 --> aggregation-161,Serial7*)
  63-272; (*aggregation-278,Serial9 --> edge-281,Serial6*)
  117-166; (*edge-121,Serial0 --> aggregation-112,Serial9*)
  315-158; (*core-15,Serial12 --> aggregation-113,Serial7*)
  36-74; (*aggregation-176,Serial6 --> core-6,Serial14*)
  66-107; (*core-2,Serial4 --> aggregation-256,Serial2*)
  186-62; (*aggregation-224,Serial0 --> core-0,Serial9*)
  26-11; (*edge-203,Serial6 --> aggregation-198,Serial3*)
  74-135; (*core-6,Serial2 --> aggregation-240,Serial6*)
  103-171; (*aggregation-263,Serial3 --> edge-267,Serial7*)
  175-136; (*aggregation-230,Serial3 --> edge-235,Serial6*)
  183-309; (*aggregation-98,Serial11 --> core-19,Serial9*)
  95-224; (*edge-217,Serial5 --> aggregation-213,Serial9*)
  88-279; (*edge-222,Serial1 --> aggregation-209,Serial14*)
  191-290; (*aggregation-227,Serial13 --> core-29,Serial6*)
  69-16; (*core-4,Serial8 --> aggregation-192,Serial4*)
  192-76; (*core-56,Serial6 --> aggregation-151,Serial4*)
  133-193; (*aggregation-128,Serial9 --> edge-137,Serial0*)
  174-104; (*edge-266,Serial3 --> aggregation-259,Serial2*)
  225-255; (*aggregation-212,Serial1 --> core-33,Serial7*)
  29-307; (*aggregation-293,Serial5 --> edge-299,Serial5*)
  131-190; (*edge-239,Serial4 --> aggregation-228,Serial15*)
  84-225; (*edge-220,Serial4 --> aggregation-212,Serial12*)
  152-276; (*aggregation-64,Serial10 --> edge-74,Serial0*)
  252-150; (*edge-79,Serial3 --> aggregation-67,Serial7*)
  316-154; (*core-14,Serial4 --> aggregation-65,Serial12*)
  219-198; (*aggregation-86,Serial5 --> core-53,Serial13*)
  298-45; (*aggregation-307,Serial9 --> edge-313,Serial3*)
  75-274; (*aggregation-272,Serial8 --> edge-280,Serial0*)
  101-302; (*aggregation-145,Serial4 --> core-12,Serial5*)
  229-118; (*core-43,Serial4 --> aggregation-133,Serial1*)
  1-213; (*edge-89,Serial3 --> aggregation-83,Serial9*)
  156-290; (*aggregation-115,Serial3 --> core-29,Serial12*)
  301-64; (*aggregation-304,Serial3 --> core-3,Serial13*)
  312-48; (*core-17,Serial4 --> aggregation-162,Serial9*)
  27-196; (*aggregation-294,Serial14 --> core-54,Serial3*)
  100-269; (*aggregation-260,Serial6 --> core-38,Serial15*)
  290-5; (*core-29,Serial5 --> aggregation-195,Serial13*)
  259-104; (*core-30,Serial7 --> aggregation-259,Serial14*)
  85-265; (*aggregation-276,Serial10 --> edge-282,Serial4*)
  232-75; (*edge-287,Serial0 --> aggregation-272,Serial15*)
  216-21; (*aggregation-80,Serial14 --> edge-94,Serial0*)
  312-184; (*core-17,Serial10 --> aggregation-226,Serial9*)
  40-316; (*aggregation-289,Serial12 --> core-14,Serial10*)
  303-185; (*core-11,Serial8 --> aggregation-225,Serial0*)
  128-218; (*aggregation-247,Serial7 --> core-57,Serial2*)
  54-237; (*aggregation-167,Serial0 --> edge-168,Serial7*)
  10-27; (*edge-300,Serial6 --> aggregation-294,Serial4*)
  30-8; (*edge-205,Serial1 --> aggregation-193,Serial7*)
  151-247; (*aggregation-69,Serial12 --> core-46,Serial14*)
  191-297; (*aggregation-227,Serial8 --> core-24,Serial6*)
  16-26; (*aggregation-192,Serial11 --> edge-203,Serial0*)
  50-315; (*aggregation-161,Serial14 --> core-15,Serial6*)
  45-318; (*edge-313,Serial5 --> aggregation-309,Serial9*)
  231-202; (*core-41,Serial13 --> aggregation-101,Serial13*)
  63-235; (*aggregation-278,Serial14 --> edge-286,Serial6*)
  101-254; (*aggregation-145,Serial15 --> edge-159,Serial1*)
  309-93; (*core-19,Serial15 --> aggregation-146,Serial3*)
  170-128; (*core-61,Serial2 --> aggregation-247,Serial3*)
  229-224; (*core-43,Serial7 --> aggregation-213,Serial3*)
  247-98; (*core-46,Serial3 --> aggregation-261,Serial12*)
  155-270; (*aggregation-116,Serial5 --> core-37,Serial5*)
  119-158; (*edge-124,Serial1 --> aggregation-113,Serial12*)
  294-143; (*core-26,Serial9 --> aggregation-243,Serial5*)
  98-230; (*aggregation-261,Serial15 --> core-42,Serial3*)
  262-101; (*edge-157,Serial1 --> aggregation-145,Serial13*)
  61-173; (*aggregation-279,Serial1 --> core-60,Serial1*)
  213-259; (*aggregation-83,Serial6 --> core-30,Serial1*)
  223-198; (*aggregation-214,Serial5 --> core-53,Serial1*)
  132-195; (*aggregation-129,Serial2 --> edge-136,Serial1*)
  255-319; (*core-33,Serial9 --> aggregation-308,Serial1*)
  176-103; (*edge-271,Serial7 --> aggregation-263,Serial7*)
  303-210; (*core-11,Serial3 --> aggregation-81,Serial3*)
  298-291; (*aggregation-307,Serial2 --> core-28,Serial0*)
  38-289; (*aggregation-181,Serial13 --> edge-189,Serial5*)
  305-58; (*edge-298,Serial3 --> aggregation-291,Serial2*)
  189-146; (*aggregation-229,Serial3 --> edge-233,Serial5*)
  189-131; (*aggregation-229,Serial9 --> edge-239,Serial5*)
  153-285; (*aggregation-66,Serial12 --> core-20,Serial1*)
  246-55; (*edge-171,Serial6 --> aggregation-166,Serial3*)
  77-188; (*edge-106,Serial6 --> aggregation-102,Serial2*)
  152-64; (*aggregation-64,Serial3 --> core-3,Serial0*)
  89-285; (*aggregation-274,Serial4 --> core-20,Serial0*)
  204-141; (*edge-253,Serial4 --> aggregation-244,Serial13*)
  181-70; (*aggregation-96,Serial13 --> edge-109,Serial0*)
  118-234; (*aggregation-133,Serial10 --> core-40,Serial4*)
  243-63; (*core-49,Serial10 --> aggregation-278,Serial1*)
  267-57; (*core-39,Serial2 --> aggregation-164,Serial7*)
  161-117; (*aggregation-117,Serial9 --> edge-121,Serial5*)
  227-98; (*core-44,Serial3 --> aggregation-261,Serial11*)
  299-43; (*aggregation-306,Serial15 --> edge-319,Serial2*)
  236-284; (*aggregation-210,Serial5 --> core-21,Serial6*)
  190-137; (*aggregation-228,Serial14 --> edge-238,Serial4*)
  160-198; (*aggregation-118,Serial5 --> core-53,Serial9*)
  213-15; (*aggregation-83,Serial10 --> edge-90,Serial3*)
  157-312; (*aggregation-114,Serial1 --> core-17,Serial11*)
  218-128; (*core-57,Serial2 --> aggregation-247,Serial7*)
  98-234; (*aggregation-261,Serial10 --> core-40,Serial3*)
  175-206; (*aggregation-230,Serial10 --> core-50,Serial6*)
  103-173; (*aggregation-263,Serial12 --> core-60,Serial0*)
  72-301; (*core-7,Serial13 --> aggregation-304,Serial7*)
  167-222; (*core-62,Serial8 --> aggregation-215,Serial6*)
  236-99; (*aggregation-210,Serial8 --> edge-216,Serial2*)
  253-57; (*core-34,Serial2 --> aggregation-164,Serial2*)
  11-35; (*aggregation-198,Serial1 --> edge-201,Serial6*)
  215-76; (*core-59,Serial6 --> aggregation-151,Serial0*)
  60-216; (*core-1,Serial12 --> aggregation-80,Serial1*)
  201-135; (*edge-254,Serial0 --> aggregation-240,Serial14*)
  271-39; (*core-36,Serial8 --> aggregation-180,Serial4*)
  153-275; (*aggregation-66,Serial1 --> edge-73,Serial2*)
  265-89; (*edge-282,Serial2 --> aggregation-274,Serial10*)
  313-39; (*edge-185,Serial4 --> aggregation-180,Serial9*)
  16-72; (*aggregation-192,Serial7 --> core-7,Serial8*)
  101-304; (*aggregation-145,Serial2 --> core-10,Serial5*)
  300-316; (*aggregation-305,Serial6 --> core-14,Serial1*)
  110-199; (*aggregation-130,Serial2 --> edge-138,Serial2*)
  66-250; (*core-2,Serial15 --> aggregation-208,Serial2*)
  215-128; (*core-59,Serial2 --> aggregation-247,Serial0*)
  72-75; (*core-7,Serial3 --> aggregation-272,Serial7*)
  158-121; (*aggregation-113,Serial11 --> edge-123,Serial1*)
  166-72; (*aggregation-112,Serial7 --> core-7,Serial10*)
  157-309; (*aggregation-114,Serial3 --> core-19,Serial11*)
  86-223; (*edge-223,Serial6 --> aggregation-214,Serial15*)
  223-94; (*aggregation-214,Serial10 --> edge-218,Serial6*)
  239-48; (*edge-172,Serial2 --> aggregation-162,Serial4*)
  168-103; (*edge-264,Serial7 --> aggregation-263,Serial0*)
  306-25; (*edge-297,Serial7 --> aggregation-295,Serial1*)
  315-50; (*core-15,Serial6 --> aggregation-161,Serial14*)
  8-317; (*aggregation-193,Serial11 --> core-13,Serial7*)
  153-277; (*aggregation-66,Serial3 --> edge-75,Serial2*)
  78-147; (*core-9,Serial14 --> aggregation-241,Serial1*)
  0-220; (*edge-88,Serial5 --> aggregation-85,Serial8*)
  134-189; (*edge-236,Serial5 --> aggregation-229,Serial6*)
  300-81; (*aggregation-305,Serial0 --> core-8,Serial1*)
  31-12; (*edge-204,Serial5 --> aggregation-197,Serial6*)
  296-156; (*core-25,Serial12 --> aggregation-115,Serial7*)
  56-247; (*aggregation-165,Serial12 --> core-46,Serial0*)
  120-269; (*aggregation-132,Serial6 --> core-38,Serial3*)
  90-262; (*aggregation-149,Serial13 --> edge-157,Serial5*)
  287-13; (*edge-188,Serial7 --> aggregation-183,Serial12*)
  22-288; (*aggregation-179,Serial11 --> edge-187,Serial3*)
  155-111; (*aggregation-116,Serial13 --> edge-125,Serial4*)
  210-19; (*aggregation-81,Serial12 --> edge-92,Serial1*)
  298-49; (*aggregation-307,Serial11 --> edge-315,Serial3*)
  93-260; (*aggregation-146,Serial11 --> edge-155,Serial2*)
  260-76; (*edge-155,Serial7 --> aggregation-151,Serial11*)
  90-234; (*aggregation-149,Serial0 --> core-40,Serial10*)
  0-221; (*edge-88,Serial7 --> aggregation-87,Serial8*)
  233-259; (*aggregation-211,Serial6 --> core-30,Serial15*)
  262-92; (*edge-157,Serial3 --> aggregation-147,Serial13*)
  143-127; (*aggregation-243,Serial11 --> edge-251,Serial3*)
  279-81; (*aggregation-209,Serial0 --> core-8,Serial13*)
  170-9; (*core-61,Serial14 --> aggregation-199,Serial13*)
  287-38; (*edge-188,Serial5 --> aggregation-181,Serial12*)
  235-85; (*edge-286,Serial4 --> aggregation-276,Serial14*)
  288-36; (*edge-187,Serial0 --> aggregation-176,Serial11*)
  74-186; (*core-6,Serial9 --> aggregation-224,Serial6*)
  280-154; (*edge-72,Serial1 --> aggregation-65,Serial2*)
  129-112; (*aggregation-246,Serial9 --> edge-249,Serial6*)
  214-108; (*edge-141,Serial7 --> aggregation-135,Serial5*)
  41-319; (*edge-317,Serial4 --> aggregation-308,Serial13*)
  109-160; (*edge-126,Serial6 --> aggregation-118,Serial14*)
  125-280; (*aggregation-70,Serial0 --> edge-72,Serial6*)
  232-73; (*edge-287,Serial1 --> aggregation-273,Serial15*)
  116-203; (*aggregation-134,Serial11 --> core-51,Serial14*)
  180-142; (*aggregation-97,Serial8 --> edge-110,Serial1*)
  309-59; (*core-19,Serial8 --> aggregation-290,Serial11*)
  158-111; (*aggregation-113,Serial13 --> edge-125,Serial1*)
  301-49; (*aggregation-304,Serial11 --> edge-315,Serial0*)
  3-253; (*aggregation-196,Serial2 --> core-34,Serial11*)
  180-304; (*aggregation-97,Serial15 --> core-10,Serial2*)
  38-295; (*aggregation-181,Serial14 --> edge-190,Serial5*)
  190-146; (*aggregation-228,Serial9 --> edge-233,Serial4*)
  211-253; (*aggregation-84,Serial2 --> core-34,Serial10*)
  208-310; (*aggregation-82,Serial2 --> core-18,Serial2*)
  221-218; (*aggregation-87,Serial7 --> core-57,Serial5*)
  219-0; (*aggregation-86,Serial8 --> edge-88,Serial6*)
  6-28; (*aggregation-194,Serial2 --> edge-202,Serial2*)
  312-153; (*core-17,Serial1 --> aggregation-66,Serial9*)
  299-44; (*aggregation-306,Serial14 --> edge-318,Serial2*)
  308-13; (*edge-184,Serial7 --> aggregation-183,Serial8*)
  56-242; (*aggregation-165,Serial8 --> edge-174,Serial5*)
  48-248; (*aggregation-162,Serial2 --> edge-170,Serial2*)
  52-237; (*aggregation-160,Serial8 --> edge-168,Serial0*)
  12-24; (*aggregation-197,Serial8 --> edge-206,Serial5*)
  253-205; (*core-34,Serial1 --> aggregation-100,Serial2*)
  277-152; (*edge-75,Serial0 --> aggregation-64,Serial11*)
  152-277; (*aggregation-64,Serial11 --> edge-75,Serial0*)
  100-270; (*aggregation-260,Serial5 --> core-37,Serial15*)
  18-220; (*edge-91,Serial5 --> aggregation-85,Serial11*)
  116-195; (*aggregation-134,Serial0 --> edge-136,Serial6*)
  189-139; (*aggregation-229,Serial2 --> edge-232,Serial5*)
  267-211; (*core-39,Serial10 --> aggregation-84,Serial7*)
  16-31; (*aggregation-192,Serial12 --> edge-204,Serial0*)
  281-51; (*aggregation-311,Serial10 --> edge-314,Serial7*)
  87-297; (*aggregation-275,Serial4 --> core-24,Serial8*)
  130-201; (*aggregation-245,Serial14 --> edge-254,Serial5*)
  285-89; (*core-20,Serial0 --> aggregation-274,Serial4*)
  282-184; (*core-23,Serial10 --> aggregation-226,Serial15*)
  225-88; (*aggregation-212,Serial14 --> edge-222,Serial4*)
  133-195; (*aggregation-128,Serial8 --> edge-136,Serial0*)
  274-75; (*edge-280,Serial0 --> aggregation-272,Serial8*)
  268-61; (*edge-284,Serial7 --> aggregation-279,Serial12*)
  283-208; (*core-22,Serial2 --> aggregation-82,Serial6*)
  299-282; (*aggregation-306,Serial7 --> core-23,Serial14*)
  49-300; (*edge-315,Serial1 --> aggregation-305,Serial11*)
  119-156; (*edge-124,Serial3 --> aggregation-115,Serial12*)
  87-274; (*aggregation-275,Serial8 --> edge-280,Serial3*)
  179-100; (*edge-270,Serial4 --> aggregation-260,Serial14*)
  85-264; (*aggregation-276,Serial11 --> edge-283,Serial4*)
  170-54; (*core-61,Serial13 --> aggregation-167,Serial13*)
  230-29; (*core-42,Serial12 --> aggregation-293,Serial15*)
  261-102; (*edge-154,Serial0 --> aggregation-144,Serial10*)
  196-79; (*core-54,Serial4 --> aggregation-150,Serial6*)
  281-218; (*aggregation-311,Serial7 --> core-57,Serial10*)
  286-47; (*aggregation-310,Serial8 --> edge-312,Serial6*)
  220-247; (*aggregation-85,Serial6 --> core-46,Serial11*)
  34-293; (*aggregation-177,Serial15 --> edge-191,Serial1*)
  113-135; (*edge-248,Serial0 --> aggregation-240,Serial8*)
  163-98; (*edge-268,Serial5 --> aggregation-261,Serial6*)
  66-166; (*core-2,Serial10 --> aggregation-112,Serial2*)
  103-218; (*aggregation-263,Serial9 --> core-57,Serial0*)
  76-218; (*aggregation-151,Serial7 --> core-57,Serial6*)
  113-147; (*edge-248,Serial1 --> aggregation-241,Serial8*)
  48-312; (*aggregation-162,Serial9 --> core-17,Serial4*)
  258-143; (*core-31,Serial9 --> aggregation-243,Serial1*)
  103-165; (*aggregation-263,Serial1 --> edge-265,Serial7*)
  175-146; (*aggregation-230,Serial1 --> edge-233,Serial6*)
  26-9; (*edge-203,Serial7 --> aggregation-199,Serial3*)
  88-250; (*edge-222,Serial0 --> aggregation-208,Serial14*)
  35-6; (*edge-201,Serial2 --> aggregation-194,Serial1*)
  210-81; (*aggregation-81,Serial0 --> core-8,Serial3*)
  276-125; (*edge-74,Serial6 --> aggregation-70,Serial2*)
  213-290; (*aggregation-83,Serial3 --> core-29,Serial1*)
  39-269; (*aggregation-180,Serial6 --> core-38,Serial8*)
  154-304; (*aggregation-65,Serial15 --> core-10,Serial4*)
  230-12; (*core-42,Serial1 --> aggregation-197,Serial15*)
  206-14; (*core-50,Serial2 --> aggregation-182,Serial2*)
  19-208; (*edge-92,Serial2 --> aggregation-82,Serial12*)
  63-198; (*aggregation-278,Serial5 --> core-53,Serial10*)
  158-302; (*aggregation-113,Serial4 --> core-12,Serial12*)
  210-1; (*aggregation-81,Serial9 --> edge-89,Serial1*)
  63-200; (*aggregation-278,Serial4 --> core-52,Serial10*)
  291-104; (*core-28,Serial7 --> aggregation-259,Serial12*)
  237-57; (*edge-168,Serial4 --> aggregation-164,Serial8*)
  72-36; (*core-7,Serial14 --> aggregation-176,Serial7*)
  153-312; (*aggregation-66,Serial9 --> core-17,Serial1*)
  183-67; (*aggregation-98,Serial3 --> edge-107,Serial2*)
  230-318; (*core-42,Serial9 --> aggregation-309,Serial2*)
  177-91; (*edge-153,Serial4 --> aggregation-148,Serial9*)
  222-170; (*aggregation-215,Serial3 --> core-61,Serial8*)
  133-209; (*aggregation-128,Serial12 --> edge-140,Serial0*)
  87-266; (*aggregation-275,Serial13 --> edge-285,Serial3*)
  200-286; (*core-52,Serial8 --> aggregation-310,Serial4*)
  243-223; (*core-49,Serial1 --> aggregation-214,Serial1*)
  182-71; (*aggregation-99,Serial0 --> edge-104,Serial3*)
  32-10; (*aggregation-292,Serial12 --> edge-300,Serial4*)
  241-56; (*edge-175,Serial5 --> aggregation-165,Serial9*)
  224-230; (*aggregation-213,Serial2 --> core-42,Serial7*)
  68-152; (*core-5,Serial0 --> aggregation-64,Serial5*)
  93-263; (*aggregation-146,Serial12 --> edge-156,Serial2*)
  182-294; (*aggregation-99,Serial10 --> core-26,Serial2*)
  187-70; (*aggregation-103,Serial5 --> edge-109,Serial7*)
  223-96; (*aggregation-214,Serial11 --> edge-219,Serial6*)
  123-192; (*aggregation-71,Serial8 --> core-56,Serial11*)
  206-116; (*core-50,Serial14 --> aggregation-134,Serial10*)
  227-151; (*core-44,Serial14 --> aggregation-69,Serial11*)
  300-315; (*aggregation-305,Serial7 --> core-15,Serial1*)
  32-271; (*aggregation-292,Serial4 --> core-36,Serial6*)
  193-133; (*edge-137,Serial0 --> aggregation-128,Serial9*)
  146-184; (*edge-233,Serial2 --> aggregation-226,Serial1*)
  249-123; (*edge-77,Serial7 --> aggregation-71,Serial5*)
  87-272; (*aggregation-275,Serial9 --> edge-281,Serial3*)
  98-168; (*aggregation-261,Serial2 --> edge-264,Serial5*)
  77-202; (*edge-106,Serial5 --> aggregation-101,Serial4*)
  310-236; (*core-18,Serial6 --> aggregation-210,Serial2*)
  9-192; (*aggregation-199,Serial8 --> core-56,Serial14*)
  225-86; (*aggregation-212,Serial15 --> edge-223,Serial4*)
  175-244; (*aggregation-230,Serial8 --> core-48,Serial6*)
  271-32; (*core-36,Serial6 --> aggregation-292,Serial4*)
  296-298; (*core-25,Serial0 --> aggregation-307,Serial7*)
  281-215; (*aggregation-311,Serial0 --> core-59,Serial10*)
  188-142; (*aggregation-102,Serial6 --> edge-110,Serial6*)
  186-138; (*aggregation-224,Serial13 --> edge-237,Serial0*)
  302-132; (*core-12,Serial0 --> aggregation-129,Serial1*)
  75-62; (*aggregation-272,Serial0 --> core-0,Serial3*)
  176-97; (*edge-271,Serial6 --> aggregation-262,Serial7*)
  245-161; (*core-47,Serial15 --> aggregation-117,Serial7*)
  247-220; (*core-46,Serial11 --> aggregation-85,Serial6*)
  247-151; (*core-46,Serial14 --> aggregation-69,Serial12*)
  317-101; (*core-13,Serial5 --> aggregation-145,Serial5*)
  285-110; (*core-20,Serial3 --> aggregation-130,Serial12*)
  175-203; (*aggregation-230,Serial11 --> core-51,Serial6*)
  160-115; (*aggregation-118,Serial10 --> edge-122,Serial6*)
  52-66; (*aggregation-160,Serial2 --> core-2,Serial7*)
  170-25; (*core-61,Serial3 --> aggregation-295,Serial13*)
  78-132; (*core-9,Serial0 --> aggregation-129,Serial13*)
  262-93; (*edge-157,Serial2 --> aggregation-146,Serial13*)
  79-261; (*aggregation-150,Serial10 --> edge-154,Serial6*)
  282-153; (*core-23,Serial1 --> aggregation-66,Serial15*)
  44-299; (*edge-318,Serial2 --> aggregation-306,Serial14*)
  114-155; (*edge-127,Serial4 --> aggregation-116,Serial15*)
  56-241; (*aggregation-165,Serial9 --> edge-175,Serial5*)
  161-148; (*aggregation-117,Serial8 --> edge-120,Serial5*)
  59-282; (*aggregation-290,Serial15 --> core-23,Serial8*)
  127-128; (*edge-251,Serial7 --> aggregation-247,Serial11*)
  78-210; (*core-9,Serial3 --> aggregation-81,Serial1*)
  193-132; (*edge-137,Serial1 --> aggregation-129,Serial3*)
  278-153; (*edge-76,Serial2 --> aggregation-66,Serial4*)
  184-134; (*aggregation-226,Serial4 --> edge-236,Serial2*)
  131-175; (*edge-239,Serial6 --> aggregation-230,Serial7*)
  310-145; (*core-18,Serial7 --> aggregation-242,Serial2*)
  302-40; (*core-12,Serial10 --> aggregation-289,Serial1*)
  104-163; (*aggregation-259,Serial4 --> edge-268,Serial3*)
  205-269; (*aggregation-100,Serial6 --> core-38,Serial1*)
  173-221; (*core-60,Serial5 --> aggregation-87,Serial1*)
  215-187; (*core-59,Serial4 --> aggregation-103,Serial11*)
  247-118; (*core-46,Serial4 --> aggregation-133,Serial12*)
  42-286; (*edge-316,Serial6 --> aggregation-310,Serial12*)
  116-200; (*aggregation-134,Serial12 --> core-52,Serial14*)
  135-68; (*aggregation-240,Serial5 --> core-5,Serial2*)
  145-204; (*aggregation-242,Serial13 --> edge-253,Serial2*)
  216-17; (*aggregation-80,Serial15 --> edge-95,Serial0*)
  22-287; (*aggregation-179,Serial12 --> edge-188,Serial3*)
  310-183; (*core-18,Serial9 --> aggregation-98,Serial10*)
  224-95; (*aggregation-213,Serial9 --> edge-217,Serial5*)
  52-239; (*aggregation-160,Serial12 --> edge-172,Serial0*)
  106-174; (*aggregation-257,Serial4 --> edge-266,Serial1*)
  211-255; (*aggregation-84,Serial1 --> core-33,Serial10*)
  279-315; (*aggregation-209,Serial7 --> core-15,Serial13*)
  158-119; (*aggregation-113,Serial12 --> edge-124,Serial1*)
  301-51; (*aggregation-304,Serial10 --> edge-314,Serial0*)
  100-165; (*aggregation-260,Serial9 --> edge-265,Serial4*)
  156-258; (*aggregation-115,Serial1 --> core-31,Serial12*)
  201-147; (*edge-254,Serial1 --> aggregation-241,Serial14*)
  48-285; (*aggregation-162,Serial12 --> core-20,Serial4*)
  69-53; (*core-4,Serial11 --> aggregation-288,Serial4*)
  242-54; (*edge-174,Serial7 --> aggregation-167,Serial6*)
  62-52; (*core-0,Serial7 --> aggregation-160,Serial0*)
  286-42; (*aggregation-310,Serial12 --> edge-316,Serial6*)
  70-187; (*edge-109,Serial7 --> aggregation-103,Serial5*)
  149-269; (*aggregation-68,Serial6 --> core-38,Serial0*)
  206-55; (*core-50,Serial15 --> aggregation-166,Serial10*)
  310-105; (*core-18,Serial12 --> aggregation-258,Serial10*)
  70-205; (*edge-109,Serial4 --> aggregation-100,Serial13*)
  72-102; (*core-7,Serial6 --> aggregation-144,Serial7*)
  7-25; (*edge-303,Serial7 --> aggregation-295,Serial7*)
  78-158; (*core-9,Serial12 --> aggregation-113,Serial1*)
  64-181; (*core-3,Serial5 --> aggregation-96,Serial3*)
  101-303; (*aggregation-145,Serial3 --> core-11,Serial5*)
  181-65; (*aggregation-96,Serial12 --> edge-108,Serial0*)
  287-14; (*edge-188,Serial6 --> aggregation-182,Serial12*)
  260-79; (*edge-155,Serial6 --> aggregation-150,Serial11*)
  302-8; (*core-12,Serial7 --> aggregation-193,Serial1*)
  220-20; (*aggregation-85,Serial13 --> edge-93,Serial5*)
  153-276; (*aggregation-66,Serial2 --> edge-74,Serial2*)
  155-109; (*aggregation-116,Serial14 --> edge-126,Serial4*)
  298-42; (*aggregation-307,Serial12 --> edge-316,Serial3*)
  156-291; (*aggregation-115,Serial2 --> core-28,Serial12*)
  291-191; (*core-28,Serial6 --> aggregation-227,Serial12*)
  0-219; (*edge-88,Serial6 --> aggregation-86,Serial8*)
  36-311; (*aggregation-176,Serial10 --> edge-186,Serial0*)
  286-203; (*aggregation-310,Serial3 --> core-51,Serial8*)
  206-125; (*core-50,Serial7 --> aggregation-70,Serial10*)
  153-284; (*aggregation-66,Serial13 --> core-21,Serial1*)
  214-120; (*edge-141,Serial4 --> aggregation-132,Serial13*)
  69-52; (*core-4,Serial7 --> aggregation-160,Serial4*)
  204-143; (*edge-253,Serial3 --> aggregation-243,Serial13*)
  257-91; (*core-32,Serial12 --> aggregation-148,Serial0*)
  4-58; (*edge-301,Serial3 --> aggregation-291,Serial5*)
  133-74; (*aggregation-128,Serial6 --> core-6,Serial1*)
  235-87; (*edge-286,Serial3 --> aggregation-275,Serial14*)
  230-56; (*core-42,Serial0 --> aggregation-165,Serial15*)
  304-300; (*core-10,Serial1 --> aggregation-305,Serial2*)
  100-162; (*aggregation-260,Serial13 --> edge-269,Serial4*)
  253-225; (*core-34,Serial7 --> aggregation-212,Serial2*)
  284-183; (*core-21,Serial9 --> aggregation-98,Serial13*)
  168-97; (*edge-264,Serial6 --> aggregation-262,Serial0*)
  86-224; (*edge-223,Serial5 --> aggregation-213,Serial15*)
  257-120; (*core-32,Serial3 --> aggregation-132,Serial0*)
  306-27; (*edge-297,Serial6 --> aggregation-294,Serial1*)
  255-190; (*core-33,Serial4 --> aggregation-228,Serial1*)
  108-170; (*aggregation-135,Serial13 --> core-61,Serial12*)
  239-46; (*edge-172,Serial3 --> aggregation-163,Serial4*)
  46-296; (*aggregation-163,Serial9 --> core-25,Serial4*)
  166-74; (*aggregation-112,Serial6 --> core-6,Serial10*)
  217-222; (*core-58,Serial8 --> aggregation-215,Serial5*)
  70-188; (*edge-109,Serial6 --> aggregation-102,Serial5*)
  270-149; (*core-37,Serial0 --> aggregation-68,Serial5*)
  162-106; (*edge-269,Serial1 --> aggregation-257,Serial7*)
  291-150; (*core-28,Serial10 --> aggregation-67,Serial12*)
  306-32; (*edge-297,Serial4 --> aggregation-292,Serial9*)
  128-126; (*aggregation-247,Serial12 --> edge-252,Serial7*)
  163-105; (*edge-268,Serial2 --> aggregation-258,Serial4*)
  128-207; (*aggregation-247,Serial15 --> edge-255,Serial7*)
  226-189; (*core-45,Serial2 --> aggregation-229,Serial0*)
  229-151; (*core-43,Serial14 --> aggregation-69,Serial1*)
  200-219; (*core-52,Serial13 --> aggregation-86,Serial4*)
  290-58; (*core-29,Serial11 --> aggregation-291,Serial13*)
  204-129; (*edge-253,Serial6 --> aggregation-246,Serial13*)
  265-87; (*edge-282,Serial3 --> aggregation-275,Serial10*)
  62-152; (*core-0,Serial0 --> aggregation-64,Serial0*)
  245-130; (*core-47,Serial8 --> aggregation-245,Serial7*)
  10-40; (*edge-300,Serial1 --> aggregation-289,Serial6*)
  115-157; (*edge-122,Serial2 --> aggregation-114,Serial10*)
  175-194; (*aggregation-230,Serial15 --> core-55,Serial6*)
  270-120; (*core-37,Serial3 --> aggregation-132,Serial5*)
  3-30; (*aggregation-196,Serial13 --> edge-205,Serial4*)
  14-243; (*aggregation-182,Serial1 --> core-49,Serial2*)
  231-161; (*core-41,Serial15 --> aggregation-117,Serial1*)
  33-314; (*aggregation-178,Serial0 --> core-16,Serial13*)
  246-56; (*edge-171,Serial5 --> aggregation-165,Serial5*)
  319-253; (*aggregation-308,Serial2 --> core-34,Serial9*)
  79-243; (*aggregation-150,Serial1 --> core-49,Serial4*)
  197-118; (*edge-139,Serial5 --> aggregation-133,Serial5*)
  17-216; (*edge-95,Serial0 --> aggregation-80,Serial15*)
  15-211; (*edge-90,Serial4 --> aggregation-84,Serial10*)
  301-69; (*aggregation-304,Serial4 --> core-4,Serial13*)
  282-48; (*core-23,Serial4 --> aggregation-162,Serial15*)
  192-222; (*core-56,Serial8 --> aggregation-215,Serial4*)
  259-87; (*core-30,Serial8 --> aggregation-275,Serial6*)
  72-53; (*core-7,Serial11 --> aggregation-288,Serial7*)
  57-238; (*aggregation-164,Serial13 --> edge-173,Serial4*)
  223-86; (*aggregation-214,Serial15 --> edge-223,Serial6*)
  216-69; (*aggregation-80,Serial4 --> core-4,Serial12*)
  132-78; (*aggregation-129,Serial13 --> core-9,Serial0*)
  39-308; (*aggregation-180,Serial8 --> edge-184,Serial4*)
  227-220; (*core-44,Serial11 --> aggregation-85,Serial4*)
  100-271; (*aggregation-260,Serial4 --> core-36,Serial15*)
  234-202; (*core-40,Serial13 --> aggregation-101,Serial10*)
  218-103; (*core-57,Serial0 --> aggregation-263,Serial9*)
  154-275; (*aggregation-65,Serial3 --> edge-73,Serial1*)
  40-317; (*aggregation-289,Serial11 --> core-13,Serial10*)
  167-128; (*core-62,Serial2 --> aggregation-247,Serial6*)
  175-200; (*aggregation-230,Serial12 --> core-52,Serial6*)
  319-270; (*aggregation-308,Serial5 --> core-37,Serial9*)
  269-3; (*core-38,Serial11 --> aggregation-196,Serial6*)
  273-85; (*core-35,Serial14 --> aggregation-276,Serial3*)
  291-143; (*core-28,Serial9 --> aggregation-243,Serial2*)
  294-122; (*core-26,Serial3 --> aggregation-131,Serial10*)
  50-304; (*aggregation-161,Serial15 --> core-10,Serial6*)
  159-167; (*aggregation-119,Serial6 --> core-62,Serial9*)
  167-221; (*core-62,Serial5 --> aggregation-87,Serial6*)
  81-101; (*core-8,Serial5 --> aggregation-145,Serial0*)
  45-319; (*edge-313,Serial4 --> aggregation-308,Serial9*)
  48-310; (*aggregation-162,Serial10 --> core-18,Serial4*)
  136-184; (*edge-235,Serial2 --> aggregation-226,Serial3*)
  71-187; (*edge-104,Serial7 --> aggregation-103,Serial0*)
  106-304; (*aggregation-257,Serial15 --> core-10,Serial9*)
  206-129; (*core-50,Serial11 --> aggregation-246,Serial2*)
  56-248; (*aggregation-165,Serial4 --> edge-170,Serial5*)
  56-238; (*aggregation-165,Serial7 --> edge-173,Serial5*)
  234-29; (*core-40,Serial12 --> aggregation-293,Serial10*)
  130-230; (*aggregation-245,Serial2 --> core-42,Serial8*)
  152-278; (*aggregation-64,Serial12 --> edge-76,Serial0*)
  198-11; (*core-53,Serial0 --> aggregation-198,Serial13*)
  223-84; (*aggregation-214,Serial12 --> edge-220,Serial6*)
  40-81; (*aggregation-289,Serial10 --> core-8,Serial10*)
  294-22; (*core-26,Serial14 --> aggregation-179,Serial5*)
  103-217; (*aggregation-263,Serial10 --> core-58,Serial0*)
  226-83; (*core-45,Serial6 --> aggregation-277,Serial5*)
  208-284; (*aggregation-82,Serial5 --> core-21,Serial2*)
  220-1; (*aggregation-85,Serial9 --> edge-89,Serial5*)
  218-9; (*core-57,Serial14 --> aggregation-199,Serial9*)
  16-28; (*aggregation-192,Serial10 --> edge-202,Serial0*)
  45-300; (*edge-313,Serial1 --> aggregation-305,Serial9*)
  13-293; (*aggregation-183,Serial15 --> edge-191,Serial7*)
  154-278; (*aggregation-65,Serial6 --> edge-76,Serial1*)
  216-72; (*aggregation-80,Serial7 --> core-7,Serial12*)
  309-6; (*core-19,Serial5 --> aggregation-194,Serial11*)
  260-93; (*edge-155,Serial2 --> aggregation-146,Serial11*)
  93-314; (*aggregation-146,Serial0 --> core-16,Serial15*)
  298-294; (*aggregation-307,Serial5 --> core-26,Serial0*)
  60-133; (*core-1,Serial1 --> aggregation-128,Serial1*)
  257-141; (*core-32,Serial13 --> aggregation-244,Serial0*)
  3-273; (*aggregation-196,Serial3 --> core-35,Serial11*)
  3-269; (*aggregation-196,Serial6 --> core-38,Serial11*)
  17-213; (*edge-95,Serial3 --> aggregation-83,Serial15*)
  78-50; (*core-9,Serial6 --> aggregation-161,Serial13*)
  200-14; (*core-52,Serial2 --> aggregation-182,Serial4*)
  282-93; (*core-23,Serial15 --> aggregation-146,Serial7*)
  297-22; (*core-24,Serial14 --> aggregation-179,Serial4*)
  117-158; (*edge-121,Serial1 --> aggregation-113,Serial9*)
  249-151; (*edge-77,Serial5 --> aggregation-69,Serial7*)
  24-3; (*edge-206,Serial4 --> aggregation-196,Serial14*)
  253-155; (*core-34,Serial5 --> aggregation-116,Serial2*)
  315-8; (*core-15,Serial7 --> aggregation-193,Serial14*)
  15-210; (*edge-90,Serial1 --> aggregation-81,Serial10*)
  61-164; (*aggregation-279,Serial2 --> core-63,Serial1*)
  152-252; (*aggregation-64,Serial15 --> edge-79,Serial0*)
  310-299; (*core-18,Serial14 --> aggregation-306,Serial2*)
  86-279; (*edge-223,Serial1 --> aggregation-209,Serial15*)
  81-8; (*core-8,Serial7 --> aggregation-193,Serial10*)
  239-50; (*edge-172,Serial1 --> aggregation-161,Serial6*)
  97-198; (*aggregation-262,Serial13 --> core-53,Serial12*)
  61-272; (*aggregation-279,Serial9 --> edge-281,Serial7*)
  78-73; (*core-9,Serial15 --> aggregation-273,Serial1*)
  253-100; (*core-34,Serial15 --> aggregation-260,Serial2*)
  196-188; (*core-54,Serial5 --> aggregation-102,Serial14*)
  280-125; (*edge-72,Serial6 --> aggregation-70,Serial0*)
  196-160; (*core-54,Serial9 --> aggregation-118,Serial6*)
  115-161; (*edge-122,Serial5 --> aggregation-117,Serial10*)
  260-90; (*edge-155,Serial5 --> aggregation-149,Serial11*)
  309-153; (*core-19,Serial1 --> aggregation-66,Serial11*)
  72-135; (*core-7,Serial2 --> aggregation-240,Serial7*)
  36-68; (*aggregation-176,Serial5 --> core-5,Serial14*)
  247-12; (*core-46,Serial1 --> aggregation-197,Serial12*)
  12-28; (*aggregation-197,Serial4 --> edge-202,Serial5*)
  60-186; (*core-1,Serial9 --> aggregation-224,Serial1*)
  170-108; (*core-61,Serial12 --> aggregation-135,Serial13*)
  191-146; (*aggregation-227,Serial1 --> edge-233,Serial3*)
  31-8; (*edge-204,Serial1 --> aggregation-193,Serial6*)
  242-55; (*edge-174,Serial6 --> aggregation-166,Serial6*)
  291-5; (*core-28,Serial5 --> aggregation-195,Serial12*)
  18-219; (*edge-91,Serial6 --> aggregation-86,Serial11*)
  145-309; (*aggregation-242,Serial3 --> core-19,Serial7*)
  123-249; (*aggregation-71,Serial5 --> edge-77,Serial7*)
  105-284; (*aggregation-258,Serial13 --> core-21,Serial12*)
  69-216; (*core-4,Serial12 --> aggregation-80,Serial4*)
  24-5; (*edge-206,Serial3 --> aggregation-195,Serial6*)
  136-186; (*edge-235,Serial0 --> aggregation-224,Serial11*)
  279-88; (*aggregation-209,Serial14 --> edge-222,Serial1*)
  140-182; (*edge-111,Serial3 --> aggregation-99,Serial7*)
  270-39; (*core-37,Serial8 --> aggregation-180,Serial5*)
  23-12; (*edge-207,Serial5 --> aggregation-197,Serial9*)
  73-274; (*aggregation-273,Serial8 --> edge-280,Serial1*)
  75-268; (*aggregation-272,Serial12 --> edge-284,Serial0*)
  284-33; (*core-21,Serial13 --> aggregation-178,Serial5*)
  4-27; (*edge-301,Serial6 --> aggregation-294,Serial5*)
  243-188; (*core-49,Serial5 --> aggregation-102,Serial9*)
  151-252; (*aggregation-69,Serial9 --> edge-79,Serial5*)
  106-317; (*aggregation-257,Serial11 --> core-13,Serial9*)
  187-192; (*aggregation-103,Serial8 --> core-56,Serial4*)
  38-288; (*aggregation-181,Serial11 --> edge-187,Serial5*)
  56-234; (*aggregation-165,Serial10 --> core-40,Serial0*)
  116-169; (*aggregation-134,Serial7 --> edge-143,Serial6*)
  152-72; (*aggregation-64,Serial7 --> core-7,Serial0*)
  221-164; (*aggregation-87,Serial2 --> core-63,Serial5*)
  57-271; (*aggregation-164,Serial4 --> core-36,Serial2*)
  137-190; (*edge-238,Serial4 --> aggregation-228,Serial14*)
  314-93; (*core-16,Serial15 --> aggregation-146,Serial0*)
  180-71; (*aggregation-97,Serial2 --> edge-104,Serial1*)
  203-175; (*core-51,Serial6 --> aggregation-230,Serial11*)
  79-177; (*aggregation-150,Serial9 --> edge-153,Serial6*)
  102-72; (*aggregation-144,Serial7 --> core-7,Serial6*)
  202-65; (*aggregation-101,Serial6 --> edge-108,Serial5*)
  163-107; (*edge-268,Serial0 --> aggregation-256,Serial12*)
  188-206; (*aggregation-102,Serial10 --> core-50,Serial5*)
  172-173; (*aggregation-231,Serial12 --> core-60,Serial15*)
  160-117; (*aggregation-118,Serial9 --> edge-121,Serial6*)
  315-40; (*core-15,Serial10 --> aggregation-289,Serial14*)
  316-50; (*core-14,Serial6 --> aggregation-161,Serial12*)
  304-210; (*core-10,Serial3 --> aggregation-81,Serial2*)
  227-12; (*core-44,Serial1 --> aggregation-197,Serial11*)
  79-200; (*aggregation-150,Serial4 --> core-52,Serial4*)
  83-235; (*aggregation-277,Serial14 --> edge-286,Serial5*)
  137-191; (*edge-238,Serial3 --> aggregation-227,Serial6*)
  102-254; (*aggregation-144,Serial15 --> edge-159,Serial0*)
  125-196; (*aggregation-70,Serial14 --> core-54,Serial7*)
  227-224; (*core-44,Serial7 --> aggregation-213,Serial4*)
  184-284; (*aggregation-226,Serial13 --> core-21,Serial10*)
  147-78; (*aggregation-241,Serial1 --> core-9,Serial14*)
  308-14; (*edge-184,Serial6 --> aggregation-182,Serial8*)
  40-304; (*aggregation-289,Serial15 --> core-10,Serial10*)
  133-72; (*aggregation-128,Serial7 --> core-7,Serial1*)
  250-62; (*aggregation-208,Serial0 --> core-0,Serial15*)
  243-55; (*core-49,Serial15 --> aggregation-166,Serial9*)
  105-162; (*aggregation-258,Serial5 --> edge-269,Serial2*)
  219-19; (*aggregation-86,Serial12 --> edge-92,Serial6*)
  231-38; (*core-41,Serial5 --> aggregation-181,Serial1*)
  154-302; (*aggregation-65,Serial1 --> core-12,Serial4*)
  253-319; (*core-34,Serial9 --> aggregation-308,Serial2*)
  130-226; (*aggregation-245,Serial5 --> core-45,Serial8*)
  134-185; (*edge-236,Serial1 --> aggregation-225,Serial6*)
  214-116; (*edge-141,Serial6 --> aggregation-134,Serial5*)
  101-81; (*aggregation-145,Serial0 --> core-8,Serial5*)
  278-150; (*edge-76,Serial3 --> aggregation-67,Serial4*)
  84-223; (*edge-220,Serial6 --> aggregation-214,Serial12*)
  255-57; (*core-33,Serial2 --> aggregation-164,Serial1*)
  114-158; (*edge-127,Serial1 --> aggregation-113,Serial15*)
  29-231; (*aggregation-293,Serial13 --> core-41,Serial12*)
  180-302; (*aggregation-97,Serial1 --> core-12,Serial2*)
  110-314; (*aggregation-130,Serial8 --> core-16,Serial3*)
  235-63; (*edge-286,Serial6 --> aggregation-278,Serial14*)
  104-294; (*aggregation-259,Serial10 --> core-26,Serial7*)
  147-201; (*aggregation-241,Serial14 --> edge-254,Serial1*)
  14-289; (*aggregation-182,Serial13 --> edge-189,Serial6*)
  131-189; (*edge-239,Serial5 --> aggregation-229,Serial9*)
  126-141; (*edge-252,Serial4 --> aggregation-244,Serial12*)
  185-78; (*aggregation-225,Serial13 --> core-9,Serial8*)
  233-99; (*aggregation-211,Serial8 --> edge-216,Serial3*)
  14-200; (*aggregation-182,Serial4 --> core-52,Serial2*)
  48-284; (*aggregation-162,Serial13 --> core-21,Serial4*)
  124-145; (*edge-250,Serial2 --> aggregation-242,Serial10*)
  266-85; (*edge-285,Serial4 --> aggregation-276,Serial13*)
  299-45; (*aggregation-306,Serial9 --> edge-313,Serial2*)
  90-260; (*aggregation-149,Serial11 --> edge-155,Serial5*)
  264-89; (*edge-283,Serial2 --> aggregation-274,Serial11*)
  62-301; (*core-0,Serial13 --> aggregation-304,Serial0*)
  27-10; (*aggregation-294,Serial4 --> edge-300,Serial6*)
  155-121; (*aggregation-116,Serial11 --> edge-123,Serial4*)
  106-171; (*aggregation-257,Serial5 --> edge-267,Serial1*)
  141-113; (*aggregation-244,Serial8 --> edge-248,Serial4*)
  186-144; (*aggregation-224,Serial10 --> edge-234,Serial0*)
  150-277; (*aggregation-67,Serial3 --> edge-75,Serial3*)
  171-100; (*edge-267,Serial4 --> aggregation-260,Serial11*)
  149-280; (*aggregation-68,Serial8 --> edge-72,Serial4*)
  161-227; (*aggregation-117,Serial4 --> core-44,Serial15*)
  135-74; (*aggregation-240,Serial6 --> core-6,Serial2*)
  278-149; (*edge-76,Serial4 --> aggregation-68,Serial12*)
  301-60; (*aggregation-304,Serial1 --> core-1,Serial13*)
  9-35; (*aggregation-199,Serial1 --> edge-201,Serial7*)
  265-75; (*edge-282,Serial0 --> aggregation-272,Serial10*)
  6-309; (*aggregation-194,Serial11 --> core-19,Serial5*)
  192-187; (*core-56,Serial4 --> aggregation-103,Serial8*)
  139-172; (*edge-232,Serial7 --> aggregation-231,Serial0*)
  173-172; (*core-60,Serial15 --> aggregation-231,Serial12*)
  189-138; (*aggregation-229,Serial7 --> edge-237,Serial5*)
  197-110; (*edge-139,Serial2 --> aggregation-130,Serial3*)
  98-162; (*aggregation-261,Serial7 --> edge-269,Serial5*)
  49-281; (*edge-315,Serial7 --> aggregation-311,Serial11*)
  36-313; (*aggregation-176,Serial9 --> edge-185,Serial0*)
  173-128; (*core-60,Serial2 --> aggregation-247,Serial1*)
  83-226; (*aggregation-277,Serial5 --> core-45,Serial6*)
  161-111; (*aggregation-117,Serial13 --> edge-125,Serial5*)
  319-49; (*aggregation-308,Serial11 --> edge-315,Serial4*)
  286-43; (*aggregation-310,Serial15 --> edge-319,Serial6*)
  159-114; (*aggregation-119,Serial15 --> edge-127,Serial7*)
  165-103; (*edge-265,Serial7 --> aggregation-263,Serial1*)
  46-294; (*aggregation-163,Serial10 --> core-26,Serial4*)
  74-75; (*core-6,Serial3 --> aggregation-272,Serial6*)
  213-21; (*aggregation-83,Serial14 --> edge-94,Serial3*)
  229-29; (*core-43,Serial12 --> aggregation-293,Serial1*)
  305-25; (*edge-298,Serial7 --> aggregation-295,Serial2*)
  246-48; (*edge-171,Serial2 --> aggregation-162,Serial3*)
  160-203; (*aggregation-118,Serial3 --> core-51,Serial9*)
  32-255; (*aggregation-292,Serial1 --> core-33,Serial6*)
  106-81; (*aggregation-257,Serial10 --> core-8,Serial9*)
  284-59; (*core-21,Serial8 --> aggregation-290,Serial13*)
  179-104; (*edge-270,Serial3 --> aggregation-259,Serial6*)
  87-292; (*aggregation-275,Serial0 --> core-27,Serial8*)
  192-25; (*core-56,Serial3 --> aggregation-295,Serial8*)
  90-247; (*aggregation-149,Serial6 --> core-46,Serial10*)
  155-257; (*aggregation-116,Serial0 --> core-32,Serial5*)
  155-273; (*aggregation-116,Serial3 --> core-35,Serial5*)
  262-102; (*edge-157,Serial0 --> aggregation-144,Serial13*)
  188-71; (*aggregation-102,Serial0 --> edge-104,Serial6*)
  183-310; (*aggregation-98,Serial10 --> core-18,Serial9*)
  303-154; (*core-11,Serial4 --> aggregation-65,Serial0*)
  132-199; (*aggregation-129,Serial4 --> edge-138,Serial1*)
  110-282; (*aggregation-130,Serial15 --> core-23,Serial3*)
  298-292; (*aggregation-307,Serial0 --> core-27,Serial0*)
  271-57; (*core-36,Serial2 --> aggregation-164,Serial4*)
  244-160; (*core-48,Serial9 --> aggregation-118,Serial0*)
  175-243; (*aggregation-230,Serial9 --> core-49,Serial6*)
  300-44; (*aggregation-305,Serial14 --> edge-318,Serial1*)
  97-165; (*aggregation-262,Serial1 --> edge-265,Serial6*)
  236-282; (*aggregation-210,Serial7 --> core-23,Serial6*)
  240-29; (*edge-296,Serial5 --> aggregation-293,Serial2*)
  81-40; (*core-8,Serial10 --> aggregation-289,Serial10*)
  143-259; (*aggregation-243,Serial6 --> core-30,Serial9*)
  144-175; (*edge-234,Serial6 --> aggregation-230,Serial2*)
  157-111; (*aggregation-114,Serial13 --> edge-125,Serial2*)
  300-49; (*aggregation-305,Serial11 --> edge-315,Serial1*)
  167-9; (*core-62,Serial14 --> aggregation-199,Serial14*)
  259-143; (*core-30,Serial9 --> aggregation-243,Serial6*)
  185-303; (*aggregation-225,Serial0 --> core-11,Serial8*)
  94-250; (*edge-218,Serial0 --> aggregation-208,Serial10*)
  274-87; (*edge-280,Serial3 --> aggregation-275,Serial8*)
  16-62; (*aggregation-192,Serial0 --> core-0,Serial8*)
  207-147; (*edge-255,Serial1 --> aggregation-241,Serial15*)
  195-110; (*edge-136,Serial2 --> aggregation-130,Serial0*)
  13-170; (*aggregation-183,Serial3 --> core-61,Serial7*)
  60-250; (*core-1,Serial15 --> aggregation-208,Serial1*)
  279-317; (*aggregation-209,Serial5 --> core-13,Serial13*)
  172-192; (*aggregation-231,Serial8 --> core-56,Serial15*)
  40-307; (*aggregation-289,Serial5 --> edge-299,Serial1*)
  141-127; (*aggregation-244,Serial11 --> edge-251,Serial4*)
  203-97; (*core-51,Serial12 --> aggregation-262,Serial11*)
  187-164; (*aggregation-103,Serial15 --> core-63,Serial4*)
  267-32; (*core-39,Serial6 --> aggregation-292,Serial7*)
  233-88; (*aggregation-211,Serial14 --> edge-222,Serial3*)
  175-131; (*aggregation-230,Serial7 --> edge-239,Serial6*)
  203-55; (*core-51,Serial15 --> aggregation-166,Serial11*)
  224-231; (*aggregation-213,Serial1 --> core-41,Serial7*)
  123-170; (*aggregation-71,Serial13 --> core-61,Serial11*)
  42-300; (*edge-316,Serial1 --> aggregation-305,Serial12*)
  111-156; (*edge-125,Serial3 --> aggregation-115,Serial13*)
  190-269; (*aggregation-228,Serial6 --> core-38,Serial4*)
  34-78; (*aggregation-177,Serial1 --> core-9,Serial11*)
  9-23; (*aggregation-199,Serial7 --> edge-207,Serial7*)
  55-206; (*aggregation-166,Serial10 --> core-50,Serial15*)
  36-293; (*aggregation-176,Serial15 --> edge-191,Serial0*)
  256-92; (*edge-158,Serial3 --> aggregation-147,Serial14*)
  8-315; (*aggregation-193,Serial14 --> core-15,Serial7*)
  284-208; (*core-21,Serial2 --> aggregation-82,Serial5*)
  184-138; (*aggregation-226,Serial5 --> edge-237,Serial2*)
  16-64; (*aggregation-192,Serial3 --> core-3,Serial8*)
  93-283; (*aggregation-146,Serial6 --> core-22,Serial15*)
  290-298; (*core-29,Serial0 --> aggregation-307,Serial3*)
  81-180; (*core-8,Serial2 --> aggregation-97,Serial10*)
  27-206; (*aggregation-294,Serial10 --> core-50,Serial3*)
  177-90; (*edge-153,Serial5 --> aggregation-149,Serial9*)
  314-153; (*core-16,Serial1 --> aggregation-66,Serial8*)
  219-203; (*aggregation-86,Serial3 --> core-51,Serial13*)
  148-161; (*edge-120,Serial5 --> aggregation-117,Serial8*)
  141-201; (*aggregation-244,Serial14 --> edge-254,Serial4*)
  318-230; (*aggregation-309,Serial2 --> core-42,Serial9*)
  37-6; (*edge-200,Serial2 --> aggregation-194,Serial0*)
  108-195; (*aggregation-135,Serial0 --> edge-136,Serial7*)
  255-205; (*core-33,Serial1 --> aggregation-100,Serial1*)
  294-182; (*core-26,Serial2 --> aggregation-99,Serial10*)
  123-218; (*aggregation-71,Serial9 --> core-57,Serial11*)
  25-306; (*aggregation-295,Serial1 --> edge-297,Serial7*)
  302-185; (*core-12,Serial8 --> aggregation-225,Serial1*)
  210-303; (*aggregation-81,Serial3 --> core-11,Serial3*)
  38-226; (*aggregation-181,Serial5 --> core-45,Serial5*)
  72-216; (*core-7,Serial12 --> aggregation-80,Serial7*)
  189-230; (*aggregation-229,Serial15 --> core-42,Serial2*)
  293-39; (*edge-191,Serial4 --> aggregation-180,Serial15*)
  33-283; (*aggregation-178,Serial6 --> core-22,Serial13*)
  212-133; (*edge-142,Serial0 --> aggregation-128,Serial14*)
  285-184; (*core-20,Serial10 --> aggregation-226,Serial12*)
  113-129; (*edge-248,Serial6 --> aggregation-246,Serial8*)
  277-125; (*edge-75,Serial6 --> aggregation-70,Serial3*)
  72-16; (*core-7,Serial8 --> aggregation-192,Serial7*)
  230-118; (*core-42,Serial4 --> aggregation-133,Serial15*)
  78-185; (*core-9,Serial8 --> aggregation-225,Serial13*)
  182-77; (*aggregation-99,Serial2 --> edge-106,Serial3*)
  158-315; (*aggregation-113,Serial7 --> core-15,Serial12*)
  191-296; (*aggregation-227,Serial9 --> core-25,Serial6*)
  281-47; (*aggregation-311,Serial8 --> edge-312,Serial7*)
  250-74; (*aggregation-208,Serial6 --> core-6,Serial15*)
  213-0; (*aggregation-83,Serial8 --> edge-88,Serial3*)
  253-211; (*core-34,Serial10 --> aggregation-84,Serial2*)
  27-198; (*aggregation-294,Serial13 --> core-53,Serial3*)
  83-264; (*aggregation-277,Serial11 --> edge-283,Serial5*)
  203-116; (*core-51,Serial14 --> aggregation-134,Serial11*)
  54-241; (*aggregation-167,Serial7 --> edge-175,Serial7*)
  315-147; (*core-15,Serial14 --> aggregation-241,Serial7*)
  189-245; (*aggregation-229,Serial14 --> core-47,Serial2*)
  297-143; (*core-24,Serial9 --> aggregation-243,Serial4*)
  165-107; (*edge-265,Serial0 --> aggregation-256,Serial9*)
  61-265; (*aggregation-279,Serial10 --> edge-282,Serial7*)
  16-35; (*aggregation-192,Serial9 --> edge-201,Serial0*)
  103-163; (*aggregation-263,Serial4 --> edge-268,Serial7*)
  129-124; (*aggregation-246,Serial10 --> edge-250,Serial6*)
  6-30; (*aggregation-194,Serial5 --> edge-205,Serial2*)
  48-238; (*aggregation-162,Serial5 --> edge-173,Serial2*)
  175-134; (*aggregation-230,Serial4 --> edge-236,Serial6*)
  217-76; (*core-58,Serial6 --> aggregation-151,Serial5*)
  85-274; (*aggregation-276,Serial8 --> edge-280,Serial4*)
  39-295; (*aggregation-180,Serial14 --> edge-190,Serial4*)
  42-319; (*edge-316,Serial4 --> aggregation-308,Serial12*)
  53-4; (*aggregation-288,Serial13 --> edge-301,Serial0*)
  111-160; (*edge-125,Serial6 --> aggregation-118,Serial13*)
  298-297; (*aggregation-307,Serial4 --> core-24,Serial0*)
  55-198; (*aggregation-166,Serial13 --> core-53,Serial15*)
  236-310; (*aggregation-210,Serial2 --> core-18,Serial6*)
  102-60; (*aggregation-144,Serial1 --> core-1,Serial6*)
  194-223; (*core-55,Serial1 --> aggregation-214,Serial7*)
  5-292; (*aggregation-195,Serial11 --> core-27,Serial5*)
  51-318; (*edge-314,Serial5 --> aggregation-309,Serial10*)
  121-159; (*edge-123,Serial7 --> aggregation-119,Serial11*)
  203-79; (*core-51,Serial4 --> aggregation-150,Serial3*)
  31-9; (*edge-204,Serial7 --> aggregation-199,Serial4*)
  122-297; (*aggregation-131,Serial8 --> core-24,Serial3*)
  113-143; (*edge-248,Serial3 --> aggregation-243,Serial8*)
  153-314; (*aggregation-66,Serial8 --> core-16,Serial1*)
  230-202; (*core-42,Serial13 --> aggregation-101,Serial15*)
  274-63; (*edge-280,Serial6 --> aggregation-278,Serial8*)
  53-68; (*aggregation-288,Serial5 --> core-5,Serial11*)
  129-206; (*aggregation-246,Serial2 --> core-50,Serial11*)
  8-303; (*aggregation-193,Serial0 --> core-11,Serial7*)
  20-208; (*edge-93,Serial2 --> aggregation-82,Serial13*)
  225-257; (*aggregation-212,Serial0 --> core-32,Serial7*)
  303-132; (*core-11,Serial0 --> aggregation-129,Serial0*)
  69-102; (*core-4,Serial6 --> aggregation-144,Serial4*)
  190-270; (*aggregation-228,Serial5 --> core-37,Serial4*)
  94-233; (*edge-218,Serial3 --> aggregation-211,Serial10*)
  85-272; (*aggregation-276,Serial9 --> edge-281,Serial4*)
  245-220; (*core-47,Serial11 --> aggregation-85,Serial7*)
  139-184; (*edge-232,Serial2 --> aggregation-226,Serial0*)
  29-240; (*aggregation-293,Serial2 --> edge-296,Serial5*)
  284-110; (*core-21,Serial3 --> aggregation-130,Serial13*)
  233-86; (*aggregation-211,Serial15 --> edge-223,Serial3*)
  72-52; (*core-7,Serial7 --> aggregation-160,Serial7*)
  193-108; (*edge-137,Serial7 --> aggregation-135,Serial1*)
  24-11; (*edge-206,Serial6 --> aggregation-198,Serial6*)
  50-302; (*aggregation-161,Serial1 --> core-12,Serial6*)
  226-202; (*core-45,Serial13 --> aggregation-101,Serial0*)
  189-144; (*aggregation-229,Serial4 --> edge-234,Serial5*)
  143-258; (*aggregation-243,Serial1 --> core-31,Serial9*)
  153-280; (*aggregation-66,Serial0 --> edge-72,Serial2*)
  108-164; (*aggregation-135,Serial15 --> core-63,Serial12*)
  296-5; (*core-25,Serial5 --> aggregation-195,Serial9*)
  87-294; (*aggregation-275,Serial5 --> core-26,Serial8*)
  97-206; (*aggregation-262,Serial10 --> core-50,Serial12*)
  151-234; (*aggregation-69,Serial10 --> core-40,Serial14*)
  258-87; (*core-31,Serial8 --> aggregation-275,Serial1*)
  234-189; (*core-40,Serial2 --> aggregation-229,Serial10*)
  292-92; (*core-27,Serial13 --> aggregation-147,Serial0*)
  6-31; (*aggregation-194,Serial4 --> edge-204,Serial2*)
  81-34; (*core-8,Serial11 --> aggregation-177,Serial0*)
  76-256; (*aggregation-151,Serial14 --> edge-158,Serial7*)
  142-180; (*edge-110,Serial1 --> aggregation-97,Serial8*)
  183-71; (*aggregation-98,Serial0 --> edge-104,Serial2*)
  12-30; (*aggregation-197,Serial7 --> edge-205,Serial5*)
  251-123; (*edge-78,Serial7 --> aggregation-71,Serial6*)
  58-7; (*aggregation-291,Serial7 --> edge-303,Serial3*)
  20-221; (*edge-93,Serial7 --> aggregation-87,Serial13*)
  53-307; (*aggregation-288,Serial11 --> edge-299,Serial0*)
  227-161; (*core-44,Serial15 --> aggregation-117,Serial4*)
  122-258; (*aggregation-131,Serial15 --> core-31,Serial3*)
  66-181; (*core-2,Serial5 --> aggregation-96,Serial2*)
  217-187; (*core-58,Serial4 --> aggregation-103,Serial10*)
  89-266; (*aggregation-274,Serial13 --> edge-285,Serial2*)
  195-118; (*edge-136,Serial5 --> aggregation-133,Serial2*)
  295-33; (*edge-190,Serial2 --> aggregation-178,Serial14*)
  252-154; (*edge-79,Serial1 --> aggregation-65,Serial9*)
  188-80; (*aggregation-102,Serial1 --> edge-105,Serial6*)
  309-145; (*core-19,Serial7 --> aggregation-242,Serial3*)
  85-257; (*aggregation-276,Serial0 --> core-32,Serial14*)
  72-166; (*core-7,Serial10 --> aggregation-112,Serial7*)
  52-248; (*aggregation-160,Serial10 --> edge-170,Serial0*)
  256-93; (*edge-158,Serial2 --> aggregation-146,Serial14*)
  179-98; (*edge-270,Serial5 --> aggregation-261,Serial8*)
  16-69; (*aggregation-192,Serial4 --> core-4,Serial8*)
  118-169; (*aggregation-133,Serial9 --> edge-143,Serial5*)
  222-218; (*aggregation-215,Serial7 --> core-57,Serial8*)
  45-281; (*edge-313,Serial7 --> aggregation-311,Serial9*)
  244-223; (*core-48,Serial1 --> aggregation-214,Serial0*)
  182-258; (*aggregation-99,Serial15 --> core-31,Serial2*)
  195-132; (*edge-136,Serial1 --> aggregation-129,Serial2*)
  123-217; (*aggregation-71,Serial10 --> core-58,Serial11*)
  187-167; (*aggregation-103,Serial14 --> core-62,Serial4*)
  215-222; (*core-59,Serial8 --> aggregation-215,Serial0*)
  62-36; (*core-0,Serial14 --> aggregation-176,Serial0*)
  86-222; (*edge-223,Serial7 --> aggregation-215,Serial15*)
  119-166; (*edge-124,Serial0 --> aggregation-112,Serial12*)
  149-275; (*aggregation-68,Serial9 --> edge-73,Serial4*)
  246-52; (*edge-171,Serial0 --> aggregation-160,Serial11*)
  269-190; (*core-38,Serial4 --> aggregation-228,Serial6*)
  7-40; (*edge-303,Serial1 --> aggregation-289,Serial9*)
  294-58; (*core-26,Serial11 --> aggregation-291,Serial10*)
  141-207; (*aggregation-244,Serial15 --> edge-255,Serial4*)
  59-284; (*aggregation-290,Serial13 --> core-21,Serial8*)
  259-150; (*core-30,Serial10 --> aggregation-67,Serial14*)
  266-87; (*edge-285,Serial3 --> aggregation-275,Serial13*)
  275-149; (*edge-73,Serial4 --> aggregation-68,Serial9*)
  317-73; (*core-13,Serial15 --> aggregation-273,Serial5*)
  171-97; (*edge-267,Serial6 --> aggregation-262,Serial3*)
  216-60; (*aggregation-80,Serial1 --> core-1,Serial12*)
  98-174; (*aggregation-261,Serial4 --> edge-266,Serial5*)
  179-105; (*edge-270,Serial2 --> aggregation-258,Serial6*)
  213-18; (*aggregation-83,Serial11 --> edge-91,Serial3*)
  124-143; (*edge-250,Serial3 --> aggregation-243,Serial10*)
  79-194; (*aggregation-150,Serial7 --> core-55,Serial4*)
  285-236; (*core-20,Serial6 --> aggregation-210,Serial4*)
  210-17; (*aggregation-81,Serial15 --> edge-95,Serial1*)
  224-234; (*aggregation-213,Serial0 --> core-40,Serial7*)
  292-22; (*core-27,Serial14 --> aggregation-179,Serial0*)
  275-153; (*edge-73,Serial2 --> aggregation-66,Serial1*)
  92-263; (*aggregation-147,Serial12 --> edge-156,Serial3*)
  14-194; (*aggregation-182,Serial7 --> core-55,Serial2*)
  157-119; (*aggregation-114,Serial12 --> edge-124,Serial2*)
  300-51; (*aggregation-305,Serial10 --> edge-314,Serial1*)
  194-125; (*core-55,Serial7 --> aggregation-70,Serial15*)
  255-91; (*core-33,Serial12 --> aggregation-148,Serial1*)
  8-316; (*aggregation-193,Serial12 --> core-14,Serial7*)
  169-132; (*edge-143,Serial1 --> aggregation-129,Serial9*)
  216-20; (*aggregation-80,Serial13 --> edge-93,Serial0*)
  222-96; (*aggregation-215,Serial11 --> edge-219,Serial7*)
  33-308; (*aggregation-178,Serial8 --> edge-184,Serial2*)
  150-251; (*aggregation-67,Serial6 --> edge-78,Serial3*)
  46-292; (*aggregation-163,Serial11 --> core-27,Serial4*)
  101-178; (*aggregation-145,Serial8 --> edge-152,Serial1*)
  285-105; (*core-20,Serial12 --> aggregation-258,Serial12*)
  88-224; (*edge-222,Serial5 --> aggregation-213,Serial14*)
  159-109; (*aggregation-119,Serial14 --> edge-126,Serial7*)
  198-286; (*core-53,Serial8 --> aggregation-310,Serial5*)
  166-148; (*aggregation-112,Serial8 --> edge-120,Serial0*)
  207-130; (*edge-255,Serial5 --> aggregation-245,Serial15*)
  183-284; (*aggregation-98,Serial13 --> core-21,Serial9*)
  223-95; (*aggregation-214,Serial9 --> edge-217,Serial6*)
  1-219; (*edge-89,Serial6 --> aggregation-86,Serial9*)
  57-267; (*aggregation-164,Serial7 --> core-39,Serial2*)
  22-311; (*aggregation-179,Serial10 --> edge-186,Serial3*)
  54-215; (*aggregation-167,Serial11 --> core-59,Serial13*)
  93-261; (*aggregation-146,Serial10 --> edge-154,Serial2*)
  158-115; (*aggregation-113,Serial10 --> edge-122,Serial1*)
  70-183; (*edge-109,Serial2 --> aggregation-98,Serial5*)
  128-112; (*aggregation-247,Serial9 --> edge-249,Serial7*)
  292-143; (*core-27,Serial9 --> aggregation-243,Serial0*)
  103-164; (*aggregation-263,Serial15 --> core-63,Serial0*)
  104-290; (*aggregation-259,Serial13 --> core-29,Serial7*)
  140-187; (*edge-111,Serial7 --> aggregation-103,Serial7*)
  273-225; (*core-35,Serial7 --> aggregation-212,Serial3*)
  25-167; (*aggregation-295,Serial14 --> core-62,Serial3*)
  288-14; (*edge-187,Serial6 --> aggregation-182,Serial11*)
  301-47; (*aggregation-304,Serial8 --> edge-312,Serial0*)
  261-79; (*edge-154,Serial6 --> aggregation-150,Serial10*)
  0-211; (*edge-88,Serial4 --> aggregation-84,Serial8*)
  161-109; (*aggregation-117,Serial14 --> edge-126,Serial5*)
  319-42; (*aggregation-308,Serial12 --> edge-316,Serial4*)
  35-11; (*edge-201,Serial6 --> aggregation-198,Serial1*)
  54-242; (*aggregation-167,Serial6 --> edge-174,Serial7*)
  54-246; (*aggregation-167,Serial3 --> edge-171,Serial7*)
  179-107; (*edge-270,Serial0 --> aggregation-256,Serial14*)
  28-12; (*edge-202,Serial5 --> aggregation-197,Serial4*)
  83-268; (*aggregation-277,Serial12 --> edge-284,Serial5*)
  126-135; (*edge-252,Serial0 --> aggregation-240,Serial12*)
  159-215; (*aggregation-119,Serial0 --> core-59,Serial9*)
  183-142; (*aggregation-98,Serial6 --> edge-110,Serial2*)
  197-122; (*edge-139,Serial3 --> aggregation-131,Serial3*)
  98-163; (*aggregation-261,Serial6 --> edge-268,Serial5*)
  113-130; (*edge-248,Serial5 --> aggregation-245,Serial8*)
  244-129; (*core-48,Serial11 --> aggregation-246,Serial0*)
  126-143; (*edge-252,Serial3 --> aggregation-243,Serial12*)
  173-187; (*core-60,Serial4 --> aggregation-103,Serial12*)
  131-185; (*edge-239,Serial1 --> aggregation-225,Serial9*)
  164-61; (*core-63,Serial1 --> aggregation-279,Serial2*)
  223-243; (*aggregation-214,Serial1 --> core-49,Serial1*)
  313-22; (*edge-185,Serial3 --> aggregation-179,Serial9*)
  313-36; (*edge-185,Serial0 --> aggregation-176,Serial9*)
  215-25; (*core-59,Serial3 --> aggregation-295,Serial11*)
  79-178; (*aggregation-150,Serial8 --> edge-152,Serial6*)
  129-196; (*aggregation-246,Serial6 --> core-54,Serial11*)
  250-68; (*aggregation-208,Serial5 --> core-5,Serial15*)
  275-150; (*edge-73,Serial3 --> aggregation-67,Serial1*)
  33-284; (*aggregation-178,Serial5 --> core-21,Serial13*)
  79-196; (*aggregation-150,Serial6 --> core-54,Serial4*)
  294-92; (*core-26,Serial13 --> aggregation-147,Serial5*)
  102-66; (*aggregation-144,Serial2 --> core-2,Serial6*)
  14-203; (*aggregation-182,Serial3 --> core-51,Serial2*)
  95-236; (*edge-217,Serial2 --> aggregation-210,Serial9*)
  110-312; (*aggregation-130,Serial9 --> core-17,Serial3*)
  304-106; (*core-10,Serial9 --> aggregation-257,Serial15*)
  312-59; (*core-17,Serial8 --> aggregation-290,Serial9*)
  319-43; (*aggregation-308,Serial15 --> edge-319,Serial4*)
  105-168; (*aggregation-258,Serial0 --> edge-264,Serial2*)
  138-172; (*edge-237,Serial7 --> aggregation-231,Serial5*)
  150-276; (*aggregation-67,Serial2 --> edge-74,Serial3*)
  156-117; (*aggregation-115,Serial9 --> edge-121,Serial3*)
  234-83; (*core-40,Serial6 --> aggregation-277,Serial0*)
  187-218; (*aggregation-103,Serial9 --> core-57,Serial4*)
  37-16; (*edge-200,Serial0 --> aggregation-192,Serial8*)
  314-110; (*core-16,Serial3 --> aggregation-130,Serial8*)
  252-149; (*edge-79,Serial4 --> aggregation-68,Serial15*)
  273-3; (*core-35,Serial11 --> aggregation-196,Serial3*)
  282-105; (*core-23,Serial12 --> aggregation-258,Serial15*)
  285-93; (*core-20,Serial15 --> aggregation-146,Serial4*)
  284-153; (*core-21,Serial1 --> aggregation-66,Serial13*)
  273-141; (*core-35,Serial13 --> aggregation-244,Serial3*)
  22-292; (*aggregation-179,Serial0 --> core-27,Serial14*)
  184-312; (*aggregation-226,Serial9 --> core-17,Serial10*)
  299-284; (*aggregation-306,Serial5 --> core-21,Serial14*)
  312-33; (*core-17,Serial13 --> aggregation-178,Serial1*)
  147-113; (*aggregation-241,Serial8 --> edge-248,Serial1*)
  318-229; (*aggregation-309,Serial3 --> core-43,Serial9*)
  59-307; (*aggregation-290,Serial3 --> edge-299,Serial2*)
  270-155; (*core-37,Serial5 --> aggregation-116,Serial5*)
  4-25; (*edge-301,Serial7 --> aggregation-295,Serial5*)
  57-273; (*aggregation-164,Serial3 --> core-35,Serial2*)
  108-193; (*aggregation-135,Serial1 --> edge-137,Serial7*)
  159-121; (*aggregation-119,Serial11 --> edge-123,Serial7*)
  60-75; (*core-1,Serial3 --> aggregation-272,Serial1*)
  228-56; (*edge-169,Serial5 --> aggregation-165,Serial3*)
  281-167; (*aggregation-311,Serial6 --> core-62,Serial10*)
  147-124; (*aggregation-241,Serial10 --> edge-250,Serial1*)
  79-203; (*aggregation-150,Serial3 --> core-51,Serial4*)
  308-38; (*edge-184,Serial5 --> aggregation-181,Serial8*)
  38-245; (*aggregation-181,Serial7 --> core-47,Serial5*)
  250-66; (*aggregation-208,Serial2 --> core-2,Serial15*)
  147-81; (*aggregation-241,Serial0 --> core-8,Serial14*)
  184-283; (*aggregation-226,Serial14 --> core-22,Serial10*)
  284-6; (*core-21,Serial5 --> aggregation-194,Serial13*)
  18-221; (*edge-91,Serial7 --> aggregation-87,Serial11*)
  102-68; (*aggregation-144,Serial5 --> core-5,Serial6*)
  193-116; (*edge-137,Serial6 --> aggregation-134,Serial1*)
  124-129; (*edge-250,Serial6 --> aggregation-246,Serial10*)
  159-192; (*aggregation-119,Serial4 --> core-56,Serial9*)
  208-314; (*aggregation-82,Serial0 --> core-16,Serial2*)
  105-171; (*aggregation-258,Serial3 --> edge-267,Serial2*)
  23-8; (*edge-207,Serial1 --> aggregation-193,Serial9*)
  264-63; (*edge-283,Serial6 --> aggregation-278,Serial11*)
  93-284; (*aggregation-146,Serial5 --> core-21,Serial15*)
  178-102; (*edge-152,Serial0 --> aggregation-144,Serial8*)
  65-180; (*edge-108,Serial1 --> aggregation-97,Serial6*)
  165-105; (*edge-265,Serial2 --> aggregation-258,Serial1*)
  9-24; (*aggregation-199,Serial6 --> edge-206,Serial7*)
  97-196; (*aggregation-262,Serial14 --> core-54,Serial12*)
  69-107; (*core-4,Serial4 --> aggregation-256,Serial4*)
  97-203; (*aggregation-262,Serial11 --> core-51,Serial12*)
  116-212; (*aggregation-134,Serial6 --> edge-142,Serial6*)
  141-126; (*aggregation-244,Serial12 --> edge-252,Serial4*)
  287-34; (*edge-188,Serial1 --> aggregation-177,Serial12*)
  196-63; (*core-54,Serial10 --> aggregation-278,Serial6*)
  251-153; (*edge-78,Serial2 --> aggregation-66,Serial6*)
  202-231; (*aggregation-101,Serial13 --> core-41,Serial13*)
  3-271; (*aggregation-196,Serial4 --> core-36,Serial11*)
  3-267; (*aggregation-196,Serial7 --> core-39,Serial11*)
  66-16; (*core-2,Serial8 --> aggregation-192,Serial2*)
  178-92; (*edge-152,Serial3 --> aggregation-147,Serial8*)
  316-8; (*core-14,Serial7 --> aggregation-193,Serial12*)
  123-275; (*aggregation-71,Serial1 --> edge-73,Serial7*)
  314-105; (*core-16,Serial12 --> aggregation-258,Serial8*)
  53-64; (*aggregation-288,Serial3 --> core-3,Serial11*)
  176-106; (*edge-271,Serial1 --> aggregation-257,Serial9*)
  314-236; (*core-16,Serial6 --> aggregation-210,Serial0*)
  26-5; (*edge-203,Serial3 --> aggregation-195,Serial3*)
  12-31; (*aggregation-197,Serial6 --> edge-204,Serial5*)
  42-298; (*edge-316,Serial3 --> aggregation-307,Serial12*)
  90-230; (*aggregation-149,Serial2 --> core-42,Serial10*)
  111-161; (*edge-125,Serial5 --> aggregation-117,Serial13*)
  189-247; (*aggregation-229,Serial12 --> core-46,Serial2*)
  155-253; (*aggregation-116,Serial2 --> core-34,Serial5*)
  13-308; (*aggregation-183,Serial8 --> edge-184,Serial7*)
  55-203; (*aggregation-166,Serial11 --> core-51,Serial15*)
  280-151; (*edge-72,Serial5 --> aggregation-69,Serial2*)
  317-106; (*core-13,Serial9 --> aggregation-257,Serial11*)
  16-60; (*aggregation-192,Serial1 --> core-1,Serial8*)
  25-192; (*aggregation-295,Serial8 --> core-56,Serial3*)
  175-137; (*aggregation-230,Serial6 --> edge-238,Serial6*)
  43-318; (*edge-319,Serial5 --> aggregation-309,Serial15*)
  226-318; (*core-45,Serial9 --> aggregation-309,Serial5*)
  122-209; (*aggregation-131,Serial4 --> edge-140,Serial3*)
  132-197; (*aggregation-129,Serial5 --> edge-139,Serial1*)
  241-50; (*edge-175,Serial1 --> aggregation-161,Serial9*)
  123-164; (*aggregation-71,Serial15 --> core-63,Serial11*)
  20-211; (*edge-93,Serial4 --> aggregation-84,Serial13*)
  19-216; (*edge-92,Serial0 --> aggregation-80,Serial12*)
  151-245; (*aggregation-69,Serial14 --> core-47,Serial14*)
  69-135; (*core-4,Serial2 --> aggregation-240,Serial4*)
  139-186; (*edge-232,Serial0 --> aggregation-224,Serial8*)
  208-283; (*aggregation-82,Serial6 --> core-22,Serial2*)
  317-158; (*core-13,Serial12 --> aggregation-113,Serial5*)
  318-247; (*aggregation-309,Serial6 --> core-46,Serial9*)
  71-205; (*edge-104,Serial4 --> aggregation-100,Serial8*)
  59-240; (*aggregation-290,Serial0 --> edge-296,Serial2*)
  84-279; (*edge-220,Serial1 --> aggregation-209,Serial12*)
  129-203; (*aggregation-246,Serial3 --> core-51,Serial11*)
  48-241; (*aggregation-162,Serial7 --> edge-175,Serial2*)
  236-312; (*aggregation-210,Serial1 --> core-17,Serial6*)
  304-147; (*core-10,Serial14 --> aggregation-241,Serial2*)
  12-35; (*aggregation-197,Serial3 --> edge-201,Serial5*)
  98-231; (*aggregation-261,Serial13 --> core-41,Serial3*)
  312-208; (*core-17,Serial2 --> aggregation-82,Serial1*)
  259-122; (*core-30,Serial3 --> aggregation-131,Serial14*)
  6-23; (*aggregation-194,Serial7 --> edge-207,Serial2*)
  169-118; (*edge-143,Serial5 --> aggregation-133,Serial9*)
  142-202; (*edge-110,Serial5 --> aggregation-101,Serial8*)
  105-285; (*aggregation-258,Serial12 --> core-20,Serial12*)
  274-83; (*edge-280,Serial5 --> aggregation-277,Serial8*)
  182-65; (*aggregation-99,Serial4 --> edge-108,Serial3*)
  110-283; (*aggregation-130,Serial14 --> core-22,Serial3*)
  86-250; (*edge-223,Serial0 --> aggregation-208,Serial15*)
  94-223; (*edge-218,Serial6 --> aggregation-214,Serial10*)
  190-253; (*aggregation-228,Serial2 --> core-34,Serial4*)
  83-231; (*aggregation-277,Serial1 --> core-41,Serial6*)
  260-101; (*edge-155,Serial1 --> aggregation-145,Serial11*)
  310-184; (*core-18,Serial10 --> aggregation-226,Serial10*)
  247-90; (*core-46,Serial10 --> aggregation-149,Serial6*)
  319-273; (*aggregation-308,Serial3 --> core-35,Serial9*)
  233-84; (*aggregation-211,Serial12 --> edge-220,Serial3*)
  217-172; (*core-58,Serial15 --> aggregation-231,Serial10*)
  213-19; (*aggregation-83,Serial12 --> edge-92,Serial3*)
  227-38; (*core-44,Serial5 --> aggregation-181,Serial4*)
  216-66; (*aggregation-80,Serial2 --> core-2,Serial12*)
  154-277; (*aggregation-65,Serial5 --> edge-75,Serial1*)
  100-257; (*aggregation-260,Serial0 --> core-32,Serial15*)
  17-210; (*edge-95,Serial1 --> aggregation-81,Serial15*)
  243-175; (*core-49,Serial6 --> aggregation-230,Serial9*)
  56-239; (*aggregation-165,Serial6 --> edge-172,Serial5*)
  8-81; (*aggregation-193,Serial10 --> core-8,Serial7*)
  230-189; (*core-42,Serial2 --> aggregation-229,Serial15*)
  314-6; (*core-16,Serial5 --> aggregation-194,Serial8*)
  256-90; (*edge-158,Serial5 --> aggregation-149,Serial14*)
  98-226; (*aggregation-261,Serial0 --> core-45,Serial3*)
  269-85; (*core-38,Serial14 --> aggregation-276,Serial6*)
  55-243; (*aggregation-166,Serial9 --> core-49,Serial15*)
  233-297; (*aggregation-211,Serial4 --> core-24,Serial15*)
  136-191; (*edge-235,Serial3 --> aggregation-227,Serial3*)
  118-199; (*aggregation-133,Serial4 --> edge-138,Serial5*)
  32-305; (*aggregation-292,Serial10 --> edge-298,Serial4*)
  59-312; (*aggregation-290,Serial9 --> core-17,Serial8*)
  184-131; (*aggregation-226,Serial7 --> edge-239,Serial2*)
  56-245; (*aggregation-165,Serial14 --> core-47,Serial0*)
  251-152; (*edge-78,Serial0 --> aggregation-64,Serial14*)
  92-254; (*aggregation-147,Serial15 --> edge-159,Serial3*)
  159-217; (*aggregation-119,Serial5 --> core-58,Serial9*)
  128-201; (*aggregation-247,Serial14 --> edge-254,Serial7*)
  143-297; (*aggregation-243,Serial4 --> core-24,Serial9*)
  243-79; (*core-49,Serial4 --> aggregation-150,Serial1*)
  157-114; (*aggregation-114,Serial15 --> edge-127,Serial2*)
  300-41; (*aggregation-305,Serial13 --> edge-317,Serial1*)
  227-56; (*core-44,Serial0 --> aggregation-165,Serial11*)
  130-231; (*aggregation-245,Serial1 --> core-41,Serial8*)
  220-15; (*aggregation-85,Serial10 --> edge-90,Serial5*)
  223-194; (*aggregation-214,Serial7 --> core-55,Serial1*)
  227-130; (*core-44,Serial8 --> aggregation-245,Serial4*)
  317-210; (*core-13,Serial3 --> aggregation-81,Serial5*)
  71-182; (*edge-104,Serial3 --> aggregation-99,Serial0*)
  175-196; (*aggregation-230,Serial14 --> core-54,Serial6*)
  258-213; (*core-31,Serial1 --> aggregation-83,Serial1*)
  89-265; (*aggregation-274,Serial10 --> edge-282,Serial2*)
  188-65; (*aggregation-102,Serial4 --> edge-108,Serial6*)
  279-99; (*aggregation-209,Serial8 --> edge-216,Serial1*)
  39-313; (*aggregation-180,Serial9 --> edge-185,Serial4*)
  61-218; (*aggregation-279,Serial7 --> core-57,Serial1*)
  71-188; (*edge-104,Serial6 --> aggregation-102,Serial0*)
  24-16; (*edge-206,Serial0 --> aggregation-192,Serial14*)
  90-226; (*aggregation-149,Serial5 --> core-45,Serial10*)
  3-23; (*aggregation-196,Serial15 --> edge-207,Serial4*)
  295-22; (*edge-190,Serial3 --> aggregation-179,Serial14*)
  22-294; (*aggregation-179,Serial5 --> core-26,Serial14*)
  76-260; (*aggregation-151,Serial11 --> edge-155,Serial7*)
  148-159; (*edge-120,Serial7 --> aggregation-119,Serial8*)
  291-46; (*core-28,Serial4 --> aggregation-163,Serial12*)
  9-26; (*aggregation-199,Serial3 --> edge-203,Serial7*)
  144-189; (*edge-234,Serial5 --> aggregation-229,Serial4*)
  106-162; (*aggregation-257,Serial7 --> edge-269,Serial1*)
  137-186; (*edge-238,Serial0 --> aggregation-224,Serial14*)
  180-77; (*aggregation-97,Serial4 --> edge-106,Serial1*)
  92-262; (*aggregation-147,Serial13 --> edge-157,Serial3*)
  306-40; (*edge-297,Serial1 --> aggregation-289,Serial3*)
  259-182; (*core-30,Serial2 --> aggregation-99,Serial14*)
  279-302; (*aggregation-209,Serial4 --> core-12,Serial13*)
  183-283; (*aggregation-98,Serial14 --> core-22,Serial9*)
  266-75; (*edge-285,Serial0 --> aggregation-272,Serial13*)
  262-76; (*edge-157,Serial7 --> aggregation-151,Serial13*)
  102-260; (*aggregation-144,Serial11 --> edge-155,Serial0*)
  122-259; (*aggregation-131,Serial14 --> core-30,Serial3*)
  150-291; (*aggregation-67,Serial12 --> core-28,Serial10*)
  12-234; (*aggregation-197,Serial10 --> core-40,Serial1*)
  234-118; (*core-40,Serial4 --> aggregation-133,Serial10*)
  267-205; (*core-39,Serial1 --> aggregation-100,Serial7*)
  83-229; (*aggregation-277,Serial3 --> core-43,Serial6*)
  318-45; (*aggregation-309,Serial9 --> edge-313,Serial5*)
  69-186; (*core-4,Serial9 --> aggregation-224,Serial4*)
  242-48; (*edge-174,Serial2 --> aggregation-162,Serial6*)
  225-94; (*aggregation-212,Serial10 --> edge-218,Serial4*)
  91-177; (*aggregation-148,Serial9 --> edge-153,Serial4*)
  253-149; (*core-34,Serial0 --> aggregation-68,Serial2*)
  288-38; (*edge-187,Serial5 --> aggregation-181,Serial11*)
  219-21; (*aggregation-86,Serial14 --> edge-94,Serial6*)
  58-10; (*aggregation-291,Serial4 --> edge-300,Serial3*)
  214-110; (*edge-141,Serial2 --> aggregation-130,Serial5*)
  57-269; (*aggregation-164,Serial6 --> core-38,Serial2*)
  189-229; (*aggregation-229,Serial1 --> core-43,Serial2*)
  205-80; (*aggregation-100,Serial9 --> edge-105,Serial4*)
  216-15; (*aggregation-80,Serial10 --> edge-90,Serial0*)
  44-281; (*edge-318,Serial7 --> aggregation-311,Serial14*)
  68-301; (*core-5,Serial13 --> aggregation-304,Serial5*)
  105-312; (*aggregation-258,Serial9 --> core-17,Serial12*)
  1-221; (*edge-89,Serial7 --> aggregation-87,Serial9*)
  88-236; (*edge-222,Serial2 --> aggregation-210,Serial14*)
  207-145; (*edge-255,Serial2 --> aggregation-242,Serial15*)
  34-288; (*aggregation-177,Serial11 --> edge-187,Serial1*)
  69-250; (*core-4,Serial15 --> aggregation-208,Serial4*)
  67-181; (*edge-107,Serial0 --> aggregation-96,Serial11*)
  297-92; (*core-24,Serial13 --> aggregation-147,Serial4*)
  316-34; (*core-14,Serial11 --> aggregation-177,Serial6*)
  10-59; (*edge-300,Serial2 --> aggregation-290,Serial4*)
  40-306; (*aggregation-289,Serial3 --> edge-297,Serial1*)
  317-180; (*core-13,Serial2 --> aggregation-97,Serial11*)
  40-315; (*aggregation-289,Serial14 --> core-15,Serial10*)
  168-106; (*edge-264,Serial1 --> aggregation-257,Serial2*)
  25-170; (*aggregation-295,Serial13 --> core-61,Serial3*)
  191-259; (*aggregation-227,Serial14 --> core-30,Serial6*)
  253-39; (*core-34,Serial8 --> aggregation-180,Serial2*)
  316-40; (*core-14,Serial10 --> aggregation-289,Serial12*)
  88-225; (*edge-222,Serial4 --> aggregation-212,Serial14*)
  140-188; (*edge-111,Serial6 --> aggregation-102,Serial7*)
  48-282; (*aggregation-162,Serial15 --> core-23,Serial4*)
  207-141; (*edge-255,Serial4 --> aggregation-244,Serial15*)
  161-247; (*aggregation-117,Serial6 --> core-46,Serial15*)
  27-2; (*aggregation-294,Serial6 --> edge-302,Serial6*)
  107-168; (*aggregation-256,Serial8 --> edge-264,Serial0*)
  81-50; (*core-8,Serial6 --> aggregation-161,Serial10*)
  150-249; (*aggregation-67,Serial5 --> edge-77,Serial3*)
  14-196; (*aggregation-182,Serial6 --> core-54,Serial2*)
  270-211; (*core-37,Serial10 --> aggregation-84,Serial5*)
  90-229; (*aggregation-149,Serial3 --> core-43,Serial10*)
  102-262; (*aggregation-144,Serial13 --> edge-157,Serial0*)
  188-67; (*aggregation-102,Serial3 --> edge-107,Serial6*)
  174-98; (*edge-266,Serial5 --> aggregation-261,Serial4*)
  283-89; (*core-22,Serial0 --> aggregation-274,Serial6*)
  239-54; (*edge-172,Serial7 --> aggregation-167,Serial4*)
  110-285; (*aggregation-130,Serial12 --> core-20,Serial3*)
  235-61; (*edge-286,Serial7 --> aggregation-279,Serial14*)
  7-59; (*edge-303,Serial2 --> aggregation-290,Serial7*)
  208-282; (*aggregation-82,Serial7 --> core-23,Serial2*)
  318-47; (*aggregation-309,Serial8 --> edge-312,Serial5*)
  78-106; (*core-9,Serial9 --> aggregation-257,Serial13*)
  44-286; (*edge-318,Serial6 --> aggregation-310,Serial14*)
  231-189; (*core-41,Serial2 --> aggregation-229,Serial13*)
  68-36; (*core-5,Serial14 --> aggregation-176,Serial5*)
  178-93; (*edge-152,Serial2 --> aggregation-146,Serial8*)
  287-22; (*edge-188,Serial3 --> aggregation-179,Serial12*)
  4-32; (*edge-301,Serial4 --> aggregation-292,Serial13*)
  58-2; (*aggregation-291,Serial6 --> edge-302,Serial3*)
  20-220; (*edge-93,Serial5 --> aggregation-85,Serial13*)
  180-315; (*aggregation-97,Serial14 --> core-15,Serial2*)
  271-319; (*core-36,Serial9 --> aggregation-308,Serial4*)
  114-166; (*edge-127,Serial0 --> aggregation-112,Serial15*)
  231-90; (*core-41,Serial10 --> aggregation-149,Serial1*)
  170-222; (*core-61,Serial8 --> aggregation-215,Serial3*)
  0-210; (*edge-88,Serial1 --> aggregation-81,Serial8*)
  291-92; (*core-28,Serial13 --> aggregation-147,Serial2*)
  187-173; (*aggregation-103,Serial12 --> core-60,Serial4*)
  164-159; (*core-63,Serial9 --> aggregation-119,Serial2*)
  216-64; (*aggregation-80,Serial3 --> core-3,Serial12*)
  154-315; (*aggregation-65,Serial14 --> core-15,Serial4*)
  71-181; (*edge-104,Serial0 --> aggregation-96,Serial8*)
  35-3; (*edge-201,Serial4 --> aggregation-196,Serial9*)
  57-241; (*aggregation-164,Serial15 --> edge-175,Serial4*)
  76-263; (*aggregation-151,Serial12 --> edge-156,Serial7*)
  146-190; (*edge-233,Serial4 --> aggregation-228,Serial9*)
  312-183; (*core-17,Serial9 --> aggregation-98,Serial9*)
  124-130; (*edge-250,Serial5 --> aggregation-245,Serial10*)
  154-276; (*aggregation-65,Serial4 --> edge-74,Serial1*)
  43-319; (*edge-319,Serial4 --> aggregation-308,Serial15*)
  167-103; (*core-62,Serial0 --> aggregation-263,Serial14*)
  231-224; (*core-41,Serial7 --> aggregation-213,Serial1*)
  223-88; (*aggregation-214,Serial14 --> edge-222,Serial6*)
  184-285; (*aggregation-226,Serial12 --> core-20,Serial10*)
  13-313; (*aggregation-183,Serial9 --> edge-185,Serial7*)
  128-173; (*aggregation-247,Serial1 --> core-60,Serial2*)
  190-273; (*aggregation-228,Serial3 --> core-35,Serial4*)
  94-224; (*edge-218,Serial5 --> aggregation-213,Serial10*)
  308-39; (*edge-184,Serial4 --> aggregation-180,Serial8*)
  116-214; (*aggregation-134,Serial5 --> edge-141,Serial6*)
  100-253; (*aggregation-260,Serial2 --> core-34,Serial15*)
  237-50; (*edge-168,Serial1 --> aggregation-161,Serial2*)
  9-31; (*aggregation-199,Serial4 --> edge-204,Serial7*)
  13-295; (*aggregation-183,Serial14 --> edge-190,Serial7*)
  202-226; (*aggregation-101,Serial0 --> core-45,Serial13*)
  303-101; (*core-11,Serial5 --> aggregation-145,Serial3*)
  302-106; (*core-12,Serial9 --> aggregation-257,Serial1*)
  319-271; (*aggregation-308,Serial4 --> core-36,Serial9*)
  89-264; (*aggregation-274,Serial11 --> edge-283,Serial2*)
  271-205; (*core-36,Serial1 --> aggregation-100,Serial4*)
  279-304; (*aggregation-209,Serial2 --> core-10,Serial13*)
  147-127; (*aggregation-241,Serial11 --> edge-251,Serial1*)
  73-78; (*aggregation-273,Serial1 --> core-9,Serial15*)
  74-133; (*core-6,Serial1 --> aggregation-128,Serial6*)
  318-226; (*aggregation-309,Serial5 --> core-45,Serial9*)
  102-64; (*aggregation-144,Serial3 --> core-3,Serial6*)
  5-291; (*aggregation-195,Serial12 --> core-28,Serial5*)
  129-198; (*aggregation-246,Serial5 --> core-53,Serial11*)
  245-38; (*core-47,Serial5 --> aggregation-181,Serial7*)
  15-208; (*edge-90,Serial2 --> aggregation-82,Serial10*)
  307-29; (*edge-299,Serial5 --> aggregation-293,Serial5*)
  105-174; (*aggregation-258,Serial2 --> edge-266,Serial2*)
  73-272; (*aggregation-273,Serial9 --> edge-281,Serial1*)
  163-104; (*edge-268,Serial3 --> aggregation-259,Serial4*)
  313-33; (*edge-185,Serial2 --> aggregation-178,Serial9*)
  184-314; (*aggregation-226,Serial8 --> core-16,Serial10*)
  260-92; (*edge-155,Serial3 --> aggregation-147,Serial11*)
  233-292; (*aggregation-211,Serial0 --> core-27,Serial15*)
  35-9; (*edge-201,Serial7 --> aggregation-199,Serial1*)
  59-306; (*aggregation-290,Serial1 --> edge-297,Serial2*)
  291-156; (*core-28,Serial12 --> aggregation-115,Serial2*)
  194-129; (*core-55,Serial11 --> aggregation-246,Serial7*)
  53-66; (*aggregation-288,Serial2 --> core-2,Serial11*)
  313-34; (*edge-185,Serial1 --> aggregation-177,Serial9*)
  218-108; (*core-57,Serial12 --> aggregation-135,Serial9*)
  206-223; (*core-50,Serial1 --> aggregation-214,Serial2*)
  223-244; (*aggregation-214,Serial0 --> core-48,Serial1*)
  276-154; (*edge-74,Serial1 --> aggregation-65,Serial4*)
  299-285; (*aggregation-306,Serial4 --> core-20,Serial14*)
  26-16; (*edge-203,Serial0 --> aggregation-192,Serial11*)
  59-314; (*aggregation-290,Serial8 --> core-16,Serial8*)
  25-240; (*aggregation-295,Serial0 --> edge-296,Serial7*)
  189-227; (*aggregation-229,Serial11 --> core-44,Serial2*)
  259-213; (*core-30,Serial1 --> aggregation-83,Serial6*)
  291-22; (*core-28,Serial14 --> aggregation-179,Serial2*)
  56-246; (*aggregation-165,Serial5 --> edge-171,Serial5*)
  233-294; (*aggregation-211,Serial5 --> core-26,Serial15*)
  182-296; (*aggregation-99,Serial9 --> core-25,Serial2*)
  106-316; (*aggregation-257,Serial12 --> core-14,Serial9*)
  12-26; (*aggregation-197,Serial5 --> edge-203,Serial5*)
  22-293; (*aggregation-179,Serial15 --> edge-191,Serial3*)
  108-217; (*aggregation-135,Serial10 --> core-58,Serial12*)
  277-123; (*edge-75,Serial7 --> aggregation-71,Serial3*)
  129-200; (*aggregation-246,Serial4 --> core-52,Serial11*)
  115-156; (*edge-122,Serial3 --> aggregation-115,Serial10*)
  193-120; (*edge-137,Serial4 --> aggregation-132,Serial9*)
  309-110; (*core-19,Serial3 --> aggregation-130,Serial11*)
  234-318; (*core-40,Serial9 --> aggregation-309,Serial0*)
  296-150; (*core-25,Serial10 --> aggregation-67,Serial9*)
  259-5; (*core-30,Serial5 --> aggregation-195,Serial14*)
  182-70; (*aggregation-99,Serial5 --> edge-109,Serial3*)
  282-145; (*core-23,Serial7 --> aggregation-242,Serial7*)
  304-180; (*core-10,Serial2 --> aggregation-97,Serial15*)
  196-97; (*core-54,Serial12 --> aggregation-262,Serial14*)
  78-300; (*core-9,Serial1 --> aggregation-305,Serial1*)
  97-200; (*aggregation-262,Serial12 --> core-52,Serial12*)
  29-226; (*aggregation-293,Serial0 --> core-45,Serial12*)
  216-0; (*aggregation-80,Serial8 --> edge-88,Serial0*)
  3-270; (*aggregation-196,Serial5 --> core-37,Serial11*)
  84-250; (*edge-220,Serial0 --> aggregation-208,Serial12*)
  113-141; (*edge-248,Serial4 --> aggregation-244,Serial8*)
  60-107; (*core-1,Serial4 --> aggregation-256,Serial1*)
  122-296; (*aggregation-131,Serial9 --> core-25,Serial3*)
  59-305; (*aggregation-290,Serial2 --> edge-298,Serial2*)
  154-81; (*aggregation-65,Serial10 --> core-8,Serial4*)
  66-53; (*core-2,Serial11 --> aggregation-288,Serial2*)
  306-59; (*edge-297,Serial2 --> aggregation-290,Serial1*)
  67-187; (*edge-107,Serial7 --> aggregation-103,Serial3*)
  213-1; (*aggregation-83,Serial9 --> edge-89,Serial3*)
  84-222; (*edge-220,Serial7 --> aggregation-215,Serial12*)
  123-276; (*aggregation-71,Serial2 --> edge-74,Serial7*)
  93-285; (*aggregation-146,Serial4 --> core-20,Serial15*)
  273-190; (*core-35,Serial4 --> aggregation-228,Serial3*)
  219-206; (*aggregation-86,Serial2 --> core-50,Serial13*)
  242-46; (*edge-174,Serial3 --> aggregation-163,Serial6*)
  67-205; (*edge-107,Serial4 --> aggregation-100,Serial11*)
  81-279; (*core-8,Serial13 --> aggregation-209,Serial0*)
  50-317; (*aggregation-161,Serial11 --> core-13,Serial6*)
  25-173; (*aggregation-295,Serial12 --> core-60,Serial3*)
  15-213; (*edge-90,Serial3 --> aggregation-83,Serial10*)
  303-279; (*core-11,Serial13 --> aggregation-209,Serial3*)
  45-299; (*edge-313,Serial2 --> aggregation-306,Serial9*)
  262-79; (*edge-157,Serial6 --> aggregation-150,Serial13*)
  314-145; (*core-16,Serial7 --> aggregation-242,Serial0*)
  318-227; (*aggregation-309,Serial4 --> core-44,Serial9*)
  253-32; (*core-34,Serial6 --> aggregation-292,Serial2*)
  34-287; (*aggregation-177,Serial12 --> edge-188,Serial1*)
  106-302; (*aggregation-257,Serial1 --> core-12,Serial9*)
  2-29; (*edge-302,Serial5 --> aggregation-293,Serial8*)
  36-295; (*aggregation-176,Serial14 --> edge-190,Serial0*)
  90-261; (*aggregation-149,Serial10 --> edge-154,Serial5*)
  218-54; (*core-57,Serial13 --> aggregation-167,Serial9*)
  233-95; (*aggregation-211,Serial9 --> edge-217,Serial3*)
  26-6; (*edge-203,Serial2 --> aggregation-194,Serial3*)
  164-25; (*core-63,Serial3 --> aggregation-295,Serial15*)
  304-158; (*core-10,Serial12 --> aggregation-113,Serial2*)
  287-33; (*edge-188,Serial2 --> aggregation-178,Serial12*)
  42-299; (*edge-316,Serial2 --> aggregation-306,Serial12*)
  111-155; (*edge-125,Serial4 --> aggregation-116,Serial13*)
  242-52; (*edge-174,Serial0 --> aggregation-160,Serial14*)
  33-285; (*aggregation-178,Serial4 --> core-20,Serial13*)
  312-157; (*core-17,Serial11 --> aggregation-114,Serial1*)
  91-178; (*aggregation-148,Serial8 --> edge-152,Serial4*)
  16-66; (*aggregation-192,Serial2 --> core-2,Serial8*)
  285-299; (*core-20,Serial14 --> aggregation-306,Serial4*)
  180-67; (*aggregation-97,Serial5 --> edge-107,Serial1*)
  156-148; (*aggregation-115,Serial8 --> edge-120,Serial3*)
  197-132; (*edge-139,Serial1 --> aggregation-129,Serial5*)
  264-83; (*edge-283,Serial5 --> aggregation-277,Serial11*)
  102-69; (*aggregation-144,Serial4 --> core-4,Serial6*)
  69-166; (*core-4,Serial10 --> aggregation-112,Serial4*)
  180-81; (*aggregation-97,Serial10 --> core-8,Serial2*)
  79-198; (*aggregation-150,Serial5 --> core-53,Serial4*)
  225-96; (*aggregation-212,Serial11 --> edge-219,Serial4*)
  83-266; (*aggregation-277,Serial13 --> edge-285,Serial5*)
  141-204; (*aggregation-244,Serial13 --> edge-253,Serial4*)
  27-203; (*aggregation-294,Serial11 --> core-51,Serial3*)
  279-303; (*aggregation-209,Serial3 --> core-11,Serial13*)
  250-64; (*aggregation-208,Serial3 --> core-3,Serial15*)
  116-209; (*aggregation-134,Serial4 --> edge-140,Serial6*)
  259-58; (*core-30,Serial11 --> aggregation-291,Serial14*)
  27-244; (*aggregation-294,Serial8 --> core-48,Serial3*)
  78-180; (*core-9,Serial2 --> aggregation-97,Serial13*)
  90-227; (*aggregation-149,Serial4 --> core-44,Serial10*)
  54-239; (*aggregation-167,Serial4 --> edge-172,Serial7*)
  38-311; (*aggregation-181,Serial10 --> edge-186,Serial5*)
  108-197; (*aggregation-135,Serial3 --> edge-139,Serial7*)
  32-257; (*aggregation-292,Serial0 --> core-32,Serial6*)
  189-134; (*aggregation-229,Serial6 --> edge-236,Serial5*)
  207-143; (*edge-255,Serial3 --> aggregation-243,Serial15*)
  218-222; (*core-57,Serial8 --> aggregation-215,Serial7*)
  60-102; (*core-1,Serial6 --> aggregation-144,Serial1*)
  57-270; (*aggregation-164,Serial5 --> core-37,Serial2*)
  156-296; (*aggregation-115,Serial7 --> core-25,Serial12*)
  257-225; (*core-32,Serial7 --> aggregation-212,Serial0*)
  204-128; (*edge-253,Serial7 --> aggregation-247,Serial13*)
  138-175; (*edge-237,Serial6 --> aggregation-230,Serial5*)
  219-17; (*aggregation-86,Serial15 --> edge-95,Serial6*)
  222-173; (*aggregation-215,Serial1 --> core-60,Serial8*)
  58-4; (*aggregation-291,Serial5 --> edge-301,Serial3*)
  140-183; (*edge-111,Serial2 --> aggregation-98,Serial7*)
  55-244; (*aggregation-166,Serial8 --> core-48,Serial15*)
  214-122; (*edge-141,Serial3 --> aggregation-131,Serial5*)
  302-180; (*core-12,Serial2 --> aggregation-97,Serial1*)
  105-314; (*aggregation-258,Serial8 --> core-16,Serial12*)
  319-44; (*aggregation-308,Serial14 --> edge-318,Serial4*)
  118-193; (*aggregation-133,Serial3 --> edge-137,Serial5*)
  104-292; (*aggregation-259,Serial11 --> core-27,Serial7*)
  0-208; (*edge-88,Serial2 --> aggregation-82,Serial8*)
  83-230; (*aggregation-277,Serial2 --> core-42,Serial6*)
  288-39; (*edge-187,Serial4 --> aggregation-180,Serial11*)
  170-123; (*core-61,Serial11 --> aggregation-71,Serial13*)
  141-112; (*aggregation-244,Serial9 --> edge-249,Serial4*)
  157-109; (*aggregation-114,Serial14 --> edge-126,Serial2*)
  300-42; (*aggregation-305,Serial12 --> edge-316,Serial1*)
  159-119; (*aggregation-119,Serial12 --> edge-124,Serial7*)
  100-255; (*aggregation-260,Serial1 --> core-33,Serial15*)
  102-263; (*aggregation-144,Serial12 --> edge-156,Serial0*)
  155-115; (*aggregation-116,Serial10 --> edge-122,Serial4*)
  97-176; (*aggregation-262,Serial7 --> edge-271,Serial6*)
  58-259; (*aggregation-291,Serial14 --> core-30,Serial11*)
  266-73; (*edge-285,Serial1 --> aggregation-273,Serial13*)
  253-120; (*core-34,Serial3 --> aggregation-132,Serial2*)
  302-300; (*core-12,Serial1 --> aggregation-305,Serial4*)
  7-58; (*edge-303,Serial3 --> aggregation-291,Serial7*)
  32-307; (*aggregation-292,Serial11 --> edge-299,Serial4*)
  150-292; (*aggregation-67,Serial11 --> core-27,Serial10*)
  27-7; (*aggregation-294,Serial7 --> edge-303,Serial6*)
  183-282; (*aggregation-98,Serial15 --> core-23,Serial9*)
  309-48; (*core-19,Serial4 --> aggregation-162,Serial11*)
  161-245; (*aggregation-117,Serial7 --> core-47,Serial15*)
  97-243; (*aggregation-262,Serial9 --> core-49,Serial12*)
  68-181; (*core-5,Serial5 --> aggregation-96,Serial5*)
  75-232; (*aggregation-272,Serial15 --> edge-287,Serial0*)
  101-78; (*aggregation-145,Serial1 --> core-9,Serial5*)
  282-236; (*core-23,Serial6 --> aggregation-210,Serial7*)
  213-20; (*aggregation-83,Serial13 --> edge-93,Serial3*)
  160-206; (*aggregation-118,Serial2 --> core-50,Serial9*)
  301-62; (*aggregation-304,Serial0 --> core-0,Serial13*)
  14-198; (*aggregation-182,Serial5 --> core-53,Serial2*)
  150-278; (*aggregation-67,Serial4 --> edge-76,Serial3*)
  206-286; (*core-50,Serial8 --> aggregation-310,Serial2*)
  205-71; (*aggregation-100,Serial8 --> edge-104,Serial4*)
  304-73; (*core-10,Serial15 --> aggregation-273,Serial2*)
  64-152; (*core-3,Serial0 --> aggregation-64,Serial3*)
  126-147; (*edge-252,Serial1 --> aggregation-241,Serial12*)
  191-258; (*aggregation-227,Serial15 --> core-31,Serial6*)
  216-18; (*aggregation-80,Serial11 --> edge-91,Serial0*)
  40-240; (*aggregation-289,Serial2 --> edge-296,Serial1*)
  200-125; (*core-52,Serial7 --> aggregation-70,Serial12*)
}

let nodes = 320

let ospfIntraArea = 0u2
let ospfInterArea = 1u2
let ospfE1 = 2u2
let ospfE2 = 3u2

let protoConn = 0u8
let protoStatic = 1u8
let protoOspf = 2u8
let protoBgp = 3u8

let isProtocol fib x = fib = x
let min x y = x <u8 y

let pickOption f x y =
  match (x,y) with
  | (None, _) -> false  | (_, None) -> true
  | (Some a, Some b) -> f a b

let betterOspf o1 o2 =
  if o1.areaType >u2 o2.areaType then true
  else if o2.areaType >u2 o1.areaType then false
  else if o1.weight <=u16 o2.weight then true else false

let betterBgp b1 b2 =
  if b1.lp > b2.lp then true
  else if b2.lp > b1.lp then false
  else if b1.aslen < b2.aslen then true
  else if b2.aslen < b1.aslen then false
  else if b1.med >= b2.med then true else false

let betterEqOption o1 o2 =
  match (o1,o2) with
  | (_, None) -> true
  | (None, _) -> false
  | (Some a, Some b) -> a <=u8 b

let best c s o b =
  match (c,s,o,b) with
  | (None,None,None,None) -> None
  | _ ->
      let o = match o with | None -> None | Some o -> Some o.ospfAd in
      let b = match b with | None -> None | Some b -> Some b.bgpAd in
      let (x,p1) = if betterEqOption c s then (c,0u2) else (s,1u2) in
      let (y,p2) = if betterEqOption o b then (o,2u2) else (b,3u2) in
      Some (if betterEqOption x y then p1 else p2)

let mergeValues x y =
  let c = if (pickOption min x.connected y.connected) then x.connected else y.connected in
  let s = if (pickOption min x.static y.static) then x.static else y.static in
  let o = if (pickOption betterOspf x.ospf y.ospf) then x.ospf else y.ospf in
  let b = if (pickOption betterBgp x.bgp y.bgp) then x.bgp else y.bgp in
  { connected = c;
    static = s;
    ospf = o;
    bgp = b;
    selected = best c s o b}

let merge node x y = mergeValues x y

let init node =
  match node with
  | 0n ->
if (d = (167802880, 31u6)) || (d = (167798784, 31u6)) || (d = (167800832, 31u6)) || (d = (167794688, 31u6)) || (d = (1174427648, 31u6)) || (d = (167796736, 31u6)) || (d = (1174427648, 32u6)) || (d = (167790592, 31u6)) || (d = (1174427650, 31u6)) || (d = (167792640, 31u6)) || (d = (167788544, 31u6)) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if (d = (1174427648, 24u6)) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 1n ->
if (d = (167803136, 31u6)) || (d = (167799040, 31u6)) || (d = (167801088, 31u6)) || (d = (167794944, 31u6)) || (d = (1174427904, 31u6)) || (d = (167796992, 31u6)) || (d = (1174427904, 32u6)) || (d = (167790848, 31u6)) || (d = (1174427906, 31u6)) || (d = (167792896, 31u6)) || (d = (167788800, 31u6)) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if (d = (1174427904, 24u6)) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={};} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 2n ->
->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (298~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (298~290) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (298~297) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (298~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (298~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~310) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~283) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~312) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~284) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~309) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~285) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~282) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~314) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (299~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~302) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~315) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~81) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~304) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~317) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~303) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~78) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~316) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (300~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~74) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~64) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~62) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~68) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~66) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~72) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~69) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (301~60) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~180) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~300) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~147) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~210) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~132) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~279) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~185) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~154) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (302~158) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~132) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~279) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~185) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~158) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~154) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~180) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~147) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~300) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~210) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (303~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~180) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~147) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~279) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~300) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~210) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~132) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~154) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~185) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~158) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (304~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (305~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481408) && (prefix < 1174481664)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (305~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481408) && (prefix < 1174481664)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (305~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481408) && (prefix < 1174481664)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (305~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481408) && (prefix < 1174481664)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (305~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481408) && (prefix < 1174481664)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (305~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481408) && (prefix < 1174481664)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (305~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481408) && (prefix < 1174481664)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (305~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481408) && (prefix < 1174481664)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (306~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481152) && (prefix < 1174481408)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (306~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481152) && (prefix < 1174481408)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (306~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481152) && (prefix < 1174481408)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (306~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481152) && (prefix < 1174481408)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (306~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481152) && (prefix < 1174481408)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (306~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481152) && (prefix < 1174481408)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (306~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481152) && (prefix < 1174481408)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (306~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481152) && (prefix < 1174481408)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (307~29) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481664) && (prefix < 1174481920)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (307~25) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481664) && (prefix < 1174481920)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (307~53) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481664) && (prefix < 1174481920)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (307~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481664) && (prefix < 1174481920)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (307~58) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481664) && (prefix < 1174481920)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (307~27) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481664) && (prefix < 1174481920)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (307~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481664) && (prefix < 1174481920)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (307~32) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174481664) && (prefix < 1174481920)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (308~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452224) && (prefix < 1174452480)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (308~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452224) && (prefix < 1174452480)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (308~39) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452224) && (prefix < 1174452480)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (308~33) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452224) && (prefix < 1174452480)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (308~13) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452224) && (prefix < 1174452480)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (308~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452224) && (prefix < 1174452480)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (308~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452224) && (prefix < 1174452480)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (308~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452224) && (prefix < 1174452480)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (309~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~157) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~145) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~208) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~93) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~184) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~33) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~299) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~153) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~236) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (309~183) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~33) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~236) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~299) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~153) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~184) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~183) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~157) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~145) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~208) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~93) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (310~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (311~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452736) && (prefix < 1174452992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (311~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452736) && (prefix < 1174452992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (311~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452736) && (prefix < 1174452992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (311~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452736) && (prefix < 1174452992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (311~33) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452736) && (prefix < 1174452992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (311~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452736) && (prefix < 1174452992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (311~39) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452736) && (prefix < 1174452992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (311~13) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452736) && (prefix < 1174452992)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (312~157) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~184) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~183) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~153) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~93) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~208) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~33) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~236) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~299) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (312~145) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (313~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452480) && (prefix < 1174452736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (313~38) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452480) && (prefix < 1174452736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (313~33) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452480) && (prefix < 1174452736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (313~22) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452480) && (prefix < 1174452736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (313~39) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452480) && (prefix < 1174452736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (313~13) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452480) && (prefix < 1174452736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (313~36) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452480) && (prefix < 1174452736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (313~14) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (((prefixLen = 24u6) && ((prefix >= 1174452480) && (prefix < 1174452736)))) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
(if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None))
     in
      b

   | (314~89) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~59) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~105) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~110) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~33) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~183) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~236) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~145) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~299) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~6) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~157) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~184) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~48) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~153) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~93) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (314~208) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~180) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~279) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~158) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~132) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~185) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~210) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~300) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~154) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (315~147) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~210) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~300) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~147) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~279) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~154) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~185) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~180) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~132) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~158) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (316~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~154) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~73) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~279) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~180) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~132) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~158) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~106) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~210) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~8) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~300) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~101) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~50) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~185) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~34) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~147) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (317~40) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~226) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~230) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~227) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~245) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~231) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~229) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~234) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~247) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (318~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~45) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~44) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~253) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~49) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~271) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~255) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~267) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~47) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~51) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~257) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~269) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~41) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~273) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~43) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~270) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b

   | (319~42) ->
     let (prefix, prefixLen) = d in
     let b =
           (if (isProtocol prot 3u2) then
(Some {bgpAd= b.bgpAd; lp= b.lp; aslen= b.aslen + 1; med= b.med;comms= b.comms;})
else
None)
     in
      b
)

let trans edge x =
  let o = transferOspf edge x.ospf in
  let b = transferBgp edge x in
  {connected=None; static=None; ospf=o; bgp=b; selected=None}

let assert_node u x =
  if true then
    match x.selected with
      | None -> false
      | Some _ -> true
  else false

(* {edge-88=0, edge-89=1, edge-302=2, aggregation-196=3, edge-301=4,
   aggregation-195=5, aggregation-194=6, edge-303=7, aggregation-193=8,
   aggregation-199=9, edge-300=10, aggregation-198=11, aggregation-197=12,
   aggregation-183=13, aggregation-182=14, edge-90=15, aggregation-192=16,
   edge-95=17, edge-91=18, edge-92=19, edge-93=20, edge-94=21, aggregation-179=22,
   edge-207=23, edge-206=24, aggregation-295=25, edge-203=26, aggregation-294=27,
   edge-202=28, aggregation-293=29, edge-205=30, edge-204=31, aggregation-292=32,
   aggregation-178=33, aggregation-177=34, edge-201=35, aggregation-176=36,
   edge-200=37, aggregation-181=38, aggregation-180=39, aggregation-289=40,
   edge-317=41, edge-316=42, edge-319=43, edge-318=44, edge-313=45,
   aggregation-163=46, edge-312=47, aggregation-162=48, edge-315=49,
   aggregation-161=50, edge-314=51, aggregation-160=52, aggregation-288=53,
   aggregation-167=54, aggregation-166=55, aggregation-165=56, aggregation-164=57,
   aggregation-291=58, aggregation-290=59, core-1=60, aggregation-279=61,
   core-0=62, aggregation-278=63, core-3=64, edge-108=65, core-2=66, edge-107=67,
   core-5=68, core-4=69, edge-109=70, edge-104=71, core-7=72, aggregation-273=73,
   core-6=74, aggregation-272=75, aggregation-151=76, edge-106=77, core-9=78,
   aggregation-150=79, edge-105=80, core-8=81, edge-221=82, aggregation-277=83,
   edge-220=84, aggregation-276=85, edge-223=86, aggregation-275=87, edge-222=88,
   aggregation-274=89, aggregation-149=90, aggregation-148=91, aggregation-147=92,
   aggregation-146=93, edge-218=94, edge-217=95, edge-219=96, aggregation-262=97,
   aggregation-261=98, edge-216=99, aggregation-260=100, aggregation-145=101,
   aggregation-144=102, aggregation-263=103, aggregation-259=104,
   aggregation-258=105, aggregation-257=106, aggregation-256=107,
   aggregation-135=108, edge-126=109, aggregation-130=110, edge-125=111,
   edge-249=112, edge-248=113, edge-127=114, edge-122=115, aggregation-134=116,
   edge-121=117, aggregation-133=118, edge-124=119, aggregation-132=120,
   edge-123=121, aggregation-131=122, aggregation-71=123, edge-250=124,
   aggregation-70=125, edge-252=126, edge-251=127, aggregation-247=128,
   aggregation-246=129, aggregation-245=130, edge-239=131, aggregation-129=132,
   aggregation-128=133, edge-236=134, aggregation-240=135, edge-235=136,
   edge-238=137, edge-237=138, edge-232=139, edge-111=140, aggregation-244=141,
   edge-110=142, aggregation-243=143, edge-234=144, aggregation-242=145,
   edge-233=146, aggregation-241=147, edge-120=148, aggregation-68=149,
   aggregation-67=150, aggregation-69=151, aggregation-64=152, aggregation-66=153,
   aggregation-65=154, aggregation-116=155, aggregation-115=156,
   aggregation-114=157, aggregation-113=158, aggregation-119=159,
   aggregation-118=160, aggregation-117=161, edge-269=162, edge-268=163,
   core-63=164, edge-265=165, aggregation-112=166, core-62=167, edge-264=168,
   edge-143=169, core-61=170, edge-267=171, aggregation-231=172, core-60=173,
   edge-266=174, aggregation-230=175, edge-271=176, edge-153=177, edge-152=178,
   edge-270=179, aggregation-97=180, aggregation-96=181, aggregation-99=182,
   aggregation-98=183, aggregation-226=184, aggregation-225=185,
   aggregation-224=186, aggregation-103=187, aggregation-102=188,
   aggregation-229=189, aggregation-228=190, aggregation-227=191, core-56=192,
   edge-137=193, core-55=194, edge-136=195, core-54=196, edge-139=197, core-53=198,
   edge-138=199, core-52=200, edge-254=201, aggregation-101=202, core-51=203,
   edge-253=204, aggregation-100=205, core-50=206, edge-255=207,
   aggregation-82=208, edge-140=209, aggregation-81=210, aggregation-84=211,
   edge-142=212, aggregation-83=213, edge-141=214, core-59=215, aggregation-80=216,
   core-58=217, core-57=218, aggregation-86=219, aggregation-85=220,
   aggregation-87=221, aggregation-215=222, aggregation-214=223,
   aggregation-213=224, aggregation-212=225, core-45=226, core-44=227,
   edge-169=228, core-43=229, core-42=230, core-41=231, edge-287=232,
   aggregation-211=233, core-40=234, edge-286=235, aggregation-210=236,
   edge-168=237, edge-173=238, edge-172=239, edge-296=240, edge-175=241,
   edge-174=242, core-49=243, core-48=244, core-47=245, edge-171=246, core-46=247,
   edge-170=248, edge-77=249, aggregation-208=250, edge-78=251, edge-79=252,
   core-34=253, edge-159=254, core-33=255, edge-158=256, core-32=257, core-31=258,
   core-30=259, edge-155=260, edge-154=261, edge-157=262, edge-156=263,
   edge-283=264, edge-282=265, edge-285=266, core-39=267, edge-284=268,
   core-38=269, core-37=270, core-36=271, edge-281=272, core-35=273, edge-280=274,
   edge-73=275, edge-74=276, edge-75=277, edge-76=278, aggregation-209=279,
   edge-72=280, aggregation-311=281, core-23=282, core-22=283, core-21=284,
   core-20=285, aggregation-310=286, edge-188=287, edge-187=288, edge-189=289,
   core-29=290, core-28=291, core-27=292, edge-191=293, core-26=294, edge-190=295,
   core-25=296, core-24=297, aggregation-307=298, aggregation-306=299,
   aggregation-305=300, aggregation-304=301, core-12=302, core-11=303, core-10=304,
   edge-298=305, edge-297=306, edge-299=307, edge-184=308, core-19=309,
   core-18=310, edge-186=311, core-17=312, edge-185=313, core-16=314, core-15=315,
   core-14=316, core-13=317, aggregation-309=318,
   aggregation-308=319}*)

let sol = solution { init = init; trans = trans; merge = merge; interface = interface; rtrans = trans }

assert foldNodes (fun k v acc -> acc && assert_node k v) sol true
require (match (d, pod) with
 | ((1174427648, 24u6), 1) -> true
 | ((1174427904, 24u6), 1) -> true
 | ((1174482432, 24u6), 14) -> true
 | ((1174482176, 24u6), 14) -> true
 | ((1174482688, 24u6), 14) -> true
 | ((1174481920, 24u6), 14) -> true
 | ((1174428160, 24u6), 1) -> true
 | ((1174429440, 24u6), 1) -> true
 | ((1174428416, 24u6), 1) -> true
 | ((1174428672, 24u6), 1) -> true
 | ((1174428928, 24u6), 1) -> true
 | ((1174429184, 24u6), 1) -> true
 | ((1174458112, 24u6), 8) -> true
 | ((1174457856, 24u6), 8) -> true
 | ((1174457088, 24u6), 8) -> true
 | ((1174456832, 24u6), 8) -> true
 | ((1174457600, 24u6), 8) -> true
 | ((1174457344, 24u6), 8) -> true
 | ((1174456576, 24u6), 8) -> true
 | ((1174456320, 24u6), 8) -> true
 | ((1174486272, 24u6), 15) -> true
 | ((1174486016, 24u6), 15) -> true
 | ((1174486784, 24u6), 15) -> true
 | ((1174486528, 24u6), 15) -> true
 | ((1174485248, 24u6), 15) -> true
 | ((1174484992, 24u6), 15) -> true
 | ((1174485760, 24u6), 15) -> true
 | ((1174485504, 24u6), 15) -> true
 | ((1174432768, 24u6), 2) -> true
 | ((1174432512, 24u6), 2) -> true
 | ((1174433024, 24u6), 2) -> true
 | ((1174431744, 24u6), 2) -> true
 | ((1174432256, 24u6), 2) -> true
 | ((1174432000, 24u6), 2) -> true
 | ((1174461696, 24u6), 9) -> true
 | ((1174461440, 24u6), 9) -> true
 | ((1174462208, 24u6), 9) -> true
 | ((1174461952, 24u6), 9) -> true
 | ((1174460928, 24u6), 9) -> true
 | ((1174460672, 24u6), 9) -> true
 | ((1174461184, 24u6), 9) -> true
 | ((1174460416, 24u6), 9) -> true
 | ((1174437376, 24u6), 3) -> true
 | ((1174437120, 24u6), 3) -> true
 | ((1174468864, 24u6), 11) -> true
 | ((1174468608, 24u6), 11) -> true
 | ((1174437632, 24u6), 3) -> true
 | ((1174436352, 24u6), 3) -> true
 | ((1174436096, 24u6), 3) -> true
 | ((1174436864, 24u6), 3) -> true
 | ((1174436608, 24u6), 3) -> true
 | ((1174469120, 24u6), 11) -> true
 | ((1174469632, 24u6), 11) -> true
 | ((1174469376, 24u6), 11) -> true
 | ((1174466304, 24u6), 10) -> true
 | ((1174465536, 24u6), 10) -> true
 | ((1174465280, 24u6), 10) -> true
 | ((1174466048, 24u6), 10) -> true
 | ((1174465792, 24u6), 10) -> true
 | ((1174464512, 24u6), 10) -> true
 | ((1174433536, 24u6), 2) -> true
 | ((1174433280, 24u6), 2) -> true
 | ((1174465024, 24u6), 10) -> true
 | ((1174464768, 24u6), 10) -> true
 | ((1174435840, 24u6), 3) -> true
 | ((1174473984, 24u6), 12) -> true
 | ((1174473728, 24u6), 12) -> true
 | ((1174472960, 24u6), 12) -> true
 | ((1174472704, 24u6), 12) -> true
 | ((1174441728, 24u6), 4) -> true
 | ((1174473472, 24u6), 12) -> true
 | ((1174473216, 24u6), 12) -> true
 | ((1174474496, 24u6), 12) -> true
 | ((1174444288, 24u6), 5) -> true
 | ((1174444032, 24u6), 5) -> true
 | ((1174474240, 24u6), 12) -> true
 | ((1174440192, 24u6), 4) -> true
 | ((1174439936, 24u6), 4) -> true
 | ((1174440704, 24u6), 4) -> true
 | ((1174440448, 24u6), 4) -> true
 | ((1174470144, 24u6), 11) -> true
 | ((1174469888, 24u6), 11) -> true
 | ((1174470400, 24u6), 11) -> true
 | ((1174440960, 24u6), 4) -> true
 | ((1174441472, 24u6), 4) -> true
 | ((1174441216, 24u6), 4) -> true
 | ((1174448384, 24u6), 6) -> true
 | ((1174478592, 24u6), 13) -> true
 | ((1174478336, 24u6), 13) -> true
 | ((1174448128, 24u6), 6) -> true
 | ((1174449408, 24u6), 6) -> true
 | ((1174449152, 24u6), 6) -> true
 | ((1174480896, 24u6), 14) -> true
 | ((1174449920, 24u6), 6) -> true
 | ((1174449664, 24u6), 6) -> true
 | ((1174448896, 24u6), 6) -> true
 | ((1174448640, 24u6), 6) -> true
 | ((1174424832, 24u6), 0) -> true
 | ((1174425088, 24u6), 0) -> true
 | ((1174425344, 24u6), 0) -> true
 | ((1174445824, 24u6), 5) -> true
 | ((1174445568, 24u6), 5) -> true
 | ((1174444800, 24u6), 5) -> true
 | ((1174444544, 24u6), 5) -> true
 | ((1174445312, 24u6), 5) -> true
 | ((1174445056, 24u6), 5) -> true
 | ((1174477568, 24u6), 13) -> true
 | ((1174477312, 24u6), 13) -> true
 | ((1174478080, 24u6), 13) -> true
 | ((1174477824, 24u6), 13) -> true
 | ((1174477056, 24u6), 13) -> true
 | ((1174476800, 24u6), 13) -> true
 | ((1174423808, 24u6), 0) -> true
 | ((1174424064, 24u6), 0) -> true
 | ((1174424320, 24u6), 0) -> true
 | ((1174424576, 24u6), 0) -> true
 | ((1174423552, 24u6), 0) -> true
 | ((1174453248, 24u6), 7) -> true
 | ((1174452992, 24u6), 7) -> true
 | ((1174453504, 24u6), 7) -> true
 | ((1174454016, 24u6), 7) -> true
 | ((1174453760, 24u6), 7) -> true
 | ((1174481408, 24u6), 14) -> true
 | ((1174481152, 24u6), 14) -> true
 | ((1174481664, 24u6), 14) -> true
 | ((1174452224, 24u6), 7) -> true
 | ((1174452736, 24u6), 7) -> true
 | ((1174452480, 24u6), 7) -> true
 | _ -> false)

let partition node = match node with
  | 2n -> 0
  | 3n -> 0
  | 4n -> 0
  | 5n -> 0
  | 6n -> 0
  | 7n -> 0
  | 8n -> 0
  | 9n -> 0
  | 10n -> 0
  | 11n -> 0
  | 12n -> 0
  | 13n -> 0
  | 14n -> 0
  | 16n -> 0
  | 22n -> 0
  | 23n -> 0
  | 24n -> 0
  | 25n -> 0
  | 26n -> 0
  | 27n -> 0
  | 28n -> 0
  | 29n -> 0
  | 30n -> 0
  | 31n -> 0
  | 32n -> 0
  | 33n -> 0
  | 34n -> 0
  | 35n -> 0
  | 36n -> 0
  | 37n -> 0
  | 38n -> 0
  | 39n -> 0
  | 40n -> 0
  | 46n -> 0
  | 48n -> 0
  | 50n -> 0
  | 52n -> 0
  | 53n -> 0
  | 54n -> 0
  | 55n -> 0
  | 56n -> 0
  | 57n -> 0
  | 58n -> 0
  | 59n -> 0
  | 60n -> 0
  | 61n -> 0
  | 62n -> 0
  | 63n -> 0
  | 64n -> 0
  | 66n -> 0
  | 68n -> 0
  | 69n -> 0
  | 72n -> 0
  | 73n -> 0
  | 74n -> 0
  | 75n -> 0
  | 76n -> 0
  | 78n -> 0
  | 79n -> 0
  | 81n -> 0
  | 83n -> 0
  | 85n -> 0
  | 87n -> 0
  | 89n -> 0
  | 90n -> 0
  | 91n -> 0
  | 92n -> 0
  | 93n -> 0
  | 97n -> 0
  | 98n -> 0
  | 100n -> 0
  | 101n -> 0
  | 102n -> 0
  | 103n -> 0
  | 104n -> 0
  | 105n -> 0
  | 106n -> 0
  | 107n -> 0
  | 108n -> 0
  | 110n -> 0
  | 116n -> 0
  | 118n -> 0
  | 120n -> 0
  | 122n -> 0
  | 123n -> 0
  | 125n -> 0
  | 162n -> 0
  | 163n -> 0
  | 164n -> 0
  | 165n -> 0
  | 167n -> 0
  | 168n -> 0
  | 169n -> 0
  | 170n -> 0
  | 171n -> 0
  | 173n -> 0
  | 174n -> 0
  | 176n -> 0
  | 177n -> 0
  | 178n -> 0
  | 179n -> 0
  | 192n -> 0
  | 193n -> 0
  | 194n -> 0
  | 195n -> 0
  | 196n -> 0
  | 197n -> 0
  | 198n -> 0
  | 199n -> 0
  | 200n -> 0
  | 203n -> 0
  | 206n -> 0
  | 209n -> 0
  | 212n -> 0
  | 214n -> 0
  | 215n -> 0
  | 217n -> 0
  | 218n -> 0
  | 226n -> 0
  | 227n -> 0
  | 228n -> 0
  | 229n -> 0
  | 230n -> 0
  | 231n -> 0
  | 232n -> 0
  | 234n -> 0
  | 235n -> 0
  | 237n -> 0
  | 238n -> 0
  | 239n -> 0
  | 240n -> 0
  | 241n -> 0
  | 242n -> 0
  | 243n -> 0
  | 244n -> 0
  | 246n -> 0
  | 248n -> 0
  | 249n -> 0
  | 251n -> 0
  | 252n -> 0
  | 254n -> 0
  | 256n -> 0
  | 260n -> 0
  | 261n -> 0
  | 262n -> 0
  | 263n -> 0
  | 264n -> 0
  | 265n -> 0
  | 266n -> 0
  | 268n -> 0
  | 272n -> 0
  | 274n -> 0
  | 275n -> 0
  | 276n -> 0
  | 277n -> 0
  | 278n -> 0
  | 280n -> 0
  | 287n -> 0
  | 288n -> 0
  | 289n -> 0
  | 293n -> 0
  | 295n -> 0
  | 305n -> 0
  | 306n -> 0
  | 307n -> 0
  | 308n -> 0
  | 311n -> 0
  | 313n -> 0
  | 0n -> 1
  | 1n -> 1
  | 15n -> 1
  | 17n -> 1
  | 18n -> 1
  | 19n -> 1
  | 20n -> 1
  | 21n -> 1
  | 41n -> 1
  | 42n -> 1
  | 43n -> 1
  | 44n -> 1
  | 45n -> 1
  | 47n -> 1
  | 49n -> 1
  | 51n -> 1
  | 65n -> 1
  | 67n -> 1
  | 70n -> 1
  | 71n -> 1
  | 77n -> 1
  | 80n -> 1
  | 82n -> 1
  | 84n -> 1
  | 86n -> 1
  | 88n -> 1
  | 94n -> 1
  | 95n -> 1
  | 96n -> 1
  | 99n -> 1
  | 109n -> 1
  | 111n -> 1
  | 112n -> 1
  | 113n -> 1
  | 114n -> 1
  | 115n -> 1
  | 117n -> 1
  | 119n -> 1
  | 121n -> 1
  | 124n -> 1
  | 126n -> 1
  | 127n -> 1
  | 128n -> 1
  | 129n -> 1
  | 130n -> 1
  | 131n -> 1
  | 132n -> 1
  | 133n -> 1
  | 134n -> 1
  | 135n -> 1
  | 136n -> 1
  | 137n -> 1
  | 138n -> 1
  | 139n -> 1
  | 140n -> 1
  | 141n -> 1
  | 142n -> 1
  | 143n -> 1
  | 144n -> 1
  | 145n -> 1
  | 146n -> 1
  | 147n -> 1
  | 148n -> 1
  | 149n -> 1
  | 150n -> 1
  | 151n -> 1
  | 152n -> 1
  | 153n -> 1
  | 154n -> 1
  | 155n -> 1
  | 156n -> 1
  | 157n -> 1
  | 158n -> 1
  | 159n -> 1
  | 160n -> 1
  | 161n -> 1
  | 166n -> 1
  | 172n -> 1
  | 175n -> 1
  | 180n -> 1
  | 181n -> 1
  | 182n -> 1
  | 183n -> 1
  | 184n -> 1
  | 185n -> 1
  | 186n -> 1
  | 187n -> 1
  | 188n -> 1
  | 189n -> 1
  | 190n -> 1
  | 191n -> 1
  | 201n -> 1
  | 202n -> 1
  | 204n -> 1
  | 205n -> 1
  | 207n -> 1
  | 208n -> 1
  | 210n -> 1
  | 211n -> 1
  | 213n -> 1
  | 216n -> 1
  | 219n -> 1
  | 220n -> 1
  | 221n -> 1
  | 222n -> 1
  | 223n -> 1
  | 224n -> 1
  | 225n -> 1
  | 233n -> 1
  | 236n -> 1
  | 245n -> 1
  | 247n -> 1
  | 250n -> 1
  | 253n -> 1
  | 255n -> 1
  | 257n -> 1
  | 258n -> 1
  | 259n -> 1
  | 267n -> 1
  | 269n -> 1
  | 270n -> 1
  | 271n -> 1
  | 273n -> 1
  | 279n -> 1
  | 281n -> 1
  | 282n -> 1
  | 283n -> 1
  | 284n -> 1
  | 285n -> 1
  | 286n -> 1
  | 290n -> 1
  | 291n -> 1
  | 292n -> 1
  | 294n -> 1
  | 296n -> 1
  | 297n -> 1
  | 298n -> 1
  | 299n -> 1
  | 300n -> 1
  | 301n -> 1
  | 302n -> 1
  | 303n -> 1
  | 304n -> 1
  | 309n -> 1
  | 310n -> 1
  | 312n -> 1
  | 314n -> 1
  | 315n -> 1
  | 316n -> 1
  | 317n -> 1
  | 318n -> 1
  | 319n -> 1

let interface edge a =
  match edge with
  | 3~253 -> a = {  bgp= Some {  aslen= if pod = 8 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 3
-> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 132~193 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 132~195 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 132~197 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 132~199 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 132~209 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 132~212 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 132~214 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~60 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~62 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~64 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~66 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~68 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~69 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~72 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~74 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~169 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~193 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~195 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~197 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~199 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~209 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~212 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 133~214 -> a = {  bgp= Some {  aslen= if pod = 4 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 135~60 -> a = {  bgp= Some {  aslen= if pod = 11 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 135~62 -> a = {  bgp= Some {  aslen= if pod = 11 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 135~64 -> a = {  bgp= Some {  aslen= if pod = 11 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 135~66 -> a = {  bgp= Some {  aslen= if pod = 11 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 135~68 -> a = {  bgp= Some {  aslen= if pod = 11 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 135~69 -> a = {  bgp= Some {  aslen= if pod = 11 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 135~72 -> a = {  bgp= Some {  aslen= if pod = 11 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 135~74 -> a = {  bgp= Some {  aslen= if pod = 11 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 147~78 -> a = {  bgp= Some {  aslen= if pod = 11 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 147~81 -> a = {  bgp= Some {  aslen= if pod = 11 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 149~249 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 149~251 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 149~252 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 149~275 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 149~276 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 149~277 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 149~278 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 149~280 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 150~249 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 150~251 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 150~252 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 150~275 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 150~276 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 150~277 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 150~278 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 150~280 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~226 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~227 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~229 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~230 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~231 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~234 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~249 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~251 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~252 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~275 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~276 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~277 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~278 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 151~280 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~60 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~62 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~64 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~66 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~68 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~69 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~72 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~74 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~249 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~251 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~252 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~275 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~276 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~277 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~278 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 152~280 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 153~249 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 153~251 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 153~252 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 153~275 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 153~276 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 153~277 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 153~278 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 153~280 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 154~78 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 154~81 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 154~249 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 154~251 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 154~252 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 154~275 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 154~276 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 154~277 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 154~278 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 154~280 -> a = {  bgp= Some {  aslen= if pod = 0 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 158~78 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 158~81 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 159~164 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 159~167 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 159~170 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 159~173 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 159~192 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 159~215 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 159~217 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 159~218 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 160~194 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 160~196 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 160~198 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 160~200 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 160~203 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 160~206 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 160~243 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 160~244 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 161~226 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 161~227 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 161~229 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 161~230 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 161~231 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 161~234 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 164~128 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 164~159 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 164~172 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 164~187 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 164~221 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 164~222 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 164~281 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 166~60 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 166~62 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 166~64 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 166~66 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 166~68 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 166~69 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 166~72 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 166~74 -> a = {  bgp= Some {  aslen= if pod = 3 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 167~128 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 167~159 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 167~172 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 167~187 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 167~221 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 167~222 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 167~281 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 169~132 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174441728, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174441728, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174441728, 24u6) then Some 1u8 else None; }
  | 169~133 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174441728, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174441728, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174441728, 24u6) then Some 1u8 else None; }
  | 170~128 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 170~159 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 170~172 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 170~187 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 170~221 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 170~222 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 170~281 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 172~164 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 172~167 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 172~170 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 172~173 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 172~192 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 172~215 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 172~217 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 172~218 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 173~128 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 173~159 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 173~172 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 173~187 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 173~221 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 173~222 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 173~281 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 175~194 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 175~196 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 175~198 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 175~200 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 175~203 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 175~206 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 175~243 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 175~244 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 180~78 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 180~81 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 181~60 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 181~62 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 181~64 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 181~66 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 181~68 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 181~69 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 181~72 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 181~74 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 185~78 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 185~81 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 186~60 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 186~62 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 186~64 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 186~66 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 186~68 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 186~69 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 186~72 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 186~74 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 187~164 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 187~167 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 187~170 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 187~173 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 187~192 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 187~215 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 187~217 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 187~218 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 188~194 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 188~196 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 188~198 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 188~200 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 188~203 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 188~206 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 188~243 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 188~244 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 189~226 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 189~227 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 189~229 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 189~230 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 189~231 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 189~234 -> a = {  bgp= Some {  aslen= if pod = 10 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 192~128 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 192~159 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 192~172 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 192~187 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 192~221 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 192~222 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 192~281 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 193~132 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174440192, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174440192, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174440192, 24u6) then Some 1u8 else None; }
  | 193~133 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174440192, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174440192, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174440192, 24u6) then Some 1u8 else None; }
  | 194~129 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 194~160 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 194~175 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 194~188 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 194~219 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 194~223 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 194~286 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 195~132 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174439936, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174439936, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174439936, 24u6) then Some 1u8 else None; }
  | 195~133 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174439936, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174439936, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174439936, 24u6) then Some 1u8 else None; }
  | 196~129 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 196~160 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 196~175 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 196~188 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 196~219 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 196~223 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 196~286 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 197~132 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174440704, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174440704, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174440704, 24u6) then Some 1u8 else None; }
  | 197~133 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174440704, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174440704, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174440704, 24u6) then Some 1u8 else None; }
  | 198~129 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 198~160 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 198~175 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 198~188 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 198~219 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 198~223 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 198~286 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 199~132 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174440448, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174440448, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174440448, 24u6) then Some 1u8 else None; }
  | 199~133 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174440448, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174440448, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174440448, 24u6) then Some 1u8 else None; }
  | 200~129 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 200~160 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 200~175 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 200~188 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 200~219 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 200~223 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 200~286 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 202~226 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 202~227 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 202~229 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 202~230 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 202~231 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 202~234 -> a = {  bgp= Some {  aslen= if pod = 2 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 203~129 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 203~160 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 203~175 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 203~188 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 203~219 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 203~223 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 203~286 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 206~129 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 206~160 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 206~175 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 206~188 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 206~219 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 206~223 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 206~286 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 209~132 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174440960, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174440960, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174440960, 24u6) then Some 1u8 else None; }
  | 209~133 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174440960, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174440960, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174440960, 24u6) then Some 1u8 else None; }
  | 210~78 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 210~81 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 212~132 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174441472, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174441472, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174441472, 24u6) then Some 1u8 else None; }
  | 212~133 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174441472, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174441472, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174441472, 24u6) then Some 1u8 else None; }
  | 214~132 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174441216, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174441216, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174441216, 24u6) then Some 1u8 else None; }
  | 214~133 -> a = {  bgp= Some {  aslen= if pod = 4 && d = (1174441216, 24u6) then 0 else if pod = 4 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174441216, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174441216, 24u6) then Some 1u8 else None; }
  | 215~128 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 215~159 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 215~172 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 215~187 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 215~221 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 215~222 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 215~281 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 216~60 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 216~62 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 216~64 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 216~66 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 216~68 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 216~69 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 216~72 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 216~74 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 217~128 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 217~159 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 217~172 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 217~187 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 217~221 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 217~222 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 217~281 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 218~128 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 218~159 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 218~172 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 218~187 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 218~221 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 218~222 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 218~281 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 219~194 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 219~196 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 219~198 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 219~200 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 219~203 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 219~206 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 219~243 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 219~244 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 220~226 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 220~227 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 220~229 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 220~230 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 220~231 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 220~234 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 221~164 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 221~167 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 221~170 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 221~173 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 221~192 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 221~215 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 221~217 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 221~218 -> a = {  bgp= Some {  aslen= if pod = 1 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 222~164 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 222~167 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 222~170 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 222~173 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 222~192 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 222~215 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 222~217 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 222~218 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 223~194 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 223~196 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 223~198 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 223~200 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 223~203 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 223~206 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 223~243 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 223~244 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 224~226 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 224~227 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 224~229 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 224~230 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 224~231 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 224~234 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 226~130 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 226~151 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 226~161 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 226~189 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 226~202 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 226~220 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 226~224 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 226~318 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 227~130 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 227~151 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 227~161 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 227~189 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 227~202 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 227~220 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 227~224 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 227~318 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 229~130 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 229~151 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 229~161 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 229~189 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 229~202 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 229~220 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 229~224 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 229~318 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 230~130 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 230~151 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 230~161 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 230~189 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 230~202 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 230~220 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 230~224 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 230~318 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 231~130 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 231~151 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 231~161 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 231~189 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 231~202 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 231~220 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 231~224 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 231~318 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 234~130 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 234~151 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 234~161 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 234~189 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 234~202 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 234~220 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 234~224 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 234~318 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 243~129 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 243~160 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 243~175 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 243~188 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 243~219 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 243~223 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 243~286 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 244~129 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 244~160 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 244~175 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 244~188 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 244~219 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 244~223 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 244~286 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 245~12 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 245~29 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 245~38 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 245~56 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 245~83 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 245~90 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 245~98 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 245~118 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 247~12 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 247~29 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 247~38 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 247~56 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 247~83 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 247~90 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 247~98 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 247~118 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 249~149 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424832, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424832, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424832, 24u6) then Some 1u8 else None; }
  | 249~150 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424832, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424832, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424832, 24u6) then Some 1u8 else None; }
  | 249~151 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424832, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424832, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424832, 24u6) then Some 1u8 else None; }
  | 249~152 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424832, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424832, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424832, 24u6) then Some 1u8 else None; }
  | 249~153 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424832, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424832, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424832, 24u6) then Some 1u8 else None; }
  | 249~154 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424832, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424832, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424832, 24u6) then Some 1u8 else None; }
  | 250~60 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 250~62 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 250~64 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 250~66 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 250~68 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 250~69 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 250~72 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 250~74 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 251~149 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174425088, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425088, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425088, 24u6) then Some 1u8 else None; }
  | 251~150 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174425088, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425088, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425088, 24u6) then Some 1u8 else None; }
  | 251~151 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174425088, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425088, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425088, 24u6) then Some 1u8 else None; }
  | 251~152 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174425088, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425088, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425088, 24u6) then Some 1u8 else None; }
  | 251~153 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174425088, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425088, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425088, 24u6) then Some 1u8 else None; }
  | 251~154 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174425088, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425088, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425088, 24u6) then Some 1u8 else None; }
  | 252~149 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174425344, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425344, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425344, 24u6) then Some 1u8 else None; }
  | 252~150 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174425344, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425344, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425344, 24u6) then Some 1u8 else None; }
  | 252~151 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174425344, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425344, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425344, 24u6) then Some 1u8 else None; }
  | 252~152 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174425344, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425344, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425344, 24u6) then Some 1u8 else None; }
  | 252~153 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174425344, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425344, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425344, 24u6) then Some 1u8 else None; }
  | 252~154 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174425344, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174425344, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174425344, 24u6) then Some 1u8 else None; }
  | 253~3 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 253~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 253~39 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 253~57 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 253~85 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 253~91 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 253~100 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 253~120 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 255~3 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 255~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 255~39 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 255~57 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 255~85 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 255~91 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 255~100 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 255~120 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 257~3 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 257~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 257~39 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 257~57 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 257~85 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 257~91 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 257~100 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 257~120 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 258~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 258~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 258~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 258~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 258~87 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 258~92 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 258~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 258~122 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 259~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 259~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 259~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 259~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 259~87 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 259~92 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 259~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 259~122 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 267~3 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 267~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 267~39 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 267~57 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 267~85 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 267~91 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 267~100 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 267~120 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 269~3 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 269~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 269~39 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 269~57 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 269~85 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 269~91 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 269~100 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 269~120 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 270~3 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 270~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 270~39 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 270~57 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 270~85 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 270~91 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 270~100 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 270~120 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 271~3 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 271~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 271~39 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 271~57 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 271~85 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 271~91 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 271~100 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 271~120 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 273~3 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 273~32 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 273~39 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 273~57 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 273~85 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 273~91 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 273~100 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 273~120 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 275~149 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174423808, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423808, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423808, 24u6) then Some 1u8 else None; }
  | 275~150 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174423808, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423808, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423808, 24u6) then Some 1u8 else None; }
  | 275~151 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174423808, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423808, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423808, 24u6) then Some 1u8 else None; }
  | 275~152 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174423808, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423808, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423808, 24u6) then Some 1u8 else None; }
  | 275~153 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174423808, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423808, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423808, 24u6) then Some 1u8 else None; }
  | 275~154 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174423808, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423808, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423808, 24u6) then Some 1u8 else None; }
  | 276~149 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424064, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424064, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424064, 24u6) then Some 1u8 else None; }
  | 276~150 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424064, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424064, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424064, 24u6) then Some 1u8 else None; }
  | 276~151 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424064, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424064, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424064, 24u6) then Some 1u8 else None; }
  | 276~152 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424064, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424064, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424064, 24u6) then Some 1u8 else None; }
  | 276~153 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424064, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424064, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424064, 24u6) then Some 1u8 else None; }
  | 276~154 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424064, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424064, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424064, 24u6) then Some 1u8 else None; }
  | 277~149 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424320, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424320, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424320, 24u6) then Some 1u8 else None; }
  | 277~150 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424320, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424320, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424320, 24u6) then Some 1u8 else None; }
  | 277~151 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424320, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424320, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424320, 24u6) then Some 1u8 else None; }
  | 277~152 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424320, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424320, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424320, 24u6) then Some 1u8 else None; }
  | 277~153 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424320, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424320, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424320, 24u6) then Some 1u8 else None; }
  | 277~154 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424320, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424320, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424320, 24u6) then Some 1u8 else None; }
  | 278~149 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424576, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424576, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424576, 24u6) then Some 1u8 else None; }
  | 278~150 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424576, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424576, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424576, 24u6) then Some 1u8 else None; }
  | 278~151 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424576, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424576, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424576, 24u6) then Some 1u8 else None; }
  | 278~152 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424576, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424576, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424576, 24u6) then Some 1u8 else None; }
  | 278~153 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424576, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424576, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424576, 24u6) then Some 1u8 else None; }
  | 278~154 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174424576, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174424576, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174424576, 24u6) then Some 1u8 else None; }
  | 279~78 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 279~81 -> a = {  bgp= Some {  aslen= if pod = 9 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 280~149 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174423552, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423552, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423552, 24u6) then Some 1u8 else None; }
  | 280~150 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174423552, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423552, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423552, 24u6) then Some 1u8 else None; }
  | 280~151 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174423552, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423552, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423552, 24u6) then Some 1u8 else None; }
  | 280~152 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174423552, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423552, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423552, 24u6) then Some 1u8 else None; }
  | 280~153 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174423552, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423552, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423552, 24u6) then Some 1u8 else None; }
  | 280~154 -> a = {  bgp= Some {  aslen= if pod = 0 && d = (1174423552, 24u6) then 0 else if pod = 0 then 2 else 4; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= if d = (1174423552, 24u6) then Some 1u2 else Some 3u2; static= if d = (1174423552, 24u6) then Some 1u8 else None; }
  | 281~164 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 281~167 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 281~170 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 281~173 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 281~192 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 281~215 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 281~217 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 281~218 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 282~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 282~33 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 282~48 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 282~59 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 282~89 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 282~93 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 282~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 282~110 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 283~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 283~33 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 283~48 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 283~59 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 283~89 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 283~93 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 283~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 283~110 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 284~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 284~33 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 284~48 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 284~59 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 284~89 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 284~93 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 284~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 284~110 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 285~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 285~33 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 285~48 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 285~59 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 285~89 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 285~93 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 285~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 285~110 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 286~194 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 286~196 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 286~198 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 286~200 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 286~203 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 286~206 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 286~243 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 286~244 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 290~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 290~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 290~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 290~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 290~87 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 290~92 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 290~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 290~122 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 291~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 291~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 291~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 291~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 291~87 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 291~92 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 291~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 291~122 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 292~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 292~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 292~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 292~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 292~87 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 292~92 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 292~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 292~122 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 294~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 294~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 294~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 294~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 294~87 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 294~92 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 294~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 294~122 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 296~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 296~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 296~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 296~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 296~87 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 296~92 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 296~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 296~122 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 297~5 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 297~22 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 297~46 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 297~58 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 297~87 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 297~92 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 297~104 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 297~122 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 300~78 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 300~81 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 301~60 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 301~62 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 301~64 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 301~66 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 301~68 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 301~69 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 301~72 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 301~74 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 302~8 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 302~34 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 302~40 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 302~50 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 302~73 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 302~101 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 302~106 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 303~8 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 303~34 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 303~40 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 303~50 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 303~73 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 303~101 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 303~106 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 304~8 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 304~34 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 304~40 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 304~50 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 304~73 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 304~101 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 304~106 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 309~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 309~33 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 309~48 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 309~59 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 309~89 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 309~93 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 309~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 309~110 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 310~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 310~33 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 310~48 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 310~59 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 310~89 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 310~93 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 310~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 310~110 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 312~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 312~33 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 312~48 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 312~59 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 312~89 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 312~93 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 312~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 312~110 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 314~6 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 314~33 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 314~48 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 314~59 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 314~89 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 314~93 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 314~105 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 314~110 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 315~8 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 315~34 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 315~40 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 315~50 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 315~73 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 315~101 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 315~106 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 316~8 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 316~34 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 316~40 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 316~50 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 316~73 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 316~101 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 316~106 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 317~8 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 317~34 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 317~40 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 317~50 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 317~73 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 317~101 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 317~106 -> a = {  bgp= Some {  aslen= 2u32; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 318~226 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 318~227 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 318~229 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 318~230 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 318~231 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
  | 318~234 -> a = {  bgp= Some {  aslen= if pod = 15 then 1 else 3; bgpAd= 20u8; comms= { _ |-> false }; lp= 100u32; med= 80u32; }; connected= None; ospf= None; selected= Some 3u2; static= None; }
